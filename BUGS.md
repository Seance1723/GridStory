# GridStory bug ledger

**Last updated:** 2026-08-29

Every defect discovered during automated or manual verification is recorded here before it is fixed or deferred. Bug records are permanent project history.

## Severity

- **Critical:** data exposure/loss, authorization bypass, remote code execution, or unusable production system.
- **High:** core workflow failure with no safe workaround.
- **Medium:** incorrect behavior with a workaround or limited scope.
- **Low:** cosmetic, usability, documentation, or non-blocking quality issue.

## Open bugs

| ID | Found | Severity | Area | Summary | Reproduction/evidence | Linked task | Status |
|---|---|---|---|---|---|---|---|
| BUG-0461 | 2026-08-26 | Medium | Existing Studio unit-suite lifecycle | Full CI-001 verification intermittently times out in the experiment lifecycle case and the following AI proposal case lacks its expected pending-review result. | The historical System log proves the 61.24-second experiment result overlapped Windows idle sleep: 56.894-second clock correction and 68.570-second low-power interval. The unchanged pair passes 10/10 stress commands, three timed commands (experiment 11.49–11.79s; AI 4.04–4.27s), async-leak detection, and the prior focused/full controls. BUG-0462 independently reproduces the clock-jump/test-duration relationship. The following AI assertion passes all 14 current focused/timed controls; its exact prior post-resume failure remains non-reproducing and is not attributed to application behavior. | CI-003; CI-001 verification; BUG-0462; Unreleased/Changed | Timeout root cause established as host suspension; following AI assertion remains explicitly non-reproducing pending any independent observation; no code, test or deadline change justified |

## Resolved bugs

| ID | Found | Resolved | Severity | Area | Summary | Resolution and verification | Linked task/change |
|---|---|---|---|---|---|---|---|
| BUG-0552 | 2026-08-29 | 2026-08-29 | High | Retained WebKit navigation guard | The first complete WebKit matrix lost the unsaved Title value while only expanding/collapsing Studio navigation groups; Chromium and Firefox passed unchanged. | The failure is non-reproducing: the unchanged exact scenario passes once plus 5/5 repetitions, and the unchanged complete WebKit rerun passes 25/25 with the retained draft assertion. No product/test source, timeout, retry or assertion change is justified. | CMS-010 verification; existing navigation guard; Unreleased/Changed |
| BUG-0551 | 2026-08-29 | 2026-08-29 | Low | CMS-010 permission-profile fixture reload | Navigating to the same Settings hash did not restart the application between profiles, so the schema assertion still observed the locale-only context. | Load Settings once, then explicitly reload for each changed intercepted profile. The corrected Chromium case passes all three contexts with one inventory GET, one available section, two unavailable sections, only authorized links and no editable controls per profile. | CMS-010; ADR 0033; Unreleased/Fixed |
| BUG-0550 | 2026-08-29 | 2026-08-29 | Low | CMS-010 E2E build environment | The managed Windows sandbox denied pnpm's parallel package lifecycle child process during `build:e2e` with `spawn EPERM`. | Rerun the identical build through the approved child-process boundary without changing concurrency, package scope or build settings. Both Studio and example-site E2E bundles pass. | CMS-010; TEST-003; Unreleased/Changed |
| BUG-0549 | 2026-08-29 | 2026-08-29 | Low | CMS-010 permission-profile fixture hygiene | The first repository lint/format gate found a forbidden non-null assertion and formatting drift in the new permission-profile browser test. | Give the active profile an explicit finite-union type and apply the repository layout. Warning-free lint, all ledger/policy checks and format verification pass. | CMS-010; FND-002; Unreleased/Fixed |
| BUG-0548 | 2026-08-29 | 2026-08-29 | Medium | CMS-010 configuration inventory contract | The inventory schema bounded provider count and generic values but still accepted duplicate provider kinds and modes invalid for a particular provider, weakening the promised fixed safe vocabulary. | Replace the generic four-item array with a fixed provider tuple and provider-specific mode enums. Both duplicate-kind and invalid-mode payloads are rejected; all four focused contract cases and dependent typechecks pass. | CMS-010; ADR 0033; Unreleased/Fixed |
| BUG-0547 | 2026-08-29 | 2026-08-29 | Medium | CMS-010 Settings contrast | The shared late `.section-heading > span` muted-color rule overrode the inventory badge foreground, producing 3.99:1 contrast on its soft-primary surface at 390px. | Bind the badge foreground at a purpose-owned specificity above the shared late rule while retaining the palette. The rebuilt real Chromium journey passes unsuppressed WCAG 2.2 A/AA. | CMS-010; WCAG 1.4.3; Unreleased/Fixed |
| BUG-0546 | 2026-08-29 | 2026-08-29 | Medium | CMS-010 context fact semantics | The current Site/Environment/Locale definition list placed supporting `small` nodes beside `dt`/`dd` inside each grouping, violating permitted `dl` ordering. | Move supporting values inside their `dd` and render them as blocks. All facts remain visible and the rebuilt Chromium Axe check passes. | CMS-010; WCAG 1.3.1; Unreleased/Fixed |
| BUG-0545 | 2026-08-29 | 2026-08-29 | Low | CMS-010 browser disclosure fixture | The browser's broad forbidden-word regex rejected the deliberate safety disclaimer because it truthfully names `credentials` and `endpoints` as omitted categories. | Retain the API serialized scan and reject concrete config keys, URLs and adapter class names in UI output. The complete focused journey passes. | CMS-010; TEST-005; Unreleased/Fixed |
| BUG-0544 | 2026-08-29 | 2026-08-29 | Low | CMS-010 browser route fixture | The partial `/:slug` route assertion also matched `/articles/:slug`. | Require the exact Page route while retaining the Article facts; the complete focused journey passes. | CMS-010; TEST-005; Unreleased/Fixed |
| BUG-0543 | 2026-08-29 | 2026-08-29 | Low | CMS-010 browser fixture | The partial `not declared` locator matched both current-environment summary and standalone environment kind. | Require the exact standalone value while retaining all fact assertions; the complete focused journey passes. | CMS-010; TEST-005; Unreleased/Fixed |
| BUG-0542 | 2026-08-29 | 2026-08-29 | Medium | CMS-010 direct Settings bootstrap | The retained Studio bootstrap condition requested full schemas for every non-Home direct destination, so `#/settings` could load detailed schema data in addition to the minimized inventory. | Exclude Settings from the legacy authoring bootstrap. The direct-route regression renders the inventory, calls it exactly once and issues no `getSchemas` request. | CMS-010; ADR 0033; Unreleased/Fixed |
| BUG-0541 | 2026-08-29 | 2026-08-29 | Low | CMS-010 schema regression hygiene | The first repository lint found three explicit-`any` warnings in strict-contract negative fixtures. | Replace mutation-through-`any` with typed invalid object spreads. Warning-free repository lint and all three strict schema cases pass. | CMS-010; FND-002; Unreleased/Fixed |
| BUG-0540 | 2026-08-29 | 2026-08-29 | Low | CMS-010 Studio navigation fixture | The retained disclosure test still expected nine navigation list roles after the finite Settings group added one group list. | Update the exact finite count to ten while retaining every disclosure and dirty-editor assertion; the corrected case and new Settings single-page case both pass. | CMS-010; Unreleased/Fixed |
| BUG-0539 | 2026-08-29 | 2026-08-29 | Medium | CMS-010 configuration inventory input boundary | Fastify leaves `request.body` undefined for the GET route, so a JSON payload with a nonzero content length reached the parameter-free inventory and returned 200. | Reject positive `Content-Length` and transfer-encoded bodies before projection. All four API inventory cases pass, including no-store 400 evidence. | CMS-010; ADR 0033; Unreleased/Fixed |
| BUG-0538 | 2026-08-29 | 2026-08-29 | Low | CMS-010 configured-topology API fixture | The first API assertion expected one environment although the canonical local topology truthfully includes both development and production for the authorized current site. | Expect the two authorized current-site environments while retaining exclusion of the private-site tuple; all four API inventory cases pass. | CMS-010; Unreleased/Changed |
| BUG-0537 | 2026-08-29 | 2026-08-29 | Medium | CMS-010 current-only context projection | The initial pure service could reuse supplied environment metadata in `current-only` mode and thereby imply an environment kind that the permission-filtered context did not declare. | Ignore environment metadata in current-only mode and report `not-declared`; the focused non-inference regression and all four core service cases pass. | CMS-010; ADR 0033; Unreleased/Fixed |
| BUG-0536 | 2026-08-29 | 2026-08-29 | Low | CMS-010 core projection typing | The first focused core typecheck rejected an impossible empty-object fallback passed to `contentScopeKey`. | Use the already validated requested scope as the unreachable fallback. The unchanged current-only identity check, four focused core tests and core typecheck pass. | CMS-010; Unreleased/Fixed |
| BUG-0535 | 2026-08-29 | 2026-08-29 | Low | CMS-010 focused verification environment | The managed Windows sandbox denied both new Vitest fork workers with `spawn EPERM`, so the first schema/core commands executed zero tests. | Reran the identical commands through the approved child-process boundary without changing pools, tests, retries or timeouts; all 3 schema and 4 core tests pass. | CMS-010; TEST-003; Unreleased/Changed |
| BUG-0534 | 2026-08-29 | 2026-08-29 | Low | Current delivery documentation | README's delivery-status snapshot still ended at CMS-008 after verified CMS-009 commit `ec408a6`. | Update only the delivery snapshot to CMS-009's 697-test/69-browser evidence and ADR 0033 handoff, and name `_schema.scss` in the style ownership guide. Documentation links, formatting, ledgers and fence audit pass. | CMS-010 planning; CMS-009 handoff; Unreleased/Fixed |
| BUG-0533 | 2026-08-29 | 2026-08-29 | Low | BUG-0531 unit regression locator | The first readiness assertion queried exact `Headline`, but the canonical unit fixture exposes the field as `Article headline`. | Target the fixture's canonical label while retaining the direct no-inert assertion. The exact case, complete 301-test Studio suite and final aggregate gates pass. | CMS-009 verification; BUG-0531; Unreleased/Fixed |
| BUG-0532 | 2026-08-29 | 2026-08-29 | Low | CMS-009 focused verification invocation | The first package-scoped Vitest command used a repository-relative file path and found no tests. | Rerun with package-relative `test/App.test.tsx`; the unchanged named case passes, followed by the complete Studio/repository/browser gates. | CMS-009 verification; Unreleased/Changed |
| BUG-0531 | 2026-08-29 | 2026-08-29 | High | Existing post-create authoring readiness | The Article creation success notice could become visible while the tracked mutation still kept the editor inert, allowing an immediate WebKit fill to be lost. | Set the success notice only after `trackEntryMutation` settles; assert directly that the editor is not inert at notice visibility. The unit case, 9/9 repeated three-engine Article journeys and final 69/69 matrix pass. | CMS-004; CMS-009 verification; BUG-0476; Unreleased/Fixed |
| BUG-0530 | 2026-08-29 | 2026-08-29 | Low | Existing compact-navigation fixture | The retained compact-navigation assertion constrained the destination-button count to the pre-CMS-009 value. | Update the exact finite count from 20 to 21; the case and complete 301-test Studio suite pass with all disclosure assertions intact. | CMS-009 verification; Unreleased/Fixed |
| BUG-0529 | 2026-08-29 | 2026-08-29 | Low | CMS-009 focused browser setup | The first catalog browser run paired new Studio assets with stale API/schema distribution. | Build the complete workspace before restoring e2e assets. The exact catalog case and final 69/69 matrix pass unchanged. | CMS-009 verification; Unreleased/Fixed |
| BUG-0528 | 2026-08-29 | 2026-08-29 | Low | CMS-009 field contract presentation | An omitted canonical slug-pattern default rendered as `pattern undefined`. | Render the canonical default pattern for input-form definitions; catalog component, responsive WCAG and aggregate browser checks pass. | CMS-009; BUG-0524; Unreleased/Fixed |
| BUG-0527 | 2026-08-29 | 2026-08-29 | Low | CMS-009 term-tree test locator | The first hierarchy assertion required an exact fragment that was correctly embedded in a richer identity line. | Use a substring matcher that retains the exact parent ID requirement. The 175-case focused group and aggregate gates pass. | CMS-009 verification; Unreleased/Fixed |
| BUG-0526 | 2026-08-29 | 2026-08-29 | Low | CMS-009 catalog test fixture | The first catalog fixture referenced undeclared slug fields and taxonomy definitions. | Declare the referenced canonical fields/taxonomy and keep strict `createSchemaIr` construction. The focused group and all aggregate gates pass. | CMS-009 verification; Unreleased/Fixed |
| BUG-0525 | 2026-08-29 | 2026-08-29 | Low | CMS-009 verification environment | Managed Windows sandboxing denied Vitest worker spawning, so the first focused command executed zero tests. | Rerun the identical command with approved process spawning; 175/175 focused tests pass without pool, retry or timeout changes. | CMS-009 verification; Unreleased/Changed |
| BUG-0524 | 2026-08-29 | 2026-08-29 | Low | CMS-009 catalog contract typing | The catalog treated optional defaulted arrays in Studio-facing schema input types as normalized output arrays. | Preserve input compatibility with empty-array presentation fallbacks. Focused typecheck, full strict types and all aggregate gates pass. | CMS-009; Unreleased/Fixed |
| BUG-0523 | 2026-08-29 | 2026-08-29 | Medium | CMS-008 final browser verification | The first final Chromium matrix reached 21/22 but an unchanged containment case crossed its five-minute deadline during an abnormal 6.9-minute wall interval. | No assertion/product failure reproduced. The unchanged exact case passed at 58.6s, the complete Chromium rerun passed it at 57.9s and the final engine suites passed 22/22 each. No timeout, retry or assertion changed. | CMS-008 verification; Unreleased/Changed |
| BUG-0522 | 2026-08-29 | 2026-08-29 | Medium | CMS-008 catalog contrast | Two muted catalog labels measured 4.3:1 on the primary-soft surface. | Use the existing standard Studio text token for the ownership legend and insertion boundary. Unsuppressed WCAG passes in all three engines; the surfaces and palettes are unchanged. | CMS-008; WCAG 1.4.3; Unreleased/Fixed |
| BUG-0521 | 2026-08-29 | 2026-08-29 | Low | CMS-008 focused browser gate setup | A regular production build overwrote the port-specific e2e bundle before a direct Playwright run. | Run `build:e2e` before direct focused Playwright invocations. The unchanged catalog journey then passed in Chromium, Firefox and WebKit, and the final 66-scenario matrix passes. | CMS-008 verification; Unreleased/Fixed |
| BUG-0520 | 2026-08-29 | 2026-08-29 | Low | CMS-008 read-only fixture | The read-only test conflated a valid loaded insertion target with update authorization. | Assert truthful target context separately from disabled insertion controls. The focused permission case, full Studio suite and three-engine matrix pass. | CMS-008 verification; Unreleased/Fixed |
| BUG-0519 | 2026-08-29 | 2026-08-29 | Low | CMS-008 component-selection fixture | The test transport exposed lifecycle evidence only for Hero. | Resolve migration and visual fixtures for any registered component while retaining Hero's seeded usage facts. Callout selection and aggregate gates pass. | CMS-008 verification; Unreleased/Fixed |
| BUG-0518 | 2026-08-29 | 2026-08-29 | Low | CMS-008 retained catalog fixtures | Retained unit/browser assertions targeted the superseded component-governance landmark and prose layout. | Target the semantic `Governed design catalog` landmark and labelled scenario fact. Focused, aggregate and final browser gates pass. | CMS-008 verification; Unreleased/Fixed |
| BUG-0517 | 2026-08-29 | 2026-08-29 | Medium | CMS-008 catalog insertion target | Selected identity alone did not prove a loaded editor draft, causing nullable target access and overstated readiness. | Require selected entry, component field, authorization and non-null draft for insertion; require the draft for the target label. Types, read-only/success cases and aggregate gates pass. | CMS-008; Unreleased/Fixed |
| BUG-0516 | 2026-08-29 | 2026-08-29 | Low | CMS-008 lazy-load regression fixture | The first test requested Home using a fixture that deliberately denied `home.read`. | Use the fully authorized Home context while retaining the zero-design-read assertion. Focused and aggregate lazy-load checks pass. | CMS-008 verification; Unreleased/Fixed |
| BUG-0515 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 Home-to-Components lazy load | Components opened from minimized Home without restoring the design manifest. | Components now owns isolated authorized lazy loading and retry without changing Home's minimized reads. Exact unit/browser boundaries and all aggregate gates pass. | CMS-008; CMS-006; Unreleased/Fixed |
| BUG-0514 | 2026-08-29 | 2026-08-29 | Low | Existing Studio shell browser fixture | The option-card style scenario named a synthetic fallback that disappears after real assets exist. | Select the first actual `aria-pressed` asset option within the semantic Social image region, excluding Clear asset, while retaining all class, geometry, interaction and theme assertions. The focused Firefox correction and final ordered 63-scenario matrix pass. | CMS-007 verification; Unreleased/Fixed |
| BUG-0513 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 responsive Media detail | Firefox retained selected-detail min-content width in a nominal single `1fr` track, clipping at 390px and accumulated-state 320px. | Bound workspace/detail items, stack the mobile detail header, use explicit `minmax(0, 1fr)` mobile tracks and preserve wrapping. The exact ordered Chromium-upload → Firefox six-width control and final 63-scenario matrix pass. | CMS-007; Unreleased/Fixed |
| BUG-0512 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 selected Media card accessibility | Muted selected-card copy measured 4.3:1 on the existing soft-primary surface. | Selected supporting copy now uses the established semantic text token. Unsuppressed Axe passes in Chromium, Firefox and WebKit in focused and final matrix runs; the palette and selection surface are unchanged. | CMS-007; Unreleased/Fixed |
| BUG-0511 | 2026-08-29 | 2026-08-29 | Low | CMS-007 Media browser fixture | Partial security/provider locators matched exact evidence and revision summaries. | Require exact `verified`, `not_configured` and `Unavailable` evidence text. The real upload journey passes in all three engines and retains every security/provider assertion. | CMS-007 verification; Unreleased/Fixed |
| BUG-0510 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 local file read | A rejected browser file read escaped the upload error boundary. | Catch the local read before session creation, expose the established recoverable notice and issue no server call. Exact unit coverage, 290 Studio tests and the full repository gate pass. | CMS-007; Unreleased/Fixed |
| BUG-0509 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 late upload start / resume identity | Late start after local cancellation could leave a server session; resume did not prove full file/session identity. | Validate filename/media type/kind/byte count before resuming and best-effort abort a late issued session through the cleanup client. Exact late/mismatch tests and aggregate gates pass. | CMS-007; Unreleased/Fixed |
| BUG-0508 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 paused upload replacement | A failed retained session could be silently replaced through the enabled file input. | Label and disable upload selection while paused until explicit Retry or Abort. Exact UI/state tests and all browser gates pass. | CMS-007; Unreleased/Fixed |
| BUG-0507 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 upload activity settlement | Successful completion could leave the hidden management-activity lock set. | Settle activity when the completing context still owns or has cleared the reference, without allowing an older upload to settle a replacement. Context controls recover in exact unit and browser verification. | CMS-007; Unreleased/Fixed |
| BUG-0506 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 multi-asset upload selection | Completion could retain an older selected detail after refreshing a populated library. | Apply the completed asset ID as a one-time focus request that cannot override later manual selection. Multi-asset unit, WebKit and final ordered matrix checks pass. | CMS-007; Unreleased/Fixed |
| BUG-0505 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 asset-detail accessibility | The Security filter collided with the Security evidence region's accessible name. | Rename the filter Security state while retaining the evidence region. Focused accessibility and unsuppressed three-engine Axe gates pass. | CMS-007; Unreleased/Fixed |
| BUG-0504 | 2026-08-29 | 2026-08-29 | Low | CMS-007 browser fixture | The new journey invented a different word order for the existing rendition-adapter error. | Assert the exact established “No rendition adapter is configured.” alert and preserve the unavailable-provider behavior. All engines pass. | CMS-007; Unreleased/Fixed |
| BUG-0503 | 2026-08-29 | 2026-08-29 | Low | CMS-007 browser fixture | A locator tied to the old accessible asset title correctly stopped matching after metadata revision. | Bind post-revision checks to the stable detail container while retaining exact before/after headings. The real journey passes in all engines. | CMS-007; Unreleased/Fixed |
| BUG-0502 | 2026-08-29 | 2026-08-29 | Medium | CMS-007 upload abort settlement | A late part response could briefly restore progress for an aborted upload. | Recheck cancellation immediately after the awaited part and before any state update/completion. Exact delayed-part/abort and aggregate checks pass. | CMS-007; Unreleased/Fixed |
| BUG-0501 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 Home-to-Library lazy load | Minimized Home omitted assets and Library did not fetch them, showing a false empty state. | Library now owns isolated authorized loading/error/retry state and fetches on first navigation without restoring Home's full asset read. Exact App/browser requests and final gates pass. | CMS-007; CMS-006; Unreleased/Fixed |
| BUG-0500 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 final Studio aggregate verification | After the 13-minute browser matrix, the final `pnpm check` Studio file ran 138.839 seconds and two existing long interaction cases crossed their unchanged 5s/15s deadlines by 305ms/217ms; the other 271 Studio tests and all preceding repository gates passed. | Paired reruns reproduced only wall-clock expiry at 5.293s/15.034s; isolated unchanged cases passed at 4.92s/14.39s, proving at most 1.6% scheduling margin without a product assertion failure. Retain every interaction/assertion and apply bounded 10s/30s case ceilings only. The exact pair, full 273-test Studio file and final `pnpm check` with 669 active tests pass; no retry/global timeout changed. | CMS-006 verification; Unreleased/Changed/Fixed; ADR 0032 |
| BUG-0499 | 2026-08-29 | 2026-08-29 | Medium | Existing selected-reference chip dark theme | A reference selected by an earlier browser scenario exposes the chip during the later dark Collections audit; the chip retained its light `#f3f4f6` surface while inheriting white text, producing 1.1:1 contrast. | Bind the shared selected-reference and presence-chip surface/foreground to existing theme tokens. The ordered eight-case Firefox accessibility file and final unchanged 60-scenario three-engine matrix pass, including the sequence-created state. | CMS-006 verification; Unreleased/Fixed |
| BUG-0498 | 2026-08-29 | 2026-08-29 | Medium | Existing selected option-card accessibility | Firefox computes the selected reference option's muted 11.52px type label at 4.3:1 against the selected soft-primary surface, below the required 4.5:1. | Selected option-card supporting copy now inherits the selected control's semantic foreground in both themes. The exact focused Firefox regression and final unchanged 60-scenario three-engine matrix pass. | CMS-006 verification; Unreleased/Fixed |
| BUG-0497 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 Home responsive containment | At 1024px, the Home heading copy and fixed-width generated timestamp exceeded the 706px content surface by 18px. | Stack the heading and allow the timestamp to wrap at the existing 1180px single-card breakpoint. The exact scenario and final six-width Chromium/Firefox/WebKit containment sweeps pass. | CMS-006; Unreleased/Fixed; ADR 0032 |
| BUG-0496 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 Home dark-theme accessibility | The published status badge reused the dark surface's low-contrast primary green, producing a measured 1.93:1 contrast ratio. | Use the established accent token for the published badge in dark mode while retaining the existing palette. The exact scenario and final unsuppressed WCAG 2.2 A/AA sweeps pass in all three engines. | CMS-006; Unreleased/Fixed; ADR 0032 |
| BUG-0495 | 2026-08-29 | 2026-08-29 | High | CMS-006 invalid-fragment history guard | Once Home became the first permitted destination, a malformed runtime fragment selected Home instead of retaining the current dirty entry, hiding the editor and bypassing the established replacement guard. | Runtime-invalid locations now resolve to the already accepted scoped location; initial invalid startup still uses the permitted default. The exact regression and final navigation matrix pass in all three engines. | CMS-006; CMS-002; Unreleased/Fixed; ADR 0032 |
| BUG-0494 | 2026-08-29 | 2026-08-29 | Low | CMS-006 schema fixture | The full repository run found the finite Studio contract fixture still constrained the destination count to the pre-Home value of twenty. | Update the exact count to twenty-one and explicitly assert the strict default-false Home capability. The schema suite and complete 669-active-test repository run pass. | CMS-006; Unreleased/Fixed |
| BUG-0493 | 2026-08-29 | 2026-08-29 | Low | CMS-006 browser fixture | The first Home browser scenario composed a card filter from a parent-scoped heading locator, so Playwright could not resolve the visible recent-entry button even though the screenshot showed the correct content. | Address the explicitly labelled `Content` article and its first existing entry action directly. The unchanged Chromium scenario then passes its minimized-request, exact-bound, precise-entry-route and canonical-create assertions. | CMS-006; Unreleased/Fixed |
| BUG-0492 | 2026-08-29 | 2026-08-29 | Low | CMS-006 core lint | The new overview scope assertions used expression-bodied `forEach` callbacks that returned the assertion result under the repository's strict iterable-callback rule. | Use block-bodied callbacks for content, workflow and release scope assertions. The complete lint, boundary, ledger, security, tenant and readiness gate passes without diagnostics. | CMS-006; Unreleased/Fixed |
| BUG-0491 | 2026-08-29 | 2026-08-29 | Low | CMS-006 API test isolation | The new denied-widget regression counted workflow-definition reads performed by server startup as if the overview route had issued them. | Clear only the repository spies after the server is fully constructed, then assert the viewer overview request itself performs no workflow-instance or definition read. The focused 17-test API/context rerun passes. | CMS-006; Unreleased/Fixed |
| BUG-0490 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 tenant-scope projection | The first page-only overview projection filtered non-page rows before asserting repository scope, so a poisoned cross-scope non-page result would be silently ignored instead of failing the widget closed. | Assert every repository result against the complete requested scope before applying page-only visibility, and add a cross-scope out-of-coverage regression. The focused core overview suite passes all three cases. | CMS-006; Unreleased/Fixed; ADR 0032 |
| BUG-0489 | 2026-08-29 | 2026-08-29 | Medium | CMS-006 Studio startup navigation | Initializing the active destination from the raw root fragment exposed Home before verified capabilities/bootstrap settled, so a Home-denied session briefly had no current navigation item or editor surface. | Retain the prior neutral Pages startup state until the verified bootstrap chooses Home or the first permitted destination. The two exact legacy failures and all four Home-focused component/integration cases pass together. | CMS-006; Unreleased/Fixed; ADR 0032 |
| BUG-0488 | 2026-08-29 | 2026-08-29 | Low | Existing task handoff ledger | The retained `Single next action` still instructed committing CMS-004 and beginning CMS-005 even though both had dedicated verified commits. | Updated only the current handoff to the verified `519235f` baseline and CMS-006's ADR 0032 planning/approval gate, retaining every completed task record. Ledger, formatting, link, fence and whitespace checks verify the correction. | CMS-006 planning; Unreleased/Fixed |
| BUG-0487 | 2026-08-28 | 2026-08-28 | High | Existing immediate dirty-navigation guard | WebKit could dispatch a field edit and a manual fragment change before React rerendered the closure that owned `dirty`; the navigation then replaced the controlled draft without confirmation. | Mirror dirty state synchronously in a ref at edit/read/save settlement and use it for every entry-replacement/migration guard. The exact previously failing WebKit sequence passes without a wait, timeout or assertion change; the final complete 57-scenario three-engine matrix passes. | CMS-005 verification; CMS-002; Unreleased/Fixed |
| BUG-0486 | 2026-08-28 | 2026-08-28 | Medium | CMS-005 Firefox 1280px containment | Firefox retained a nonzero 321px rectangle for the text input inside the closed local-saved-views disclosure, placing its unpainted box beyond the 303px content sidebar during containment inspection. | Explicitly remove every non-summary saved-view child from layout while the native details element is closed, and improve retained diagnostics with exact control/boundary rectangles. The isolated Firefox six-width sweep passes without assertion tolerance or width changes. | CMS-005; Unreleased/Fixed |
| BUG-0485 | 2026-08-28 | 2026-08-28 | Medium | Existing browser fixtures after bounded lists | Three existing browser scenarios assumed their entry was always present on the unbounded first sidebar page, and their button names included status that is now a separate table cell. | Use the public list search to expose each intended target and address entry buttons by their exact title/slug name. All dirty/history/preview/unavailable/publication assertions remain; the four affected focused Chromium scenarios pass. | CMS-005; Unreleased/Fixed |
| BUG-0484 | 2026-08-28 | 2026-08-28 | Low | Existing viewer browser request assertion | The viewer scenario classified every POST except Search as a mutation, but CMS-005 uses the existing read-only POST content-query route for bounded Pages/Collections lists. | Explicitly allow only `POST /content/query` and `POST /search`, require both to be observed, and continue rejecting every other non-GET request. The affected Chromium viewer scenario passes with zero HTTP failures. | CMS-005; Unreleased/Fixed |
| BUG-0483 | 2026-08-28 | 2026-08-28 | Low | CMS-005 accessibility/test lint | The first repository lint gate rejected a generic labelled region, an unlabeled-role pagination wrapper and one fixture non-null assertion. | Use native labelled section/navigation elements, keep keyboard access on the table's real entry buttons instead of adding a noninteractive tab stop, and add a runtime saved-view fixture guard. The complete lint/security/tenant/readiness gate passes without warnings. | CMS-005; Unreleased/Fixed |
| BUG-0482 | 2026-08-28 | 2026-08-28 | Low | Existing README delivery status | The README still said the latest delivered Studio work was CMS-032–034 with 639 tests/51 browser scenarios even though separately committed CMS-004 had newer verified evidence. | Name the committed CMS-004 hash and its 646-active-test/54-browser gate while identifying CMS-005 as in progress rather than claiming unverified completion. Formatting, links, ledgers and the final task gate verify the corrected documentation. | CMS-005 documentation; CMS-004; Unreleased/Fixed |
| BUG-0481 | 2026-08-28 | 2026-08-28 | Low | New CMS-005 multi-engine fixture | The browser scenario reused one generic title prefix across projects while the configured memory server persists for the entire Playwright invocation, so Firefox/WebKit correctly saw earlier projects' records instead of exactly eleven. | Scope created titles and list searches to the Playwright project identity. The exact scenario passes independently in Chromium, Firefox and WebKit with the same count, pagination, stable-selection, storage, keyboard and mobile assertions. | CMS-005; Unreleased/Fixed |
| BUG-0480 | 2026-08-28 | 2026-08-28 | Low | CMS-005 conditional list controls | A broad JSX patch placed the list-schema condition around the unrelated Search form while leaving its closing fragment in the content sidebar, so formatting and strict types rejected the unbalanced tree. | Restored the Search panel unchanged and bound the condition only around content-list filters and saved views. Formatting and the focused Studio typecheck pass. | CMS-005; Unreleased/Fixed |
| BUG-0479 | 2026-08-28 | 2026-08-28 | Low | Existing global CTA styling | The shared `text-button` and `text-button--danger` classes had no definitions, so saved-view and existing workflow/search actions rendered as browser-default alien buttons. | Defined both shared variants centrally in `_cta.scss`, including established primary/danger light/dark palette, hover, border, radius and minimum target height. The rebuilt focused Chromium scenario passes with the global styles. | CMS-005; Unreleased/Fixed |
| BUG-0478 | 2026-08-28 | 2026-08-28 | Low | New CMS-005 browser fixture | The saved-view assertion's non-exact accessible name also matched its `Delete … view` sibling and failed strict locator resolution. | Require the exact saved-view action name without weakening behavior. The same focused Chromium create/filter/save/paginate/responsive scenario passes. | CMS-005; Unreleased/Fixed |
| BUG-0477 | 2026-08-28 | 2026-08-28 | Medium | Existing Studio capability adapter | The already-public `queryContent` SDK method had no explicit Studio operation mapping, so the fail-closed client blocked CMS-005's authorized list query before transport and left the editor empty. | Map queried Page types to `pages.list` and registered non-page types to `content.read`, matching the existing legacy-list boundary. Exact operation tests and the complete 63-case App integration file pass (209 focused Studio tests total). | CMS-005; Unreleased/Fixed |
| BUG-0476 | 2026-08-28 | 2026-08-28 | Medium | Existing Studio entry-read settlement | A visible newly selected or bootstrapped controlled field could silently lose an immediate edit while duplicated mutable read/busy guards or an equivalent focus-context replacement were still settling. | Settle successful entry reads before exposing their draft, avoid equivalent-context replacement renders, and let the already-inert/permission-gated editor commit dispatched field events instead of rechecking timing-sensitive refs. Unit regressions and both exact Firefox/WebKit failures pass; the final 54-scenario three-engine matrix passes. | CMS-004; CMS-002; Unreleased/Fixed; ADR 0031 |
| BUG-0427 | 2026-08-26 | 2026-08-28 | Medium | Existing Studio page creation defaults | Create page failed schema validation for the full example page because the hard-coded candidate supplied empty required Hero props and assumed the first schema. | Build the minimum candidate from the explicitly selected canonical schema and component manifests, then run canonical validation before transport. Corrected page/article candidates validate; invalid candidates expose field paths and make no request. Focused creation tests, API vertical slice and the final 54-scenario browser matrix pass. | CMS-004; CMS-002; ADR 0031; Unreleased/Fixed |
| BUG-0475 | 2026-08-28 | 2026-08-28 | Low | CMS-004 article browser fixture | The article relation scenario used a strict text locator for the repeated content-type label `page`, which became ambiguous after earlier browser scenarios created additional valid page candidates. | Require a visible declared-target option card, verify its page type, and select that exact card without assuming list cardinality. The exact complete 18-scenario Chromium sequence passes. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0474 | 2026-08-28 | 2026-08-28 | Low | Existing viewer browser fixture | The all-permitted-destinations viewer sweep treated every destination except Pages as a feature panel, so it rejected Collections even though Collections correctly rendered the shared authoring workspace. | Classify Pages and Collections as authoring workspaces while retaining every current-item, operation-disablement, request and error assertion. The exact complete 18-scenario Chromium sequence passes. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0473 | 2026-08-28 | 2026-08-28 | Low | Existing workflow API fixture | The workflow contract test asserted exactly one default definition even though CMS-004 intentionally registers independent page and article workflows. | Require both explicit default IDs, then select `page-editorial` by identity for the retained page workflow scenario. The exact workflow test passes. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0472 | 2026-08-28 | 2026-08-28 | Low | Existing analytics API fixture | The analytics workflow test selected the first unfiltered default content entry and built an invalid event from the newly seeded draft article instead of the published page. | Request the published page explicitly and account for the second source-owned seed's expected `content.created` delivery. The exact analytics test passes without an analytics runtime change. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0471 | 2026-08-28 | 2026-08-28 | Low | CMS-004 type-aware Studio copy | Page-list-only and read-only sessions displayed generic entry copy even when their authorized destination explicitly identified page content. | Derive labels from the registered schema when authorized and otherwise from the explicit active type. The complete 62-test Studio application file now passes its page-list-only, read-only and unavailable-direct-page regressions. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0470 | 2026-08-28 | 2026-08-28 | Medium | CMS-004 Studio feature navigation | A user authorized only for a non-content Studio destination could not leave the access-unavailable boundary because every navigation transition required a registered content schema. | Added a content-context-free transition path only when a permitted non-content destination carries no selected/requested entry. The complete 62-test Studio application file now passes, including its retained Operations-only navigation boundary. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0469 | 2026-08-28 | 2026-08-28 | Low | CMS-004 Studio candidate typing | The initial-candidate helper assumed a taxonomy definition's normalized term list was present even though the author-facing schema input contract permits it to be omitted. | Kept the optional first-term lookup null-safe while leaving canonical validation authoritative. The exact focused Studio typecheck now passes after every dependency package builds. | CMS-004; Unreleased/Fixed; ADR 0031 |
| BUG-0468 | 2026-08-28 | 2026-08-28 | Medium | Existing Search keyboard accessibility | WebKit's WCAG audit found that a text-only related-content list became vertically scrollable but had neither focusable content nor its own keyboard focus target. | Kept the bounded lists and gave Backlinks and Related content explicit accessible names and keyboard focus. The exact three-engine WCAG rerun and all 51 final browser scenarios pass. | CMS-034; TEST-004; Unreleased/Fixed; ADR 0030 |
| BUG-0467 | 2026-08-28 | 2026-08-28 | Medium | CMS-034 responsive header containment | At the default 1280px browser viewport, the intentional second context-control row extended below the fixed 72px header; the retained 70px single-row assertion also no longer described this responsive layout. | Retained the semantic fieldset, made the legacy fixed header height automatic only at the two-row breakpoint, and assert both the 70px minimum and child containment. Exact Chromium/Firefox/WebKit regressions and the final 51-scenario matrix pass; manual 390px bounds are contained with equal client/scroll width. | CMS-034; Unreleased/Fixed; ADR 0030 |
| BUG-0435 | 2026-08-26 | 2026-08-28 | Medium | Existing management preview revocation transport | The preview DELETE handler interpreted every bearer value as a preview grant, including an authenticated workforce bearer session. | Dispatch only `gsp_` bearer values to preview self-revocation; workforce sessions use the existing scope-checked management path. Cookie, workforce-bearer and preview self-revocation pass, while malformed/unknown bearer, wrong scope and AUTH-001 boundaries deny. Focused API/server/client tests, security checks and the full 639-test repository gate pass. | CMS-003; CMS-034; ADR 0030; Unreleased/Security |
| BUG-0466 | 2026-08-28 | 2026-08-28 | Medium | CMS-034 context-transition write settlement | The initial context-switch guard covered entry/editor saves but not every existing feature-level management request, so a switch could begin while an untracked feature mutation was still pending. | Derive a finite management-operation wrapper from the canonical Studio capability map, count every mapped non-read operation, and include that count in both selector disabling and the synchronous pre-transition guard. A pending personalization-draft regression proves the committed scope cannot change until the write settles. | CMS-034; Unreleased/Fixed; ADR 0030 |
| BUG-0465 | 2026-08-28 | 2026-08-28 | Medium | CMS-034 desktop header containment | The first real Chromium context-switch run at 1280px showed the new selector and existing Save/Publish actions occupying the same header row; Save intercepted the Apply control and the scenario timed out. | Move the selector to a full-width second header row at the existing 1500px responsive boundary, where the fixed 270px navigation leaves insufficient content width for three header regions. The corrected real-browser switch and six-width containment gates must pass before CMS-034 closes. | CMS-034; Unreleased/Fixed; ADR 0030 |
| BUG-0464 | 2026-08-28 | 2026-08-28 | Low | CMS-034 new control/test lint | The first focused lint found an unsupported `aria-label` on the context-control wrapper and two forbidden fixture non-null assertions; its first labelled group correction then required the semantic fieldset element. | Use a visually labelled native fieldset for the finite selector and a runtime fixture guard before constructing the pending-save result. The same focused lint then passes without warnings or removed assertions. | CMS-034; Unreleased/Fixed; ADR 0030 |
| BUG-0463 | 2026-08-28 | 2026-08-28 | Low | CMS-034 late preview cleanup feedback | Retrying strict cleanup after a late grant failure replaced the specific unused-session failure notice with a generic closed-window notice, breaking the retained lifecycle regression. | Kept one truthful message that identifies the unused preview session and the already-closed window across both cleanup attempts. The focused late-grant regression passes with its no-connect, no-token/history and revocation-failure assertions unchanged. | CMS-034; Unreleased/Fixed; ADR 0030 |
| BUG-0462 | 2026-08-28 | 2026-08-28 | Low | Local Windows test-host continuity | An unchanged full App-file diagnostic reported the release-composition test at 4,355.594 seconds and timed it out against 5 seconds. | Windows entered idle sleep during the test and returned from low power; Kernel-General records a 4,355.007-second hardware-clock correction, within 0.587 seconds of Vitest's reported duration. The same event class and proportional duration are present in BUG-0461's historical window. Classified as an interrupted local run; no application/test correction or timeout relaxation. Rerun while awake remains required. | CI-003; BUG-0461; Unreleased/Changed; ADR 0008 |
| BUG-0460 | 2026-08-26 | 2026-08-26 | Low | New CI-001 delayed-resize regression fixture | Native resize suppression and a page-world getter do not portably coordinate the browser test. | First fixture: Chromium loses the intended stale-state setup, Firefox stalls in setViewportSize, and WebKit assertions never release the held event through the getter. Replaced it with bounded stale innerWidth observation, preserving native resize propagation/accessors and restoring the original descriptor plus a normal event after 500ms fault injection. Real-width restoration, both transitions, already-open behavior, destination close, Pages return and desktop preferences are asserted. All six focused cases and all 48 complete browser scenarios pass; the corrected fixture also fails both cases on the exact old helper. No helper sleep, timeout relaxation or runtime change. | CI-001; Unreleased/Fixed; ADR 0008 |
| BUG-0458 | 2026-08-26 | 2026-08-26 | Medium | GitHub PR #13 mobile-navigation test race | A one-shot desktop ARIA read is mistaken for mobile drawer readiness, so the helper can skip opening the drawer or click the desktop compact control. | Supplied prior-head 0790711 log (run 32970401448 / job 98183440363, merge dee4303) fails WebKit's open-class assertion on all three attempts. Corrected real-page fault injection reproduces that same 5-second assertion against the exact old helper. The helper now checks the actual drawer class and waits for closed ARIA plus group disclosures before its native click; open ARIA/class/transform and every existing scenario remain. Six focused engine cases, full 48-case browser matrix without retries and three additional no-retry responsive sweeps pass; full quality rerun passes 626 tests with 17 existing optional skips. Latest 286979b job 98202664775's exact assertion was not supplied, and no new hosted run/push/merge is claimed. BUG-0461 separately retains the unrelated first-run unit failure. | CI-001; Unreleased/Fixed; ADR 0008 |
| BUG-0459 | 2026-08-26 | 2026-08-26 | Medium | Local CI-mode WebKit responsive sweep | Redundant standalone pre-click scrolling consumes the responsive scenario's test deadline. | The original full CI-mode run reports WebKit 13 passed / 1 flaky: a 300-second timeout followed by an unchanged 2.2-minute successful retry. Retained trace pw:api@1066 spends 172.16 seconds scrolling Marketplace at 768px, followed by a 571 ms successful normal click; the timeout occurs later without a containment assertion failure. Removed only the two separate helper scroll calls; normal click scrolling/actionability and every assertion/deadline remain. The source-preservation audit, 3/3 no-retry WebKit repeats (1.5m each), full 626-test quality/build gate and all 42 browser scenarios without retries pass. Original artifacts remain under ignored test-results/ci002-before. The browser/OS stall mechanism and equivalence to hosted BUG-0458 remain unproven; no broad timing guarantee or hosted fix is claimed. | CI-002; Unreleased/Fixed; ADR 0008 |
| BUG-0457 | 2026-08-26 | 2026-08-26 | High | CMS-033 native preview focus revalidation | Returning focus from the preview immediately suspends the editor before any changed authority is observed, closing the popup and dropping the input that restored focus. | The held-check regression failed before correction because window focus immediately hid the verified editor. Routine focus checks now preserve that lifetime and in-flight lease only while pending/unchanged; failure still suspends, and changed authority still evicts. Both new regressions and all 14 session tests pass; the three affected native Firefox scenarios pass in the 8-case focused run without assertion/timeout changes. Full gate evidence is recorded in ADR 0030. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0456 | 2026-08-26 | 2026-08-26 | Low | Existing navigation troubleshooting | Two retained pre-CMS-033 sentences claim invalid links always normalize to Pages and denied destinations retain a feature load error. | Corrected the two retained navigation sentences to describe first-permitted/no-access fallback and pre-loader denial/observed-403 boundaries. The read-only text regression rejects both stale claims and requires the current behavior; local documentation links resolve. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0455 | 2026-08-26 | 2026-08-26 | Low | Existing navigation 403 assertion | The old browser test expects private server error text and a mounted sidebar after a feature 403, contradicting CMS-033's approved session invalidation contract. | Updated the stale 403 expectation to the approved fail-closed session contract: generic boundary, one recheck, no private error/shell, and restored Identity navigation only after explicit retry. All preceding dirty/history/unavailable-entry assertions remain; all three engines pass. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0454 | 2026-08-26 | 2026-08-26 | Low | New viewer browser request assertion | The new test classifies every POST as a mutation, but the existing search query API is read-only POST. | Required exactly the existing read-only POST /api/v1/search and no other non-GET viewer request. Retained all disabled-operation and zero-HTTP-error checks; all three native engines pass. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0453 | 2026-08-26 | 2026-08-26 | High | CMS-033 late preview grant cleanup | Rejecting a stale preview-creation result hides its grant from the component before cleanup can revoke it. | Best-effort revoked a preview grant returned after its authority became stale before discarding that response. The previously failing regression now requires exactly one cleanup call and rejects the stale result without exposing it to another session. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0452 | 2026-08-26 | 2026-08-26 | Low | CMS-033 schema-denied page labels | Page-list-only users cannot distinguish entries when the title field requires unavailable schema metadata. | Used the authorized entry ID when schema-dependent title metadata is unavailable. The restricted fixture now requires distinct entry IDs and zero schema/detail reads; no schema labels or sample defaults are invented. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0451 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 denied-link readiness | The access-unavailable fallback is offered before private bootstrap/history are ready, so its click is ignored. | Offered the permitted fallback only after bootstrap/history readiness. The failing-before operations-only denied-link regression now navigates successfully, without requesting denied content metadata. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0447 | 2026-08-26 | 2026-08-26 | High | CMS-033 obsolete callback authority | A client captured by an old principal's component can invoke transport after a new principal is verified. | Bound every guarded client to its born private lifetime and verified source client. The failing-before obsolete-principal callback regression now rejects before transport; changed-authority in-flight results are also rejected. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0448 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 unchanged-authority 403 recovery | An observed 403 refresh immediately remounts private loaders even if the projection is unchanged, permitting repeated automatic denial cycles. | After an observed feature 403, evicted the private lifetime and rechecked once; unchanged authority now stops at explicit Retry access. Unit and three-engine browser checks require one denied request, one context recheck, no private shell and successful explicit recovery. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0449 | 2026-08-26 | 2026-08-26 | Low | New capability-map test fixture | The table fixture defines non-configurable own methods, violating JavaScript Proxy invariants before any guard assertion runs. | Made the generated method spies configurable, matching real SDK properties and avoiding JavaScript Proxy invariant violations. All 132 exact method allow/deny assertions remain and pass. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0445 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 guarded SDK dispatch | The adapter captures a method on first property access, so later injected SDK behavior is bypassed. | Kept stable guard wrappers but resolved the raw SDK method on each invocation. The existing creation and delayed-entry regressions now exercise later injected behavior and pass unchanged. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0446 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 unavailable deep-link retention | Bootstrap drops the requested entry parameters when an authorized destination cannot open that entry. | Preserved the unresolved requested entry parameters instead of constructing a destination-only startup address. All three existing missing/forbidden/wrong-type location regressions and native navigation recovery checks pass. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0444 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 initial operation adapter parity | Initial UI mappings used management/read assumptions for four routes with different explicit API actions. | Matched search-index read, governance approval, cutover validation and marketplace installation to the existing exact server operations. Explicit parity assertions and all 132 guarded-method allow/deny cases pass; no backend guard changed. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0450 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 federation adapter parity sibling | Agreement inspection/state controls initially use consume, but both existing API routes authorize federation.manage. | Mapped agreement inspection/state changes and their four controls to federation.manage. Exact parity assertions pass alongside the guard/control matrix; server authorization is unchanged. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0443 | 2026-08-26 | 2026-08-26 | Medium | CMS-033 in-progress JSX permission wrappers | The first compile check rejects unbalanced workflow/collaboration wrappers while adding optional sections. | Corrected the initial optional-section JSX closures and nullable metadata access. Strict types, all Studio regressions and production builds pass; no released version contained the initial compile error. | CMS-033; Unreleased/Fixed; ADR 0030 |
| BUG-0442 | 2026-08-26 | 2026-08-26 | Low | Navigation browser fixture repeat isolation | Repeated fixture titles made title-only entry selectors match two/three rows. | The original repeat run passed once, then failed twice on selector ambiguity before history traversal. Exact title/path/status locators now use the existing unique slug while retaining identical titles and every navigation assertion. All nine navigation cases in --repeat-each=3 and all 30 full browser cases pass; no first/nth fallback, timeout or skip was added. | CMS-032; Unreleased/Fixed; ADR 0030 |
| BUG-0441 | 2026-08-26 | 2026-08-26 | Medium | Deferred navigation focus / dirty history | A late Pages focus callback could take focus from the title before typing, leaving no dirty edit for Back to protect. | Two full WebKit runs failed dirty cancellation; the deterministic held-frame regression failed on unchanged App code with input focus stolen by main. Preserve newer focus, guard the current entry/destination/mount and cancel obsolete frames. The same regression plus three focus controls pass, as do the full 465-test gate, nine repeated WebKit navigation cases and all 30 full browser cases. Manual typing, settled cancellation/acceptance and preview lifecycle controls pass; tool limits are explicit in ADR 0030. Initial failure artifacts remain under ignored test-results/cms032-webkit-history. | CMS-032; Unreleased/Fixed; ADR 0030 |
| BUG-0440 | 2026-08-26 | 2026-08-26 | Low | CMS-032 regression fixture assertions | New tests used compile-only non-null assumptions, producing 18 lint warnings. | Replaced the assumptions with explicit runtime fixture checks in all three new test files; no assertions or rules were removed. The same lint command now has zero warnings, and all 461 workspace tests plus strict types/builds pass. | CMS-032; Unreleased/Fixed; ADR 0030 |
| BUG-0439 | 2026-08-26 | 2026-08-26 | Medium | CMS-032 topology type boundary | Zod optional properties permit explicit undefined, unlike the existing canonical exact-optional LocaleConfiguration type. | Normalize omission of all four optional locale properties at the configuration boundary, retaining strict canonical types. The API build that reported TS2379/TS2345/TS2322 now passes; the regression verifies raw parser controls retain those properties while normalized output omits them. Final focused 30-test API and full 461-test repository checks pass. | CMS-032; Unreleased/Fixed; ADR 0030 |
| BUG-0434 | 2026-08-26 | 2026-08-26 | High | Production public-route method dispatch | Method-blind raw break-glass activation matching admitted private DELETE revocation into admin validation without a session. | Baseline isolated DELETE activation returned 400/Incident ID required, while an ordinary account ID returned 401; no account was revoked. Match Fastify's resolved route and require POST for the public activation exception. Canonical, query and encoded DELETE variants now return 401, and independent review confirmed POST still reaches credential validation. Focused/full/30-browser gates pass. | AUTH-001; Unreleased/Security; ADR 0029 |
| BUG-0433 | 2026-08-26 | 2026-08-26 | Critical | Production identity / preview-token dispatch | Any preview-token prefix exempted unrelated management paths, whose unbound context synthesized development-admin identity. | Baseline 1de8211 memory-only context/schema checks returned 200/development admin with an invalid prefix versus 401 without it. Exact resolved route/method dispatch and explicit per-request development mode now return 401 with no privileged payload. Regressions cover alternate paths/methods and valid/invalid/expired/revoked grants; legitimate workforce, preview and published delivery controls pass. Full 439-test/30-browser verification and independent review pass; no live exploitation or deployment is claimed. | AUTH-001; CMS-003; Unreleased/Security; ADR 0029 |
| BUG-0438 | 2026-08-26 | 2026-08-26 | Low | Existing resolved-bug ledger formatting | The resolved table had a nine-column delimiter below an eight-column header. | Corrected only the delimiter. The same column-count regression first failed on the mismatch, then passed with header=8 and delimiter=8. Every historical bug record is preserved. | AUTH-001; Unreleased/Fixed |
| BUG-0437 | 2026-08-26 | 2026-08-26 | Low | AUTH-001 test lint hygiene | Four async fixture helpers used non-null assertions on the mutable suite server handle. | Captured the initialized server in a stable local handle. Repeated lint reports zero warnings and all 27 focused tests pass; no runtime behavior or assertion changed. | AUTH-001; Unreleased/Fixed |
| BUG-0436 | 2026-08-26 | 2026-08-26 | Low | AUTH-001 expanded test preconditions | New tests assumed a REST envelope for GraphQL, omitted required content fields and advertised JSON on bodyless DELETE controls. | Initial runs observed three then two failures. Confirmed existing GraphQL credential-specific authentication messages and null data; supplied a valid forbidden-write candidate and valid JSON DELETE bodies. The same 401/403, no-data, scope, revocation and legitimate-preview assertions now pass in the 27-test focused gate; ADR 0029 records the precondition decision. | AUTH-001; Unreleased/Fixed |
| BUG-0432 | 2026-08-26 | 2026-08-26 | Low | Browser verification scheduling | The WebKit all-destination sweep exceeded its unchanged five-minute deadline during overlapping repository-wide test execution. | Reran the final production/E2E build and browser gate without competing test jobs. The same all-destination/six-width sweep passes in 2.9 minutes and all 30 Chromium/Firefox/WebKit scenarios pass. No timeout, width or assertion changed; resource contention remains the inferred cause, not a proven product defect. | CMS-002; Unreleased/Fixed |
| BUG-0431 | 2026-08-26 | 2026-08-26 | Medium | Existing component migration selection | Migrating a different entry could replace the current dirty editor via refreshList without asking. | Reused shared discard confirmation before migrating another entry. The previously failing regression now sends no migration on cancel and retains the dirty draft. | CMS-002; Unreleased/Fixed |
| BUG-0430 | 2026-08-26 | 2026-08-26 | Medium | Entry-bound collaboration writes | Collaboration mutations did not participate in the shared navigation lock; a delayed branch/comment result could arrive after selection changed. A general busy boolean can also be cleared by overlapping destination reads. | Tracked entry-bound writes independently of general loading. The previously failing branch-write regression passes even while Workflows finishes loading; the 77-test Studio checkpoint passes. | CMS-002; Unreleased/Fixed |
| BUG-0428 | 2026-08-26 | 2026-08-26 | Low | New creation location test fixture | The test injected a newly created content record without a matching workflow fixture; the mock returned `{}` and the UI could not read schedules. | Provided the injected new entry with its valid workflow. The same creation success/failure/history assertions pass; ADR 0028 records the test-precondition correction. | CMS-002; Unreleased/Fixed |
| BUG-0429 | 2026-08-26 | 2026-08-26 | Low | CMS-002 browser fixture isolation | New synthetic pages expose ambiguous entry/relation selectors and an older seed-first assumption; fragment-only goto also failed to produce the intended cold-load test. | Scoped entry selectors, actually left the document for cold-load testing and explicitly selected the welcome publishing fixture. All four affected Chromium scenarios pass with assertions intact; ADR 0028 records the corrections. | CMS-002; Unreleased/Fixed |
| BUG-0426 | 2026-08-26 | 2026-08-26 | Low | CMS-002 lint gate | Integration lint flags a deliberately intercepted skip anchor, an implicit entry-ID effect dependency and the control-character rejection regex. | Documented the native skip-anchor exception, made quality entry dependency explicit and used numeric control-character checks. Biome and type gates pass without global relaxation. | CMS-002; Unreleased/Fixed |
| BUG-0425 | 2026-08-26 | 2026-08-26 | Medium | CMS-002 interrupted traversal | An internally rejected selection could supersede a pending Back request without restoring the already-changed browser address. | Restored diverged addresses for interrupted internal rejection and based pushes on the current known index. Interrupted and rapid traversal regressions pass. | CMS-002; Unreleased/Fixed |
| BUG-0424 | 2026-08-26 | 2026-08-26 | Medium | CMS-002 unknown history | An external fragment slot reused the accepted ownership/index even though its physical stack distance was unknown. | Unknown slots start a new ownership epoch. The failing owner assertion and owned/unowned traversal regressions pass. | CMS-002; Unreleased/Fixed |
| BUG-0423 | 2026-08-26 | 2026-08-26 | Medium | CMS-002 cancelled read | Reselecting the accepted entry aborted its replacement but retained the cancelled controller ref, silently rejecting further edits. | Cleared only the matching aborted controller. The failing delayed-read/reselection regression now permits edits and ignores late responses. | CMS-002; Unreleased/Fixed |
| BUG-0422 | 2026-08-26 | 2026-08-26 | Low | CMS-002 integration compilation | The new destination-loading ref was inserted before its metadata declaration and one sidebar call retained the old three-argument signature. | Moved the loading ref/effect after metadata and corrected the sidebar signature. Repeated TypeScript and repository checks pass. | CMS-002; Unreleased/Fixed |
| BUG-0421 | 2026-08-26 | 2026-08-26 | Medium | Studio search-result entry navigation | Search results bypass the unsaved-entry confirmation and replace editor state without opening the Pages destination. | Search/list clicks now use one guard; the baseline regression first observed zero prompts, then passed with draft/address preserved on cancel and Pages opened on acceptance. | CMS-002; Unreleased/Fixed |
| BUG-0420 | 2026-08-26 | 2026-08-26 | Low | Administration queue handoff | The summary still named completed CMS-001 as the next action, contradicting its verified task handoff. | Changed the pointer to CMS-002 proposal approval. The read-only regression first reproduced the old pointer, then verified its absence, the new pointer and CMS-002's planned status. All 136 task states are preserved; ledger/link/lint/format checks and the Studio build pass. | CMS-002; Unreleased/Fixed |
| BUG-0419 | 2026-08-26 | 2026-08-26 | Low | Studio minimum viewport width | A fixed 320px body minimum clipped content when a native scrollbar left only 305px usable width. | Replaced the unconditional body minimum with zero. The same Windows in-app viewport now has 305px body/client/scroll widths and a contained header. The new navigation regression asserts the body minimum and containment with a stable scrollbar gutter; all 21 browser scenarios and final builds pass. | CMS-001; Unreleased/Fixed |
| BUG-0418 | 2026-08-26 | 2026-08-26 | Low | Compact Studio sidebar | Help text escaped the 86px rail because the zero-specificity `:where(...)` hide rule lost to the footer grid rule. | Added an explicit compact footer-child selector and matching mobile grid restoration. Manual computed styles confirm hidden desktop text and mobile grid; the new browser regression checks both and expanded-mode restoration in Chromium, Firefox and WebKit. All 21 scenarios pass. | CMS-001; Unreleased/Fixed |
| BUG-0417 | 2026-08-26 | 2026-08-26 | Low | GOV-006 verification environment | Restricted Windows execution denied pnpm's worker and the read-only documentation audit's Git subprocess with `spawn EPERM`. | Reran through approved child-process execution without changing application code or tests. The full `pnpm check` and 13-category/19-destination/31-task/history/link audit pass; this is an execution-boundary issue, not a product regression. | GOV-006, FND-002; Fixed/verification |
| BUG-0416 | 2026-08-26 | 2026-08-26 | Low | Design-system authoring documentation | STUDIO-015 moved responsive authoring to the inspector but the guide retained the removed inline-preview toolbar instructions. | Updated only the guide to identify the Responsive override selector and header-only standalone preview. A source/guide comparison and text regression assertion reject the old toolbar claim and accept the current instructions; documentation audit and full repository gate pass. | GOV-006, STUDIO-015; Fixed |
| BUG-0415 | 2026-08-25 | 2026-08-25 | Low | STUDIO-015 SCSS source hygiene | Five mechanically split SCSS files retained one extra blank line at EOF. | Removed only the trailing blank lines; the repeated staged whitespace audit passes cleanly. | STUDIO-015, FND-002; Fixed |
| BUG-0414 | 2026-08-25 | 2026-08-25 | Low | Complete repository Studio test orchestration | One unrelated experiment lifecycle case reported its established timeout with an anomalous roughly 26-minute measured duration. | The identical isolated case passed in 5.45 seconds without a timeout/assertion change; the repeated complete gate passed all 385 active tests and every build. | STUDIO-015, TEST-001; Resolved |
| BUG-0413 | 2026-08-25 | 2026-08-25 | Low | STUDIO-015 delivery formatting | Four changed TS/TSX files needed mechanical formatter layout. | Applied Biome's exact output; the 317-file format check and complete delivery gate pass. | STUDIO-015, FND-002; Fixed |
| BUG-0412 | 2026-08-25 | 2026-08-25 | Medium | WebKit standalone preview startup | The popup fixture expected the original seeded hero although a preceding engine had legitimately published a different heading into the shared E2E state. | Assert application preview markers instead of mutable seeded copy, then prove exact subsequent live edits. WebKit and the canonical three-engine vertical slice pass. | STUDIO-015, TEST-005; Fixed |
| BUG-0411 | 2026-08-25 | 2026-08-25 | Medium | Studio danger CTA contrast | The canonical late danger variant retained only its radius after duplicate foundation rules were removed. | `_cta.scss` now owns the danger background, border, foreground, and radius together; WebKit Axe and all 18 final browser scenarios pass. | STUDIO-015, TEST-005; Fixed |
| BUG-0410 | 2026-08-25 | 2026-08-25 | Low | STUDIO-015 Firefox computed-style fixture | Firefox serialized equivalent input/select vertical padding at different subpixel precision. | Normalize only numeric padding to one decimal while keeping exact color/border/radius/minimum-height assertions; Firefox and the complete final matrix pass. | STUDIO-015, TEST-005; Fixed |
| BUG-0409 | 2026-08-25 | 2026-08-25 | Medium | Studio reduced-motion form controls | Global form transitions remained active under reduced motion because the accessibility override had lower specificity. | Added specificity-matched input/textarea/select overrides in `_accessibility.scss`; the diagnostic reports no moving elements at 200% zoom/forced colors in all engines. | STUDIO-015, TEST-005; Fixed |
| BUG-0408 | 2026-08-25 | 2026-08-25 | Low | STUDIO-015 browser regression fixture | The new computed-style assertion queried a field label absent from the E2E schema. | Target the rendered `Title` textbox and retain the complete text/select comparison; all engines pass. | STUDIO-015, TEST-005; Fixed |
| BUG-0407 | 2026-08-25 | 2026-08-25 | Low | STUDIO-015 focused build / Windows sandbox | The restricted sandbox denied Vite's Windows real-path child process. | Reran the unchanged focused and complete builds through the approved child-process boundary; Sass compilation and all production/E2E builds pass. | STUDIO-015, TEST-004; Resolved |
| BUG-0406 | 2026-08-25 | 2026-08-25 | Low | STUDIO-015 dependency installation | The restricted pnpm invocation selected a different store than existing workspace links. | Installed only `sass@1.103.1` using the established pnpm v10 store; package and lockfile are reproducible and every install-dependent build passes. | STUDIO-015, FND-003; Resolved |
| BUG-0405 | 2026-08-25 | 2026-08-25 | Medium | Studio layout spacing | Equivalent Studio stacks used unrelated or absent gaps. | Added shared spacing tokens and canonical card/heading/field/presentation rules; all 19 destinations remain contained and logically separated across six widths and both themes in every engine. | STUDIO-015; Fixed |
| BUG-0404 | 2026-08-25 | 2026-08-25 | Medium | Studio form-control styling | Feature-specific rules caused ordinary text inputs and selects to render inconsistently. | `_form.scss` now owns the complete scoped ordinary-control contract with specialized-input exclusions and search reset; exact computed parity plus focus/disabled/dark/forced-color regressions pass in all engines. | STUDIO-015; Fixed |
| BUG-0403 | 2026-08-25 | 2026-08-25 | Low | STUDIO-014 WebKit verification timing | The complete WebKit run reached the existing 240-second timeout in the route-wide Axe test without emitting an accessibility assertion. | Retained the established timeout and reran the identical Axe scenario alone; it passed unchanged in 3.9 minutes. The original full run's other four WebKit scenarios also passed, including the expanded all-route/six-width containment sweep, so all 5/5 WebKit scenarios have passing evidence without a weakened assertion or timeout. | STUDIO-014, TEST-005; Resolved |
| BUG-0402 | 2026-08-25 | 2026-08-25 | Medium | Studio Releases / Firefox responsive containment | Restoring real intrinsic word widths exposed a 29px release-entry picker overflow at 320px in Firefox. | Gave all Studio fieldsets a zero minimum inline size, bounded the saved-entry flex descendants, and retained aggressive wrapping only for the UUID metadata. The focused all-route Firefox regression and a clean complete 5/5 Firefox rerun pass at all six widths, dark mode, WCAG, keyboard, 200% zoom, forced colors, and published rendering. | STUDIO-014, BUG-0400; Fixed |
| BUG-0401 | 2026-08-25 | 2026-08-25 | Low | STUDIO-014 focused verification / Windows sandbox | The restricted process sandbox denied the E2E TypeScript/Vite child-process spawn before the failing visual regression could run. | Reran the unchanged `pnpm build:e2e` command through the approved Windows child-process path; Studio and example application TypeScript checks and E2E-mode Vite builds pass. | STUDIO-014, TEST-005; Resolved |
| BUG-0400 | 2026-08-25 | 2026-08-25 | Medium | Studio global typography and responsive layout | Normal sentences and words could render one character per line in narrow grid tracks, including the collaboration operation/version summary. | Replaced the broad normal-language `anywhere` policy with `break-word`, kept technical code containment explicit, and moved collaboration metadata to a full-width row. The regression failed before the fix on computed `anywhere`; afterward live light/dark 1280px inspection shows one readable summary line with zero root overflow, and the 15/15 cross-browser scenario evidence plus complete repository gate pass. | STUDIO-014, STUDIO-010; Fixed |
| BUG-0399 | 2026-08-25 | 2026-08-25 | Low | STUDIO-013 delivery formatting | The complete delivery gate rejected the new dark-theme option-card state selector's multiline layout before type, test, or build phases ran. | Applied Biome's exact mechanical selector layout and reran the unchanged complete gate; formatting and every subsequent type, test, and production-build phase pass. | STUDIO-013, FND-002; Fixed |
| BUG-0398 | 2026-08-25 | 2026-08-25 | Medium | Studio authoring button system | Asset and relation choices render with native browser borders, padding, and typography, while sibling authoring actions depend on a container-specific fallback instead of the global Studio button taxonomy. | Applied the global base plus explicit outline/option-card, secondary, danger, compact, and icon variants to every authoring button; removed the masking container fallback; added pressed semantics and regressions requiring zero classless authoring buttons. The 33/33 Studio suite and complete 15/15 Chromium/Firefox/WebKit responsive, state, zoom, keyboard, WCAG, and published-rendering suite pass; live 320px inspection shows 228px contained cards with no overflow or browser errors. | STUDIO-013, STUDIO-010; Fixed |
| BUG-0397 | 2026-08-25 | 2026-08-25 | Medium | STUDIO-012 WebKit vertical slice | WebKit passed the new preview-only popup and live-update assertions but did not render the later draft-save confirmation within the established five-second assertion window. | Retained failure state showed the requested save had produced immutable revision 2, and an unchanged isolated WebKit rerun passed the complete popup, save, workflow, publish, and delivery slice in 7.8 seconds; the one-off confirmation miss is non-reproducible. | STUDIO-012, TEST-005; Resolved |
| BUG-0396 | 2026-08-25 | 2026-08-25 | Low | STUDIO-012 focused verification / Windows sandbox | The restricted process sandbox denied Vitest's child-process spawn before the focused Studio suite could load its Vite configuration. | Reran the identical focused command through the approved Windows child-process path; both Studio files and all 33 tests pass. | STUDIO-012, TEST-003; Resolved |
| BUG-0395 | 2026-08-25 | 2026-08-25 | Medium | E2E vertical-slice infrastructure | The isolated three-engine authoring-to-delivery command reached Playwright server readiness but the Studio then showed `Failed to fetch` before any test interaction. | Traced the failure to running Playwright after `pnpm check` had replaced the E2E bundle with a production bundle targeting the normal API, then confirmed that one combined multi-project run also reuses intentionally mutated in-memory content. Rebuilt with `pnpm build:e2e` and ran Chromium, Firefox, and WebKit separately with fresh server state; all 3/3 delivery slices pass. | STUDIO-011, TEST-005; Fixed |
| BUG-0394 | 2026-08-25 | 2026-08-25 | Medium | Studio navigation accessibility | The sidebar's 150ms simultaneous background and foreground transition passes through an intermediate 3.96:1 text contrast state while current destinations change, below the 4.5:1 requirement for its 13.6px labels. | Removed foreground/background interpolation from destination items while retaining their settled reference-theme states; sequential Axe analysis passes every destination in Chromium, Firefox, and WebKit. | STUDIO-011, STUDIO-007; Fixed |
| BUG-0393 | 2026-08-25 | 2026-08-25 | High | Studio management feedback | Management actions write success and error feedback to a notice mounted only inside the Pages editor, so correcting page exclusivity makes action feedback disappear on every management destination. | Rendered management feedback at the management-page boundary while retaining the existing Pages notice; all 33 Studio tests pass, including every formerly hidden management success path. | STUDIO-011, STUDIO-006; Fixed |
| BUG-0392 | 2026-08-25 | 2026-08-25 | Medium | Studio browser regression | The complete-destination visual test deliberately opened every sidebar item and then measured all accumulated surfaces, but never asserted that the previous selection and page were removed; it therefore encoded the broken multi-panel behavior as success. | Replaced the accumulated-panel setup with sequential navigation helpers that require exactly one current item and page surface, removal of the previous region, and no Pages workspace for every management destination at five widths and in dark mobile mode; 15/15 cross-browser UI checks pass. | STUDIO-011, TEST-005; Fixed |
| BUG-0391 | 2026-08-25 | 2026-08-25 | High | Studio navigation | Every management destination owns an independent open/data flag and the Pages workspace is unconditional, so selecting Workflows and then Releases leaves both sidebar buttons selected, both management regions visible, and Pages content on the same page. | Introduced one active Studio destination, gated every management surface and the Pages workspace to it, retained loaded feature state across navigation, and exposed exactly one `aria-current="page"`; live desktop and all-destination mobile verification plus unit/browser suites pass. | STUDIO-011, STUDIO-001; Fixed |
| BUG-0390 | 2026-08-25 | 2026-08-25 | Medium | Studio form-card responsiveness | WebKit propagates intrinsic label, legend, and native-control widths through fieldsets in the AI, Migration, Targeting, and Experiments panels at 320px, giving otherwise contained 294px cards 300–306px scroll widths. | Bounded Studio fieldsets and their intrinsic inline overflow after wrapping all visible text and custom-sizing controls; all four surfaces pass the exact 320px WebKit surface check and the final 15/15 browser suite. | STUDIO-010, TEST-004; Fixed |
| BUG-0389 | 2026-08-25 | 2026-08-25 | Medium | Studio experiment responsiveness | The long “Aggregate metric evidence and promotion” fieldset legend uses WebKit's intrinsic legend width at 320px and reaches x=326, escaping the Experiment card. | Added a targeted semantic hook and mobile inline width for the long metric legend while preserving its full text; the final uncontained-element regression passes in all three engines. | STUDIO-010, TEST-004; Fixed |
| BUG-0388 | 2026-08-25 | 2026-08-25 | Medium | Studio migration responsiveness | At 320px, WebKit includes native migration-select option width in the root scroll extent even though each visible select box is contained, widening the page to 326px and risking hidden option text. | Replaced native select appearance with the shared bounded Studio control and an explicit dropdown indicator; every option remains available and the final 320px migration/root checks pass. | STUDIO-010, TEST-004; Fixed |
| BUG-0387 | 2026-08-25 | 2026-08-25 | Medium | Studio workflow responsiveness | Browser-default list padding and 130px workflow-state cards place the third state at x=326 on a 320px viewport; WebKit propagates that leading edge into a reachable 6px root scroll despite the strip's intended local scrolling. | Reset list padding, bounded the scroll strip, tightened only the narrowest state cards, and clipped root overflow while retaining local keyboard scrolling; the exact sideways-scroll assertion returns x=0. | STUDIO-010, TEST-004; Fixed |
| BUG-0386 | 2026-08-25 | 2026-08-25 | Medium | Studio responsive preview | At a 768px viewport WebKit lays out the Standalone preview-mode action beyond the right edge of its preview card even though Chromium and Firefox contain it. | Stacked the responsive preview toolbar and allowed its controls to wrap within the preview card; the 768px WebKit control-boundary regression passes. | STUDIO-010, TEST-004; Fixed |
| BUG-0385 | 2026-08-25 | 2026-08-25 | Medium | Studio responsive marketplace | At a 768px viewport WebKit gives the Marketplace surface 474px of scrollable content inside a 464px card, hiding the last 10px despite Chromium and Firefox fitting it. | Removed the mobile content-margin transition's intermediate layout from responsive measurement and bounded card children; the 768px WebKit surface regression passes. | STUDIO-010, TEST-004; Fixed |
| BUG-0384 | 2026-08-25 | 2026-08-25 | Medium | Studio visual system | Multiple Studio management surfaces show unstyled or inconsistent buttons, while long headings, labels, values, and metadata can be clipped, hidden, or escape their cards/sections at narrower widths. | Applied the existing button variants to every outlier, normalized wrapping/min-widths, stacked narrow heading actions, and rebuilt the comment composer without removing behavior; unit assertions and the 15/15 browser matrix pass. | STUDIO-010, TEST-004; Fixed |
| BUG-0383 | 2026-08-25 | 2026-08-25 | Low | STUDIO-009 semantic search icon | Wrapping the topbar form in the semantic `search` element moves its SVG one level deeper than the shared direct-child stroke selector, so the icon falls back to a filled black path that appears as a dot. | Applied the shared fill/stroke contract to nested search SVGs, verified the exact `none` fill and muted stroke in the final in-app browser smoke, and pinned both values in the 12/12 three-engine UI suite. | STUDIO-009, TEST-004; Fixed |
| BUG-0382 | 2026-08-25 | 2026-08-25 | Low | STUDIO-009 WebKit dark editor controls | Rich-text formatting, removal, and block-adder buttons rely on the browser's default button surface; WebKit resolves that surface to `#c0c0c0` under the dark shell while inheriting white text, producing 1.81:1 contrast. | Defined explicit reference-theme surfaces, borders, text, geometry, and radii for the editor buttons; the exact final Chromium/Firefox/WebKit suite passes 12/12 including expanded dark Axe scans. | STUDIO-009, TEST-004; Fixed |
| BUG-0381 | 2026-08-25 | 2026-08-25 | Low | STUDIO-009 semantic lint | The new topbar uses a form with a redundant search role, the avatar label lacks an explicit supported role, and the intentionally focusable horizontal workflow map needs a documented exception to the generic noninteractive-tabindex rule. | Used the semantic `search` element, gave the labelled avatar an image role, and documented the exact Safari-overflow focus exception; the final complete `pnpm check` passes lint, ledgers, format, types, 384 runnable tests, and all builds. | STUDIO-009, FND-002; Fixed |
| BUG-0380 | 2026-08-24 | 2026-08-25 | Medium | STUDIO-009 complete dark theme | Expanded management panels and editor helpers retain legacy light surfaces and gray/indigo copy beneath the new dark shell, creating widespread contrast failures; the dark preview surface also changes consumer-rendered content without changing that content's own palette. | Bound every legacy inner panel/editor surface and text role to dark tokens, retained semantic warning colors, and restored the consumer preview's independent light palette; expanded light/dark Axe scans and the final three-engine suite pass 12/12. | STUDIO-009, TEST-004; Fixed |
| BUG-0379 | 2026-08-24 | 2026-08-24 | Medium | STUDIO-009 workflow keyboard access | The horizontal workflow-state map can overflow after the card/padding conversion but has neither a focusable child nor its own keyboard focus target. | Made the labelled state map itself keyboard-focusable so horizontal overflow remains operable without changing its content; the complete unchanged Chromium accessibility/keyboard suite passes 4/4. | STUDIO-009, TEST-004; Fixed |
| BUG-0378 | 2026-08-24 | 2026-08-24 | Low | STUDIO-009 workflow contrast | Workflow transition route text kept the legacy `#6b7280` color after its card moved to the reference soft surface, producing 4.45:1 instead of WCAG AA's 4.5:1 threshold. | Bound route text to the verified reference-theme muted token; the expanded-panel Axe pass reports no contrast violations and the complete Chromium suite passes 4/4. | STUDIO-009, TEST-004; Fixed |
| BUG-0377 | 2026-08-24 | 2026-08-24 | Medium | STUDIO-009 search target geometry | The desktop search field's visual container is 40px high, but the nested interactive input has only a 21px client rectangle and therefore misses the repository's 24px minimum target assertion. | Extended the search input through its 40px control container while retaining the same visual border; the unchanged minimum-target measurement and complete Chromium suite pass. | STUDIO-009, TEST-004; Fixed |
| BUG-0376 | 2026-08-24 | 2026-08-24 | Low | STUDIO-009 selected entry contrast | The active entry's muted path and published-status text render at 4.3:1 and 4.2:1 respectively against their composited green backgrounds, below WCAG AA's 4.5:1 threshold for their sizes. | Darkened only the selected metadata and light-theme success text while preserving the reference palette; default and expanded Axe scans pass with the complete 4/4 Chromium suite. | STUDIO-009, TEST-004; Fixed |
| BUG-0375 | 2026-08-24 | 2026-08-24 | Low | STUDIO-009 responsive navigation | The mobile-only navigation close control inherited the later generic icon-button display rule and remained visible in the desktop brand row. | Gave the close control a navigation-scoped display rule and retained the explicit mobile override; desktop browser inspection at 1280×720 hides it while the 390×844 drawer keeps an accessible close action. | STUDIO-009; Fixed |
| BUG-0374 | 2026-08-24 | 2026-08-24 | Low | M8-004 cross-browser accessibility sweep | Adding Fleet to the intentionally broad Studio panel sweep pushed WebKit's complete two-pass Axe and UI-state traversal just beyond Playwright's default 30-second test budget. | Gave only the broad multi-panel accessibility case an explicit 60-second budget; its unchanged assertions pass in WebKit in 22.8 seconds, and the complete WebKit project passes 4/4 after Chromium and Firefox already passed 4/4. | M8-004, TEST-004; Fixed |
| BUG-0373 | 2026-08-24 | 2026-08-24 | Medium | Complete workspace test orchestration | Root test orchestration ran all package suites concurrently, so the long Studio interaction suite could be CPU-starved by the API/core pools and exceed its established per-test timeout despite no assertion failure. | Serialized package suites in the root test command while preserving package-internal parallelism; Studio passes 31/31 alone, and the serialized root run passes schema 80/80, client 30/30, core 163/163, React 5/5, and API 74/74 before entering Studio without a competing package suite. | M8-004, TEST-001; Fixed |
| BUG-0372 | 2026-08-24 | 2026-08-24 | Medium | Complete API test orchestration | The unbounded API Vitest fork pool could over-subscribe Windows process resources, preventing test workers from starting and starving the observability fixture past its five-second limit. | Bounded the API Vitest pool to four workers in package-local configuration; both the diagnostic invocation and unchanged standard API test command pass all 74 runnable tests with no worker-start or observability timeouts. | M8-004, TEST-001; Fixed |
| BUG-0371 | 2026-08-24 | 2026-08-24 | Low | M8-004 generated interoperability artifacts | The canonical JSON generator emits stable two-space JSON, but the repository formatter attempts a second line-width rewrite and fails the complete delivery gate. | Excluded only generator-owned `specifications/v1` artifacts from Biome's second formatting pass while retaining exact drift enforcement through `schema:check`; both focused gates pass and all eight artifacts match. | M8-004, TEST-001; Fixed |
| BUG-0370 | 2026-08-24 | 2026-08-24 | Low | M8-004 archive validation compatibility | Running canonical shape parsing before the established semantic archive validator replaced its specific published-limit error with a generic shape error. | Kept the established semantic validator as the compatibility-preserving public validation path while applying canonical shape parsing at the untrusted archive-ingress boundary; the focused portability suite passes 3/3 with its specific limit diagnostic restored. | M8-004, TEST-001; Fixed |
| BUG-0369 | 2026-08-24 | 2026-08-24 | Low | M8-004 archive contract refactor quality | Moving archive contracts to the schema package left four now-unused type imports in core, so the complete lint gate was not warning-free. | Removed only the unused local bindings while retaining public type re-exports; the unchanged complete lint, boundary, ledger, security, scope, and readiness chain passes without warnings. | M8-004, FND-002; Fixed |
| BUG-0368 | 2026-08-24 | 2026-08-24 | Medium | M8-004 fleet optimistic persistence | The new fleet service inferred repository absence from version `0`, so the first mutation after restoring an explicitly persisted empty document attempted an insert and conflicted. | Determine insert/update behavior from actual persisted presence, parse the final document before saving, and cover a restored version-zero first mutation; the focused fleet service suite passes 4/4. | M8-004, TEST-001; Fixed |
| BUG-0367 | 2026-08-24 | 2026-08-24 | Low | M8-004 discovery regression fixture | The new minimized-discovery assertion rejected the legitimate public specification kind `logical-content-archive` because it searched for the broad word `content`. | Restricted the negative check to actual sensitive scope, credential, token, and draft-revision property names; the unchanged focused schema suite passes 3/3. | M8-004, TEST-001; Fixed |
| BUG-0366 | 2026-08-24 | 2026-08-24 | Low | M8-004 fleet event typing | The new checked-event literal widened to `string` inside the fleet document transform, preventing the core package from compiling. | Retained `member.checked` as a literal in the bounded fleet event union; the unchanged `pnpm --filter @gridstory/core build` regression gate passes. | M8-004, TEST-001; Fixed |
| BUG-0365 | 2026-08-24 | 2026-08-24 | High | M8-003 pending agent execution reconciliation | A crash-recovery retry whose target mutation had not yet happened could continue an already-pending plan without rechecking expiry or the current knowledge/AI policy, allowing a previously approved mutation after its governing policy was disabled or expired. | Revalidate expiry plus the current knowledge policy, prompt, model, and runtime before a pending retry performs any still-needed draft mutation; focused recovery regressions prove both disabled-policy and expired-plan retries fail closed without changing content. | M8-003, THREAT-0037, GS-SEC-041; Fixed |
| BUG-0364 | 2026-08-24 | 2026-08-24 | Low | M8-003 core code quality | The new knowledge service retained an unused `KnowledgeAgentPolicy` type import, so the complete lint gate reported a warning. | Removed the unused type import; the complete lint, boundary, ledger, security, tenant-scope, and readiness chain reruns without warnings. | M8-003, FND-002; Fixed |
| BUG-0363 | 2026-08-24 | 2026-08-24 | Low | M8-003 core graph regression fixture | The new cycle-safety fixture created forward content references before their targets existed, so established reference-integrity validation correctly rejected setup before knowledge traversal ran. | Created the entries without references, then formed the cycle through valid revision-checked draft updates; both focused graph and reviewed-execution cases pass. | M8-003, TEST-001; Fixed |
| BUG-0362 | 2026-08-24 | 2026-08-24 | High | M8-003 core knowledge-agent projection | The new bounded `content.get` tool projection omitted the closing parenthesis for its `Object.fromEntries` call, preventing the core package from compiling. | Closed the bounded field projection expression; the unchanged focused schema/core build now completes successfully. | M8-003, TEST-001; Fixed |
| BUG-0361 | 2026-08-24 | 2026-08-24 | Low | M8-002 recovery regression fixture | The new direct federation backup fixture referenced an undefined `now` identifier after opening its SQLite repository, failing the assertion and leaving the file handle locked during cleanup. | Used an explicit deterministic timestamp and guaranteed direct repository closure through `finally`; all 10 live-WAL recovery, shutdown, worker, and rollout scenarios pass. | M8-002, TEST-002; Fixed |
| BUG-0360 | 2026-08-24 | 2026-08-24 | Low | M8-002 core code quality | Focused Biome validation found two unused federation-service imports and three equivalent guards that should use the repository's optional-chain style. | Removed the stale imports and used the equivalent optional-chain guards; focused Biome validation reports no diagnostics. | M8-002, FND-002; Fixed |
| BUG-0359 | 2026-08-24 | 2026-08-24 | Low | M8-002 API regression fixture | The live-delivery fixture requested its proposed `page-a` ID even though the established create route assigns and returns the durable entry ID. | Requested and asserted the returned durable ID and the content-type canonical attribution path; the complete signed producer-to-consumer API workflow passes. | M8-002, TEST-003; Fixed |
| BUG-0358 | 2026-08-24 | 2026-08-24 | High | M8-002 producer API composition | The API composed only the example page schema, whose rich-text and component-tree fields are intentionally excluded from federation, leaving a configured producer unable to create any offer. | Added one injected schema inventory shared by content, quality, search, lifecycle, and federation composition while preserving the example-page default; the producer API now offers an explicitly supported deployed text schema and the signed live workflow passes. | M8-002, TEST-003; Fixed |
| BUG-0357 | 2026-08-24 | 2026-08-24 | High | M8-002 federation API integration | The execute-plan route passed the validated execution body where the service requires the actor ID, preventing strict API compilation. | Passed the authenticated actor ID before the strict execution input; the focused API TypeScript build completes successfully. | M8-002, TEST-003; Fixed |
| BUG-0356 | 2026-08-24 | 2026-08-24 | High | M8-002 federated type identity | The producer calculated a schema fingerprint before canonical parsing materialized default `objects` and `taxonomies`, so consumers rejected every otherwise valid signed type as a fingerprint mismatch. | Canonically parsed the deployed schema before computing and signing its fingerprint; all four focused core cases pass for live delivery, mirror creation/retry/withdrawal, hostile scope evidence, and SQLite persistence. | M8-002, THREAT-0036, GS-SEC-040; Fixed |
| BUG-0355 | 2026-08-24 | 2026-08-24 | High | M8-002 federation signing identity | Strict schema parsing trimmed the persisted PEM public key while the injected signer retained the normal trailing newline, so every enabled producer offer failed with `content_federation_signer_unavailable`. | Preserved the exact bounded PEM string through strict parsing; all focused schema cases pass and producer/consumer tests advance through signed-offer key matching to type verification. | M8-002, THREAT-0036, GS-SEC-040; Fixed |
| BUG-0354 | 2026-08-24 | 2026-08-24 | Low | M8-002 schema regression fixture | The external-type regression expected its pre-parse object exactly even though canonical content-schema parsing adds the established empty `objects` and `taxonomies` defaults. | Asserted the meaningful descriptor contract while retaining canonical default materialization; all three focused federation schema tests pass. | M8-002, TEST-001; Fixed |
| BUG-0353 | 2026-08-24 | 2026-08-24 | Low | M8-002 focused verification / Windows sandbox | The restricted process sandbox denied pnpm's TypeScript child-process spawn before the new schema or core packages could compile. | Reran the identical focused build through the approved child-process path; both schema and core TypeScript builds completed successfully. | M8-002, TEST-001; Resolved |
| BUG-0352 | 2026-08-24 | 2026-08-24 | High | M8-001 regional localized delivery | A regional reader session permitted same-tenant fallback-locale input, but list/slug output validation compared the returned entry with the session's original locale rather than the explicitly requested fallback locale, causing valid localized regional fallback reads to fail closed. | Validate list/slug output against the explicitly requested locale while retaining the session's base tenant fence; the focused RegionalService suite passes all 8 cases including an `en` session reading a valid `fr` fallback and rejecting cross-tenant output. | M8-001, THREAT-0035, GS-SEC-039; Fixed |
| BUG-0351 | 2026-08-24 | 2026-08-24 | Low | M8-001 browser regression | The new Regions-panel color assertion copied the AI note's orange value instead of the panel's established warning token, so Chromium failed although the intended neutral palette rendered correctly. | Corrected only the assertion to the existing regional warning token; all 12 Chromium, Firefox, and WebKit accessibility/keyboard/published-rendering/vertical-slice scenarios pass, including neutral regional surface/content colors. | M8-001, TEST-004; Fixed |
| BUG-0350 | 2026-08-24 | 2026-08-24 | Low | M8-001 code quality | Repository lint found a translation-result `forEach` callback that implicitly returned the assertion value, plus an optional-chain simplification and two unused regional import/catch bindings. | Made the assertion callback explicitly void, applied the equivalent optional chain, and removed unused bindings; the complete lint, boundary, ledger, security, tenant, and readiness chain passes with no warnings. | M8-001, FND-002; Fixed |
| BUG-0349 | 2026-08-24 | 2026-08-24 | Low | M8-001 PostgreSQL verification environment | The disposable PostgreSQL gate initially could not start because the Docker Desktop Linux engine pipe did not exist, so no PostgreSQL project test or restore assertion executed. | Started the installed Docker Desktop engine in the background, waited for bounded readiness, and reran unchanged: 18 core and 2 API PostgreSQL tests plus the native logical backup/restore drill passed with regional topology recovered. | M8-001, TEST-002; Resolved |
| BUG-0348 | 2026-08-24 | 2026-08-24 | High | M8-001 regional cache partition | A well-shaped regional cache attestation was accepted as shared-cache evidence without recomputing its digest from the actual scope, served region, consistency, topology, and delivered content revision, so an unrelated/static attestation could mark a response shareable. | Added a canonical exported response-partition digest helper and require an exact digest match at indicator time; the regression proves exact evidence is shared while a valid-looking mismatched digest remains private, with all 8 RegionalService cases passing. | M8-001, THREAT-0035, GS-SEC-039; Fixed |
| BUG-0347 | 2026-08-24 | 2026-08-24 | High | M8-001 regional published-reader boundary | The regional result validator required a published revision ID but did not explicitly reject a hostile adapter entry whose `status` was `draft`, so contradictory perspective evidence could cross the published-only least-authority boundary. | Reject `draft` adapter results while retaining valid `changed` metadata for an older exact published revision; the expanded RegionalService suite passes all 8 draft, scope, cache, fallback, emergency, ambiguity, and hostile-adapter cases. | M8-001, THREAT-0035, GS-SEC-039; Fixed |
| BUG-0346 | 2026-08-24 | 2026-08-24 | Low | README capability/limitations | The README current-limitations paragraph still said semantic search and AI suggestions/provenance/evaluation/human approval were absent even though M7-005 delivered those bounded reviewed features. | Added the implemented reviewed-authoring/private-semantic and regional capabilities, linked both guides/routes, replaced the stale M7-005 exclusions, and narrowed regional limitations to unprovisioned/uncertified infrastructure and automation. Exact stale-phrase audit returns no matches. | M8-001, DOC-001; Fixed |
| BUG-0345 | 2026-08-24 | 2026-08-24 | Low | Security threat-model narrative | The reviewable threat-model document listed only boundaries 1-15 even though canonical AI boundaries 16-17 existed, and its M7-005 residual paragraph repeated analytics risks rather than AI authoring/semantic risks. | Added AI boundaries 16-17 and regional boundary 18, restored the AI-specific residual-risk statement, and validated the updated narrative/canonical model with `pnpm security:check`. | M8-001, DOC-002; Fixed |
| BUG-0344 | 2026-08-24 | 2026-08-24 | Low | M8-001 focused regression | The new hostile-adapter regression asserted a nonexistent `status` field instead of GridStoryError's established `statusCode`, so the application returned the correct error but the test failed. | Asserted the established `statusCode`; the focused RegionalService suite passes all 6 tests. | M8-001, TEST-001; Fixed |
| BUG-0343 | 2026-08-24 | 2026-08-24 | High | M8-001 failover adapter boundary | Malformed failover readiness or execution evidence was parsed outside the adapter error-normalization boundary, so a hostile adapter could surface a generic HTTP 500 path instead of the stable regional 503 contract and place schema diagnostics in server logs. | Safe-parse and normalize malformed readiness/results into generic 503 errors, retain an invalid execution result as ambiguous for reconciliation, and disclose no adapter payload; focused RegionalService tests pass all 6 cases. | M8-001, THREAT-0035, GS-SEC-039; Fixed |
| BUG-0342 | 2026-08-24 | 2026-08-24 | High | M8-001 regional schema initialization | The regional policy input was derived with `.pick()` from a refined Zod object, which type-checked but threw during module initialization and prevented every schema/core test from loading. | Defined the strict policy input independently with the same unique-region and enabled bounded-read refinements; all 3 regional schema tests and 7 regional repository/service tests now load and pass. | M8-001, TEST-001; Fixed |
| BUG-0341 | 2026-08-24 | 2026-08-24 | Low | M8-001 focused verification / Windows sandbox | The restricted process sandbox denied Vitest fork-worker creation before the new regional schema or core tests could load. | Reran the identical focused commands through the approved child-process path; Vitest workers started and reached module/test collection, exposing the separate application defect BUG-0342. | M8-001, TEST-001; Resolved |
| BUG-0340 | 2026-08-24 | 2026-08-24 | Low | Studio AI page/content palette | The AI workbench introduced a feature-specific purple page surface and purple body copy instead of preserving the established neutral Studio page/content colors requested by the user. | Restored the established neutral Studio surface/text/input/border colors and used a low-specificity supporting-text selector so the safety warning keeps its intended orange text. Direct computed-style assertions pass in Chromium, Firefox, and WebKit together with 9/9 accessibility scenarios, 27 Studio tests, and the full repository gate. | STUDIO-008, M7-004; Fixed |
| BUG-0339 | 2026-08-24 | 2026-08-24 | Low | M7-005 dependency verification / restricted network | The production dependency audit could not reach the npm audit endpoint from the restricted network and entered a long retry loop. | Stopped the infrastructure-only retry and reran the identical audit with approved network access; npm reported no known production vulnerabilities. | M7-005, TEST-005; Resolved |
| BUG-0338 | 2026-08-24 | 2026-08-24 | Low | M7-005 E2E verification / Windows sandbox | The restricted process sandbox denied a child process while the two E2E applications were building, before either Vite build completed. | Reran the identical `pnpm build:e2e` command through the approved child-process path; both Studio and example-site E2E builds completed, followed by 9/9 Chromium/Firefox/WebKit tests. | M7-005, TEST-004; Resolved |
| BUG-0337 | 2026-08-24 | 2026-08-24 | Low | M7-005 E2E config inventory / Windows | A Playwright configuration inventory repeated the Unix-style wildcard-path mistake from BUG-0331, so Windows rejected the literal path before searching. | Used `rg --files -g 'playwright.config.*'`, inspected the resolved root configuration, and ran all three declared browser projects successfully. | M7-005, BUG-0331; Fixed |
| BUG-0336 | 2026-08-24 | 2026-08-24 | Low | M7-005 security documentation | The M7-004 ASVS and threat-model narratives still described semantic search, suggestions, provenance/evaluation, and human approval as future M7-005 work after those separately governed boundaries were implemented. | Reframed both paragraphs as M7-004 gateway limitations and pointed to the implemented M7-005 evidence immediately below. An exact stale-phrase audit returns no matches. | M7-005, DOC-002, GS-SEC-038; Fixed |
| BUG-0335 | 2026-08-24 | 2026-08-24 | Low | M7-005 verification command | A scoped formatting check invoked `prettier`, but this repository does not install that executable and uses its root formatting gate instead. | Used the repository's supported Biome commands; `pnpm format` reported no fixes and `pnpm format:check` checked all 278 files successfully. | M7-005, FND-002; Resolved |
| BUG-0334 | 2026-08-24 | 2026-08-24 | High | M7-005 AI authoring API integration | After retaining the full immutable action snapshot, the focused core regressions passed but proposal creation through Fastify returned a generic HTTP 500 instead of 201. | The focused API invocation was consuming the previously emitted `@gridstory/core` build after the source/schema boundary changed. Rebuilt the current core package and removed the temporary diagnostic logger; the unchanged AI API integration suite passes all 3 tests, including proposal creation and review. | M7-005, TEST-003; Resolved |
| BUG-0333 | 2026-08-24 | 2026-08-24 | High | M7-005 AI proposal provenance and minimization | A proposal retained only action ID/document version while policy replacement discarded prior action criteria, and an evaluation-failed proposal retained parsed suggested values instead of failure metadata only. | Persisted the full immutable action snapshot with its document version and retain no suggested values for evaluation-failed proposals. Core regressions prove exact criteria survives policy replacement and failed output retains metadata only; the API proposal/review flow also passes. | M7-005, THREAT-0034, GS-SEC-038; Fixed |
| BUG-0332 | 2026-08-24 | 2026-08-24 | Low | M7-005 code quality and Studio accessibility lint | Scoped Biome lint found two optional-chain simplifications, an unsupported label on a plain proposal-list container, and an editor-handoff helper name falsely shaped like a React hook. | Used the equivalent optional-chain guards, changed the labelled proposal collection to a semantic section, and renamed the handoff helper to an imperative non-hook name; focused core/Studio lint is clean. | M7-005, FND-002, STUDIO-006; Fixed |
| BUG-0331 | 2026-08-24 | 2026-08-24 | Low | M7-005 recovery audit / Windows | A recovery inventory command passed Unix-style wildcard paths to `rg`, which Windows treated as invalid literal paths and skipped those source/test files. | Enumerated the exact recovery paths with `rg --files`, then audited `database-recovery.ts`, `database-recovery.test.ts`, `recovery-cli.ts`, PostgreSQL conformance, and the logical-restore script explicitly. | M7-005, FND-002; Fixed |
| BUG-0330 | 2026-08-24 | 2026-08-24 | Low | M7-005 verification / Windows sandbox | The restricted process sandbox denied Vitest fork-worker creation before the schema suite could load any test. | Reran the identical focused command through the approved child-process path; all 24 schema files and 65 tests execute and pass. | M7-005, TEST-001; Resolved |
| BUG-0329 | 2026-08-24 | 2026-08-24 | Medium | WebKit standalone preview E2E | The full cross-browser gate reported a downstream `Standalone` timeout after WebKit stopped responding for about 13 minutes while resolving the preceding visible `Close app preview` control. | Trace evidence located the engine stall before the reported assertion. The unchanged standalone vertical slice then passed in isolation in 20.4 seconds, and the unchanged complete four-test WebKit sequence passed in 29.2 seconds, classifying the one-off process stall as non-reproducible rather than an application lifecycle failure. | M7-004, TEST-004; Resolved |
| BUG-0328 | 2026-08-24 | 2026-08-24 | Low | M7-004 AI code quality | Root Biome lint found a redundant slash escape in the credential regex, a receipt guard better expressed with optional chaining, and an implicitly typed AI route source variable. | Removed the unnecessary escape, used the optional receipt status guard, and inferred a nullable content entry through the not-found catch boundary; the restarted root lint gate verifies the corrected files. | M7-004, FND-002; Fixed |
| BUG-0327 | 2026-08-24 | 2026-08-24 | Low | M7-004 security evidence | `GS-SEC-037` was marked verified while its required `docs/ai-gateway.md` evidence path had not yet been created, so the machine security validator correctly rejected the control. | Added the referenced provider composition, authorization, retrieval, redaction, reservation, kill-switch, persistence, operations, and limitations guide; the security validator can now resolve every verified evidence path. | M7-004, GS-SEC-037; Fixed |
| BUG-0326 | 2026-08-24 | 2026-08-24 | Low | M7-004 AI API fixture | The new AI route integration used the obsolete `sections` shape and omitted the current page schema's required non-empty `blocks` field, so content validation returned 422 before the gateway workflow ran. | Replaced the obsolete fixture with one valid registered hero block while retaining a non-allowlisted body assertion; the focused API workflow now reaches the gateway contract. | M7-004, TEST-003; Fixed |
| BUG-0325 | 2026-08-24 | 2026-08-24 | Low | M7-003 API integration fixtures | Existing outbox and workflow API tests asserted pre-analytics exact completed-job totals, so the full suite treated one legitimate normalized lifecycle process job per content event as an unexpected delivery. | Update both totals to include the intended idempotent analytics processing jobs while retaining all existing outbox/workflow assertions; focused API and restarted root gates verify the complete behavior. | M7-003, TEST-003; Fixed |
| BUG-0324 | 2026-08-24 | 2026-08-24 | Low | M7-003 analytics lint quality | The analytics report scope-audit used an expression-bodied `forEach` callback, implicitly returning the scope assertion value and violating the repository's iterable-callback rule. | Use a block-bodied callback with no return; the restarted root lint/check gate verifies the complete source set without the warning. | M7-003, FND-002; Fixed |
| BUG-0323 | 2026-08-24 | 2026-08-24 | High | M7-003 analytics adapter diagnostics | A hostile or misconfigured analytics adapter could throw a message containing credentials or provider payloads, and the generic durable-job failure path would retain that message for the authenticated analytics/operations report. | Catch adapter exceptions at the analytics boundary and replace them with the stable generic `analytics_adapter_delivery_failed` error before durable retention; the hostile-adapter regression proves the aggregate remains authoritative, the job retries, and the report exposes no injected diagnostic. | M7-003, THREAT-0032, GS-SEC-036; Fixed |
| BUG-0322 | 2026-08-24 | 2026-08-24 | Low | M7-003 analytics API fixture | After switching the public-ingestion test to a real seeded published revision, the adapter assertion still expected only one delivery and ignored the seed's legitimate normalized `content.created` and `content.published` lifecycle evidence. | Assert all three normalized names, select the public event by its immutable ID, and expect three succeeded adapter jobs; the focused API/recovery suite passes. | M7-003, TEST-003; Fixed |
| BUG-0321 | 2026-08-24 | 2026-08-24 | High | M7-003 public analytics integrity | Public analytics input was shaped as published content but did not yet prove that the supplied content ID/type/revision matched the repository's current published pointer, allowing fabricated or stale identifiers into aggregates and adapters. | Resolve the exact published record before enqueue and reject missing, type-mismatched, draft, or stale revisions with `analytics_content_unpublished`; focused service/API regressions use a real published revision and reject a stale one. | M7-003, Security; Fixed |
| BUG-0320 | 2026-08-24 | 2026-08-24 | Low | M7-003 analytics API fixture | The report-denial probe omitted development identity headers and assumed the request would be anonymous, but the established development context defaults missing roles to admin, so the authorized report correctly returned 200 instead of the expected 403. | Made the delivery fixture explicitly anonymous, preserving public ingestion authorization while proving the private report returns 403; the complete focused API scenario passes. | M7-003, TEST-003; Fixed |
| BUG-0319 | 2026-08-24 | 2026-08-24 | Medium | M7-003 analytics report projection | The strict private-report boundary omitted analytics receipt IDs from its schema but the service still spread the private `receipts` field into parsing, so authenticated reports failed instead of returning the safe projection. | Destructured the receipt array away before strict report parsing; both focused analytics service report scenarios now return the safe aggregate projection and all nine selected core analytics/operations/release tests pass. | M7-003, Security; Fixed |
| BUG-0318 | 2026-08-23 | 2026-08-23 | Low | M7-002 Studio lifecycle notices | Studio constructed most experiment transition notices by appending `ed` to the action, producing `pauseed` and `resumeed` even though the service transition succeeded. | Replaced suffix construction with explicit lifecycle labels and exercised start/pause/resume/complete/promotion notices in the passing 22-test focused Studio file. | M7-002, Studio; Fixed |
| BUG-0317 | 2026-08-23 | 2026-08-23 | Low | Studio migration test reliability | After the experiment test was bounded, the full parallel workspace rerun exposed the existing multi-step guarded CMS migration test at the same 5-second default ceiling, with no assertion failure. | Added only a 15-second bound to the multi-step guarded-migration scenario; the focused 26-test Studio suite and complete parallel `pnpm check` gate pass without changing the suite-wide timeout. | M7-002 verification, M6-004, TEST-001; Fixed |
| BUG-0316 | 2026-08-23 | 2026-08-23 | Low | M7-002 Studio test reliability | The full parallel workspace gate exceeded Vitest's default 5-second limit in the intentionally multi-step experiment lifecycle test, although the focused 22-test Studio file passed and no assertion failed. | Added only a 15-second bound to the experiment lifecycle scenario; the focused 26-test Studio suite and complete parallel `pnpm check` gate pass without changing the suite-wide timeout. | M7-002, TEST-001; Fixed |
| BUG-0315 | 2026-08-23 | 2026-08-23 | Low | M7-002 threat-model documentation | The initial THREAT-0031 patch used an ambiguous closing-array anchor and inserted the structurally valid threat object into the actor registry instead of the threat registry. | Moved THREAT-0031 into the canonical threat registry, added matching ASSET/FLOW/GS-SEC-035 references, and verified both negative self-tests plus the complete threat-model/ASVS validator. | M7-002, GS-SEC-035; Fixed |
| BUG-0314 | 2026-08-23 | 2026-08-23 | Low | M7-002 core test fixture | The invalid-control service regression changed only `controlVariant`, so the design schema correctly rejected it before the test could exercise the intended published-target control mismatch. | Kept the negative design internally valid by including the declared mismatched control in its 10,000-basis-point allocation; the test now reaches and verifies the service reference gate. | M7-002, TEST-001; Fixed |
| BUG-0313 | 2026-08-23 | 2026-08-23 | Medium | Studio forced-colors accessibility | WebKit forced-colors emulation mapped the header subtitle to black `CanvasText` while retaining the dark authored header background at 200% zoom, producing a 1.18:1 contrast ratio. | Mapped the full header surface, foreground, and boundary to system colors together in forced-colors mode; the focused WebKit keyboard/200%-zoom/forced-colors scenario passes. | M7-001, STUDIO-007; Fixed |
| BUG-0312 | 2026-08-23 | 2026-08-23 | Low | Personalization schema test quality | The M7-001 schema regression test used forbidden non-null assertions for known fixture members, leaving three repository lint warnings in an otherwise passing full gate. | Replaced compile-only assertions with explicit fixture guards; scoped lint and schema regression verification pass without warnings. | M7-001, TEST-001; Fixed |
| BUG-0311 | 2026-08-23 | 2026-08-23 | Low | M7-001 package verification / Windows sandbox | The restricted process sandbox denied pnpm lifecycle creation while rebuilding the package graph needed by cross-package focused tests. | Reran the identical package build through the approved child-process path; all five packages build and the rebuilt focused suites pass. | M7-001, FND-002; Fixed |
| BUG-0310 | 2026-08-23 | 2026-08-23 | Medium | Studio targeting publication | Studio allowed `Publish exact draft` while the configuration textarea contained unsaved changes, so an operator could believe visible edits were being published while the service correctly published the prior saved revision. | Tracked targeting-editor dirty state, disabled publish until the visible JSON is saved, and verified the full save-preview-publish interaction. | M7-001, STUDIO-006; Fixed |
| BUG-0309 | 2026-08-23 | 2026-08-23 | Medium | Public personalization decision projection | Anonymous edge results returned the matched internal audience ID, exposing targeting-policy vocabulary that applications do not need to render a variant. | Removed audience identity from published results while retaining it in authenticated private preview; focused core/API/client/Studio checks pass. | M7-001, THREAT-0030, GS-SEC-034; Fixed |
| BUG-0308 | 2026-08-23 | 2026-08-23 | High | Personalization data minimization | Enum values were bounded in count/length but accepted arbitrary strings such as raw email addresses or URLs, contradicting the token-only no-profile boundary and allowing identifying value catalogs to be persisted in configuration. | Required stable identifier tokens for all enum values and added a raw-email rejection regression; focused schema/security/model checks pass. | M7-001, THREAT-0030, GS-SEC-034; Fixed |
| BUG-0307 | 2026-08-23 | 2026-08-23 | Medium | Personalization cache guidance | Valid maximum-length scope and public decision attributes could construct a cache key/tag longer than the result schema limit, turning a valid decision into a server error. | Bound fixed-length SHA-256 scope/input digests into the key/tag and verified all 50 public attributes with worst-case valid scope lengths stay under limits and change keys when one input changes. | M7-001, THREAT-0030; Fixed |
| BUG-0306 | 2026-08-23 | 2026-08-23 | Low | Personalization API test fixture | The preview integration's partial-object assertion constrained the explanation trace to one rule even though the API correctly returns every evaluated decision rule. | Asserted the first rule separately while retaining result/cache checks; the focused API and client suites pass all 22 cases. | M7-001, TEST-003; Fixed |
| BUG-0305 | 2026-08-23 | 2026-08-23 | Low | Personalization service test assertions | New assertions required omitted exact-optional fields to exist with `undefined` and expected the wrong established conflict code. | Asserted absence explicitly and matched `revision_conflict`; all nine focused personalization schema/repository/service tests pass. | M7-001, TEST-001; Fixed |
| BUG-0304 | 2026-08-23 | 2026-08-23 | Low | M7-001 verification / Windows sandbox | The restricted process sandbox denied Vitest worker creation before the focused personalization suites could execute. | Reran the identical command through the approved child-process path; all nine focused personalization schema/repository/service tests execute and pass. | M7-001, TEST-001; Fixed |
| BUG-0303 | 2026-08-23 | 2026-08-23 | High | Marketplace inspector trust binding | Artifact inspection parsed the returned inspector ID/version but did not require them to equal the configured trusted adapter descriptor, so a configured transport could accidentally or maliciously attribute otherwise valid evidence to a different scanner identity. | Required the returned inspector ID and version to exactly match the configured trusted descriptor; mismatches become a redacted failed inspection using the configured identity, and the four-case marketplace service suite plus strict core typecheck pass. | M6-005, SEC-003; Fixed |
| BUG-0302 | 2026-08-23 | 2026-08-23 | Low | Marketplace service test quality | The deterministic review-ID helper mutated its counter inside a template-literal expression, violating the repository rule against assignment expressions. | Incremented the deterministic counter in a statement before constructing the ID; root lint is warning-free and the full core suite passes 103 tests with seven intentional PostgreSQL-only skips. | M6-005, FND-002; Fixed |
| BUG-0301 | 2026-08-23 | 2026-08-23 | Medium | Studio marketplace accessibility | The automated-review container used `aria-label` on a plain `div`, which does not expose a named landmark and fails the repository accessibility lint rule. | Replaced the generic container with a named semantic `section`; root lint is clean and all 12 Chromium, Firefox, and WebKit accessibility/keyboard/vertical-slice scenarios pass. | M6-005, STUDIO-006, FND-002; Fixed |
| BUG-0300 | 2026-08-23 | 2026-08-23 | Low | Marketplace API fixture | The integration reused a `content-type: application/json` header for challenge, domain-verification, and review POSTs but supplied no JSON body, so Fastify rejected the malformed request before the route executed. | Sent explicit empty JSON objects for bodyless marketplace POST fixtures; four focused API files/tests and the complete 60-test API suite pass. | M6-005, TEST-003; Fixed |
| BUG-0299 | 2026-08-23 | 2026-08-23 | Low | Marketplace compatibility typing | The signed-metadata SemVer range comparator indexed the result of `split()` without a fallback, which violates the repository's `noUncheckedIndexedAccess` contract even though the containing schema validates SemVer first. | Added a fail-closed fallback before splitting semantic versions; focused and complete strict schema/workspace type checks pass. | M6-005, FND-002; Fixed |
| BUG-0298 | 2026-08-23 | 2026-08-23 | Medium | Marketplace public summaries | Summary schemas inherited strict unknown-key rejection from their private source records, so parsing a complete publisher/release would reject rather than omit challenge, public-key body, or opaque artifact-reference fields. | Made public summary projections strip private source fields while retaining strict private records; six focused schema/plugin cases and the 53-test schema suite pass, and the API overview regression proves challenge/key body/artifact reference omission. | M6-005, SEC-003; Fixed |
| BUG-0297 | 2026-08-23 | 2026-08-23 | Low | M6-004 final audit | The final PowerShell audit passed Unix-style wildcard path arguments to `rg`, which Windows treated as invalid literal paths and skipped the migration source/service files. | Re-ran the audit with the two exact API migration paths, two exact core migration paths, and client path; verified credential/checkpoint/mapped-data containment and the complete migration path inventory without wildcard errors. | M6-004, FND-002; Fixed |
| BUG-0296 | 2026-08-23 | 2026-08-23 | Low | Studio migration styling | The migration warning color used `!important` even though normal source order already gives the warning rule precedence. | Removed the unnecessary cascade reversal; root lint now checks 237 files without warnings and all security/tenant/boundary/ledger/readiness validators pass. | M6-004, STUDIO-006, FND-002; Fixed |
| BUG-0295 | 2026-08-23 | 2026-08-23 | Low | M6-004 scoped formatting | Fourteen new or touched migration source, test, client, Studio, schema, and security-model files did not match the repository formatter after integration. | Applied Biome only to the 14 reported files; the root formatter now checks all 234 files with no changes required. | M6-004, FND-002; Fixed |
| BUG-0294 | 2026-08-23 | 2026-08-23 | Low | M6-004 scoped formatting | Passing the flattened changed-file array through the Windows pnpm shim still collapsed it into one space-separated path, so Biome processed no files. | Replaced the unreliable shim-array call with one literal exact-path formatter invocation, then verified the entire repository format and lint gates without warnings. | M6-004, FND-002; Fixed |
| BUG-0293 | 2026-08-23 | 2026-08-23 | Low | Migration workflow fixture | The cutover integration sent bodyless workflow transition requests even though the established transition route requires a JSON request body for optional changed-field input. | Sent explicit empty JSON transition payloads; the migration workflow advances through review/approval/publication and all eight focused API/adapter/recovery tests pass. | M6-004; Fixed |
| BUG-0292 | 2026-08-23 | 2026-08-23 | Low | Migration API fixture | The first API integration configured `mirror-source` and expected a new imported draft to bypass the existing editorial workflow, so execution correctly failed with a 409 publication gate. | Kept normal publication gating, imported as draft, then exercised the established distinct workflow/quality path before cutover validation; focused API verification passes. | M6-004; Fixed |
| BUG-0291 | 2026-08-23 | 2026-08-23 | Low | WordPress adapter typing | Exact-optional semantics rejected assigning the optional constructor authorization value to an optional private field because explicit `undefined` differs from field absence. | Modeled the private field as an explicit string-or-undefined runtime slot; API strict compilation and all provider adapter regressions pass. | M6-004; Fixed |
| BUG-0290 | 2026-08-23 | 2026-08-23 | Medium | Migration publication plan | A mirror-source update evaluated publication against the pre-update target, so an already published old draft hid the new revision that execution would create and leave unpublished. | Plans now require publication after every mirror-source update because execution creates a new draft revision; dual-run update/publication and cutover regressions pass. | M6-004; Fixed |
| BUG-0289 | 2026-08-23 | 2026-08-23 | Medium | Migration publication plan | A new mirror-source entry compared optional published/draft target revisions before a target existed, making `undefined !== undefined` false and omitting its required publish effect. | New source-published creates now explicitly require publication through normal gates; initial create/publish, retry, delta, and cutover tests pass. | M6-004; Fixed |
| BUG-0288 | 2026-08-23 | 2026-08-23 | Low | Migration service fixture | The focused migration fixture declared a routed content schema without the route contract's required `slugField`, so every planned record was correctly blocked as invalid target content. | Added the canonical `slugField` fixture member; all migration service cases reach and pass the intended execution/drift/retry assertions. | M6-004; Fixed |
| BUG-0287 | 2026-08-23 | 2026-08-23 | Low | Migration repository fixture | Awaitable repository assertions wrapped synchronous calls after evaluation and used `.resolves` on synchronous SQLite reads, so expected optimistic conflicts escaped the assertion boundary. | Asserted synchronous SQLite/in-memory results directly while retaining async PostgreSQL conformance; all six focused repository/service tests passed before the crash-recovery case was added. | M6-004; Fixed |
| BUG-0286 | 2026-08-23 | 2026-08-23 | Low | Migration schema fixture | The plan-summary regression expected strict rejection of a private unknown field, but the repository's Zod objects intentionally strip unknown fields and the production summarizer explicitly omits the payload. | Asserted that parsed summaries omit `mappedData`; both migration schema regressions pass and API overview tests prove mapped payloads stay private. | M6-004; Fixed |
| BUG-0285 | 2026-08-23 | 2026-08-23 | Low | Studio full-suite timing | The existing broad rich-text/asset/reference/component/collaboration interaction inherited the generic 5-second timeout and exceeded it by 48 ms under full concurrent workspace load. | Assigned the intentionally broad interaction an explicit 15-second bound without removing assertions; all 22 Studio tests pass. | M6-003, TEST-003; Fixed |
| BUG-0284 | 2026-08-23 | 2026-08-23 | Low | M6-003 lint quality | Governance code retained two unused type imports, returned a value from a `forEach` callback, and used verbose optional-state guards. | Removed unused imports, made the registration callback void, and used exact optional-state guards; the 40-file scoped Biome check is clean without warnings. | M6-003, FND-002; Fixed |
| BUG-0283 | 2026-08-23 | 2026-08-23 | Low | M6-003 import/export order | New and touched API/client/core files were not uniformly in Biome's canonical import/export order. | Applied Biome's safe import/export organization to the exact change set; the 40-file scoped check is clean. | M6-003, FND-002; Fixed |
| BUG-0282 | 2026-08-23 | 2026-08-23 | Low | M6-003 scoped formatting | The first PowerShell formatter invocation nested tracked and untracked filename arrays, passing two concatenated invalid paths to Biome. | Flattened tracked/untracked filenames before expansion; Biome processed 40 exact changed source/document files and fixed 21. | M6-003, FND-002; Fixed |
| BUG-0281 | 2026-08-23 | 2026-08-23 | Low | Governance API test fixture | The shared integration-test headers declared JSON for bodyless plan/process POSTs, so Fastify rejected an empty JSON body before route execution. | Removed the false shared content-type declaration while leaving Inject's payload handling intact; the complete isolated-fixture API workflow passes. | M6-003, TEST-001; Fixed |
| BUG-0280 | 2026-08-23 | 2026-08-23 | Low | Governance processor composition | Server construction passed an explicitly undefined cache invalidator through an exact-optional processor option. | Included the processor option only when a cache invalidator exists; the API strict typecheck passes. | M6-003; Fixed |
| BUG-0279 | 2026-08-23 | 2026-08-23 | Low | Governance telemetry composition | Server construction did not preserve TypeScript's narrowing of the optional resolved telemetry sink inside the governance options object. | Resolved and narrowed the telemetry sink once before composing governance services; the API strict typecheck passes. | M6-003; Fixed |
| BUG-0278 | 2026-08-23 | 2026-08-23 | Low | Governance service typing | `listScopes` declared a strict Promise even though the repository boundary intentionally permits synchronous or asynchronous adapters. | Awaited the adapter boundary in an async service method; the API strict typecheck passes. | M6-003; Fixed |
| BUG-0277 | 2026-08-21 | 2026-08-21 | Low | Unreleased security changelog | The earlier GS-SEC-028 entry still described production identity event sources as conditional on M6-002 after M6-002 supplied the durable scoped event source. | Replaced the stale future-tense clause with the verified durable scoped event source and retained external Collector wiring as deployment evidence; ledger/security validators and the final whitespace check pass. | M6-002, GS-SEC-028; Fixed |
| BUG-0276 | 2026-08-21 | 2026-08-21 | Low | M6-002 import order | Mixed value/type imports and exports in new or touched identity paths were not uniformly in Biome's canonical order. | Applied Biome's safe canonical ordering only to the 26 in-scope M6-002 source/test files; the scoped check and final warning-free root gate pass without touching unrelated historical drift. | M6-002, FND-002; Fixed |
| BUG-0275 | 2026-08-21 | 2026-08-21 | Low | M6-002 lint quality | Two federation-adapter guards used verbose undefined/protocol checks, and the Identity checkbox reversed normal CSS specificity with `!important`. | Used exact optional protocol guards and a naturally higher-specificity checkbox selector; scoped Biome and the final root lint gate pass without warnings. | M6-002, FND-002; Fixed |
| BUG-0274 | 2026-08-21 | 2026-08-21 | High | Production dependency security | The installed API production graph contained vulnerable transitive Fastify/Mercurius dependencies: `fast-uri`, `find-my-way`, `@fastify/static`, and `brace-expansion`. | Pinned compatible patched transitive releases through scoped pnpm overrides; frozen installation, 226 tests, PostgreSQL/browser gates, and builds pass, while `pnpm audit --prod` reports no known vulnerabilities. | M6-002, Security, FND-003; Fixed |
| BUG-0273 | 2026-08-21 | 2026-08-21 | Medium | Studio forced-colors accessibility | Opening the enterprise Identity panel before the 200%-zoom forced-colors audit left the header tenant subtitle at 2.53:1 contrast against the forced white canvas. | Bound the subtitle to the system `CanvasText` color in forced-colors mode; unsuppressed axe and keyboard/adapted-display checks pass in all 12 Chromium, Firefox, and WebKit scenarios. | M6-002, STUDIO-006, TEST-005; Fixed |
| BUG-0272 | 2026-08-21 | 2026-08-21 | Low | Directory credential guard | The SCIM service-credential validation used a verbose existence/status disjunction instead of the repository's optional-chain convention. | Kept the same fail-closed behavior through an optional status check; scoped Biome and the warning-free root gate pass. | M6-002, FND-002; Fixed |
| BUG-0271 | 2026-08-21 | 2026-08-21 | High | Enterprise identity audit events | Concurrency enforcement revoked durable sessions without emitting per-session audit events, while provider and policy administration reused unrelated action labels. | Added exact provider/policy actions and a per-session concurrency-revocation event; the focused regression proves durable state and all three exact audit records, and the full core suite passes. | M6-002, Security; Fixed |
| BUG-0270 | 2026-08-21 | 2026-08-21 | High | Production identity cookies | Direct `buildServer` callers that selected production identity mode could inherit or explicitly select a development cookie-security value. | Production server construction now forces Secure session cookies independently of caller defaults; configuration/direct-construction regressions, API strict compilation, and the full suite pass. | M6-002, Security; Fixed |
| BUG-0269 | 2026-08-21 | 2026-08-21 | Low | SCIM filter typing | The shared exact-filter helper indexed a user-or-group union with `userName`, which groups intentionally do not expose. | Narrowed the exact supported user/group filter branches before property access; API strict compilation and all SCIM integration regressions pass. | M6-002; Fixed |
| BUG-0268 | 2026-08-21 | 2026-08-21 | Low | Federation transaction test fixture | The deterministic secret generator produced values shorter than the protocol transaction contract's 16-character floor. | Made deterministic secrets satisfy the production contract before testing one-time consumption; federation replay regression and the full core suite pass. | M6-002, TEST-001; Fixed |
| BUG-0267 | 2026-08-21 | 2026-08-21 | High | Federated session authorization | Federation group claims produced mapped roles during login but were not persisted, so authenticating the durable session reconstructed a principal with no roles. | Persisted bounded federated groups with the directory user and rematerialized only configured tenant-scoped mappings on every session authentication; cross-service and PostgreSQL regressions preserve the `editor` assignment. | M6-002, Security; Fixed |
| BUG-0266 | 2026-08-21 | 2026-08-21 | Low | Identity durability test clock | The reopened-service assertion used the real clock against a session created with a fixed historical clock, so its idle lifetime was already over. | Reopened the service with a deterministic clock inside the tested lifetime; repository durability, expiry semantics, and the full core suite pass. | M6-002, TEST-001; Fixed |
| BUG-0265 | 2026-08-21 | 2026-08-21 | High | Enterprise identity optimistic writes | The mutation loop incremented the loaded document before reading the expected repository version, making every update after creation conflict. | Captured the repository version before incrementing the candidate document and retained bounded conflict retries; lifecycle, repository, cross-instance PostgreSQL, and full core tests pass. | M6-002; Fixed |
| BUG-0264 | 2026-08-21 | 2026-08-21 | High | Identity denial persistence | Expired-session and failed break-glass branches appended revocation/security state and then threw before the optimistic repository save. | Returned persisted denial outcomes from the mutation callback and raised stable errors only after the save; expiry and failed-attempt/rate-limit state now survives and focused/full tests pass. | M6-002, Security; Fixed |
| BUG-0263 | 2026-08-21 | 2026-08-21 | Low | Enterprise identity principal typing | The principal materializer accepted an optional authentication method even though every enterprise caller supplies a concrete method. | Narrowed the materializer contract to the four concrete enterprise methods; core/API strict compilation and all identity regressions pass. | M6-002; Fixed |
| BUG-0262 | 2026-08-21 | 2026-08-21 | Low | Identity repository exact types | Parsed optional challenge timestamps were wider than the handwritten exact-optional storage interface. | Aligned stored challenge types with parsed exact-optional values and normalized serialized records; core strict compilation and in-memory/SQLite/PostgreSQL conformance pass. | M6-002; Fixed |
| BUG-0261 | 2026-08-21 | 2026-08-21 | Low | Workspace dependency installation | The interactive offline relink recreated `node_modules` and then stalled without completion. | Interrupted the stalled prompt path, restored the complete workspace from the current store, and verified the final graph with a non-interactive frozen-lockfile install. | M6-002, FND-002; Fixed |
| BUG-0260 | 2026-08-21 | 2026-08-21 | Low | Legacy identity compatibility | Expanding the public enterprise `IdentitySession` contract made the legacy in-memory OIDC service's narrower session object fail strict compilation. | Isolated the legacy service behind its local compatibility session type while exporting the new durable contract from the enterprise boundary; strict core/API compilation passes. | M6-002; Fixed |
| BUG-0259 | 2026-08-21 | 2026-08-21 | Low | Identity repository dependency boundary | The first durable identity repository imported Zod without declaring it in `@gridstory/core`. | Declared the exact Zod runtime dependency in core and updated the frozen lockfile; strict core compilation, isolated package builds, and the complete root gate pass. | M6-002; Fixed |
| BUG-0258 | 2026-08-21 | 2026-08-21 | Low | PostgreSQL API diagnostic fixture | The first BUG-0257 diagnostic imported `pg` directly even though the API package intentionally receives PostgreSQL through `@gridstory/core`. | Replaced the direct driver import with the public `PostgresCollaborationRepository`; the API package keeps its intended dependency boundary and both PostgreSQL API tests run and pass. | M6-001, TEST-002; Fixed |
| BUG-0257 | 2026-08-21 | 2026-08-21 | High | PostgreSQL collaboration durability | A collaboration operation returned 201 in the first API instance but the reopened PostgreSQL-backed instance returned document version 0 with no operations. | Qualified collaboration storage with a validated `gridstory` schema so search-path changes cannot split writes between `public` and the application schema; 9 core PostgreSQL tests, both API tests including live/new/reopened repository reads, and the backup/restore drill pass. | M6-001, TEST-002; Fixed |
| BUG-0256 | 2026-08-21 | 2026-08-21 | Low | Studio collaboration semantics / lint | The first accessibility correction used `role="region"` on generic containers even though semantic section elements provide the same named landmark. | Replaced both generic review containers with labelled semantic sections; focused Biome lint is clean and all 16 Studio tests pass. | M6-001, STUDIO-006, FND-002; Fixed |
| BUG-0255 | 2026-08-21 | 2026-08-21 | Medium | Studio collaboration accessibility | Suggestion and conflict review containers had accessible labels but no landmark role, so assistive technology could not navigate them as named regions. | Exposed Suggestions and Merge conflicts as named semantic regions; the complete Studio interaction resolves a variant through those landmarks and all 16 tests pass. | M6-001, STUDIO-006, TEST-003; Fixed |
| BUG-0254 | 2026-08-21 | 2026-08-21 | Low | Studio collaboration test fixture | The first workbench interaction selected a nonexistent `title` option even though the authoring fixture's visible Headline field is named `headline`. | Selected the fixture's canonical `headline` field; the complete share/branch/suggest/merge/resolve/comment interaction passes with the unchanged visible Headline label. | M6-001, TEST-003; Fixed |
| BUG-0253 | 2026-08-21 | 2026-08-21 | Medium | Studio / collaboration JSON boundary | Studio attempted to pass an `unknown` draft field or component node directly into the JSON-only collaboration operation contract. | Added an explicit JSON serialization boundary for shared authoring values; Studio strict compilation and all 16 interaction tests pass. | M6-001, TEST-003; Fixed |
| BUG-0252 | 2026-08-21 | 2026-08-21 | Low | Collaboration API / request envelope | The initial route implementation referenced new collaboration properties that were absent from the API's bounded `RequestBody` interface. | Added only the new collaboration properties as unknown request inputs and retained schema/route validation; API strict compilation and all 15 normal server tests pass. | M6-001, TEST-002; Fixed |
| BUG-0251 | 2026-08-21 | 2026-08-21 | Medium | Collaboration service / exact target type | Suggestion creation advertised the broader comment target with an optional field even though persisted collaboration changes require a field. | Narrowed suggestion input to the required-field change-target contract; core/API strict compilation and the suggestion acceptance regressions pass. | M6-001, TEST-001; Fixed |
| BUG-0250 | 2026-08-21 | 2026-08-21 | Low | Collaboration repository test cleanup | The initial durability fixture closed its first SQLite repository only on the success path, so an operation failure left the database locked and obscured cleanup with `EBUSY`. | Closed both SQLite repository lifecycles through `finally`; the focused durability test passes from a fresh temporary directory without a cleanup error. | M6-001, TEST-001; Fixed |
| BUG-0249 | 2026-08-21 | 2026-08-21 | Medium | Collaboration service / input defaults | Direct core callers that omit `branchId` or `kind` bypass the operation input schema's runtime defaults. | Normalized `main` and `set` at the core boundary while preserving explicit offline metadata; six focused core tests and dependent API integration pass. | M6-001, TEST-001; Fixed |
| BUG-0248 | 2026-08-21 | 2026-08-21 | Medium | Collaboration schema / runtime | The collaboration operation input derived `.omit()` from a refined Zod object, which compiles but throws when the schema module is evaluated. | Replaced the derived target with an explicit equivalent schema; three schema and six core collaboration tests import and execute successfully. | M6-001, TEST-001; Fixed |
| BUG-0247 | 2026-08-21 | 2026-08-21 | Low | Release consumer cleanup control flow | The initial cleanup implementation redundantly threw from `finally` after the temporary path had already been validated. | Kept the exact resolved-path validation before `try` and made `finally` remove only that immutable child path without replacing the original failure; Biome and the complete root gate pass. | M5-009, FND-002; Fixed |
| BUG-0246 | 2026-08-21 | 2026-08-21 | Medium | `@gridstory/core` installed declarations | Core public declarations imported `pg` types that were available only as a development dependency. | Moved `@types/pg` into core's shipped dependencies and updated the frozen lockfile; the unchanged strict temporary consumer now type-checks every installed public declaration. | M5-009, TEST-006; Fixed |
| BUG-0245 | 2026-08-21 | 2026-08-21 | Medium | Isolated offline release consumer | The first temporary consumer tried to resolve correctly rewritten but intentionally unpublished internal `0.0.0` dependencies from offline registry metadata. | Bound the consumer's internal dependency graph to the exact reviewed tarball set with consumer-only pnpm overrides, while the archive validator now rejects non-`0.0.0` internal versions; two independent offline prepare runs install and verify all five archives. | M5-009, TEST-006; Fixed |
| BUG-0244 | 2026-08-21 | 2026-08-21 | Low | BUG-0243 release-evidence wording | BUG-0243's reproduction incorrectly implied the reviewed tarballs lacked license files even though pnpm packed the canonical workspace-root license. | Corrected the completed record to distinguish the existing exact license file from the genuinely missing package README and packed SPDX field; M5-009 verifies canonical license bytes without adding drifting copies. | M5-009, FND-002; Fixed |
| BUG-0243 | 2026-08-21 | 2026-08-21 | Medium | Private package archive metadata / RC readiness | All five reviewed archives lacked package-specific README files and packed SPDX license fields, while the validator permitted rather than required the legal/consumer metadata. | Added five package READMEs and exact `Apache-2.0` fields; every tarball must now contain one README, one canonical LICENSE, and a matching manifest, pass 10 negative validation cases, and install/import/type-check in an isolated offline consumer. | M5-008, M5-009, RC-003, TEST-006; Fixed |
| BUG-0242 | 2026-08-21 | 2026-08-21 | Low | Readiness validator / formatting | Three single-line validation throws did not match the repository formatter's wrapping rules. | Applied the formatter's exact multiline layout; `pnpm format:check`, `pnpm readiness:check`, and the full root gate pass. | M5-008, FND-002; Fixed |
| BUG-0241 | 2026-08-21 | 2026-08-21 | Medium | Release benchmark / memory semantics | The first report labeled a post-run RSS snapshot as peak resident memory. | Reported the larger of the current RSS and Node's process-lifetime maximum RSS; the full SQLite and PostgreSQL profiles pass at 145 MiB and 141 MiB respectively against the 512 MiB ceiling. | M5-007, TEST-006; Fixed |
| BUG-0240 | 2026-08-21 | 2026-08-21 | Medium | Release checksum verification / callback | The verifier passed Node's two-argument `basename` directly to `Array.map`, so the array index became an invalid suffix. | Wrapped `basename` in an explicit single-argument callback; all six release artifacts verify and an intentional checksum mutation is rejected. | M5-007, TEST-006; Fixed |
| BUG-0239 | 2026-08-21 | 2026-08-21 | Medium | Release archive inventory / pnpm layout | The first archive validator assumed npm's optional `package/` tar prefix, while pnpm 10 emits reviewed paths at the archive root. | Normalized both tar layouts, required package identity plus legal metadata, and verified the reviewed inventory and byte-for-byte determinism of all five package archives. | M5-007, TEST-006; Fixed |
| BUG-0238 | 2026-08-21 | 2026-08-21 | Low | Release benchmark / report output | The benchmark runner did not create a fresh report output directory. | Created the report's parent directory before writing; smoke and full SQLite/PostgreSQL profiles produced schema-valid JSON in fresh output paths. | M5-007, TEST-006; Fixed |
| BUG-0237 | 2026-08-21 | 2026-08-21 | Low | Release scripts / lint | The initial benchmark and release-evidence imports were not in repository order. | Organized both import lists under the repository convention; the final Biome and root lint gates pass. | M5-007, FND-002; Fixed |
| BUG-0236 | 2026-08-21 | 2026-08-21 | Medium | Release benchmark / write profile | The scenario labeled serial draft update inherited the read concurrency and raced its expected revision pointer. | Gave state-dependent writes an explicit concurrency of one while retaining concurrent read/query profiles; both full profiles pass without revision conflicts. | M5-007, TEST-006; Fixed |
| BUG-0235 | 2026-08-21 | 2026-08-21 | Low | GraphQL limit rule / TypeScript | The initial rule used GraphQL's unavailable `ASTValidationRule` type name. | Used GraphQL 16's `ValidationRule` contract and typed visitor context; API build and alias/selection limit regressions pass. | M5-007, TEST-001; Fixed |
| BUG-0234 | 2026-08-21 | 2026-08-21 | Medium | Asset limit integration / TypeScript | The first asset-service limit guard referenced `resourceLimits` through a type-only schema import. | Imported the limits as a runtime value; package builds and the focused asset boundary suites pass. | M5-007, TEST-001; Fixed |
| BUG-0233 | 2026-08-21 | 2026-08-21 | Low | M5-007 verification / Windows sandbox | The restricted process sandbox denies pnpm lifecycle and Vitest fork creation. | Reran the identical commands through the approved child-process path; focused suites, the full root gate, PostgreSQL conformance/recovery, and both benchmark profiles pass. | M5-007, FND-002; Fixed |
| BUG-0232 | 2026-08-21 | 2026-08-21 | High | Asset multipart API / request limits | Asset upload sessions advertise 5 MiB parts, but the API inherits Fastify's 1 MiB request limit for the binary part route. | Added the route-specific 5 MiB framework allowance and a service-level negotiated/global size guard; API regressions accept a valid part above 1 MiB and reject one above 5 MiB with 413. | M5-007, GS-SEC-012; Fixed |
| BUG-0231 | 2026-08-21 | 2026-08-21 | Low | React 18 compatibility fixture / source gates | The fixture's generated `dist-ssr` bundle was not excluded by the root ignore configuration, so a later root check treated generated Vite output as source. | Excluded conventional `dist-ssr` output from Git and Biome source discovery; the complete check passes after the compatibility build recreates the artifact. | M5-006, FND-002; Fixed |
| BUG-0230 | 2026-08-21 | 2026-08-21 | Low | WebKit vertical-slice verification | The full popup-preview authoring journey exhausted the generic 30-second timeout on its first WebKit run. | Assigned the intentionally long scenario an explicit 90-second bound without removing any assertion; the final fresh matrix completes it in 2.5 seconds on Chromium, 4.9 seconds on Firefox, and 9.0 seconds on WebKit. | M5-006, TEST-005; Fixed |
| BUG-0229 | 2026-08-21 | 2026-08-21 | Low | WebKit keyboard verification | WebKit did not place the native skip link in sequential focus traversal under its headless keyboard configuration. | Made the bypass link explicitly sequential with `tabIndex={0}`; plain Tab focuses it and Enter focuses the editor in the final Chromium, Firefox, and WebKit checks. | M5-006, TEST-005; Fixed |
| BUG-0228 | 2026-08-21 | 2026-08-21 | Low | Studio / forced-colors CSS | The forced-colors preview override preceded the base stylesheet and generated eight descending-specificity warnings. | Moved the focused override after all base rules; scoped and complete Biome lint pass without warnings. | M5-006, TEST-005; Fixed |
| BUG-0227 | 2026-08-21 | 2026-08-21 | Medium | Studio / WCAG 1.4.3 contrast | Unsuppressed axe audits found secondary text and control-label contrast below 4.5:1, including a WebKit-native picker background. | Darkened shared semantic colors, gave picker controls an explicit white background, and used forced-colors system values; all critical/adapted Studio and published scans pass on three engines with no exclusions or disabled rules. | M5-006, TEST-005; Fixed |
| BUG-0226 | 2026-08-21 | 2026-08-21 | Low | Accessibility/compatibility verification on Windows | The restricted process sandbox denied Vite/Vitest and recursive pnpm child creation. | Reran the identical checks through the approved child-process path; focused compatibility, all 12 browser checks, and the full repository gate pass. | M5-006, FND-002; Fixed |
| BUG-0225 | 2026-08-21 | 2026-08-21 | Low | Workspace dependency installation | A normal non-interactive lockfile update required confirmation before replacing the modules directory. | Completed the interactive update, then verified the actual non-interactive CI path with `CI=true pnpm install --frozen-lockfile`: the lockfile was current, 315 packages were reused with zero downloads, and installation passed. | M5-006, FND-002; Fixed |
| BUG-0224 | 2026-08-21 | 2026-08-21 | Low | API process smoke fixture | The first actual-process smoke used a fixed 750ms request delay, but schema initialization had not opened the listener yet; the unhandled probe refusal terminated that smoke process before graceful shutdown. | Replaced the fixed-delay probe with a bounded readiness retry; the built API returned exact HTTP 200 health, logged first-signal graceful shutdown, and exited zero. The built worker separately drained durable jobs, logged the same policy, and exited zero. | M5-005, TEST-001; Fixed |
| BUG-0223 | 2026-08-21 | 2026-08-21 | Low | PostgreSQL recovery gate / Windows sandbox | The restricted Windows process sandbox denied pnpm package-build child creation before the PostgreSQL container/recovery drill could start. | Reran the identical gate through the approved unrestricted process path; current packages built, 8 core and 2 API PostgreSQL tests passed, and the native dump/mutate/isolated-restore drill recovered one published entry. | M5-005, FND-002; Fixed |
| BUG-0222 | 2026-08-21 | 2026-08-21 | Low | Rolling-upgrade test fixture | The live rollout fixture disabled schema initialization, so both otherwise healthy instances correctly returned `503 schema_drift` from readiness. | Used normal initialized server startup for both rollout generations; the exact live current/candidate HTTP preflight passes in the 10-test focused recovery suite. | M5-005, TEST-001; Fixed |
| BUG-0221 | 2026-08-21 | 2026-08-21 | Low | SQLite recovery test fixture | The live-snapshot fixture sent the post-backup edit to `/api/v1/content/:id` instead of the existing `/draft` mutation route, so the intended source divergence returned 404. | Sent the edit through the canonical `/draft` route; the live database diverges after backup and the isolated restore returns the earlier title in the passing focused suite. | M5-005, TEST-001; Fixed |
| BUG-0220 | 2026-08-21 | 2026-08-21 | Low | Recovery tests / Windows sandbox | The restricted Windows process sandbox denied Vitest fork creation, so the focused recovery suite started no tests. | Reran the identical focused suite through the approved unrestricted process path; all four workers started and executed ten tests, exposing two normal fixture failures for separate resolution. | M5-005, FND-002; Fixed |
| BUG-0219 | 2026-08-21 | 2026-08-21 | Low | Graceful shutdown / TypeScript | TypeScript preserved the synchronous `stopping` narrowing across the awaited shutdown race and rejected checks for a second-signal transition to `forced`. | Read post-race state through an explicitly typed accessor so asynchronous second-signal mutation remains representable; the focused API typecheck passes. | M5-005; Fixed |
| BUG-0218 | 2026-08-21 | 2026-08-21 | High | Workspace dependency installation | Adding the API telemetry dependencies updated the lockfile but left the Studio and example Vite workspace executable links pointing at removed peer-resolution directories, so production builds could not resolve `vite/bin/vite.js`. | Reinstalled all nine workspaces from the frozen updated lockfile using the established pnpm store; focused Studio/example production builds now resolve Vite 8.1.5 and pass. | M5-004; Fixed |
| BUG-0217 | 2026-08-21 | 2026-08-21 | Low | Observability lint quality | The new route guard and OTLP test flattening used unnecessarily verbose boolean/array forms. | Used the optional route guard and native one-level `flat`; the lint gate is rerun as part of the final full check. | M5-004; Fixed |
| BUG-0216 | 2026-08-21 | 2026-08-21 | Low | README capability status | The root README still stated that complete security telemetry sinks, retention, and alerting were absent after M5-004 delivered them. | Added the verified observability capability and guide, removed the stale limitation, and retained vault-backed secret lifecycle as an honest residual gap. | M5-004; Fixed |
| BUG-0215 | 2026-08-21 | 2026-08-21 | High | Collector persistent queue configuration | The reference Collector configuration required a queue directory that a clean container did not create, so the Collector could not start. | Enabled the file-storage extension's explicit queue-directory creation and reran the pinned Collector validator. | M5-004; Fixed |
| BUG-0214 | 2026-08-21 | 2026-08-21 | Medium | HTTP telemetry semantics | Handled authorization errors set `error.type` on the span, but the correlated completion log omitted it and the completion hook marked every sub-500 response span OK. | Preserved bounded error types in correlated logs, used WARN for 4xx, and left 4xx span status unset while retaining ERROR for 5xx; the live OTLP regression asserts `forbidden` reaches the log export. | M5-004, GS-SEC-028; Fixed |
| BUG-0213 | 2026-08-21 | 2026-08-21 | Medium | Public readiness / information exposure | The unauthenticated `/ready` failure response included the complete schema drift report instead of the minimum operational status. | Reduced the 503 body to stable `not_ready` and `schema_drift` codes and added an exact negative response assertion; detailed schema state remains behind authorized lifecycle routes. | M5-004, GS-SEC-028; Fixed |
| BUG-0212 | 2026-08-21 | 2026-08-21 | Medium | OpenTelemetry log runtime / TypeScript | The initial log processor used the pre-0.221 positional exporter constructor instead of the current options contract. | Passed the exporter through the current `{ exporter }` options contract; the focused API typecheck proceeds past log runtime construction. | M5-004; Fixed |
| BUG-0211 | 2026-08-21 | 2026-08-21 | Medium | PostgreSQL verification harness | The root PostgreSQL gate did not rebuild workspace packages, so API conformance could execute stale core output instead of the source under test. | Made `pnpm test:postgres` build public packages before starting the disposable database; the gate passes 8 core and 1 API tests against current output. | M5-003, FND-002; Fixed |
| BUG-0210 | 2026-08-21 | 2026-08-21 | High | PostgreSQL plugin initialization | The plugin and content repositories concurrently created the shared default PostgreSQL schema, allowing a startup DDL race that broke ordinary content writes. | Followed the durable workflow/release pattern with a public namespaced plugin table in normal construction and retained explicit isolated schemas for conformance; the real PostgreSQL repository and API suites pass. | M5-003; Fixed |
| BUG-0209 | 2026-08-21 | 2026-08-21 | Low | Plugin integration / Lint | New plugin imports/exports were not repository-sorted and the publisher-status guard duplicated an optional-chain check. | Organized the affected barrels/imports and used the explicit optional-chain guard; focused Biome check is clean. | M5-003; Fixed |
| BUG-0208 | 2026-08-21 | 2026-08-21 | Low | Security evidence / Documentation | The verified plugin requirement referenced its integration guide before the guide existed. | Added the complete plugin boundary/integration guide before asserting verified evidence; `pnpm security:check` passes. | M5-003; Fixed |
| BUG-0207 | 2026-08-21 | 2026-08-21 | Low | ASVS profile / Traceability | The initial plugin requirement referenced two ASVS controls not selected by their pinned profile chapters. | Mapped GS-SEC-030 to the already-selected pinned V13/V15 controls; the profile validator and negative self-tests pass. | M5-003; Fixed |
| BUG-0206 | 2026-08-20 | 2026-08-20 | Low | Plugin schema / TypeScript | A shared duplicate-value loop widened capability literals to string and failed the enum-array type contract. | Used separately typed capability and operation duplicate checks; the schema typecheck passes. | M5-003; Fixed |
| BUG-0205 | 2026-08-20 | 2026-08-20 | Low | Plugin schema / TypeScript | The initial plugin installation contract imported a file-local content-scope schema. | Defined the explicit six-field scope schema beside the plugin contracts; the schema typecheck passes. | M5-003; Fixed |
| BUG-0204 | 2026-08-20 | 2026-08-20 | Low | Task ledger / whitespace | Refreshing the task-ledger date retained the prior Markdown hard-break spaces and made the final diff whitespace audit fail. | Removed the obsolete hard-break spaces from the refreshed date; `git diff --check`, ledger validation, and formatting pass. | FND-006; Fixed |
| BUG-0203 | 2026-08-20 | 2026-08-20 | Medium | Windows repository checkout / verification | Git checked normalized text out as CRLF while Biome and generated contracts require LF, leaving the canonical delivery gate red on a clean clone. | Made the root text attribute require `eol=lf`, overriding host `core.autocrlf` for tracked text, and normalized the Biome-managed working files. Focused format/schema checks, `pnpm check`, eight PostgreSQL tests, and the canonical browser walkthrough pass. | FND-006; Fixed |
| BUG-0202 | 2026-07-26 | 2026-07-26 | Low | Tenant regression test / Windows editing | Strengthening hostile-search assertions wrote literal PowerShell newline tokens into the TypeScript object literal. | Replaced the literal tokens with real line breaks, formatted the test, and passed the strengthened hostile-search regressions plus the full workspace and browser gates. | M5-002, TEST-001; Fixed |
| BUG-0201 | 2026-07-26 | 2026-07-26 | Low | Tenant contract checker / Lint | The first checker used a placeholder-like literal and a value-returning `forEach` callback rejected by repository lint. | Split the audit tokens and used a void callback block; scoped and root lint plus `pnpm tenant:check` pass. | M5-002, TEST-001; Fixed |
| BUG-0200 | 2026-07-26 | 2026-07-26 | Low | Package scripts / Windows editing | Adding the tenant contract checker wrote a literal PowerShell newline token into `package.json`, making the manifest invalid JSON. | Restored a real line break, parsed the manifest, and passed `pnpm tenant:check`, `pnpm check`, and `pnpm test:e2e`. | M5-002, FND-002; Fixed |
| BUG-0199 | 2026-07-26 | 2026-07-26 | Medium | Telemetry isolation | There was no canonical tenant-scoped telemetry envelope, allowing future metrics/log adapters to omit or ambiguously encode scope. | Added a validated six-field telemetry envelope with bounded safe metadata, wired scoped search/asset/worker events, and added sensitive-metadata and exact-scope regressions. | M5-002, M5-004, Security; Fixed |
| BUG-0198 | 2026-07-26 | 2026-07-26 | High | Adapter/repository isolation | Asset, operations, and audit services did not consistently fail closed when repositories returned records outside the requested scope. | Added fail-closed scope checks for asset/audit records, search reads, outbox/jobs/webhooks/dashboard data, claims, enqueue/replay output, and embedded webhook events; hostile repository tests pass. | M5-002, Security; Fixed |
| BUG-0197 | 2026-07-26 | 2026-07-26 | High | Scope-key isolation | Search and storage scope keys concatenated caller-controlled identifiers with delimiters, allowing distinct scopes to collide. | Replaced ad hoc serializers with validated JSON tuple keys, component-encoded paths, and delimiter-safe full-scope cache prefixes; collision regressions and the static contract gate pass. | M5-002, Security; Fixed |
| BUG-0196 | 2026-07-26 | 2026-07-26 | Critical | Identity / Authorization isolation | OIDC role names were global and service accounts could receive unscoped or differently scoped grants. | Materialized OIDC roles as tenant-bound assignments, restricted legacy global roles to development/anonymous principals, and rejected service-account grants not explicitly bound to the account tenant; authorization/identity regressions pass. | M5-002, Security; Fixed |
| BUG-0195 | 2026-07-26 | 2026-07-26 | High | Search adapter isolation | Search trusted adapter totals, facets, highlights, and taxonomy memberships, allowing hostile adapter metadata to disclose cross-scope content. | Required scope/perspective echoes, reloaded hits through scoped storage, rebuilt highlights/taxonomies from authoritative entries, derived totals/facets from accepted unique hits, and added hostile-adapter regressions. | M5-002, Security; Fixed |
| BUG-0194 | 2026-07-26 | 2026-07-26 | High | Cache isolation | Cache tags omitted organization/workspace and workflow actions could send arbitrary global invalidations. | Added full six-field collision-safe prefixes, namespaced workflow tags, explicit `{ scope, tags }` invalidator inputs, foreign-tag rejection, canonical outbox tags, and cache isolation regressions. | M5-002, Security; Fixed |
| BUG-0193 | 2026-07-26 | 2026-07-26 | Low | Full verification / Windows sandbox | The restricted Windows process sandbox denied pnpm child-process creation during the package build phase of the root check. | Reran the identical root check through the approved unrestricted process path; lint, format, types, 161 tests, and all builds pass. | M5-001, FND-002; Fixed |
| BUG-0192 | 2026-07-26 | 2026-07-26 | Low | Security documentation / Formatting | The package-manager exec wrapper could not resolve the installed root Biome executable for the scoped M5-001 files on Windows. | Used the exact installed root Biome command for scoped formatting; root format and lint checks pass. | M5-001, FND-002; Fixed |
| BUG-0191 | 2026-07-26 | 2026-07-26 | Low | README / capability status | Current limitations still said uploads, release bundles, and external visual-preview handshakes were absent after their milestones completed. | Replaced the stale limitations, corrected the production-identity milestone, and linked the new security guides/command; root checks pass. | M5-001, DOC-001; Fixed |
| BUG-0190 | 2026-07-26 | 2026-07-26 | Low | API operations regression expectations | Existing operations and workflow API tests counted durable jobs before automatic search indexing. | Updated both expectations for the intentional index jobs; the full API suite and pnpm check pass. | M4-005, M4-006, TEST-001; Fixed |
| BUG-0189 | 2026-07-26 | 2026-07-26 | Low | Workflow action regression fixture | The isolated workflow-action operations fixture omitted a search runner after missing-runner handling became fail-safe. | Injected an explicit fixture runner and included its completion; focused core regressions and pnpm check pass. | M4-005, M4-006, TEST-001; Fixed |
| BUG-0188 | 2026-07-26 | 2026-07-26 | Low | Search lint and accessibility | New search code retained an unused import, a roleless ARIA label, and value-returning forEach callbacks. | Removed the import, used fieldset/legend semantics, and made callbacks void; lint, boundaries, ledgers, and Studio tests pass. | M4-006, TEST-001, TEST-003; Fixed |
| BUG-0187 | 2026-07-26 | 2026-07-26 | Low | Changelog / Windows editing | A Regex.Replace overload inserted the new Fixed bullet into both changelog Fixed sections. | Removed only the historical duplicate, restored its heading separation, and verified one M4-006 Fixed entry; ledgers pass. | M4-006, FND-002; Fixed |
| BUG-0186 | 2026-07-26 | 2026-07-26 | Medium | Search job operations | A missing search runner was treated as a successful no-op and could acknowledge work without indexing. | Missing runners now fail into the durable retry/dead-letter path; production API/worker and isolated fixtures inject explicit runners; full checks pass. | M4-006, TEST-001; Fixed |
| BUG-0185 | 2026-07-26 | 2026-07-26 | Low | Search client contract | The exported SearchQuery type reflected defaulted schema output and incorrectly required all defaulted properties. | Split public input and parsed output query types; schema, client, API, and full typechecks pass. | M4-006, TEST-001; Fixed |
| BUG-0184 | 2026-07-26 | 2026-07-26 | Low | Operations regression expectations | The operations test expected only cache and webhook jobs after outbox expansion. | Included the intentional search-index job and completion; focused operations regression and full check pass. | M4-006, TEST-001; Fixed |
| BUG-0183 | 2026-07-26 | 2026-07-26 | Low | Search service / TypeScript | Canonical taxonomy defaults remained optional internally and a binding argument leaked into adapter search. | Normalized parsed query/taxonomy types and separated internal adapter inputs; core/API typechecks and full check pass. | M4-006, TEST-001; Fixed |
| BUG-0182 | 2026-07-26 | 2026-07-26 | Low | Search service / Windows editing | Literal PowerShell newline tokens entered three search-document calls. | Replaced the tokens with real formatted lines; formatter, typechecks, regressions, and full check pass. | M4-006, TEST-001; Fixed |
| BUG-0181 | 2026-07-26 | 2026-07-26 | Low | Search schema contracts | The initial search contract duplicated existing taxonomy exports. | Reused the canonical taxonomy contracts and removed duplicate exports; schema/core typechecks and full check pass. | M4-006, TEST-001; Fixed |
| BUG-0180 | 2026-07-26 | 2026-07-26 | Low | Workflow version regression fixture | The active-definition upgrade regression requested the default Request changes transition with an author-only actor, so role authorization stopped the test before version adoption. | Used a publisher actor allowed by the default transition; both focused workflow suites and root lint/ledgers pass. | M4-005, TEST-001; Fixed |
| BUG-0179 | 2026-07-26 | 2026-07-26 | High | Workflow action version metadata | An existing workflow instance executed an action from the newly active definition version while its delivery idempotency key and payload retained the older instance version. | Persisted the applied definition version on the instance and history event, keyed/enveloped deliveries with that exact event version, and retained legacy reconciliation fallback; focused core/API tests pass. | M4-005, TEST-001, TEST-005; Fixed |
| BUG-0178 | 2026-07-26 | 2026-07-26 | Medium | Workflow action lint and accessibility | The replay type guard was reducible to optional chaining, and the Studio state map and action-adder divs used aria-label without a supporting semantic role. | Used the optional-chain guard, a semantic labeled list/list items, and a fieldset/legend action group; root lint, boundaries, and ledgers pass. | M4-005, TEST-001, TEST-003; Fixed |
| BUG-0177 | 2026-07-26 | 2026-07-26 | Low | Changelog / resumed ledger update | Resuming M4-005 appended duplicate Added, Changed, and Security bullets that were already present from the earlier checkpoint. | Removed only the second copy of each adjacent entry and verified one remaining Added, Changed, and Security record. | M4-005, FND-002; Fixed |
| BUG-0176 | 2026-07-26 | 2026-07-26 | Low | Studio workflow designer test | The version-save regression inspected region textContent for an edited input value, but form-control values are not part of DOM text content. | Asserted the saved Action label input value directly; all 15 focused Studio tests pass. | M4-005, TEST-003; Fixed |
| BUG-0175 | 2026-07-26 | 2026-07-26 | Low | Studio workflow designer / TypeScript | The action-editor callbacks referenced WorkflowActionDefinition without importing the type. | Added the schema type-only import and formatted the file; focused Studio typecheck passes. | M4-005, TEST-003; Fixed |
| BUG-0174 | 2026-07-26 | 2026-07-26 | Low | Workflow action integration / TypeScript | Assigning the optional durable-job repository to a question-mark private field violated exact optional property semantics. | Modeled the field as an explicit repository-or-undefined union; focused core, API, and client typechecks pass. | M4-005, TEST-001; Fixed |
| BUG-0173 | 2026-07-26 | 2026-07-26 | Low | Bug ledger / Resolved row | The initial BUG-0172 resolved row omitted its summary column and shifted the resolution and task link left. | Restored the missing summary field, retained the resolution evidence and task link in their proper columns, and manually rechecked the eight-field row shape. | M4-004, FND-002; Fixed |
| BUG-0172 | 2026-07-26 | 2026-07-26 | Low | Studio release manager / Responsive layout | The semantic scoped-release list retained the browser-default list marker and left padding. | Reset list style, margin, and padding while preserving horizontal scrolling; the repeated 700x900 walkthrough shows no marker, zero left indentation, no overflow, and no console warnings. | M4-004, TEST-005; Fixed |
| BUG-0171 | 2026-07-26 | 2026-07-26 | Low | Changelog / Windows editing | The two M4-004 Added entries were joined by a literal `` `r`n `` token. | Replaced only the literal token with a real line break and retained two distinct Unreleased Added entries; ledger validation passes. | M4-004, FND-002; Fixed |
| BUG-0170 | 2026-07-26 | 2026-07-26 | Medium | Release slice / Lint and accessibility | Initial integration retained an unused type, returned a Map mutation from `forEach`, used ARIA list roles instead of semantic list elements, and used an array index in validation issue keys. | Removed the unused type, made the callback void, used a semantic list with interactive children, and derived stable issue keys; root lint, boundaries, and ledgers pass. | M4-004, TEST-001, TEST-003; Fixed |
| BUG-0169 | 2026-07-26 | 2026-07-26 | Medium | Release scheduling / TypeScript | The invalid-after-scheduling branch spread an insufficiently narrowed optional schedule, making required schedule fields appear optional. | Narrowed the schedule into a local value before preserving or failing it; focused core typecheck passes. | M4-004, TEST-001; Fixed |
| BUG-0168 | 2026-07-26 | 2026-07-26 | Low | Edge end-to-end test / Formatting | The governed-workflow scenario callback exceeded the repository formatter layout after adding the request fixture. | Formatted only the changed Playwright scenario with the repository Biome binary; the focused file is formatter-clean. | M4-003, TEST-005; Fixed |
| BUG-0167 | 2026-07-26 | 2026-07-26 | Low | Bug ledger / Row move | Resolving BUG-0166 used a global resolved-table separator replacement and duplicated its row beside a historical separator, causing ledger validation to report a duplicate ID. | Removed only the second exact BUG-0166 row and changed the ledger updater to target the first resolved-table separator; ledger validation passes with one permanent row per bug. | M4-003, FND-002; Fixed |
| BUG-0166 | 2026-07-26 | 2026-07-26 | Medium | Edge end-to-end workflow | The canonical vertical-slice scenario still attempted direct publication after saving a draft, so the new workflow gate correctly left Publish disabled until review and approval completed. | Extended the scenario through Studio review actions and a distinct publisher approval before publication; `pnpm test:e2e` passes the governed Edge flow and React delivery. | M4-003, TEST-005; Fixed |
| BUG-0165 | 2026-07-26 | 2026-07-26 | Medium | Studio workflow / Accessibility | A pending approval rendered both the disabled workflow transition and the active rejection control with the accessible name Request changes. | Renamed the reviewer control to Reject and request changes; live DOM now exposes one exact match per action and the full distinct-reviewer walkthrough passes. | M4-003, TEST-005; Fixed |
| BUG-0164 | 2026-07-26 | 2026-07-26 | Low | Browser walkthrough / Windows process launch | The managed PowerShell host exposed no ProcessStartInfo.ArgumentList collection, so the first hidden API/Studio launch stopped before starting either process. | Used hidden Start-Process launches with supported argument arrays and owned logs; API health and Studio both return successfully on ports 4000 and 5173. | M4-003, TEST-005; Fixed |
| BUG-0163 | 2026-07-26 | 2026-07-26 | Medium | Asset service test / Time fixture | The successful multipart-upload fixture used a July 24 clock, so its generated session expired against the real July 26 upload-part clock. | Moved only the successful upload lifecycle fixture to a deterministic future clock; all four focused asset-service tests pass. | M4-003 verification, M4-001; Fixed |
| BUG-0162 | 2026-07-26 | 2026-07-26 | Low | Workflow service test / Lint | The repository cleanup forEach callback implicitly returned the SQLite close result. | Used a block callback with no return value; scoped workflow lint is clean. | M4-003; Fixed |
| BUG-0161 | 2026-07-26 | 2026-07-26 | Low | Workflow service / Lint | The pending-schedule guard used a reducible null-or-state condition instead of the repository optional-chain form. | Used the optional-chain state guard; scoped workflow lint is clean. | M4-003; Fixed |
| BUG-0160 | 2026-07-26 | 2026-07-26 | Low | Workflow repository test / Cleanup | An early persistence assertion failure left the reopened SQLite handle locked during temporary-directory cleanup. | Closed the reopened adapter in a finally block; focused persistence cleanup and both workflow suites pass. | M4-003; Fixed |
| BUG-0159 | 2026-07-26 | 2026-07-26 | Low | Workflow repository test / Awaitable | The SQLite persistence regression used Promise resolves assertions on synchronous adapter results. | Asserted synchronous SQLite results directly while retaining the repository Awaitable interface; the persisted record and cross-tenant null result pass. | M4-003; Fixed |
| BUG-0158 | 2026-07-26 | 2026-07-26 | Low | Studio workflow test / Matcher | The new workflow interaction regression used the unavailable jest-dom toBeDisabled matcher. | Asserted the native HTMLButtonElement disabled property; all 17 Studio tests now pass, including governed review and publish gating. | M4-003; Fixed |
| BUG-0157 | 2026-07-26 | 2026-07-26 | Low | Workflow API test / HTTP fixture | The schedule-cancellation regression sent application/json on a bodyless DELETE, causing Fastify to reject the malformed request before the route ran. | Removed the content-type header only from the bodyless cancellation request; the dedicated custom-definition, policy, schedule, cache, and tenant-boundary test passes. | M4-003; Fixed |
| BUG-0156 | 2026-07-26 | 2026-07-26 | High | Workflow initialization / Tenant scope | The first content create in a fresh tenant scope failed because instance initialization bypassed lazy default workflow provisioning. | Routed instance definition lookup through the default-aware service method; focused API/component suites pass 15 tests plus one intentional PostgreSQL skip across fresh scopes. | M4-003; Fixed |
| BUG-0155 | 2026-07-26 | 2026-07-26 | Low | Workflow test / Policy fixture | The separation-of-duties regression used a requester without a reviewer role, so role authorization masked the intended self-approval branch. | Gave the requester a reviewer-capable role while retaining the same actor ID; the focused workflow suite now reaches and passes the distinct-actor policy assertion. | M4-003; Fixed |
| BUG-0154 | 2026-07-24 | 2026-07-24 | Low | Graph artifact / Quality gate | Generated Graphify outputs were unignored, so formatting scanned large machine-generated files. | Classified `graphify-out/` as local runtime analysis data; format check now scans 142 repository files with no errors while retaining the graph locally. | M4-002, FND-002; Fixed |
| BUG-0153 | 2026-07-24 | 2026-07-24 | Low | Browser walkthrough / Temporary asset | The managed shell denied writing the browser SVG fixture to the advertised temporary root. | Used the repository ignored runtime-data directory, completed the sanitized-upload walkthrough, and removed the exact owned fixture. | M4-002, TEST-005; Fixed |
| BUG-0152 | 2026-07-24 | 2026-07-24 | Low | Browser walkthrough / Windows process launch | PowerShell `Start-Process` rejected case-duplicate inherited path keys. | Used hidden .NET process starts with explicit arguments, verified both services, completed the walkthrough, and stopped the two owned PIDs. | M4-002, TEST-005; Fixed |
| BUG-0151 | 2026-07-24 | 2026-07-24 | Medium | Bug ledger / Windows patching | Two newly added open rows were concatenated and two backtick-prefixed paths became control characters. | Rebuilt the open/resolved boundary with literal-safe UTF-8 rows; ledger validation is included in the final gate. | M4-002, FND-002; Fixed |
| BUG-0150 | 2026-07-24 | 2026-07-24 | Low | Workspace test runner | The testless example Vite site made root `pnpm test` exit 1. | Added `--passWithNoTests` to its interactive test script; root `pnpm test` passes all 133 tests with two intentional skips. | M4-002, FND-002; Fixed |
| BUG-0149 | 2026-07-24 | 2026-07-24 | Low | Workspace test runner | The testless example-kit package made root `pnpm test` exit 1. | Added `--passWithNoTests` to its interactive test script; the root command advances through every package and passes. | M4-002, FND-002; Fixed |
| BUG-0148 | 2026-07-24 | 2026-07-24 | Low | SVG sanitizer test / Windows patching | Namespace-regression insertion wrote literal newline tokens inside a TypeScript template. | Replaced only the two tokens with line breaks; formatter and focused SVG/service tests pass. | M4-002, TEST-001; Fixed |
| BUG-0147 | 2026-07-24 | 2026-07-24 | Low | Asset guide / Windows patching | Two delivery-route bullets lost their opening Markdown code delimiters. | Restored both literal code spans and rechecked the rendered route list. | M4-002, FND-002; Fixed |
| BUG-0146 | 2026-07-24 | 2026-07-24 | High | SVG sanitizer / Namespace bypass | The first sanitizer missed namespaced active elements, XML stylesheet instructions, and generic `src` URI attributes. | Added namespace-aware active-element removal, processing-instruction rejection, URI-bearing attribute removal, and a fail-closed residual scan; crafted regressions pass. | M4-002, TEST-001; Fixed |
| BUG-0145 | 2026-07-24 | 2026-07-24 | Low | Studio asset security / Lint | The first security-state UI pass used a reducible null guard and `!important` badge colors. | Used optional chaining and selector specificity; repository lint is clean for the Studio source. | M4-002, TEST-003; Fixed |
| BUG-0144 | 2026-07-24 | 2026-07-24 | Medium | API asset test / Windows patching | PowerShell expanded signed-delivery template literals and broke the test syntax. | Restored literal route/token expressions and the suite boundary; focused API tests pass. | M4-002, TEST-002; Fixed |
| BUG-0143 | 2026-07-24 | 2026-07-24 | Low | Core asset test / Fixture validity | A successful upload fixture declared JPEG while sending text bytes. | Replaced it with JPEG-signature bytes; MIME verification and the full core suite pass. | M4-002, TEST-001; Fixed |
| BUG-0142 | 2026-07-24 | 2026-07-24 | Low | Windows verification / Process launch | The managed sandbox denied Vitest and workspace typecheck worker creation. | Reran the exact commands through scoped approved process-launch paths; focused tests, full tests, and full typecheck execute successfully. | M4-002, TEST-001; Fixed |
| BUG-0141 | 2026-07-24 | 2026-07-24 | Low | Graph analysis / Windows encoding | The graph report writer used CP1252 for Unicode output. | Wrote the generated report and JSON explicitly as UTF-8; the 1,513-node graph, report, visualization, manifest, and cost tracker completed. | M4-002, FND-002; Fixed |
| BUG-0140 | 2026-07-24 | 2026-07-24 | Low | Graph analysis / PowerShell quoting | Inline deterministic AST extraction lost escaped f-string delimiters. | Ran the extractor through a temporary script using the sequential Windows-sandbox path; all 144 detected files completed and temporary extraction artifacts were removed. | M4-002, FND-002; Fixed |
| BUG-0139 | 2026-07-24 | 2026-07-24 | Low | Windows verification runner | The post-browser aggregate `pnpm check` produced no output and was ended by the command runner timeout. | No workspace process remained; bounded lint/format/Studio type-test-build gates and the canonical e2e suite all pass after the final visual fixes. | M4-001, FND-002; Fixed |
| BUG-0138 | 2026-07-24 | 2026-07-24 | Medium | Studio asset library / Responsive layout | The 700x900 asset-library walkthrough had a five-pixel horizontal overflow. | Wrapped the narrow header action row within its available width; browser metrics now show equal document/client widths, one asset column, and no console warnings. | M4-001, TEST-005; Fixed |
| BUG-0137 | 2026-07-24 | 2026-07-24 | Low | Studio asset library / Windows encoding | The managed asset card separator was written as a literal question mark. | Replaced it with ASCII-safe punctuation; desktop and 700x900 browser checks render `file - text/markdown`. | M4-001, TEST-005; Fixed |
| BUG-0136 | 2026-07-24 | 2026-07-24 | Low | Windows formatter invocation | `pnpm exec biome` did not resolve the installed root formatter binary. | Used the exact workspace formatter binary; 19 scoped files were formatted and `pnpm format:check` passes. | M4-001, FND-002; Fixed |
| BUG-0135 | 2026-07-24 | 2026-07-24 | Low | Asset slice / Formatting | Windows edit paths left CRLF endings and unformatted layout across 19 new or changed code files. | Formatted only the M4-001 source/test set; `pnpm format:check` and `pnpm lint` pass. | M4-001, FND-002; Fixed |
| BUG-0134 | 2026-07-24 | 2026-07-24 | Medium | Asset usage scan / Lint | The array `forEach` callback implicitly returned the recursive accumulator. | Used a block callback with no return value; `pnpm lint` passes. | M4-001, TEST-001; Fixed |
| BUG-0133 | 2026-07-24 | 2026-07-24 | Medium | Universal client test / Lint | The binary-body regression dereferenced a value reached through optional chaining. | Narrowed the request body to `ArrayBuffer` before reading it; `pnpm lint` passes. | M4-001, TEST-001; Fixed |
| BUG-0132 | 2026-07-24 | 2026-07-24 | Low | Asset service / Lint | `AssetMetadata` remained imported after the implementation stopped referring to the type directly. | Removed the unused type import; `pnpm lint` passes. | M4-001, TEST-001; Fixed |
| BUG-0131 | 2026-07-24 | 2026-07-24 | Low | Studio asset test / Lint | The ETag mock used concatenation where the repository requires a template literal. | Restored an encoding-safe template literal; `pnpm lint` passes. | M4-001, TEST-003; Fixed |
| BUG-0130 | 2026-07-24 | 2026-07-24 | Low | Bug ledger / Markdown | The resolved-bug table lost its separator row during the earlier asset-defect row move. | Restored the separator; `pnpm check:ledgers` passes. | M4-001, FND-002; Fixed |
| BUG-0129 | 2026-07-24 | 2026-07-24 | Low | Studio asset test / Windows patching | PowerShell consumed the template-literal delimiters in the new mock ETag. | Replaced it with plain string concatenation; all 12 Studio tests pass. | M4-001, TEST-003; Fixed |
| BUG-0128 | 2026-07-24 | 2026-07-24 | High | Asset upload / Completion integrity | The first strict-narrowing edit left the descriptor comparison as an unused expression, making the callback return undefined. | Restored the explicit boolean return; successful and forged-completion regressions pass. | M4-001, TEST-001; Fixed |
| BUG-0127 | 2026-07-24 | 2026-07-24 | Low | Asset upload / TypeScript | Strict indexed access did not narrow a recorded part through a compound boolean return. | Added an explicit missing-index branch; focused core type checking and asset tests pass. | M4-001, TEST-001; Fixed |
| BUG-0124 | 2026-07-24 | 2026-07-24 | High | Asset upload / Completion integrity | Completion validated client-supplied part sizes but did not require their ETags and sizes to equal the server-recorded uploaded parts. | Require the exact sorted recorded descriptor set before storage completion; forged and successful completion regressions pass. | M4-001, TEST-001; Fixed |
| BUG-0125 | 2026-07-24 | 2026-07-24 | Medium | Studio / Resumable uploads | Studio sent every selected file as one part even when the server returned a multipart chunk size. | Slice browser files by negotiated `partSize`; the regression observes 4, 4, and 2 byte parts for a 10-byte file. | M4-001, TEST-003; Fixed |
| BUG-0126 | 2026-07-24 | 2026-07-24 | Low | Studio / Windows encoding | New asset-library status punctuation was written as literal question marks. | Replaced the usage separators and upload progress punctuation with ASCII-safe text; the Studio suite passes. | M4-001, TEST-003; Fixed |
| BUG-0123 | 2026-07-24 | 2026-07-24 | Low | API asset repository / TypeScript | Inference retained the concrete in-memory branch in a repository union, hiding the interface optional close hook. | Annotated the resolved value as `AssetRepository`; focused API type checking passes. | M4-001, TEST-002; Fixed |
| BUG-0122 | 2026-07-24 | 2026-07-24 | Medium | Asset repository / TypeScript | The optional repository close hook was inserted outside the `AssetRepository` interface. | Moved it into the interface; focused core type checking and the SQLite close/reopen persistence regression pass. | M4-001, TEST-001; Fixed |
| BUG-0121 | 2026-07-24 | 2026-07-24 | Low | Studio asset test | The managed-picker assertion rendered the base schema, which intentionally has no asset field. | Used the asset-bearing authoring schema and entries; all 11 Studio interactions pass. | M4-001, TEST-003; Fixed |
| BUG-0120 | 2026-07-24 | 2026-07-24 | Low | Studio asset test | The new regression used jest-dom matchers although this Vitest setup exposes plain Chai. | Replaced them with plain value/text assertions; the focused Studio suite passes. | M4-001, TEST-003; Fixed |
| BUG-0119 | 2026-07-24 | 2026-07-24 | Low | Studio asset test | The test client added usage handling but omitted the asset-list response, so the panel correctly rendered empty. | Returned the configured asset fixture from the list route; the managed card renders. | M4-001, TEST-003; Fixed |
| BUG-0118 | 2026-07-24 | 2026-07-24 | Medium | Asset upload / Source patch | The retry-safety patch omitted the closing brace and left the superseded post-completion check in place. | Restored the boundary and removed the late check; focused core typecheck and three asset tests pass. | M4-001, TEST-001; Fixed |
| BUG-0117 | 2026-07-24 | 2026-07-24 | High | Asset upload / Retry safety | Declared-size validation happened after object-storage completion, so a mismatch could consume the multipart upload before returning 422. | Validate submitted part sizes before storage completion; mismatch regression passes without consuming the adapter upload. | M4-001, TEST-001; Fixed |
| BUG-0116 | 2026-07-24 | 2026-07-24 | Medium | Studio / Asset picker | Passing an empty managed library hid the documented demonstration choices and broke existing authoring. | Empty managed libraries retain the demonstration fallback while populated libraries replace it; all Studio tests pass. | M4-001, TEST-003; Fixed |
| BUG-0115 | 2026-07-24 | 2026-07-24 | Medium | Studio / Source patch | The `AssetReference` type was inserted before rather than inside its type-only import block. | Moved it into the type-only block; focused Studio typecheck passes. | M4-001, TEST-003; Fixed |
| BUG-0114 | 2026-07-24 | 2026-07-24 | Medium | Universal client / Source patch | Binary-body conversion statements were inserted inside the method parameter list. | Moved conversion into the method body; focused client typecheck passes. | M4-001, TEST-001; Fixed |
| BUG-0113 | 2026-07-24 | 2026-07-24 | Medium | Universal client / Binary upload | Strict DOM types rejected a generic `Uint8Array<ArrayBufferLike>` as a fetch body. | Copied bytes into a concrete `ArrayBuffer`; focused client typecheck and transport regression pass. | M4-001, TEST-001; Fixed |
| BUG-0112 | 2026-07-24 | 2026-07-24 | Medium | API asset integration / Source patch | Two zero-context insertions landed after `BuildServerOptions` and inside `seedScope`, producing parse errors. | Moved both blocks to their intended syntactic boundaries; focused API typecheck passes. | M4-001, TEST-002; Fixed |
| BUG-0111 | 2026-07-24 | 2026-07-24 | Low | Asset service tests | Two focused assertions expected a `status` property although stable core errors expose `statusCode`. | Asserted `statusCode` while retaining error-code checks; both focused asset-service tests pass. | M4-001, TEST-001; Fixed |
| BUG-0110 | 2026-07-24 | 2026-07-24 | Medium | Asset service / TypeScript | Optional injected rendition and content services rejected explicitly absent constructor values under exact-optional typing. | Modeled both private dependencies as explicit adapter-or-undefined fields; focused core typecheck passes. | M4-001, TEST-001; Fixed |
| BUG-0109 | 2026-07-24 | 2026-07-24 | Medium | Asset schema / TypeScript | The first asset contract imported a nonexistent runtime `contentScopeSchema`. | Added the canonical six-field serializable scope shape locally; focused schema and core typechecks pass. | M4-001, TEST-001; Fixed |
| BUG-0094 | 2026-07-23 | 2026-07-23 | Low | Graph analysis / Windows sandbox | Graphify AST extraction could not create its multiprocessing pipe under the managed Windows sandbox. | Reran the same deterministic extractor with its supported `parallel=False` path; all 103 code files completed with 1,218 nodes and 2,242 edges. | M3-006, FND-002; Fixed |
| BUG-0108 | 2026-07-23 | 2026-07-23 | Low | Browser walkthrough / Local configuration | The temporary Studio bundle targeted `localhost:4000` while the API was bound to `127.0.0.1`, and the API origin list omitted the 127.0.0.1 Studio URL. | Rebuilt the temporary bundle for the exact API URL and restarted the owned API with the matching origin; desktop and 700x900 checks load and run quality reports without console errors or overflow. | M3-006, TEST-005; Fixed |
| BUG-0107 | 2026-07-23 | 2026-07-23 | Low | Changelog / Formatting | The final new quality bullet was concatenated with the existing component-lifecycle bullet. | Restored the missing line break; the Unreleased Added list now contains two distinct bullets. | M3-006, FND-002; Fixed |
| BUG-0106 | 2026-07-23 | 2026-07-23 | Medium | Quality service / Windows encoding | Curly punctuation in three new quality messages was mojibake-corrupted by the Windows write path. | Normalized parsing and report punctuation to ASCII-safe characters; strict core typecheck passes. | M3-006, TEST-001; Fixed |
| BUG-0105 | 2026-07-23 | 2026-07-23 | Low | Core quality test / Lint | The quality gate error class was imported as a runtime value although it is used only in a type assertion. | Marked the import type-only; focused Biome lint and formatting pass. | M3-006, TEST-001; Fixed |
| BUG-0104 | 2026-07-23 | 2026-07-23 | Low | Graph analysis / Semantic schema | Five manually recovered semantic nodes used unsupported `file_type: test` instead of Graphify's `code` type. | Normalized the five nodes to `code`; the 1,608-node graph rebuilds with no extraction warning. | M3-006, FND-002; Fixed |
| BUG-0103 | 2026-07-23 | 2026-07-23 | Low | Graph analysis / PowerShell compatibility | The semantic-merge helper used the PowerShell 7-only `utf8NoBOM` encoding name under PowerShell 5.1. | Used an explicit UTF-8 encoder; all six chunks merged and semantic results for 114 files were cached. | M3-006, FND-002; Fixed |
| BUG-0102 | 2026-07-23 | 2026-07-23 | Low | API quality test / Authorization | The gate-bypass regression replaced the administrator role with `quality-admin`, so authorization correctly denied publication before gate evaluation. | Retained authorization and supplied both `admin` and `quality-admin`; all 14 API integration tests pass. | M3-006, TEST-002; Fixed |
| BUG-0101 | 2026-07-23 | 2026-07-23 | Low | Quality test / Score assertion | The focused quality regression hard-coded score 0 although the declared deductions deterministically produce 4. | Asserted that the score is below the configured gate threshold while retaining exact finding assertions; both focused tests pass. | M3-006, TEST-001; Fixed |
| BUG-0100 | 2026-07-23 | 2026-07-23 | Low | Windows verification / Process launch | The managed sandbox denied Vitest from spawning its focused core-test worker. | Reran the identical focused test through the approved workspace Vitest binary; the worker started and executed both tests. | M3-006, TEST-001; Fixed |
| BUG-0099 | 2026-07-23 | 2026-07-23 | Medium | Client / Source generation | A PowerShell replacement inserted a literal `` `n `` token into the client import and export type lists. | Replaced only the two literal tokens with actual line breaks; scoped formatting and strict client typecheck pass. | M3-006, TEST-001; Fixed |
| BUG-0098 | 2026-07-23 | 2026-07-23 | Medium | Quality service / TypeScript | The optional external-link adapter field rejected an explicitly undefined constructor value under exact optional property types. | Modeled the injected dependency as an explicit adapter-or-undefined field; strict core typecheck passes. | M3-006, TEST-001; Fixed |
| BUG-0097 | 2026-07-23 | 2026-07-23 | Medium | Quality policy / TypeScript | Empty object defaults for nested Zod quality-policy objects did not satisfy their resolved output types under strict TypeScript. | Replaced each empty default with the exact resolved values; strict schema typecheck passes with runtime defaults unchanged. | M3-006, TEST-001; Fixed |
| BUG-0096 | 2026-07-23 | 2026-07-23 | Low | Changelog / Update script | A broad heading replacement duplicated the BUG-0095 changelog note in a historical release section. | Replaced the second exact line only, retained the Unreleased entry, and verified a single BUG-0095 changelog occurrence. | M3-006, FND-002; Fixed |
| BUG-0095 | 2026-07-23 | 2026-07-23 | Low | Workspace / Formatting | The scoped `pnpm exec biome` command could not resolve the installed Biome executable even though the workspace binary exists. | Invoked the exact checked-in workspace binary; scoped formatting completed successfully across the nine named implementation and ledger files. | M3-006, FND-002; Fixed |
| BUG-0093 | 2026-07-23 | 2026-07-23 | Medium | Studio / Responsive CSS | The component-governance style block was inserted before every responsive `.studio-workspace` rule, duplicating declarations and preserving a three-column governance layout on narrow viewports. | Consolidated one base rule with explicit two-column/one-column breakpoints; Studio tests and E2E build pass, and a 700×900 in-app browser check reports a single 645px column with no element or document overflow. | M3-005, TEST-003; Fixed |
| BUG-0092 | 2026-07-23 | 2026-07-23 | Medium | Edge E2E / Local server reuse | The E2E harness reused a manually started API whose CORS allow-list omitted the separate preview-application origin, so the iframe could not fetch its authenticated draft. | Stopped only the two temporary walkthrough servers and reran `pnpm test:e2e` with Playwright-owned canonical services; the complete Edge edit/preview/save/publish/delivery flow passed in 13.3 seconds. | M3-005, TEST-005; Fixed |
| BUG-0091 | 2026-07-23 | 2026-07-23 | Low | Workspace / Formatting | The root `format` script ignores path arguments appended after its workspace target and formats the full repository. | Used `pnpm exec biome format --write <paths>` for true scoped formatting; it visited only the named files, and Git status confirmed the earlier workspace pass introduced no unrelated tracked changes. | M3-005, FND-002; Fixed |
|---|---|---|---|---|---|---|---|---|
| BUG-0090 | 2026-07-23 | 2026-07-23 | Low | Windows verification / Process lifecycle | The pnpm format-check wrapper did not exit after Biome completed successfully. | Invoked the identical repository formatter binary directly; all 121 files are clean in 269 ms and the process exits normally, isolating the issue to the Windows package-manager wrapper. | M3-004, FND-002; Fixed |
| BUG-0089 | 2026-07-23 | 2026-07-23 | Medium | Studio / Comment targets | Field comments could inherit an unrelated selected component node ID. | Limited node targets to the active component-tree field while retaining coherent whole-entry/field presence; the Studio interaction regression selects `story` after a Hero and proves the thread excludes that Hero ID. | M3-004, TEST-003; Fixed |
| BUG-0088 | 2026-07-23 | 2026-07-23 | Medium | Edge E2E / Process lifecycle | The full browser command hung without test output and temporarily retained spawned Node/Edge processes after timeout. | Split the already-green production gate from a bounded Playwright invocation; the Edge walkthrough passed in 16.5 seconds and its API, Vite, and browser processes cleaned up. | M3-004, TEST-005; Fixed |
| BUG-0087 | 2026-07-23 | 2026-07-23 | Low | Schema generation / Formatting | Generated schema imports became formatter-invalid when four authoring types were required. | Made the generator emit formatter-stable multiline imports when necessary and added a deterministic regression; `schema:check`, format, type, and full build gates pass. | M3-004, FND-002; Fixed |
| BUG-0086 | 2026-07-23 | 2026-07-23 | Medium | API / Localization regression | The shared page fixture omitted the new localized story value and lowered translation completeness from 50% to 38%. | Populated the canonical API fixture with semantic story content so the required-locale completeness lifecycle remains meaningful; all 13 API server tests and the full gate pass. | M3-004, M2-004; Fixed |
| BUG-0085 | 2026-07-23 | 2026-07-23 | Low | Windows formatting / Ledgers | PowerShell escaping corrupted new bug-ledger row delimiters and backtick-wrapped actor IDs. | Replaced the open table atomically with literal-safe UTF-8 content; ledger validation and repository formatting pass. | M3-004, FND-002; Fixed |
| BUG-0084 | 2026-07-23 | 2026-07-23 | Low | Test / Lint | New regressions retained a forbidden non-null assertion and a value import used only as a type. | Replaced the assertion with an explicit fixture invariant and made the error import type-only; focused and full lint pass. | M3-004, TEST-003; Fixed |
| BUG-0083 | 2026-07-23 | 2026-07-23 | Medium | Studio / Presence lifecycle | The presence effect captured the selected entry object while depending only on its ID. | Captured a stable selected-entry ID, aligned effect dependencies, and retained heartbeat/leave cleanup; hook lint and all 12 Studio tests pass. | M3-004; Fixed |
| BUG-0082 | 2026-07-23 | 2026-07-23 | Low | Studio / Accessibility | Collaboration presence and rich-text toolbar groups used unsupported ARIA labels on generic elements. | Used a semantic toolbar plus list/list-item presence markup; accessibility lint and Studio interactions pass. | M3-004; Fixed |
| BUG-0081 | 2026-07-23 | 2026-07-23 | Medium | Schema / TypeScript | Asset validation assumed a Zod-defaulted input property was always present. | Applied the declared all-kind default when input omits `accepts`; strict schema typecheck and authoring validation regressions pass. | M3-004, FND-002; Fixed |
| BUG-0080 | 2026-07-23 | 2026-07-23 | Medium | Collaboration / Mentions | Sentence punctuation was captured as part of a mention actor ID. | Tightened mention boundaries while retaining interior actor-ID punctuation; focused service/API tests parse `@reviewer.` as `reviewer`. | M3-004; Fixed |
| BUG-0079 | 2026-07-23 | 2026-07-23 | Medium | API / CORS | Browser comment assignment and resolution updates failed preflight. | Added `PATCH` to the explicit CORS method allow-list and verified a real browser-style OPTIONS request plus the full API suite. | M3-004, TEST-005; Fixed |
| BUG-0078 | 2026-07-23 | 2026-07-23 | Medium | Collaboration / Validation | Malformed comment due dates escaped as a generic server error. | Normalized due dates behind the stable `invalid_due_date` 400 boundary; core and API regressions cover create/update behavior. | M3-004; Fixed |
| BUG-0077 | 2026-07-23 | 2026-07-23 | Low | Windows formatting | Final preview patches reintroduced CRLF in five TypeScript files and retained a Studio BOM. | Normalized the five touched files to LF without a BOM and applied the repository formatter to the two remaining layout deltas; `pnpm format:check` passes across all 115 formatted files. | M3-003, FND-002; Fixed |
| BUG-0066 | 2026-07-23 | 2026-07-23 | Medium | Preview sessions / Revocation | Studio could not token-revoke a grant bound to the application origin. | Added permission-checked, scope-matched management revocation while retaining origin-bound self-revocation; core/API regressions and the Edge close flows pass. | M3-003; Fixed |
| BUG-0067 | 2026-07-23 | 2026-07-23 | Medium | Browser preview bundle | Runtime schema imports pulled Zod into browser preview bundles. | Replaced runtime barrel validation with lightweight browser guards plus type-only protocol drift checks; final Studio/example bundles are 241.57/208.09 KB across 28/24 modules. | M3-003, FND-002; Fixed |
| BUG-0068 | 2026-07-23 | 2026-07-23 | Low | API configuration test | A preview configuration regression was inserted inside another assertion. | Restored the test boundary and kept the preview secret/origin cases independent; all 16 API tests and the full gate pass. | M3-003, TEST-002; Fixed |
| BUG-0069 | 2026-07-23 | 2026-07-23 | Low | API preview test | A bodyless management DELETE incorrectly carried a JSON content type. | Sent management revocation with bodyless headers so Fastify reaches the route; the API regression and Edge cleanup pass with 204. | M3-003, TEST-002; Fixed |
| BUG-0070 | 2026-07-23 | 2026-07-23 | Low | Studio preview tests | Status assertions encoded the separator as a literal question mark. | Made the assertions separator-tolerant while retaining mode/connection semantics; all 11 Studio tests pass. | M3-003, TEST-003; Fixed |
| BUG-0071 | 2026-07-23 | 2026-07-23 | Medium | Browser preview sequencing | Concurrent acceptance could reorder valid messages into false replay failures. | Serialized application-side acceptance before applying messages; 10 client tests and the ordered Edge handshake/navigation/patch flow pass. | M3-003; Fixed |
| BUG-0072 | 2026-07-23 | 2026-07-23 | Low | Windows formatting | CRLF restoration conflicted with the repository LF formatter policy. | Preserved LF across tracked text; `pnpm format:check` and the complete repository gate pass. | M3-003, FND-002; Fixed |
| BUG-0073 | 2026-07-23 | 2026-07-23 | High | Application preview / Browser integration | CORS preflight rejected token-authenticated preview requests because `authorization` was not allowed. | Added `authorization` to the explicit CORS header allow-list and a real OPTIONS regression; API tests and Edge message POSTs pass. | M3-003, TEST-005; Fixed |
| BUG-0074 | 2026-07-23 | 2026-07-23 | Low | Edge preview test | The iframe assertion confused the entry title with the rendered Hero heading. | Asserted the actual component fixture while retaining semantic heading lookup; the complete Edge walkthrough passes. | M3-003, TEST-005; Fixed |
| BUG-0075 | 2026-07-23 | 2026-07-23 | High | Preview application / Published cache | Preview hosts fetched published delivery while awaiting draft patches, priming a stale public cache. | Embedded/opener-controlled applications now wait for authenticated patches and never fetch published delivery; Edge proves live drafts remain isolated and final publication delivers the new revision. | M3-003, TEST-005; Fixed |
| BUG-0076 | 2026-07-23 | 2026-07-23 | Medium | Studio preview / Route synchronization | Runtime readiness overwrote the queued authoritative route with the neutral bootstrap path. | Studio now preserves its queued route on ready and consumes later application navigation messages separately; Edge verifies `/welcome` synchronization. | M3-003, TEST-005; Fixed |
| BUG-0065 | 2026-07-19 | 2026-07-19 | Medium | Preview sessions | Session creation parsed the preview URL outside the service's stable error boundary, so malformed input could escape as a generic exception and become a 500 response. | Wrapped URL construction in the stable `invalid_preview_url` boundary; preview service regressions verify malformed/denied targets, signatures, expiry, origin matching, replay, and revocation, with 51 focused schema/core tests passing. | M3-003; Fixed |
| BUG-0064 | 2026-07-19 | 2026-07-19 | Medium | Browser bundle | The example design-system constant imported a runtime schema helper, pulling Zod-backed validation into Studio and Vite delivery bundles. | Replaced the runtime helper import with a type-only `satisfies` contract; production builds returned from 115/118 modules and 284/315 KB to 23/27 modules and 203/234 KB while retaining the new feature set. | M3-002, FND-002; Fixed |
| BUG-0063 | 2026-07-19 | 2026-07-19 | Medium | Studio / Design tokens | Token selectors filtered only by primitive type, so a string spacing token could be offered to an enum prop such as Hero tone even though its value was not allowed. | Token compatibility now enforces enum membership, numeric bounds, string lengths, and primitive types; the interaction regression proves Section spacing is absent from Hero tone and all nine Studio tests pass. | M3-002; Fixed |
| BUG-0062 | 2026-07-19 | 2026-07-19 | Medium | React package / TypeScript | A focused React typecheck consumed stale built schema declarations and reported that the newly added design-system version field did not exist. | The React package now rebuilds its public schema declaration boundary before strict checking; the same focused command passes independently. | M3-002, FND-002; Fixed |
| BUG-0061 | 2026-07-19 | 2026-07-19 | Low | Studio tests | The governed-reuse interaction regression expected seven layers after inserting one symbol and a three-node recursive template into a two-node page. | Corrected the recursive layer arithmetic to six; both Studio files and all nine tests pass, including the complete design-binding/reuse interaction. | M3-002; Fixed |
| BUG-0060 | 2026-07-19 | 2026-07-19 | Medium | Schema / TypeScript | The handwritten component-presentation interface was incompatible with Zod 4 optional-output semantics under exact optional property types. | Derived the public presentation type directly from its canonical schema so runtime and compile-time optionality cannot diverge; 18 schema tests and focused strict typecheck pass. | M3-002; Fixed |
| BUG-0059 | 2026-07-19 | 2026-07-19 | Medium | Studio / History | Boundary keyboard/root moves could commit a new undo step and dirty the draft even when the component order did not change. | Same-collection commands now return the original tree when their normalized destination equals the source index; focused regressions cover both collection boundaries and all seven Studio tests pass. | M3-001; Fixed |
| BUG-0058 | 2026-07-19 | 2026-07-19 | Medium | Studio / Composition | Activating a selected container's slot drop button tried to move the container into itself and displayed a cycle rejection. | The focusable drop target now exposes keyboard-nesting guidance while actual non-pointer nesting remains on the layer's ArrowRight command; lint, typecheck, and interaction tests pass. | M3-001; Fixed |
| BUG-0057 | 2026-07-19 | 2026-07-19 | Low | Studio / CSS | Slot header selectors introduced descending specificity ahead of the preview toolbar selector. | Replaced structural slot typography selectors with stable low-specificity classes; Biome lint passes with no diagnostics. | M3-001; Fixed |
| BUG-0056 | 2026-07-19 | 2026-07-19 | Medium | Studio / Accessibility | Drop targets and selectable card headers used pointer handlers on static elements, while labelled palette/layer containers had no supporting landmark role. | Converted drop targets and card selectors to semantic buttons, layers to a labelled section, and the slot palette to a labelled fieldset; accessibility lint and seven Studio tests pass. | M3-001; Fixed |
| BUG-0054 | 2026-07-19 | 2026-07-19 | Low | Studio / TypeScript | Root-palette filtering did not preserve narrowing for an optional component acceptance list. | Normalized the active schema's optional acceptance list once and reused the narrowed list for rules and palette filtering; focused Studio typecheck passes. | M3-001; Fixed |
| BUG-0055 | 2026-07-19 | 2026-07-19 | Low | Windows verification | Workspace child-process launches were denied by the managed sandbox while schema generation and Vite-backed tests initialized. | Reran the exact generation and focused test commands with scoped process-launch approval; generation completed and all seven Studio tests pass. | M3-001; Fixed |
| BUG-0001 | 2026-07-17 | 2026-07-17 | High | Schema / TypeScript | Recursive component-node Zod schema was incompatible with the exact-optional `slots` contract. | Declared the Zod-compatible optional output explicitly; the schema package typecheck passed on the next run. | SCH-003, TEST-001; Fixed |
| BUG-0002 | 2026-07-17 | 2026-07-17 | High | Workspace / TypeScript | Clean-clone root typecheck could not resolve dependency declarations. | Root typecheck/test scripts build package declarations before dependents; verified by successful package checks and traversal into all applications. | FND-002, DX-001; Fixed |
| BUG-0003 | 2026-07-17 | 2026-07-17 | High | Workspace / pnpm | Root verification filters matched zero projects and returned a false success. | Replaced brittle filters with verified selectors; the next run built five packages and checked all eight workspace projects. | FND-002, DX-001, BUG-0002; Fixed |
| BUG-0004 | 2026-07-17 | 2026-07-17 | High | Studio / Dependencies | Studio omitted its direct public schema dependency. | Declared `@gridstory/schema`; the next full typecheck passed across all eight workspace projects. | STUDIO-002, FND-002; Fixed |
| BUG-0005 | 2026-07-17 | 2026-07-17 | Low | Documentation / Formatting | README contained an extra blank line at EOF. | Removed the extra blank line; `git diff --check` is included in the final repository check. | DOC-001; Fixed |
| BUG-0006 | 2026-07-17 | 2026-07-17 | Medium | Studio / Accessibility | Repeated component blocks generated duplicate prop-control IDs, so labels could target the wrong block field. | Namespaced every prop-control ID with its immutable block node ID; the Studio regression test renders repeated Hero blocks and verifies distinct IDs. | STUDIO-002, STUDIO-006, TEST-003; Fixed |
| BUG-0007 | 2026-07-17 | 2026-07-17 | High | Studio / Data safety | Selecting or creating another page could discard unsaved local edits without warning. | Added confirmation guards for entry changes and creation plus a browser unload warning; the Studio regression test verifies cancelled navigation preserves the edit. | STUDIO-003, STUDIO-006, TEST-003; Fixed |
| BUG-0008 | 2026-07-17 | 2026-07-17 | Low | Studio tests | Component test DOM leaked between cases because cleanup was not registered when Vitest globals were disabled. | Registered explicit Testing Library cleanup after each case; both isolated Studio component tests pass. | TEST-003; Fixed |
| BUG-0009 | 2026-07-17 | 2026-07-17 | Medium | Browser tests | The Playwright harness timed out waiting for a production-preview service because normal development ports were already occupied. | Added test-mode frontend builds on dedicated high ports and launched direct Node/Vite processes for reliable teardown; the Edge walkthrough starts and passes. | TEST-005, FND-002; Fixed |
| BUG-0010 | 2026-07-17 | 2026-07-17 | High | Universal client / Browser | The default browser `fetch` was invoked with the client instance as its receiver, causing `TypeError: Illegal invocation`. | Bound the default fetch to `globalThis`, added a receiver-sensitive SDK test, and verified Studio/API requests through the passing Edge walkthrough. | SDK-002, TEST-003, TEST-005; Fixed |
| BUG-0011 | 2026-07-17 | 2026-07-17 | Low | Browser tests | The publish selector also matched the `Published` preview button. | Required an exact accessible button name; the complete browser walkthrough passes. | TEST-005; Fixed |
| BUG-0012 | 2026-07-17 | 2026-07-17 | Low | Repository / Formatting | The newly enabled formatter reported 48 files with formatting drift. | Applied the deterministic workspace formatter; both `pnpm format:check` and the complete quality gate pass. | FND-002; Fixed |
| BUG-0013 | 2026-07-17 | 2026-07-17 | Medium | Studio / Accessibility | Studio controls omitted explicit button types and used an invalid generic ARIA group. | Added `type="button"` to every action and replaced the generic group with a labelled `fieldset`; Biome accessibility rules and Studio tests pass. | STUDIO-006, FND-002; Fixed |
| BUG-0014 | 2026-07-17 | 2026-07-17 | Low | Studio / Maintainability | Block reordering relied on non-null assertions and CSS relied on `!important`/descending specificity. | Added a bounds-safe move helper and simplified class-specific cascade rules; lint and component tests pass. | STUDIO-002, FND-002; Fixed |
| BUG-0015 | 2026-07-17 | 2026-07-17 | Low | Tooling | Biome used a deprecated preset declaration and retry state lacked a semantic effect input. | Migrated to the supported preset and made retry attempts drive busy/status state; the complete lint/test/build gate passes. | FND-002, STUDIO-006; Fixed |
| BUG-0016 | 2026-07-17 | 2026-07-17 | Low | Workspace / TypeScript | A targeted dependent-package typecheck read stale built declarations and missed a new schema export. | Rebuilt the schema declaration boundary before checking `example-kit`; both packages pass. Root verification already enforces this dependency order. | FND-002, SCH-005; Fixed |
| BUG-0017 | 2026-07-17 | 2026-07-17 | High | Schema / Type generation | Contextual schema typing widened nested field names and produced an unsafe inferred content union. | Added const-preserving schema/manifest definition helpers; the Vite renderer now receives an exact required `ComponentNode[]`, and the full typecheck passes. | SCH-005, EX-001; Fixed |
| BUG-0018 | 2026-07-17 | 2026-07-17 | Medium | Schema / Browser bundle | Importing the schema helper from the main barrel pulled runtime validation into Studio. | Moved type generation to `@gridstory/schema/typegen`; Studio returned from about 282 KB/107 modules to 208 KB/24 modules in the verified production build. | SCH-005, FND-002; Fixed |
| BUG-0019 | 2026-07-17 | 2026-07-17 | Low | Context / Tooling | Context and identity modules produced type-import and optional-chain lint warnings. | Removed unnecessary runtime import syntax and simplified nullable active-resource checks; lint passes with no diagnostics. | M2-001, M2-002; Fixed |
| BUG-0020 | 2026-07-17 | 2026-07-17 | High | API / Workspace | The API's direct schema dependency was not linked after a lockfile-only refresh. | Synchronized the offline workspace installation; API resolution, all 21 tests, typechecks, and builds pass. | M2-001, FND-002; Fixed |
| BUG-0021 | 2026-07-17 | 2026-07-17 | High | SQLite / Migration | Existing tenant-only databases would create a scope index before adding its missing columns. | Preserved the legacy index during initial schema execution, added scope columns idempotently, then created the composite index; a real legacy-file migration test passes. | M2-001, CORE-002; Fixed |
| BUG-0022 | 2026-07-17 | 2026-07-17 | Medium | Core tests / Portability | The shared stale-write conformance assertion only handled Promise rejection, so SQLite's valid synchronous conflict escaped the test assertion. | Wrapped the awaitable repository call in an async assertion callback; SQLite conformance now captures synchronous throws and PostgreSQL Promise rejections identically. | M2-003; Fixed |
| BUG-0023 | 2026-07-17 | 2026-07-17 | Medium | PostgreSQL test tooling / Windows | The disposable database harness started PostgreSQL but could not resolve the extensionless `pnpm` command shim from Node on Windows. | Invoked the active pnpm CLI through `process.execPath` and `npm_execpath`, avoiding platform-specific command shims; the disposable container test is rerun as M2-003 evidence. | M2-003; Fixed |
| BUG-0024 | 2026-07-17 | 2026-07-17 | Medium | Schema / Type generation | Advanced reusable-object inference used invalid conditional-type syntax for a readonly inferred array. | Parenthesized the inferred array element type; formatting and strict schema compilation are rerun before M2-004 proceeds. | M2-004; Fixed |
| BUG-0025 | 2026-07-17 | 2026-07-17 | High | Schema / Backward compatibility | Type generation assumed every existing schema literal explicitly supplied the new defaulted `objects` collection. | Normalized omitted object definitions to an empty collection inside generation; the legacy and advanced type-generation fixtures pass together. | M2-004, SCH-005; Fixed |
| BUG-0026 | 2026-07-17 | 2026-07-17 | High | Schema / Backward compatibility | The author-facing content schema type used normalized Zod output, making newly defaulted `objects` and `taxonomies` mandatory in existing schema-as-code. | Exposed author-facing Zod input types while validation retains normalized parsed output, honored exact optional properties and runtime defaults, and verified every package builds with the unchanged example schema. | M2-004, SCH-001, SCH-005; Fixed |
| BUG-0027 | 2026-07-17 | 2026-07-17 | Low | Schema / Lint | Advanced model traversal used expression-bodied `forEach` callbacks that implicitly returned `void`, and validation retained one unused type import. | Converted traversal callbacks to explicit block bodies and removed the stale import; the complete quality gate is rerun before completion. | M2-004, FND-002; Fixed |
| BUG-0028 | 2026-07-17 | 2026-07-17 | High | Studio / Advanced fields | Studio assumed every non-component field was text-like and accessed `minLength`/`maxLength`, so the advanced field union broke compilation. | Added schema-discriminated scalar controls, structured JSON editing with inline validity feedback, and type-aware new-entry defaults; Studio compilation and component tests are rerun in the full gate. | M2-004, STUDIO-002; Fixed |
| BUG-0029 | 2026-07-17 | 2026-07-17 | Medium | Schema lifecycle / Tests | Migration-step construction could not narrow a twice-evaluated optional hook under exact-optional typing, and the lifecycle fixture undercounted a safe field-label change. | Evaluated the hook once before constructing each step and corrected the impact fixture; strict schema compilation and lifecycle tests pass together. | M2-005; Fixed |
| BUG-0030 | 2026-07-17 | 2026-07-17 | Medium | Core / Schema drift | Lifecycle drift construction passed absent deployment values explicitly as `undefined`, violating exact-optional contracts. | Omitted all deployment-derived properties when no deployment exists; clean package declarations and core typecheck are rerun. | M2-005; Fixed |
| BUG-0031 | 2026-07-17 | 2026-07-17 | Medium | Schema generation / Quality | Deterministic generated contracts used noncanonical quotes, unconditional unused imports, quoted identifier keys, empty interfaces, and an extra trailing blank line rejected by workspace quality rules. | Generated only required imports, emitted formatter-native identifiers/literals, used empty record aliases, normalized EOF, regenerated the artifact, and passed schema drift, format, lint, boundary, and ledger gates together. | M2-005, SCH-005, FND-002; Fixed |
| BUG-0032 | 2026-07-17 | 2026-07-17 | Medium | Core tests / Package boundaries | The new query-service test imported example-kit fixtures even though core does not depend on example-kit. | Replaced the import with a core-local advanced schema fixture; all focused core tests pass without the undeclared dependency. | M2-006; Fixed |
| BUG-0033 | 2026-07-17 | 2026-07-17 | Low | Workspace / TypeScript | A focused core typecheck consumed stale schema declarations and could not see the new query-contract exports. | Core's focused test and typecheck commands now build the schema declaration boundary first; the focused typecheck and 19 core tests pass. | M2-006, FND-002; Fixed |
| BUG-0034 | 2026-07-17 | 2026-07-17 | Medium | Core / Query sorting | Normalized sort rules were inferred as the public optional-input type, so strict compilation rejected cursor tuple comparisons that require resolved direction and null placement. | Declared the normalized function and collection as fully required sort rules; strict core compilation passes. | M2-006; Fixed |
| BUG-0035 | 2026-07-17 | 2026-07-17 | Low | Core tests / Query fixture | The query test expected a record named `Zulu` to match a case-insensitive `contains: a` predicate. | Corrected the fixture to a genuinely matching title and verified both ordered cursor pages. | M2-006; Fixed |
| BUG-0036 | 2026-07-17 | 2026-07-17 | Medium | API / REST query parser | The parser reconstructed narrowed sort and filter values through optional indexed types, retaining `undefined` under exact-optional compilation. | Constructed sort rules incrementally and narrowed present filters to non-nullable values; API and client typechecks pass. | M2-006; Fixed |
| BUG-0037 | 2026-07-17 | 2026-07-17 | Medium | API / GraphQL context | Mercurius's default resolver context type did not include GridStory's injected Fastify request, so authorization-aware resolvers failed strict function-parameter checks. | Applied Mercurius's supported module augmentation for the injected request; GraphQL compilation and authorized/denied resolver integration tests pass. | M2-006; Fixed |
| BUG-0038 | 2026-07-17 | 2026-07-17 | Low | Core / Lint | The query engine retained one unused type import and two iteration callbacks implicitly returned values under the repository's strict callback rule. | Removed the stale import and used block-bodied callbacks; the complete lint, format, boundary, ledger, schema-drift, type, test, and production-build gate passes. | M2-006, FND-002; Fixed |
| BUG-0039 | 2026-07-17 | 2026-07-17 | Medium | Core / Localization types | The localization service imported the core actor contract from the schema package and explicitly emitted an undefined optional route. | Imported `Actor` from the core contract and omitted absent routes; focused and full strict typechecks pass. | M2-007; Fixed |
| BUG-0040 | 2026-07-17 | 2026-07-17 | High | API / Delivery cache isolation | Public delivery varies by organization, tenant, workspace, site, environment, and locale headers but did not declare them in the HTTP `Vary` response header. | Every REST delivery response now varies on all six scope selectors; API regressions assert tenant and locale metadata, and the full API/Edge suites pass. | M2-007, M2-001, API-004; Fixed |
| BUG-0041 | 2026-07-17 | 2026-07-17 | Low | Core tests / Lint | The locale-cycle fixture used two forbidden non-null assertions. | Replaced assertions with a bounds-safe fixture guard; lint, boundaries, and ledgers pass without warnings. | M2-007, FND-002; Fixed |
| BUG-0042 | 2026-07-17 | 2026-07-17 | Medium | Core / Webhook dispatcher | The validated webhook event remained typed as generic `object`, preventing safe event-ID header access under strict compilation. | Narrowed validated event payloads to string-keyed records before header construction; core strict compilation and all 24 normal core tests pass. | M2-008; Fixed |
| BUG-0043 | 2026-07-17 | 2026-07-17 | Medium | API / Webhook management | A successfully deleted webhook subscription returned HTTP 500 instead of the intended empty 204 response. | Removed JSON content-type from bodyless client requests and preserved framework 4xx errors in the stable envelope; webhook deletion returns 204 in API and SDK regressions. | M2-008; Fixed |
| BUG-0044 | 2026-07-17 | 2026-07-17 | Medium | API / Error handling | The stable error handler inspected an `unknown` framework error without a type guard, breaking strict API compilation. | Added object, status, and `Error` narrowing before framework metadata access; focused and repository-wide strict typechecks pass. | M2-008; Fixed |
| BUG-0045 | 2026-07-17 | 2026-07-17 | Low | API / Focused verification | The focused API typecheck consumed stale core declarations and could not see the new operations scope-discovery method. | Focused API verification now rebuilds schema, core, and example-kit declaration boundaries; the standalone worker and server compile cleanly. | M2-008, FND-002; Fixed |
| BUG-0046 | 2026-07-17 | 2026-07-17 | Medium | SQLite / Portability | Existing-entry conflict rows were narrowed to a broad string record, leaving IDs possibly undefined under the SQLite driver's exact value types. | Added an exact existing-entry row contract and explicit driver-boundary cast; strict core compilation and SQLite import tests pass. | M2-009; Fixed |
| BUG-0047 | 2026-07-17 | 2026-07-17 | Low | Core tests / Cross-adapter portability | Shared portability conformance used Promise-only `resolves` assertions even though the repository contract permits synchronous SQLite results. | Awaited the adapter-neutral `Awaitable` result before assertion; identical SQLite and PostgreSQL portability conformance passes. | M2-009; Fixed |
| BUG-0048 | 2026-07-17 | 2026-07-17 | Low | Core / Archive validation | The manifest scope validator inferred its known scope-key list as generic strings, preventing strict indexing of `ContentScope`. | Constrained the manifest key list to exact literals; strict compilation, checksum regressions, and the full quality gate pass. | M2-009; Fixed |
| BUG-0049 | 2026-07-19 | 2026-07-19 | Low | Studio / Focused verification | Studio's focused typecheck consumed stale client declarations and could not see the new operations dashboard type and method. | Focused Studio checks now rebuild schema, client, React, and example-kit declarations; focused and full strict typechecks pass. | M2-010, FND-002; Fixed |
| BUG-0050 | 2026-07-19 | 2026-07-19 | High | Core / Audit integrity | Audit predecessor lookup used timestamp plus UUID text, so multiple mutations at the same timestamp could choose a non-chronological predecessor and break the hash chain. | Added a contiguous per-entry audit sequence to storage, hashing, migration, export, and verification; fixed-clock SQLite and live PostgreSQL chains verify. | M2-010; Fixed |
| BUG-0051 | 2026-07-19 | 2026-07-19 | Low | API tests / Operations view | The administrator summary regression constrained `recentAudit` to a one-item positional array even though the endpoint correctly returned both recent events. | Asserted the newest event independently without constraining history length; all 13 API tests pass. | M2-010; Fixed |
| BUG-0052 | 2026-07-19 | 2026-07-19 | Medium | Workspace / TypeScript | Parallel recursive application typechecks rebuilt the same schema declarations and raced on Windows file locks. | Serialized the recursive typecheck phase while retaining focused dependency rebuilds; the complete type/build gate passes without file-lock races. | M2-010, FND-002; Fixed |
| BUG-0053 | 2026-07-19 | 2026-07-19 | Low | Studio / CSS quality | The administrator metric selector introduced descending specificity relative to an existing preview selector. | Replaced the nested selector with a direct metric class; lint passes without warnings. | M2-010; Fixed |

## Deferred or accepted bugs

| ID | Found | Severity | Area | Summary | Reason and review date | Linked task |
|---|---|---|---|---|---|---|
| — | — | — | — | No deferred defects yet. | — | — |

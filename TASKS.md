# GridStory implementation task list

**Last updated:** 2026-08-28
**Source plan:** `REACT_CMS_RESEARCH_AND_IMPLEMENTATION_PLAN.md`

## Working agreement

- Statuses: `[ ]` planned, `[~]` in progress, `[x]` verified, `[!]` blocked.
- New implementation work receives a stable task ID before code changes begin.
- A task is complete only when its acceptance criteria and relevant automated checks pass.
- Every completed or materially changed task must have a matching `CHANGELOG.md` entry.
- Every bug found during verification must be entered in `BUGS.md` before it is fixed or deferred.

## Current merge-blocker investigation — 2026-08-26

- [x] **CI-001** Diagnose and repair the reported browser CI failure; the bounded correction is verified locally, with a fresh hosted run still pending an authorized push.
  - Completed/verified: Codex, 2026-08-26T20:41:56+05:30. The real-page regression reproduces the supplied WebKit class assertion against the exact baseline helper, while the corrected helper passes expanded/compact transitions in all three engines. It distinguishes actual drawer state from stale desktop ARIA, waits for mobile ARIA/disclosures, then uses the original native click and open/transform assertions. Runtime, styles, dependencies, workflow and all existing scenarios/deadlines are unchanged.
  - Final evidence: `pnpm check` passes 626 tests / 17 existing optional skips on an unchanged full rerun; its first-run intermittent Studio failures remain open as BUG-0461/CI-003, not silently fixed. `CI=1 pnpm test:e2e` passes 48/48 without retries (16 per engine). The full WebKit responsive sweep additionally passes `--repeat-each=3 --retries=0`, 3/3 in 8.3m (3.2m, 2.0m, 2.7m). All original six-width/19-destination/theme/keyboard/permission/history/preview checks remain. BUG-0458's reproduced helper race and BUG-0460's new-fixture portability issue are resolved; hosted Ubuntu replacement success is not claimed.
  - Completion handoff: normal Studio/example bundles restored with unchanged production hashes; isolated ports 44000/44173/44174 released. Commit `test(studio): synchronize mobile navigation readiness [CI-001]` on `codex/cms032-context-checkpoint`; SHA in the delivery handoff. The five-file fence and historical-ledger preservation audits pass. Next obtain authorization to push and verify fresh GitHub checks; no push, merge, protection change or deployment was performed. Earlier investigation checkpoints below are historical.
  - Resumed: Codex, 2026-08-26T20:05:54+05:30, clean baseline `51eb5d2`. The supplied log is run 32970401448 / job 98183440363 (prior head `0790711`, merge ref `dee4303`), not the latest head's log. Chromium/Firefox pass 10 each; WebKit fails the responsive sweep on all three attempts because `openMobileStudioNavigation` expects `studio-navigation--open` but receives `studio-navigation` after 5 seconds. No hosted replacement success is claimed.
  - Investigation scope amendment: add `tests/e2e/accessibility.spec.ts` and `docs/adr/0008-accessibility-and-compatibility.md` to the three ledgers. First reproduce the desktop-expanded/mobile-closed helper decision with a controlled delayed resize, then correct only the existing helper if confirmed. Keep runtime, styles, permissions, dependencies, workflow, native clicks, every existing assertion and all widths/destinations/deadlines unchanged. A runtime file requires a separate evidence-backed fence amendment.
  - Verification plan: failing-before/fixed-after regression on the real Studio page, already-open/closed/mobile-desktop transition controls, repeated no-retry WebKit responsive sweeps, full `pnpm check` and three-engine `CI=1 pnpm test:e2e`; restore normal bundles and stop isolated servers. Commit the verified correction; hosted status remains explicitly unverified until an authorized subsequent CI run.
  - Focused verification: the corrected delayed-width fixture reproduces the supplied class assertion on the exact baseline helper (both WebKit cases fail); the bounded helper correction passes all six expanded/compact cases across Chromium/Firefox/WebKit without retries. BUG-0460 records and corrects the first fixture's native-event/isolated-world mistake. Source audit confirms every prior test and both destination helpers are unchanged; only mobile readiness handling and two added regression cases differ. Full gates remain pending.
  - Owner/start: Codex, 2026-08-26T19:19:34+05:30; local baseline `286979b`, clean `codex/cms032-context-checkpoint`. Tier: T1 diagnostic/targeted repair. Defect: BUG-0458. This temporarily precedes CMS-034; no new feature work starts.
  - Evidence: local main is an ancestor of HEAD, no merge is active and no files conflict. GitHub reports PR #13 mergeable=true / mergeable_state=unstable. Both prior head `0790711` (Quality run 32970401448 / browser job 98183440363) and current head `286979b` (run 32976267620 / job 98202664775) pass quality/PostgreSQL/vulnerability checks but fail `pnpm test:e2e`. The latest job completed at 2026-08-26T19:31:01+05:30. Public failure annotations expose only exit code 1; the action-runtime Node 20 deprecation is a separate warning, not the failing test assertion.
  - Scope: `TASKS.md`, `CHANGELOG.md`, `BUGS.md` for the investigation checkpoint. Amend with exact existing test/runtime files only after identifying a reproducible current failure; preserve all assertions, features, styles and backend authorization.
  - Acceptance: identify the exact failed assertion from authorized logs or establish a successful current-head CI replacement; reproduce and verify any required bounded correction, run applicable full gates, and commit completed work. Do not treat pending checks or old local passes as remote success.
  - Access/limits: public GitHub metadata is readable; the GitHub connector is not signed in and unauthenticated protected log download returns 403. No credential-store access, branch-protection bypass, force push, merge, deployment or unrelated dependency updates. Request the failing test output if current CI still fails; no root-cause or fix claim yet.
  - Necessity/rollback: the request maps directly to one observed failed browser job; existing gates are retained, no replacement CI framework or dependency is needed, and documentation changes are independently reversible. The prior failure may be superseded by the latest commit; verify that before editing application code.
  - Local checkpoint: `pnpm check` passes (including 243 Studio tests, generated contracts, policy/security/tenant/readiness checks, strict types, React 18.3.1 SSR and production builds). PR-mode ledger validation, `pnpm lint`, `pnpm format:check`, `git diff --check` and a read-only audit preserving all 140 historical task statuses / 457 bug rows pass. The unchanged `CI=1 pnpm test:e2e` completes with Chromium 14 passed, Firefox 14 passed, WebKit 13 passed / 1 flaky (the responsive sweep passes its first configured retry in 2.2 minutes after the original 300-second timeout). This is not a clean first-attempt browser pass or hosted success; no fresh manual smoke is claimed.
  - Additional verification defect: BUG-0459's retained trace isolates a 172.16-second standalone `scrollIntoViewIfNeeded` in the accessibility navigation helper; the following normal click succeeds in 571 ms. The final timeout occurs later with no containment assertion failure. CI-002 removes this redundant test-only path without changing coverage or deadlines; the browser/OS cause of the stall and any equivalence to the hosted failure remain unproven.
  - Blocked handoff: the 2026-08-26T19:56:26+05:30 public recheck still shows head `286979b`, mergeable=true, browser failure and successful quality/PostgreSQL/vulnerability checks. Obtain the first failed test/error from [job 98202664775](https://github.com/Seance1723/GridStory/actions/runs/32976267620/job/98202664775), step `Run pnpm test:e2e`, then reproduce it or verify a successful authorized replacement. CI-002's local green matrix is not hosted evidence. No push, merge, protection change or deployment was performed.

- [x] **CI-002** Remove the trace-observed redundant pre-click scroll from the existing accessibility navigation helpers.
  - Owner/start: Codex, 2026-08-26T19:41:21+05:30. Tier: T1 test-harness repair. Parent investigation: CI-001. Defect: BUG-0459.
  - Scope: `tests/e2e/accessibility.spec.ts` (only the two shared panel/Pages helper scroll calls and one explanatory comment), `docs/adr/0008-accessibility-and-compatibility.md`, `TASKS.md`, `CHANGELOG.md`, `BUGS.md`. Preserve the failing trace/screenshot/context in ignored `test-results/ci002-before`; no application, style, dependency, workflow or permission changes.
  - Evidence/necessity: the original full CI-mode suite records 300 seconds / one retry; trace step pw:api@1066 spends 172.16 seconds in explicit scrolling at 768px before a 571 ms normal click. Playwright's documented click already scrolls and checks actionability. Reuse that existing native click, not forced/DOM clicks, a new helper abstraction, longer timeouts or fewer widths/screens/assertions.
  - Acceptance: retain every assertion and all six widths / 19 destinations / dark and keyboard coverage, remove only the two redundant calls, pass repeated WebKit responsive runs without retries plus the full quality and 42-scenario browser gates. Document any failures before repair. This task cannot establish that the inaccessible hosted BUG-0458 assertion has been fixed.
  - Rollback: restore the two helper calls and remove the explanatory comment; no persisted state or public contract changes. Focused and full gates ran serially to avoid competing test jobs.
  - Focused evidence: the read-only source-preservation audit passes (only two scroll calls removed and one comment added). `CI=1 pnpm exec playwright test tests/e2e/accessibility.spec.ts --project=webkit --grep 'every Studio surface' --repeat-each=3 --retries=0` passes 3/3 in 4.7 minutes, each sweep 1.5 minutes. Every original viewport, destination, theme, control/text and deadline assertion remains intact.
  - Full verification: 2026-08-26T19:59:37+05:30 by Codex. Post-change `pnpm check` passes with 626 tests / 17 existing optional database-recovery skips, lint/format, generated contracts, boundary/security/tenant/readiness checks, strict types, React 18.3.1 SSR and production builds. Subsequent `CI=1 pnpm test:e2e` passes all 42 scenarios without retries (Chromium 14 in 1.8m, Firefox 14 in 2.3m, WebKit 14 in 3.9m; WebKit responsive sweep 2.2m). Normal Studio/example production bundles were then restored; isolated test ports 44000/44173/44174 are released. No manual UI smoke or hosted fix is claimed.
  - Completion handoff: commit `test(studio): remove redundant navigation scrolling [CI-002]` on `codex/cms032-context-checkpoint`, retaining baseline `286979b` and the original ignored failure artifacts. BUG-0459 is resolved for this bounded harness path; BUG-0458/CI-001 remains blocked on the hosted assertion. All 140 historical task statuses and 457 historical bug rows are preserved. Single next action: obtain the hosted failed-test output, not CMS-034 or a merge.

- [x] **CI-003** Investigate the Studio unit-suite timeout and following AI proposal failure observed during CI-001 verification.
  - Created: Codex, 2026-08-26T20:17:53+05:30. Started: Codex, 2026-08-28T18:43:49+05:30; clean baseline `e110d22`. Tier: T1 bounded diagnostic. Defect: BUG-0461. No runtime or test correction is authorized beyond the recorded investigation until a reproducible cause justifies an exact scope amendment.
  - Evidence: the first CI-001 `pnpm check` passes other workspace suites but reports an existing experiment lifecycle case taking 61.24s against its 15s deadline, followed by the AI proposal case missing its pending-review result. Studio source, unit tests and configuration are byte-unchanged from `51eb5d2`; the changed browser helper is not imported by these tests. Root cause is unproven.
  - Controls: both unchanged focused cases pass together in 16.27s; an unchanged complete `pnpm check` rerun passes all 626 tests, Studio 243/243 and production builds. This remains an unresolved intermittent observation, not a repaired defect. No extra skips, retries, timeout or source changes were introduced.
  - Initial scope: `apps/studio/test/App.test.tsx` and existing App/session sources for read-only diagnosis; `TASKS.md`, `CHANGELOG.md`, `BUGS.md`, `docs/adr/0008-accessibility-and-compatibility.md` for evidence. Declare exact implementation files only after reproduction; do not change timeouts or weaken assertions. No CMS-034, auth, style or feature work.
  - Acceptance/rollback: reproduce with unchanged tests, identify whether timeout cleanup/order or application behavior is responsible, add a failing-before regression for any code fix, and pass focused plus full gates. Keep any non-reproduction explicitly unresolved; no data changes and no rollback action is currently required.
  - Root-cause evidence: the unchanged focused pair passes 10/10 repeated commands plus a leak-detection control, and three timed controls place the experiment at 11.49–11.79 seconds and AI at 4.04–4.27 seconds under the slower local host. A complete unchanged App-file run then overlapped Windows idle sleep: the release test reported 4,355.594 seconds against its 5-second timeout while the System log records a 4,355.007-second hardware-clock correction and return from low power. The historical 2026-08-26 window likewise records idle sleep beginning during the original check, a 56.894-second clock correction, and a 68.570-second low-power interval corresponding to the experiment's abnormal 61.24-second result. BUG-0462 records the reproduced harness symptom.
  - Decision/scope: the timeout is caused by host low-power suspension, not App behavior, shared fixture state, worker ordering or an async-resource leak. No source, test, deadline, retry, dependency or workflow correction passes the necessity gate, so implementation scope remains only `TASKS.md`, `CHANGELOG.md`, `BUGS.md` and `docs/adr/0008-accessibility-and-compatibility.md`. The following AI assertion passes in all 14 focused/timed controls and the unchanged full controls; its exact prior post-resume failure is retained as non-reproducing BUG-0461 evidence rather than assigned an invented fix.
  - Verification plan: rerun the exact focused pair and full `pnpm check` while the host remains awake; audit the four-file documentation fence and ledgers. A failing-before regression is not applicable because no code or test behavior changes. Rollback is documentation-only.
  - Completed/verified: Codex, 2026-08-28T20:13:50+05:30. The post-wake focused pair passes unchanged (experiment 13.56s; AI 4.73s), and the full `pnpm check` passes 626 active tests / 17 existing optional skips, including Studio 243/243 in 92.61s, all policy/security/tenant/readiness checks, strict types, React 18.3.1 SSR and production builds. No independent AI failure occurs. Final ledger/format/fence checks and the completion commit are the remaining handoff mechanics, not product verification.
  - Completion handoff: four documentation/ledger files only; application, tests, timeouts, retries, workflow, dependencies, styles, features and data are byte-unchanged. BUG-0462 is resolved as an interrupted local run; BUG-0461 retains the following AI assertion as explicitly non-reproducing evidence. No browser/manual UI gate is applicable to this diagnostic, and no hosted success, push, merge or deployment is claimed.

## Current administration upgrade queue — 2026-08-26

The [CMS admin gap analysis](docs/cms-admin-gap-analysis.md) maps the supplied reference to current source and every existing destination. [ADR 0027](docs/adr/0027-cms-administration-information-architecture.md) is **proposed**, not implementation approval. The original milestones below remain permanent bounded-delivery history, not a claim that every feature-catalog bullet shipped. Existing beta/RC/GA no-go criteria in `docs/release-readiness.md` are unchanged.

Queue conventions: created 2026-08-26 by Codex; owner unassigned until started; each item's checkbox is its current delivery status, and unchecked items are not implemented by this queue. Every scope includes this task ledger, `CHANGELOG.md`, `BUGS.md`, relevant documentation and tests for the named module. Future files inside a named directory must be enumerated at task start. Each task inherits the gap analysis's common definition of done, including global SCSS, no feature removal, single-destination navigation, tenant/preview boundaries, full verification and a completion commit. T2/T3 work requires its own approved plan before implementation; amend/split a task before broadening it.

### A. Navigation and authorized context

- [x] **CMS-001** Reorganize the 19 existing Studio destinations into task-oriented groups.
  - Tier: T1. Depends: GOV-006. Scope: `apps/studio/src/App.tsx`, a finite `apps/studio/src/navigation.ts`, existing `apps/studio/src/styles/` shell/responsive partials, Studio navigation tests and `tests/e2e/accessibility.spec.ts`.
  - Acceptance: preserve a one-to-one canonical home for all 19 existing destinations per the gap-analysis mapping; exactly one leaf is active and one page is visible after every sequential selection; parent disclosure does not select a page; preserve existing actions, loaded/editor state, responsive drawer, SCSS colors/control styles and header preview. Render no unimplemented leaves.
  - Excludes: new domain screens, URL routing, permission changes, backend changes, new dependencies and broad component refactoring. Rollback: revert this presentation-only slice.
  - Owner/start: Codex, 2026-08-26. The user approved beginning implementation with this first slice. Exact fence: `apps/studio/src/App.tsx`, `apps/studio/src/navigation.ts`, `apps/studio/src/styles/_shell.scss`, `apps/studio/src/styles/_responsive.scss`, `apps/studio/test/App.test.tsx`, `apps/studio/test/navigation.test.ts`, `tests/e2e/accessibility.spec.ts`, `README.md`, `docs/cms-admin-gap-analysis.md`, `docs/adr/0027-cms-administration-information-architecture.md`, and mandatory ledgers.
  - Research/necessity: existing single-destination selection and on-demand loaders are reused; one finite metadata map covers 19 real repeated leaves. Native disclosure buttons/lists follow the WAI disclosure-navigation pattern, without menu/menubar roles, routing or a new dependency. Eight nonempty groups start expanded; compact desktop mode keeps all named icons reachable and preserves expanded-mode disclosure preferences. Group toggles never select or load a destination; header shortcuts reveal their target group. New labels are Library (Assets), Page checks (Quality), and Identity providers (Identity), with existing panel content unchanged. This satisfies the explicit menu gap with a reversible UI-only slice; the larger architecture remains proposed.
  - Verification amendment: manual compact-mode inspection found BUG-0418, a pre-existing footer hide-rule specificity defect inside the declared responsive-style fence. Correct the selector and add compact-hidden/mobile-grid assertions to the navigation regression; no feature or palette change.
  - Scope amendment: the real Windows browser also exposed BUG-0419, where a 320px body minimum exceeds the usable viewport with a native scrollbar. Add only `apps/studio/src/styles/_foundation.scss` to the fence to remove that root minimum; verify the same mobile navigation regression with a stable scrollbar gutter and repeat the full browser gate. Do not broaden this into a style refactor.
  - Verified: 2026-08-26 by Codex. `pnpm --filter @gridstory/studio test:run` passes 37 tests; `pnpm check` passes the repository lint/boundary/security/tenant/readiness, format, generated-contract, type, default workspace-test and production-build gates. The final SCSS corrections additionally pass repeated lint/format and production/E2E builds; `pnpm test:e2e` passes 21/21 Chromium/Firefox/WebKit scenarios, including all 19 destinations at 1440/1280/1024/768/390/320px, themes, keyboard, zoom/forced colors, WCAG checks, standalone preview, save/publish and published delivery. No test is weakened or skipped for this task.
  - Manual evidence: in-app browser clicks covered Content/Advanced disclosure, compact Library selection and disclosure restoration, mobile Regions selection/drawer closure, mobile header search and light/dark mode at 1440/390/320px. The compact help wrapper now computes to `display: none`, restores `grid` on mobile, and the 320px Windows viewport has matching 305px client/scroll widths with its native scrollbar. No browser errors/warnings were recorded. BUG-0418/0419 are resolved; saved project data and existing app servers were untouched. Opt-in PostgreSQL/provider/deployment certification was not rerun or claimed.
  - Handoff: completion commit uses `[CMS-001]` on `main`; all other task statuses/history are preserved. Next: prepare and obtain approval for CMS-002's scoped T2 location/history plan; do not infer approval of CMS-003 through CMS-031.
- [x] **CMS-002** Add stable Studio locations, deep links and history-safe navigation.
  - Tier: T2. Depends: CMS-001. Scope: Studio navigation/location modules, `App.tsx`, Studio unit/browser tests, and hosting documentation/config only if the approved route strategy requires it.
  - Implementation approval/start: Codex, 2026-08-26. The user's next `proceed to next`, responding to the explicit approval question, approves ADR 0028's exact page-only fragment contract, history/guard semantics, implementation fence and verification matrix. Implementation is now in progress; no other CMS task is approved. Reuse existing styles and scoped clients; commit only after the complete gate passes.
  - Acceptance: approved URLs identify the active destination and optional entry/type; reload, direct links, back/forward and invalid locations have deterministic results; cancelled dirty-state navigation preserves both URL and editor data; no tokens/draft values enter URLs; one destination and preview lifecycle invariants remain intact.
  - Delivered/verified: Codex, 2026-08-26. Finite page-only fragment parsing and a native owned/unowned history adapter restore authorized direct links without changing trusted scope, server routes or dependencies. Shared entry/search/history guards preserve dirty drafts and accepted addresses; abort/generation checks reject stale reads; independent entry-write tracking blocks replacement during mutations. Preview transport stops before another entry commits, rejects mismatched patches and revokes late grants. Same-entry destination changes preserve edits and preview; skip focus leaves the route intact. All 19 screens, existing colors and shared SCSS remain unchanged.
  - Final automated evidence: the complete `pnpm check` checkpoint passed; after the last write/migration guards, repeated `pnpm lint`, `pnpm format:check`, `pnpm typecheck` and `pnpm test:run` pass with 430 tests (78 Studio) and 17 existing opt-in skips. The final `pnpm test:e2e` rebuilds production/E2E assets and passes all 30 Chromium/Firefox/WebKit scenarios, including all 19 destinations at six widths, light/dark, readable text/control containment, keyboard, zoom/forced colors, WCAG, deep links, dirty/history guards, preview and published delivery. The isolated WebKit sweep completes in 2.9 minutes under its unchanged five-minute limit (BUG-0432); no assertion, width or timeout was relaxed. Scope audit retains all 136 task IDs and limits changes to the 16 approved files; no dependency, API, published-site or SCSS file changed.
  - Manual evidence and limits: isolated in-memory API/Studio/example services and synthetic content verified copied Library links, reload, Back/Forward, mobile selection/drawer closure, light/dark containment, live preview connection and preview closure on accepted entry replacement. At a 390px viewport, native scrollbars leave matching 375px client/scroll widths. Native cancellation is verified by automated regressions, not claimed from the manual smoke. Test tabs/services were closed, viewport reset, and existing user data/services left untouched. PostgreSQL, live-provider and production deployment certification were not rerun or claimed.
  - Defects/handoff: BUG-0421 through BUG-0426 and BUG-0428 through BUG-0432 are resolved with regression/gate evidence. Pre-existing Create page schema-default validation BUG-0427 stays open for CMS-004, without broadening this approved slice. Commit completion on `main` with `[CMS-002]`; the single next action is a scoped T2 plan and approval for CMS-003, not automatic implementation of the remaining queue. Earlier planning/start checkpoints below remain historical.
  - Excludes: a router dependency without necessity evidence, application-site routing changes, and data mutation. Decide hash versus path/history deployment compatibility before code; rollback preserves existing entry IDs and content.
  - Planning checkpoint: Codex, 2026-08-26. The user's `proceed to next` starts the scoped T2 research/plan, not implementation of an unapproved URL contract. CMS-002 remains planned until the proposal below is approved; CMS-001 stays verified. Planning-only fence: `TASKS.md`, `CHANGELOG.md`, `BUGS.md`, `docs/adr/0028-studio-locations-and-history.md`, and `docs/cms-admin-gap-analysis.md`. Preserve the repository's existing ledger/ADR conventions rather than adding parallel skill ledger files.
  - Proposed plan: [ADR 0028](docs/adr/0028-studio-locations-and-history.md), created 2026-08-26T12:46:25+05:30. Use `#/<destination>` with optional paired `entry`/`type=page`; reuse current scope/loaders and a finite native History adapter, without a dependency or server rewrite. Same-entry screen changes preserve edits/preview; entry changes share a confirmation guard, including search results (BUG-0421). Abort/generation checks reject stale reads; old preview transport stops before a replacement entry is committed. The proposal defines invalid/unavailable targets, owned/unowned history cancellation, native reload limits, exact files, rollback and three-browser acceptance. Awaiting approval; no application files changed.
  - Planning verification: 2026-08-26T12:49:16+05:30 by Codex. `pnpm lint`, `pnpm format:check`, `node scripts/check-ledgers.mjs`, `git diff --check` and `pnpm --filter @gridstory/studio build` pass. Read-only audits verify the five-file documentation fence, all 136 unchanged task statuses, 11 resolving local links, required proposal sections and BUG-0420's corrected pointer. Unit/integration/browser suites were not rerun for this proposal and no new functional verification is claimed; the implementation must run the complete acceptance matrix. BUG-0421 remains open for that implementation. Commit the planning checkpoint separately; next action is explicit approval of ADR 0028.
- [x] **AUTH-001** Close the production preview-token authentication bypass before new Studio capability work.
  - Completed/verified: Codex, 2026-08-26T16:30:37+05:30. Only the API identity hook and request-context runtime changed: exact resolved route/method preview dispatch, POST-only public activation, explicit per-request development mode and fail-closed production private context. BUG-0433 and BUG-0434 no longer reproduce; ordinary workforce sessions, scoped preview and anonymous published delivery remain intact. No Studio/client/core implementation, feature, color, dependency, migration or deployment changed.
  - Verification: failing baseline 200/400 regressions now return 401; final focused API/context/server gate passes 27 tests. `pnpm security:check`, full `pnpm check` (439 active tests; 17 existing opt-in skips), repeated zero-warning lint/API build and all 30 Chromium/Firefox/WebKit scenarios pass. Normal Studio/example production builds restored after E2E. Isolated manual header open/edit/close, save/review/publish/deliver passes; popup rendering is verified by E2E, not manual browser inspection. Fresh read-only investigation and candidate review found no surviving bypass/regression. Exact evidence/limitations: ADR 0029.
  - Defects/handoff: BUG-0433/0434/0436/0437/0438 resolved; BUG-0435 remains CMS-003's separate transport input and BUG-0427 remains CMS-004. Completion commit on `main`: `fix(auth): isolate preview credentials from management [AUTH-001]`. CMS-003 is unblocked for its scoped plan and approval, not automatic implementation. Earlier planning/start entries below are historical.
  - In-fence ledger audit correction: BUG-0438 records the existing resolved-bug table's nine-column delimiter versus eight-column header. Correct only that delimiter while recording AUTH-001 resolutions; preserve all records and identifiers. No source or contract scope expansion.
  - Implementation approval/start: Codex, 2026-08-26T16:10:13+05:30. The user replied `proceed to next` to the explicit AUTH-001 approval question, accepting ADR 0029's API-only boundary, scope and verification. Use project-delivery-loop and security fix-finding, including their required independent read-only investigation/review. No CMS-003 implementation or deployment is approved.
  - Owner/planning start: Codex, 2026-08-26T16:03:30+05:30. Tier: T2. Depends: CMS-002. Security input: BUG-0433. This is a newly discovered prerequisite, not approval to implement an unreviewed authentication change.
  - Planning fence: `TASKS.md`, `CHANGELOG.md`, `BUGS.md`, `docs/adr/0029-production-preview-authentication-boundary.md`, `docs/cms-admin-gap-analysis.md`. Preserve established README/CHANGELOG/ADR ledger mappings; no parallel skill ledgers or source changes.
  - Ask/sequence: reproduce the failing production boundary in an isolated regression; restrict preview-token handling to the exact preview routes/methods; make unbound production management context fail closed; verify ordinary sessions, anonymous delivery and legitimate preview behavior before any CMS-003 projection is added.
  - Acceptance: invalid, valid, expired or revoked preview credentials alone never authorize ordinary management routes; missing/invalid/revoked production sessions are rejected; actual preview operations still validate token, origin, entry/session and replay limits; public delivery stays published-only; no development principal is synthesized for an unbound production management request. Full negative/API/repository/browser evidence precedes completion and commit.
  - Proposed plan: [ADR 0029](docs/adr/0029-production-preview-authentication-boundary.md) freezes exact preview method/path pairs, separate session authentication, fail-closed production request context, an API-only source fence, negative/full/browser verification and safe rollback limits. Awaiting explicit approval; source behavior is unchanged.
  - Planning verification: Codex, 2026-08-26T16:06:58+05:30. `pnpm lint`, `pnpm format:check`, `pnpm --filter @gridstory/api build` and `git diff --check` pass. Read-only audit verifies the five-file planning fence, 13 local links, all 136 historical IDs/statuses except the explicit CMS-003 block, and new planned AUTH-001 (137 tasks total). Isolated read-only production API controls return 401 without a session versus 200 on context and schemas with the invalid preview prefix. These reproduce the open bug, not a fix. No unit/browser/provider/deployment suite was rerun or claimed; commit the planning checkpoint only and request approval.
  - Excludes: new permissions or identity protocols, IdP/provider changes, topology provisioning, capability/scope UI, published cache changes, real data/session revocation and production rollout.
- [x] **CMS-003** Expose authorized Studio capabilities and safe scope selection.
  - Completed/verified: Codex, 2026-08-28. CMS-032, CMS-033 and CMS-034 are each implemented, independently verified and retained as separate commits. The final child adds permitted site/environment/locale switching, write/dirty/preview/stale-lifetime guards and BUG-0435's credential-purpose dispatch repair without changing organization/tenant/workspace, permission meanings, topology ownership or published caches.
  - Aggregate evidence: final `pnpm security:check` and `pnpm check` pass 639 tests with 17 existing optional skips, strict types, generated contracts and production builds. The CMS-034 acceptance matrix passes 51/51 Chromium/Firefox/WebKit scenarios plus the isolated manual dirty-cancel/accept and 390px containment smoke recorded in ADR 0030. CMS-003's three child commits, not this aggregate line alone, are the delivery history.
  - Approval: Codex, 2026-08-26T17:11:33+05:30. User replied `proceed to next` to the explicit ADR 0030 approval question. The three bounded units are approved in sequence; only CMS-032 starts now. This aggregate remains unchecked until all children pass; the active leaf carries `[~]`.
  - Planning checkpoint: Codex, 2026-08-26T16:58:44+05:30, baseline `5882398`. Proposed [ADR 0030](docs/adr/0030-studio-capabilities-and-scope-selection.md) now freezes the minimized private contract, fixed organization/tenant/workspace anchor, deployment-owned permitted tuples, permission-aware bootstrap and guarded scope lifetime. Await explicit T2 approval; no application behavior changed.
  - Delivery split: retain this aggregate ID and deliver CMS-032 → CMS-033 → CMS-034 as separately verified commits. CMS-003 completes only when all three children pass. This avoids one broad, unfinished API/19-screen/lifecycle change; all original tasks and completed states remain intact.
  - Planning fence: `TASKS.md`, `CHANGELOG.md`, `BUGS.md`, `docs/cms-admin-gap-analysis.md`, `docs/adr/0030-studio-capabilities-and-scope-selection.md`. Established README/CHANGELOG/ADR ledger mappings remain; no duplicate skill ledgers.
  - Planning verified: Codex, 2026-08-26T17:05:12+05:30. Full `pnpm check` passes (439 tests, 17 existing optional skips, strict types and production builds); repeated ledger/whitespace checks and read-only five-file/140-task/438-bug/24-link/19-screen/fenced-path audits pass. All 137 prior statuses are unchanged. No runtime source, browser/manual UI or external environment changed; implementation acceptance is still pending. Commit this checkpoint as `docs(plan): define authorized Studio context [CMS-003]`, then request approval of ADR 0030.
  - Prerequisite cleared: 2026-08-26T16:30:37+05:30, Codex. AUTH-001 is verified; resume the minimized capability/context and safe scope-switching plan using the retained gap analysis. This task still needs its own T2 contract approval before implementation; the former block below is historical.
  - Compatibility input: BUG-0435 records existing bearer-session preview revocation dispatch; cookie-based management revocation remains supported. Review its transport contract with context/preview cleanup, not as an unapproved expansion of AUTH-001.
  - Blocked prerequisite (2026-08-26T16:03:30+05:30, Codex): BUG-0433 permits an unverified preview-token prefix to reach development-admin fallback in production. AUTH-001 must close and verify that boundary first. CMS-003 research is recorded in the gap analysis; no capability/context contract or implementation is approved.
  - Tier: T2. Depends: CMS-002, AUTH-001; aggregate completion requires CMS-032, CMS-033, CMS-034. Scope: ADR 0030's exact per-child fences supersede the earlier broad module inventory. Reuse core authorization/scope registry unchanged; new schema/API/client projection, Studio consumption and scope lifetime ship separately.
  - Acceptance: a minimized authenticated projection exposes only permitted context choices/actions; menu visibility and deep-link denial reflect effective permissions without replacing server checks; site/environment/locale switches confirm dirty state, abort stale reads, revoke preview and clear prior-scope data; cross-tenant and revoked-session tests fail closed; configuration unavailable to a user is not enumerated.
  - Excludes: a new authorization engine, editable topology provisioning, role-name guesses, secrets, or trusting URL scope. Any additional permission or contract is reviewed before implementation.

- [x] **CMS-032** Add the minimized authorized Studio context API and immutable scoped client.
  - Completed/verified: Codex, 2026-08-26T18:10:26+05:30. Delivered the API/client foundation from `ef79a8d` and its approved focus/fixture repair. BUG-0441's deterministic regression fails on unchanged App code and passes after repair; four focus controls, nine repeated WebKit navigation cases, `pnpm security:check`, full `pnpm check` (465 tests; 17 existing optional skips), and `pnpm test:e2e` (10 each in Chromium/Firefox/WebKit) pass. Manual typing, cancelled/accepted Back, Forward, preview lifecycle controls, settled editor focus and mobile/light/dark checks are recorded with tool limitations in ADR 0030. Normal builds are restored and isolated servers/tabs are closed.
  - Completion handoff: commit `feat(studio): complete authorized context foundation [CMS-032]` on `codex/cms032-context-checkpoint`, retaining the WIP checkpoint and `main` at `6e6ea95`. BUG-0441/0442 are resolved; the earlier blocked/proposed/start entries below are historical. Single next action: CMS-033 under the already approved ADR 0030 fence. CMS-003 remains unchecked until CMS-033/034 pass; BUG-0435 and BUG-0427 stay deferred to their assigned tasks. No push, merge or deployment.
  - Repair approval/start: Codex, 2026-08-26T17:47:58+05:30, clean WIP baseline `ef79a8d` on `codex/cms032-context-checkpoint`. User replied `proceed to next` to the explicit bounded-repair question, approving the three added files below for BUG-0441/0442 only. Reproduce before changing App behavior; retain all acceptance assertions and run the complete gates before a completion commit. Earlier blocked/proposed entries are historical; CMS-033/034 do not start here.
  - Blocked checkpoint: Codex, 2026-08-26T17:41:30+05:30. API/client implementation, 461 workspace tests, strict types/builds and security checks pass, but required browser acceptance does not: Chromium/Firefox pass 10 each and WebKit fails the same dirty-history scenario in both complete runs (9/10 each). BUG-0441 is not resolved by its passing isolated reruns; BUG-0442 separately prevents repeat-each fixture reuse. Do not mark this task or parent CMS-003 complete.
  - Proposed verification-repair amendment, awaiting approval: add only `apps/studio/src/App.tsx`, `apps/studio/test/App.test.tsx`, and `tests/e2e/studio-navigation.spec.ts` to this task's fence to diagnose/fix BUG-0441 and uniquely scope BUG-0442's test fixtures. Preserve dirty/history/preview semantics, colors and features; no permission, topology or selector change. Require deterministic failing-before/fixed-after coverage, repeated isolated and complete three-engine checks, and the full repository gate. This supersedes the earlier CMS-033 deferral only if approved; no UI/test repair has been implemented. Details: ADR 0030's blocked checkpoint.
  - Handoff: preserve the work as a WIP checkpoint on `codex/cms032-context-checkpoint`, not a verified completion on `main`. Normal Studio/example production builds are restored. Request the bounded amendment above; CMS-033/034 have not started. BUG-0439/0440 are resolved; BUG-0435 remains CMS-034 and BUG-0427 remains CMS-004.
  - Owner/start: Codex, 2026-08-26T17:11:33+05:30, baseline `6e6ea95`. Implement the approved ADR 0030 CMS-032 file fence only. Reuse its completed five-source research and necessity gate; sequence contract/topology validation → policy projection/API → immutable client → negative/full/security/browser checks because each consumer depends on the preceding contract. No Studio behavior or BUG-0435 repair starts here.
  - Created/planned: Codex, 2026-08-26T16:58:44+05:30. Tier: T2. Depends: CMS-002, AUTH-001. Child of CMS-003; proposal only, awaiting ADR 0030 approval.
  - Scope: ADR 0030's exact CMS-032 fence: new schema/API context modules and tests, schema export, API server/config/startup and existing identity-admin predicate reuse, client methods/tests, `.env.example` and named documentation/ledgers; the approved repair adds only App.tsx, its existing unit test and the navigation browser test. Existing core policy and management permission semantics remain unchanged.
  - Acceptance: private no-store context projects actual route permissions and only same-anchor authorized configured choices; no roles/grants/raw topology leak; operations-only/type-limited/no-access cases are truthful; revoked/preview/cross-tenant credentials fail closed; absent topology exposes current context only; strict bounded catalog validation and immutable transport-preserving client clone pass. Legacy context and AUTH-001 remain compatible; focused/full/security/three-browser gates pass before completion.
  - Excludes: Studio UI changes beyond the approved focus repair, permission-aware bootstrap, scope transition behavior, BUG-0435 dispatch repair, new grants, editable topology, cross-workspace selection and content-type authorization redesign. Rollback: isolated repair plus additive contract/client commit, preserving AUTH-001 and coordinating any independently shipped consumers.

- [x] **CMS-033** Make existing Studio bootstrap, navigation and controls capability-aware.
  - Completed/verified: Codex, 2026-08-26T19:08:19+05:30. Capability-first private startup, finite guards for 132 SDK methods, 218 explicit operation-control declarations, filtered existing navigation, optional metadata reads and keyed private-session lifetime are implemented. Routine focus checks preserve the verified editor/preview until a result; failed verification suspends, and confirmed authority/session changes evict. No roles, permissions, features or colors were invented or removed.
  - Verification: `pnpm security:check` and full `pnpm check` pass (626 tests, including 243 Studio; 17 pre-existing optional database/recovery skips), strict types, generated contracts and production builds. All 42 `pnpm test:e2e` cases pass: Chromium 14, Firefox 14, WebKit 14. BUG-0457's held-check regression failed before repair; all 14 session tests and eight focused Firefox cases then passed. No browser assertion/timeout was weakened. ADR 0030 records all initial failures and justified fixture/contract corrections.
  - Manual/cleanup: isolated memory-only services verified 19 destinations/one current item, draft editing with preview open, Library → Pages draft/preview retention, revision-2 save/reload, desktop/mobile containment and light/dark controls. Restored light/original viewport and normal production builds; closed the test tab/three servers and confirmed all isolated ports stopped. Native popup rendering/closure is verified by the automated engines; manual evidence is the lifecycle controls.
  - Handoff: BUG-0443 through BUG-0457 are resolved with regression/gate evidence. Commit `feat(studio): enforce capability-aware sessions [CMS-033]` on `codex/cms032-context-checkpoint`; resulting SHA is in the delivery handoff. CMS-034 is next under approved ADR 0030; CMS-003 remains incomplete. BUG-0435 stays CMS-034 and BUG-0427 stays CMS-004. No merge, push, deployment, provider certification or release-readiness upgrade.
  - Owner/start: Codex, 2026-08-26T18:18:23+05:30; clean baseline `0790711` on `codex/cms032-context-checkpoint`. Reuse ADR 0030's approved five-source research and necessity gate. Sequence: finite operation guards/session boundary → permission-aware bootstrap/navigation/controls → restricted-session regressions → full/security/browser/manual verification and commit. CMS-034 and CMS-004 remain out of scope.
  - Approval: included in ADR 0030's sequence accepted on 2026-08-26T17:11:33+05:30; CMS-032 is now verified and committed. The created/planned entry below is historical.
  - Created/planned: Codex, 2026-08-26T16:58:44+05:30. Tier: T2. Depends: CMS-032. Child of CMS-003; proposal only, awaiting ADR 0030 approval.
  - Scope: ADR 0030's exact CMS-033 fence: Studio App/navigation, finite capability and session modules/tests, existing and new context browser scenarios, named documentation/ledgers. No API authorization rewrite or new domain screens.
  - Acceptance: capability-first startup supports operations-only/no-access/read-only/type-limited users without denied feature calls; all 19 destinations remain for authorized users; optional history/workflow/metadata reads are isolated; denied links/shortcuts and observed session/permission loss cannot reveal stale data; individual writes remain permission-gated. Existing history/preview and responsive/keyboard/theme behavior plus full gates pass.
  - Excludes: scope selector, topology editing, new permission meanings, palette changes, removed features and CMS-004 creation defaults. Rollback: this UI commit without weakening backend checks.

- [x] **CMS-034** Add guarded site/environment/locale selection and preview cleanup compatibility.
  - Completed/verified: Codex, 2026-08-28. The header stages only complete permitted tuples and shows the committed tuple separately. Context replacement serializes candidate validation, blocks every mapped management write, confirms entry and representative feature drafts, strictly closes/revokes old preview authority, advances the keyed session, clears old data/history entry context and rejects stale reads/grants/callbacks. Cancellation or validation/cleanup failure preserves the old context and data.
  - Verification: preview DELETE cookie/workforce-bearer management and `gsp_` self-revocation pass with malformed/unknown bearer and wrong-scope denial; 88 focused Studio, 23 focused API/server and three client-preview tests pass. Final `pnpm security:check` and `pnpm check` pass 639 tests (255 Studio) with 17 existing optional skips. All 51 Chromium/Firefox/WebKit context/navigation/accessibility/vertical-slice scenarios pass, including all 19 destinations at 1440/1280/1024/768/390/320px, keyboard/zoom/WCAG and popup lifecycle. Manual configured-context smoke confirms dirty cancellation, accepted reset to scope-free `#/pages`, and 390px containment. BUG-0435 and BUG-0463–BUG-0468 are resolved.
  - Handoff: commit `feat(studio): complete guarded scope switching [CMS-034]` on `codex/cms032-context-checkpoint`; no merge, push, deployment, provider/PostgreSQL certification or release-readiness change. Parent CMS-003 is complete. Next create one new `codex/` branch for CMS-004 through CMS-010, with a separate completion commit per task and CMS-004's required T2 plan/approval first.
  - Owner/start: Codex, 2026-08-28T20:19:05+05:30; clean baseline `b2cfd7d` on `codex/cms032-context-checkpoint`. Approval is included in ADR 0030's sequence accepted on 2026-08-26T17:11:33+05:30; CMS-033 is verified and committed. Reuse the approved five-source research, necessity gate, exact fence and rollback. Sequence BUG-0435 failing-before transport coverage/repair before Studio scope lifetime because voluntary switching depends on confirmed old-preview cleanup; no CMS-004 work starts.
  - Created/planned: Codex, 2026-08-26T16:58:44+05:30. Tier: T2. Depends: CMS-033. Child of CMS-003; proposal only, awaiting ADR 0030 approval. Defect input: BUG-0435.
  - Scope: ADR 0030's exact CMS-034 fence: Studio context session/header controls, history integration, shell/responsive SCSS, scoped tests/browser scenarios, API preview DELETE dispatch and regressions, client preview tests, `.env.example` and named documentation/ledgers.
  - Acceptance: only permitted complete tuples can be committed; dirty entry/feature drafts prompt, all active management writes block switching, old preview cleanup is confirmed, stale requests/grants/callbacks cannot carry data across scopes, and failure/cancel preserves the old authorized context. Forced session loss clears data immediately. Cookie/workforce-bearer management revocation and preview self-revocation pass with wrong-scope/unknown-bearer denial. History/reload remain scope-free; full security/repository/three-browser/manual responsive evidence precedes completion of this task and parent CMS-003.
  - Excludes: organization/tenant/workspace switching, persistent scope/draft authority, autosave, provider/preview cryptography changes, global environment-lock enforcement, migration/deployment and CMS-004. Rollback: this scoped UI/dispatch commit, preserving AUTH-001.

### B. Expose existing content and configuration foundations

- [x] **CMS-004** Make entry authoring work for explicitly selected registered content types.
  - Completed/verified: Codex, 2026-08-28. Registered Page and non-component Article authoring now share the canonical schema/CRUD/history/workflow lifecycle while retaining explicit type, authorization and preview/composition boundaries. BUG-0427 and verification defects BUG-0469–BUG-0476 are resolved. Final `pnpm check` and exact Chromium/Firefox/WebKit evidence are recorded in ADR 0031; commit this task separately on the shared branch before starting CMS-005.
  - Implementation approval/start: Codex, 2026-08-28T21:51:08+05:30. The user's `proceed` explicitly approves ADR 0031's bounded T2 contract. Implement CMS-004 only on the shared `codex/content-configuration-foundations` branch, verify it completely, and commit it separately before beginning CMS-005. CMS-005 through CMS-010 remain planned and retain their own scope/risk gates.
  - Proposed plan: [ADR 0031](docs/adr/0031-registered-content-type-authoring.md), Codex, 2026-08-28T21:38:37+05:30, baseline `9bdd891` on `codex/content-configuration-foundations`. Tier remains T2; explicit approval is required before runtime/test edits. The plan reuses canonical schemas, validation, content CRUD, workflow, history and preview; adds one registered nonvisual article example and a permission-aware Collections path; and keeps one eventual verified CMS-004 implementation commit on the shared CMS-004–010 branch.
  - Planning fence/decision: documentation and ledgers only. Runtime fence, sequence, risks, rollback and observable page/article/relation/capability/browser evidence are frozen in ADR 0031. No new dependency, database/storage format, mutable runtime model, field kind, query operator, permission meaning, saved view, bulk/delete behavior, merge, push or deployment is proposed. Approved for CMS-004 implementation; CMS-005 through CMS-010 have not started.
  - Planning evidence: the exact unchanged Studio candidate algorithm evaluated against the built full example schema returns `valid: false` with required issues at `blocks.0.props.heading` and `blocks.0.props.body`, deterministically reconfirming BUG-0427 without a request or mutation. Inward source audit confirms the core/API lifecycle is already content-type keyed while Studio bootstrap/list/select/history/create/relation paths remain page-bound. Seven official pages across four prior-art product patterns reached saturation; no second schema registry or template engine is justified.
  - Planning verification: `pnpm lint`, `pnpm format:check`, `node scripts/check-ledgers.mjs`, `git diff --check` and local-link/fence audits pass. Exactly five documentation/ledger files are included. No runtime/unit/browser/database/provider/deployment evidence is newly claimed; the verified `9bdd891` baseline is unchanged.
  - Defect input: BUG-0427 reproduced during CMS-002 manual smoke: existing Create page defaults fail the full example page schema. Repair schema-aware creation in this slice; CMS-002 does not broaden into the explicitly excluded creation-model rewrite.
  - Tier: T2. Depends: CMS-003. Scope: Studio `App.tsx`, authoring/composition controls, client query calls, `packages/example-kit` model fixtures, tests and generated fixtures only if required by the approved example schema.
  - Acceptance: a registered page and a non-component-tree article/collection can independently list/create/edit/save/revise/publish using their correct schema; creation never depends on schema-array order or a Hero for nonvisual content; relations can find permitted target types; invalid defaults produce actionable field validation; pages retain all composition, workflow and preview behavior.
  - Excludes: hot model creation, new storage engines, hard-coded WordPress post semantics, and delete/archive behavior.
- [x] **CMS-005** Add useful content lists, filters, sorting, pagination and saved local views.
  - Completed/verified: Codex, 2026-08-29. Studio now uses the existing bounded content-query contract for registered-type title/slug search, draft status, approved sorts and ten-row signed-cursor pagination, with exact loaded/total wording, stable editor selection, isolated loading/empty/error recovery and keyboard/mobile table behavior. Versioned saved views are browser-local, verified-scope/type keyed and contain preference values only. Unsupported schema-dependent controls stay hidden. BUG-0477–BUG-0487 are resolved. Final `pnpm check` passes 653 active tests with 17 existing optional skips, strict types, generated/security/tenant/readiness checks, React 18 SSR and production builds; Chromium, Firefox and WebKit pass all 19 scenarios each (57/57). Commit this task separately before starting CMS-006.
  - Started: Codex, 2026-08-28T23:15:02+05:30, baseline `c100b42` on `codex/content-configuration-foundations`. Reuse the existing canonical content-query contract for type, draft status, title/slug search, approved sorts and signed forward cursors; add exact connection bounds, stable editor selection, explicit list states and versioned scope-local view preferences. No new operator, server preference, bulk mutation, dependency, migration, permission meaning, palette change, merge, push or deployment is included.
  - Tier: T1. Depends: CMS-004. Scope: Studio content-list modules, existing client query integration, Studio/browser tests and list styles.
  - Acceptance: existing query contracts drive type/status/search/sort and cursor pagination without claiming first-page totals are complete; selection is stable; local saved views are versioned and scoped without draft data or credentials; empty/loading/error states and keyboard/mobile tables work; unsupported filters are not displayed.
  - Excludes: server-persisted preferences, bulk mutation and new query operators; escalate to a separately planned T2 slice if the needed filter lacks an existing contract.
- [x] **CMS-006** Add a factual editorial Home dashboard.
  - Completed/verified: Codex, 2026-08-29. Final `pnpm check` passes lint/format, ledgers, package boundaries, generated contracts, security/threat/tenant/readiness checks, strict types, 669 active tests with 17 existing optional skips, React 18.3.1 SSR and all production builds. The unchanged final Playwright matrix passes 60/60 (20 Chromium, 20 Firefox, 20 WebKit). Focused and full-file BUG-0500 controls pass with every interaction/assertion retained. No PostgreSQL/recovery/provider/deployment gate is applicable or claimed.
  - Completion handoff: commit as `feat(studio): add factual editorial home [CMS-006]` on `codex/content-configuration-foundations`. The separate `58fc548` plan remains history; BUG-0489–BUG-0500 are resolved. CMS-007 is next on this same branch and receives its own commit; CMS-008–010 remain untouched. No merge, push, deployment, palette replacement, permission expansion or production-readiness claim.
  - Implementation result: added a strict version-1 editorial-overview contract and framework-neutral projection, one authenticated private/no-store API/client read, an explicitly capability-mapped top-level `#/home` destination, and four independently isolated Content/Reviews/Releases/Operations cards. Root startup loads only context, the minimized overview and schema/manifest metadata needed by the existing canonical quick-create path; full content, workflow, release and operations detail remains lazy behind its existing destination.
  - Verification evidence: strict schema/client scope parsing, complete-scope repository rejection, exact coverage/counts, deterministic five-item bounds, review role/separation filtering, minimized releases/operations, per-widget errors, production authentication/no-input/no-store/non-enumeration and denied-source call boundaries pass. The complete repository unit/integration run passes 669 active tests with 17 existing optional skips, and the rebuilt unchanged browser matrix passes all 60 scenarios (20 each in Chromium, Firefox and WebKit), including Home routing/create/minimized requests, one-current-page behavior, six widths, keyboard, 200% zoom, light/dark and unsuppressed WCAG 2.2 A/AA coverage.
  - Verification defects: BUG-0489–BUG-0500 record every startup, scope, fixture, history, containment, shared-theme/accessibility and test-harness issue found and corrected without weakening assertions, replacing the established palette or changing any global timeout/retry.
  - Implementation approval/start: Codex, 2026-08-29T09:18:11+05:30. The user's `proceed` explicitly approves ADR 0032's bounded T2 contract. Implement CMS-006 only on `codex/content-configuration-foundations`, verify it completely and commit it separately before CMS-007. CMS-007–010 remain outside this implementation fence and retain their own task/risk gates.
  - Proposed plan: [ADR 0032](docs/adr/0032-factual-editorial-home.md), Codex, 2026-08-29, baseline `519235f` on `codex/content-configuration-foundations`. Tier remains T2; explicit approval is required after the separate planning commit before runtime/test edits. The minimal slice adds one private/no-store, complete-scope editorial overview with independently authorized/error-isolated bounded content, eligible-review, release and operator-attention widgets; one top-level Home destination; and quick create delegated to CMS-004.
  - Planning fence/decision: strict schema/core/API/client projection, finite capability/navigation/history changes, one isolated Home component/style partial, focused/full/three-engine tests and required docs/ledgers only. No new permission action, repository/storage schema, aggregate table, analytics/trends, notification/task model, configurable widget framework, dependency, cross-tenant metric, palette change, merge, push or deployment. Current repositories perform full-scope reads; the response/browser surface is bounded and makes no database-scale optimization claim.
  - Planning evidence: current content queries can supply exact scoped totals and recent entries, releases are already scope-ordered, and operations already exposes a bounded authorized dashboard. The complete pending-review source exists only as `WorkflowRepository.listInstances`; the public API reads workflows one entry at a time, so browser fan-out over recent rows cannot truthfully find the complete eligible queue. Four official Sanity/Contentful pages support bounded recent-item, explicit status and assigned-work patterns without justifying their plugin/task systems.
  - Tier: T2. Depends: CMS-003, CMS-005. Scope: Studio Home module, existing content/workflow/release/operations projections; bounded schema/core/API/client summary additions only where current permitted reads cannot supply a truthful widget.
  - Acceptance: authorized recent work, review/release links and content-state summaries have real provenance and explicit bounds; unavailable/error widgets remain isolated; quick create uses CMS-004; no hidden operator data is loaded for authors; no fabricated counts/charts or misleading truncated totals.
  - Excludes: a configurable dashboard framework, new analytics collection and cross-tenant portfolio metrics.
- [ ] **CMS-007** Consolidate Media library, upload and asset-detail workflows.
  - Tier: T1. Depends: CMS-003. Scope: Studio asset panel/modules and authoring controls, shared SCSS and existing asset API/client integration tests.
  - Acceptance: Library, Upload action, metadata/revisions/renditions/usage details reuse current asset services; any search/filter accurately states its loaded result boundary; verified/quarantined/unavailable-provider states stay visible; asset picks and signed private delivery remain safe at all widths.
  - Excludes: a new DAM, folders/storage migration, asset deletion, provider installation, arbitrary rendition configuration and production scanner certification.
- [ ] **CMS-008** Expose the existing governed design catalog.
  - Tier: T1. Depends: CMS-003. Scope: Studio Design modules, existing component/design manifest client calls, shared styles, Studio tests and `docs/design-system.md`.
  - Acceptance: Components retains impact/migration tools; tokens/variants, templates/symbols show their immutable version and code ownership; selecting an approved pattern uses existing validated editor insertion; unsupported changes are clearly read-only; site colors and Studio theme remain independent.
  - Excludes: theme installation, raw source editing, global token mutation and a new renderer.
- [ ] **CMS-009** Add schema and taxonomy inspection using canonical contracts.
  - Tier: T1. Depends: CMS-004. Scope: Studio schema/taxonomy views, existing schema-lifecycle/taxonomy/client reads, tests and model/search guides.
  - Acceptance: registered types/fields/routes, taxonomy trees/term IDs and available drift/impact metadata are inspectable; hierarchical categories and flat tags are correctly labeled; no screen implies terms or runtime models are editable when they remain code-owned.
  - Excludes: mutable taxonomy storage, live schema activation, source editing and new schema field kinds.
- [ ] **CMS-010** Add a safe configuration inventory before editable settings.
  - Tier: T2. Depends: CMS-003. Scope: schema/core/API minimized configuration projection, client methods, Studio Settings views, config/security tests and docs.
  - Acceptance: active locales/environments, route/model ownership and media-policy/provider availability show actual effective configuration; no environment-variable dump, credential, private endpoint or unauthorized topology leaks; each setting is marked code-owned, operator-owned or editor-owned.
  - Excludes: DNS/TLS/domain provisioning, editing process environment, object-store/scanner setup, topology mutation and secret storage in Studio.

### C. Website-management gaps

- [ ] **CMS-011** Add versioned visitor navigation menus end to end.
  - Tier: T2. Depends: CMS-004, CMS-009. Scope: reserved canonical menu content definition/validation in schema/example-kit, core content/reference/release integration, API/client menu projection, Studio menu editor and Vite consumer/example tests.
  - Acceptance: named header/footer menus support ordered bounded parent/child links, labels, scoped entry targets and validated external URLs; invalid cycles/depth/schemes and unpublished targets fail validation; draft/save/preview/publish/localization and concurrent updates reuse existing revisions; public resolution is published-only and route-aware; a same-release new page/menu pair is validated against future state; application rendering remains unstyled by Studio.
  - Excludes: admin-menu configuration, automatic inclusion of all pages, arbitrary HTML/JS, a second revision store and host-router takeover. Approve representation and release rollback before code.
- [ ] **CMS-012** Add revisioned public site settings and safe editorial defaults.
  - Tier: T2. Depends: CMS-004, CMS-010. Scope: canonical settings/singleton contract, scoped content/repository enforcement as required, API/client public allowlist, Studio settings UI, application consumer example, portability/release tests.
  - Acceptance: title/tagline/branding, canonical base URL, display timezone, home/listing/privacy references and approved writing/list defaults have validation and exact-scope singleton identity; concurrent creation cannot duplicate the singleton; drafts do not change public output; publish/version/history/export work; referenced defaults are validated; no existing entry/schema is silently rewritten; host app explicitly opts in.
  - Excludes: DNS registration, SSL, legal compliance claims, server security settings and mutable locale topology. Confirm storage/cardinality and rollback in the implementation ADR.
- [ ] **CMS-013** Complete structured SEO metadata and application output.
  - Tier: T2. Depends: CMS-004, CMS-012. Scope: schema/example-kit SEO definitions, quality policies/service, API/client projections if needed, Studio metadata controls, React/server-safe helpers and example consumer tests.
  - Acceptance: title/description/canonical/robots/Open Graph/social asset/hreflang and allowlisted structured-data mappings validate and render from the published locale; draft metadata stays private; malformed URLs and unsafe script serialization are rejected/escaped; generated metadata appears in real application output, not only Studio inputs; no ranking guarantee is made.
  - Excludes: arbitrary schema.org JSON execution, external SEO providers, a crawler and claiming new framework certification.
- [ ] **CMS-014** Add durable redirect administration and slug-change impact review.
  - Tier: T2. Depends: CMS-009, CMS-012. Scope: routing schema/core/repository, API/client routes, Studio redirect UI, archive/audit/release integration and SQLite/PostgreSQL tests.
  - Acceptance: authorized scoped create/edit/disable of versioned redirects reuses normalization/loop checks; code-owned versus editor-owned precedence is explicit; route collisions and cross-scope writes fail; published slug changes offer a reviewed redirect rather than silently changing live routes; history, restart persistence, export and rollback are tested.
  - Excludes: unchecked route-pattern editing, open external redirects, deployment routing changes and irreversible automatic URL rewrites.
- [ ] **CMS-015** Add published-only sitemaps, robots and content feeds.
  - Tier: T2. Depends: CMS-011, CMS-013, CMS-014. Scope: framework-neutral published-query helpers/contracts, explicit API/server entry points as approved, Studio status/config view, Vite example integration and output/security tests.
  - Acceptance: bounded paginated sitemap/index, robots and RSS/Atom output use validated canonical origin, locale/route and publication state; no drafts/private assets/tokens enter public output; noindex/redirect/retirement exclusions are tested; XML escapes safely; empty/large sets and caching/invalidation behave deterministically; robots is documented as discovery guidance, never authorization.
  - Excludes: Search Console OAuth, external crawling, SEO traffic claims and automatic host deployment.

### D. Focused administration over existing services

- [ ] **CMS-016** Add focused People directory and access-administration screens.
  - Tier: T2. Depends: CMS-003. Scope: existing identity/authorization API/client projection, Studio People and Identity modules, security/negative tests and identity docs.
  - Acceptance: permitted users/groups/mappings/sessions are navigable without entering provider setup; provider-owned account changes remain clearly distinct from local role mappings/session actions; least-privilege and self-lockout safeguards are explicit; privileged mutations retain current step-up/audit/revocation behavior.
  - Excludes: password authentication/reset, unapproved invitations/email, workforce-to-customer identity reuse and external IdP provisioning.
- [ ] **CMS-017** Add My profile and safe local Studio preferences.
  - Tier: T1. Depends: CMS-016. Scope: Studio profile/preferences modules, existing current-user reads, styles/tests and docs.
  - Acceptance: authenticated profile information is read-only where IdP-owned; local theme/density/display-timezone preferences are versioned and actor/context scoped; unavailable translations are not offered; logout/scope changes do not leak prior-user preferences or content; preferences store no credentials, private profile data or drafts.
  - Excludes: server-persisted preference contracts, account recovery, changing IdP attributes and notification delivery.
- [ ] **CMS-018** Add installed-extension lifecycle and grant management.
  - Tier: T2. Depends: CMS-003. Scope: Studio Apps modules, existing plugin/marketplace client methods, plugin API tests, shared styles and plugin docs.
  - Acceptance: installed records display exact version, trust, capabilities, grants, lifecycle and runtime availability; authorized enable/disable/revoke/uninstall-preview/uninstall follow existing backend transitions and confirmation/audit requirements; absent runtime fails closed; Marketplace remains reachable and installation never auto-enables code.
  - Excludes: downloading/hosting binaries, provisioning sandboxes, Studio extension loading, new provider integrations and code editors.
- [ ] **CMS-019** Give content analytics a dedicated honest reporting surface.
  - Tier: T1. Depends: CMS-003. Scope: Studio Insights/Operations modules, existing analytics-report client contract, UI/browser tests and analytics docs.
  - Acceptance: real content/component counters, interaction names, adapter-delivery states and release annotations move into a dedicated view without removing Operations information; bounds/truncation/no-history and consent limitations are visible; no fabricated date trends, distinct users, conversions or revenue.
  - Excludes: a warehouse, new analytics providers, raw events, attribution/funnels and extra permissions without a separate T2 plan.
- [ ] **CMS-020** Add guarded logical import/export utilities to Studio.
  - Tier: T2. Depends: CMS-003, CMS-004. Scope: Studio Tools/archive UI, existing portability API/client services, file-handling/negative tests and portability docs.
  - Acceptance: authorized export downloads the actual checksummed archive; import validates format/scope and shows existing dry-run/conflict results before explicit execution; retries/stale candidates/failures are safe and visible; secrets are not displayed; new menu/settings/redirect records are covered when their tasks ship; Migrations remains distinct.
  - Excludes: production imports during development, database restore, source-system writes, silent replace policy and inventing asynchronous progress where no job exists.
- [ ] **CMS-021** Add Site health, version guidance and contextual Help.
  - Tier: T1. Depends: CMS-003, CMS-010. Scope: Studio Tools/Help views, existing authorized health/operations/fleet/readiness reads and operator documentation.
  - Acceptance: actual dependency/index/job/version status is separated from historical release evidence; unavailable checks say unknown; Help links resolve and explain ownership; backup/restore/update steps link to approved runbooks; no “healthy” or “up to date” inference from missing evidence.
  - Excludes: browser-triggered self-update/restore, new diagnostics endpoints without a T2 amendment, remote scans and changing release-readiness decisions.
- [ ] **CMS-022** Add a private editorial review and collaboration inbox.
  - Tier: T2. Depends: CMS-003, CMS-004, CMS-006. Scope: collaboration/workflow bounded query projections, API/client routes if needed, Studio inbox and entry links, permission/scale tests.
  - Acceptance: authorized assignments, open editorial threads and due review/schedule links resolve to the right entry/context; loading, pagination/truncation and stale access are explicit; existing per-entry comments/branches/suggestions/presence remain unchanged; no cross-tenant comments or public reader endpoint is exposed.
  - Excludes: public moderation, notification delivery, CRM and unbounded all-entry polling.

### E. Governed content and design depth

- [ ] **CMS-023** Add safe unpublish, archive, trash and restore lifecycle.
  - Tier: T2 repository implementation; any real production retirement/deletion is separately T3. Depends: CMS-004, CMS-011, CMS-014, CMS-015. Scope: schema/core content/repositories, authorization/outbox/search/release/route/reference integration, API/client, Studio controls, archive/recovery and SQLite/PostgreSQL tests.
  - Acceptance: explicit approved lifecycle semantics preserve immutable history; unpublish removes public delivery and discovery; archive/trash/restore define reference/menu/redirect/release behavior with impact checks and optimistic revisions; restore cannot violate route uniqueness; audit, events, cache invalidation and cross-adapter recovery are tested. No hard deletion is included.
  - Excludes: governance-erasure shortcuts, retention/legal-hold bypass, production actions and destructive migration without a separately approved rollback/backup plan.
- [ ] **CMS-024** Add bounded bulk content actions with exact-result reporting.
  - Tier: T2. Depends: CMS-005, CMS-023. Scope: existing job/authorization/content boundaries, new bounded batch contract as approved, API/client, Studio selection/progress and tests.
  - Acceptance: explicitly supported bulk metadata/publish/unpublish/archive operations show exact target scope, impact and confirmation; each result records success/conflict/denial with idempotent retry and bounded concurrency; permission, workflow and quality gates are not bypassed; multi-entry atomic publication uses Releases; hidden/unloaded entries are not silently selected.
  - Excludes: unbounded synchronous loops, hard delete, implicit atomicity and a generic automation platform.
- [ ] **CMS-025** Add reviewed model and taxonomy change proposals, not hot activation.
  - Tier: T2. Depends: CMS-009. Scope: Studio canonical model/taxonomy candidate editor, schema-lifecycle client/API planning path, deterministic IR export and tests/docs.
  - Acceptance: users can propose compatible field/term changes with stable IDs, canonical validation, exact diff/impact and data-migration warnings; export round-trips through the IR; deployed source remains unchanged until normal code review/deployment; destructive candidates are flagged and cannot masquerade as activated models.
  - Excludes: runtime schema/term mutation, generated source execution, data-backfill execution, shared schema authority changes and new field kinds. CMS-027 owns those decisions.
- [ ] **CMS-026** Add governed versioned appearance overrides.
  - Tier: T2. Depends: CMS-008, CMS-012. Scope: design-system/settings contracts and resolution, authorized API/client, Studio approved override controls, React/example rendering and deterministic snapshot tests.
  - Acceptance: only explicitly permitted token/brand/variant overrides can be drafted, previewed, versioned and published; already published pages pinned to old versions remain deterministic; incompatible tokens fail validation; cross-site state and Studio theme are unaffected; exact rollback is demonstrated.
  - Excludes: mutating code-owned manifests in place, arbitrary CSS/JS, theme installers and a new rendering system. Decide how pages opt into a new override version before code.

### F. Optional product decisions — not automatic implementation

- [ ] **CMS-027** Decide whether live model/taxonomy activation is needed.
  - Tier: T2 architectural discovery only. Depends: CMS-025. Scope: analysis/ADR/task documents and, only after separate approval, a disposable synthetic-data experiment.
  - Acceptance: compare continuing source-reviewed proposals against a runtime model registry; address tenant ownership, stable IDs, generated types, multi-process/worker refresh, invalid data/backfills, indexes, release compatibility and rollback; obtain owner choice; add small implementation tasks only for an approved design or record deferral.
  - Excludes: executing migrations, introducing a second schema authority or treating the current deployment-record API as hot activation.
- [ ] **CMS-028** Define optional commerce integration requirements and ownership.
  - Tier: T2 discovery only; payments/PII implementation is separately risk-tiered, potentially T3. Depends: GOV-006. Scope: commerce feasibility/ADR/task documents; no provider/account writes.
  - Acceptance: owner chooses catalog-only versus transactional commerce and provider/source of truth; identify products, inventory, orders, customers, discounts, shipping, taxes, payments, subscriptions/bookings if required, webhook/consent/security needs and evidence; either defer or create separately approved adapter slices. A product collection is not checkout.
  - Excludes: building a native commerce/accounting engine, installing integrations, collecting credentials, processing transactions and exposing an empty Commerce menu.
- [ ] **CMS-029** Define optional marketing, forms, CRM and SEO-provider integrations.
  - Tier: T2 discovery only; later personal-data operations separately T3 where applicable. Depends: GOV-006. Scope: product/provider comparison, data-flow/threat/retention analysis and ADR/task documents.
  - Acceptance: distinguish existing targeting/experiments/CTA/releases from email/social/ads/campaigns, forms/submissions, contacts/segments, search analytics and Search Console; prioritize actual use cases; identify consent/anti-abuse/retention/provider credentials and source of truth; owner approves or defers each vertical integration before tasks/code.
  - Excludes: sending messages, storing leads, external OAuth setup, raw analytics, CRM duplication and promising all providers.
- [ ] **CMS-030** Decide public comments, reviews and visitor memberships separately from workforce identity.
  - Tier: T2 discovery only; PII/production implementation separately risk-tiered. Depends: GOV-006. Scope: requirements, moderation/identity/abuse/data-flow analysis, ADR and task documents.
  - Acceptance: owner confirms whether reader comments/reviews or gated content are needed; choose external integration versus scoped native domain; define moderation/spam/rate limits, consent/retention/deletion, public author identity and entitlement enforcement; keep editorial comments and IdP workforce users private; add implementation slices only after approval or record deferral.
  - Excludes: reusing editorial comment routes publicly, authorizing visitors as editors, implementing subscriptions/payments and adding inert Discussion switches.

### G. Acceptance and handoff

- [ ] **CMS-031** Verify the shipped administration upgrade against the complete gap matrix.
  - Tier: T1 verification/docs, with new defects assigned their own scoped tasks. Depends: CMS-001 through CMS-026; CMS-027 through CMS-030 are optional decisions and do not block core acceptance.
  - Scope: Studio/API/client/browser tests, example integration tests, coverage matrix, docs and ledgers; application fixes require logged bugs and amended/new scoped tasks before edits.
  - Acceptance: every shipped destination/submenu has an authorized path, direct-link/history check, empty/loading/error/forbidden state, working controls and six-width/three-engine visual/accessibility evidence; all original 19 capabilities remain reachable; collection, menu, settings, SEO, asset, people, plugin and archive journeys demonstrate actual outcomes; draft/publication/tenant boundaries hold; unsupported optional integrations remain honestly unavailable; full repository and applicable database/recovery/browser checks pass.
  - Excludes: declaring optional programs complete, clearing external beta/RC/GA gates, silent feature removal and approving production rollout.

**Single next action:** start CMS-007's bounded T1 Media consolidation on `codex/content-configuration-foundations` and commit it separately after its own complete verification. CMS-008–010 remain planned in dependency order; CMS-010 retains its T2 approval gate. No merge, push or deployment is authorized.

## Milestone 0 — Governance and repository foundation

- [x] **GOV-006** Audit the CMS admin-menu reference against implemented capabilities and upgrade the delivery plan.
  - Owner/start: Codex, 2026-08-26. Tier: T1 documentation/research only; the proposed implementation program contains separately approval-gated T2 changes.
  - Ask: assess feasibility, distinguish existing UI from backend foundations and missing capabilities, map all reference categories and existing destinations, and queue small dependency-ordered tasks without implementing application changes.
  - Scope fence: `TASKS.md`, `CHANGELOG.md`, `BUGS.md`, `REACT_CMS_RESEARCH_AND_IMPLEMENTATION_PLAN.md`, `docs/cms-admin-gap-analysis.md`, `docs/adr/0027-cms-administration-information-architecture.md`, and `docs/design-system.md` (correct the verified preview-control documentation drift, BUG-0416).
  - Acceptance: all 13 reference categories and WordPress submenu jobs have an explicit disposition; all 19 current Studio destinations retain a proposed home; each queued task has tier, dependencies, scope, exclusions, and observable acceptance; architectural recommendations remain proposed; existing completion history and release no-go evidence remain unchanged; documentation/ledger checks and the complete repository gate pass.
  - Rollback: revert this documentation-only commit; no application, schema, dependency, stored data, deployment, or external service is changed.
  - Delivered/verified: completed the 13-category and detailed submenu analysis, all 19 existing destination mappings, proposed ADR 0027, blueprint additions and 31 planned tasks (26 core slices, four optional decisions, one acceptance audit). The read-only Node documentation audit verified 10 local links/anchors, an acyclic dependency graph, all required task fields and unchanged statuses for all 104 historical tasks. The preview-guide regression check passes. `node scripts/check-ledgers.mjs`, `pnpm format:check`, `git diff --check` and the approved `pnpm check` pass, including policy/security/tenant/readiness gates, eight generated specs, strict types, all workspace tests (33 Studio tests), React 18.3.1 SSR and production builds. BUG-0416 and BUG-0417 are resolved. Browser/PostgreSQL/deployment suites were not rerun for documentation-only changes; prior results are not represented as fresh UI evidence. Single next action: CMS-001; no application task has started.
- [x] **GOV-001** Add the research-backed product and architecture plan to the repository.
- [x] **GOV-002** Establish the permanent task-list workflow and stable task IDs.
- [x] **GOV-003** Establish a Keep a Changelog-compatible changelog.
- [x] **GOV-004** Establish a permanent bug ledger and bug lifecycle.
- [x] **GOV-005** Add repository-level instructions enforcing ledger updates for every change.
- [x] **FND-001** Create the pnpm TypeScript monorepo with applications, packages, examples, and shared scripts.
- [x] **FND-002** Add shared TypeScript, formatting, linting, test, and package-boundary configuration.
- [x] **FND-003** Add environment templates, ignored runtime data, and reproducible local-development commands.
- [x] **FND-004** Add CI workflows for type-check, unit/integration tests, builds, browser verification, and ledger validation.
- [x] **FND-005** Add architecture decision records for the schema IR, revision model, tenant context, preview protocol, and plugin capabilities.
- [x] **FND-006** Make LF checkout and validation reproducible on Windows.
  - Tier: T1 operational bug fix. Owner: Codex. Linked defect: `BUG-0203`.
  - Acceptance: with system `core.autocrlf=true`, Git resolves tracked text to `eol=lf`, generated contracts match the canonical IR byte-for-byte, and `pnpm check` passes.
  - Scope fence: `.gitattributes`, `TASKS.md`, `CHANGELOG.md`, `BUGS.md`, plus mechanical LF-only normalization of Biome-managed tracked files.
  - Out of scope: application behavior, dependency changes, platform-specific formatter output, and product milestone `M5-003`.
  - Verification: `pnpm format:check` checks 169 files clean; `pnpm schema:check` matches the canonical IR; `pnpm check` passes lint, ledgers, boundaries, security, tenant scope, all typechecks, 171 tests with two intentional skips, and production builds.
  - PostgreSQL/browser verification: `pnpm test:postgres` passes seven core conformance tests plus one live API integration; `pnpm test:e2e` passes the complete Edge/Chromium edit, govern, publish, and React delivery walkthrough.
  - Final audit: `git diff --check` and the repository ledger validator pass after resolving `BUG-0204`.

## Milestone 1 — First end-to-end CMS vertical slice

### Schema and component contracts

- [x] **SCH-001** Implement the canonical, versioned schema intermediate representation.
- [x] **SCH-002** Implement content field validation with stable field IDs and structured errors.
- [x] **SCH-003** Implement the serializable React component manifest with props, slots, defaults, and version metadata.
- [x] **SCH-004** Add schema and component-manifest fixtures and unit tests.
- [x] **SCH-005** Generate TypeScript-facing content and component types from the canonical contracts.

### Core content engine

- [x] **CORE-001** Define tenant-aware repository, revision, audit, and clock/ID contracts.
- [x] **CORE-002** Implement SQLite local persistence and automatic schema initialization.
- [x] **CORE-003** Implement create, update-draft, list, read, publish, and version-history services.
- [x] **CORE-004** Enforce optimistic revision checks and preserve immutable revisions.
- [x] **CORE-005** Record attributable audit events for every content mutation.
- [x] **CORE-006** Seed a default tenant, page schema, component manifests, and welcome page idempotently.

### Standalone control-plane API

- [x] **API-001** Implement the Fastify server, configuration validation, CORS policy, health, and readiness endpoints.
- [x] **API-002** Implement tenant-scoped schema and component-manifest delivery endpoints.
- [x] **API-003** Implement content list, draft read/write, publish, and revision-history management endpoints.
- [x] **API-004** Implement the public published-content endpoint with private/no-store draft separation.
- [x] **API-005** Add consistent error envelopes, request IDs, validation responses, and API integration tests.

### Universal client and React renderer

- [x] **SDK-001** Implement a framework-neutral typed client for schemas, content, drafts, publication, and history.
- [x] **SDK-002** Support AbortSignal, configurable fetch, normalized errors, tenant context, and draft/published perspectives.
- [x] **REACT-001** Implement a React component registry and deterministic component-tree renderer.
- [x] **REACT-002** Add safe unknown-component and invalid-props fallbacks.
- [x] **REACT-003** Add preview source attributes without shipping them in normal published rendering.
- [x] **REACT-004** Add renderer unit tests and server-render/hydration compatibility checks.

### GridStory Studio

- [x] **STUDIO-001** Build the accessible Studio shell, navigation, content list, status indicators, and responsive layout.
- [x] **STUDIO-002** Build schema-driven page fields and block/prop editing.
- [x] **STUDIO-003** Add dirty-state handling, save-draft, validation, optimistic revision conflicts, and feedback.
- [x] **STUDIO-004** Add live in-process React preview using the same registered application components.
- [x] **STUDIO-005** Add publish action, version-history panel, and published/draft perspective switching.
- [x] **STUDIO-006** Add empty, loading, error, and keyboard-accessible interaction states.
- [x] **STUDIO-007** Preserve an accessible Studio header palette when forced colors and 200% zoom are active across supported browsers.
  - Delivered: forced-colors header foreground, background, and boundary colors now resolve from the same system palette instead of mixing authored dark surfaces with browser system text colors.
  - Verification: the focused WebKit 200%-zoom/forced-colors accessibility regression passes; the complete Chromium, Firefox, and WebKit matrix is included in the M7-001 final gate.
- [x] **STUDIO-008** Preserve the established neutral page and content palette in the AI workbench.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T0 because this is a user-requested, reversible CSS regression correction with no contract, dependency, data, or architectural change.
  - Scope/acceptance: limit changes to `apps/studio/src/studio.css`, the existing browser accessibility regression, and mandatory ledgers. The AI workbench must use the same neutral page surface, supporting-copy color, content/input color, and card boundaries as existing Studio panels; warning/status colors and the editor/preview palette remain unchanged. Computed-color browser assertions, the complete cross-browser accessibility specification, Studio tests/build, formatting, ledger, and whitespace gates must pass before `[x]` and commit.
  - Delivered/verification: restored the existing neutral surface, supporting-copy, content/input, and boundary tokens; reduced the broad supporting-text selector specificity so the safety warning retains its intended status color. The 27 Studio tests, both E2E builds, 9/9 computed-color/WCAG/keyboard/browser checks across Chromium, Firefox, and WebKit, and the complete `pnpm check` gate pass. BUG-0340 is resolved; editor and preview colors were not changed.
- [x] **STUDIO-009** Re-theme the complete Studio with the inspected NiceDash dashboard visual system.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T1 because this is a broad but reversible presentation-shell and responsive-navigation change with no data, API, authorization, schema, dependency, or published-render contract impact.
  - Restated ask/research: apply the supplied NiceDash Dashboard 1 design language to every existing GridStory Studio surface after inspecting the live desktop, mobile, light, and dark states. The reference uses a 270px fixed white navigation rail, 70px sticky top bar, 24px grid rhythm, `#f5f5f5` light workspace, 16px white cards, `#165a50` primary, `#c2fd75` accent, `#030e09` text/dark surface, `#686e6b` muted copy, `#e5e6e6` borders, rounded outlined controls, compact pills, and an off-canvas mobile rail.
  - Necessity/minimal form: the current Studio already owns all workflows but exposes more than twenty management actions in a wrapping dark header and mixes indigo/neutral component styles, so CSS alone cannot reproduce the reference information architecture. The minimal change moves existing actions into a categorized accessible rail, retains Save/Publish and search in the top bar, applies one shared token layer across existing panels/forms/tables/editor/preview, and adds local light/dark plus mobile drawer state. It adds no dependency, external font/network requirement, copied Angular/Material implementation, reference branding/content/assets, dashboard analytics, routing system, generic component framework, or backend work.
  - Scope fence: `apps/studio/src/App.tsx`, `apps/studio/src/studio.css`, `apps/studio/index.html`, `apps/studio/test/App.test.tsx`, `tests/e2e/accessibility.spec.ts`, `README.md`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: at desktop widths the Studio presents a 270px fixed/scrollable categorized rail, 70px sticky top bar, reference-matched light tokens, rounded cards, controls, states, tables, editor, and preview without changing any existing workflow label or behavior. Dark mode uses `#141a18` workspace, `#030e09` surfaces, `#1c2622` borders, and white text; the preference is local-only and does not leave Studio. At mobile widths the rail is closed by default, opens as a modal drawer with a backdrop, closes from its control/Escape/navigation, and the workspace stacks without horizontal page overflow. Unit tests cover shell/theme/drawer behavior; browser checks cover computed reference tokens, keyboard/200% zoom, forced colors, WCAG 2.2 A/AA, and unchanged author/publish/delivery paths in Chromium, Firefox, and WebKit. `pnpm check`, compatibility, Studio build, and the requested local commit must pass before `[x]`.
  - Risks/rollback: broad legacy selectors may retain old colors or low-contrast combinations, and moving buttons can break exact accessibility queries or narrow-width reachability. Detect through CSS token inventory, focused component assertions, Axe/computed-style browser evidence, and the full existing suite. Roll back by reverting the single STUDIO-009 commit; no stored content or server state requires restoration.
  - Delivered/verification: reorganized all existing actions into the categorized responsive rail; added the exact inspected light/dark token system, rounded surfaces/controls, sticky search/action bar, local theme persistence, desktop condensation, and accessible mobile drawer while preserving the framework-neutral control plane and application preview palette. Final verification passes `pnpm check` (lint/ledgers/security/scope/readiness, format, strict types, 384 runnable package/application tests, and production builds), `pnpm test:compatibility` with React 18.3.1 SSR, both E2E builds, and 12/12 Chromium/Firefox/WebKit responsive/computed-token/WCAG/keyboard/zoom/forced-color/published-rendering checks. In-app browser inspection passed at 1280×720 and 390×844; BUG-0375 through BUG-0383 are resolved.
- [x] **STUDIO-010** Complete a page-by-page responsive visual QA and remediation pass for every Studio surface.
  - Owner/start: Codex, 2026-08-25. Delivery tier: T1 because this is a broad but reversible presentation-only correction inside the existing Studio component and stylesheet boundary; it changes no feature, route, API, schema, storage, authorization, or published-render contract.
  - Restated ask/assumptions: inspect every navigation destination and every visible card, section, control, table, editor, and preview in light and dark themes at desktop, tablet, and mobile widths; restore missing button treatment, readable wrapping, containment, spacing, and logical responsive stacking. Preserve every current workflow, label, action, state, and the application-owned preview palette.
  - Necessity/minimal form: the user reports missing button styles plus clipped and escaping text after the broad STUDIO-009 re-theme. Existing markup, tokens, and breakpoints remain the implementation seam; the minimal fix normalizes presentational primitives and targeted outliers, adds regression coverage, and introduces no dependency, route split, component library, backend work, new feature, or content rewrite.
  - Scope fence: `apps/studio/src/App.tsx`, `apps/studio/src/studio.css`, `apps/studio/test/App.test.tsx`, `tests/e2e/accessibility.spec.ts`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: every existing Studio navigation destination is inspected and remains reachable; interactive buttons have consistent visible states and at least 24px target geometry; headings, labels, values, metadata, code, and long tokens wrap or scroll within their owning surface; cards, grids, actions, tables, editor, and preview stay within the viewport without page-level horizontal overflow at 1440px, 1024px, 768px, 390px, and 320px; light/dark contrast and forced-colors semantics remain valid; unit, browser, full repository, compatibility, formatting, ledger, and whitespace gates pass before `[x]` and commit.
  - Risks/rollback: generic CSS normalization can unintentionally restyle application preview content or specialized semantic controls. Contain selectors beneath the Studio shell while explicitly excluding the preview rendering boundary, verify representative computed styles and every destination across viewports/themes, and roll back the single STUDIO-010 commit if required.
  - Delivered/verification: inspected all 19 existing destinations and normalized button variants, wrapping, compact metadata/status geometry, form/select/fieldset containment, mobile heading/actions, comment composition, workflow-state scrolling, preview controls, and cross-engine page-root containment without changing any workflow or preview content. BUG-0384 through BUG-0390 are resolved. `pnpm check` passes lint, ledgers, security/scope/readiness, formatting, strict types, 384 runnable tests, and production builds; `pnpm test:compatibility` certifies React 18.3.1 SSR; the 15/15 Chromium/Firefox/WebKit visual/WCAG/keyboard/published suite and isolated 3/3 authoring-to-delivery slices pass.
- [x] **STUDIO-011** Enforce single-destination Studio navigation and replace the misleading accumulated-panel visual test.
  - Owner/start: Codex, 2026-08-25. Delivery tier: T1 because this corrects a user-visible navigation regression and expands browser coverage without changing any API, schema, storage, authorization, content, or published-render contract.
  - Defects: `BUG-0391` records the independent sidebar selections and accumulated panels; `BUG-0392` records the regression test that encoded accumulation instead of asserting navigation exclusivity; `BUG-0393` records management feedback incorrectly owned by the Pages editor and exposed by the exclusivity correction; `BUG-0394` records the non-compliant intermediate contrast created by sidebar color transitions; `BUG-0395` tracks the E2E API process loss encountered while revalidating the unchanged delivery workflow.
  - Scope fence: `apps/studio/src/App.tsx`, `apps/studio/src/studio.css`, `apps/studio/test/App.test.tsx`, `tests/e2e/accessibility.spec.ts`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: all 19 sidebar destinations behave as mutually exclusive pages; exactly one navigation item exposes `aria-current="page"`; the Pages workspace renders only for Pages; switching destinations preserves already loaded feature state without removing any feature; sequential unit and browser checks prove previous content is gone at desktop, tablet, and mobile widths in light and dark themes; the proportionate Studio, cross-browser, repository, formatting, ledger, and whitespace gates pass before `[x]` and commit.
  - Delivered/verification: replaced the independent visible-panel model with one active destination while keeping loaded workflow state cached, kept authoring content exclusive to Pages, and moved management feedback outside the Pages-only boundary. Sequential unit coverage proves prior regions unmount and cached Workflows do not refetch. Live desktop reproduction is corrected, and all 18 management destinations pass at 390×844 with one current item/one surface/zero Pages workspaces. `pnpm check` passes lint, ledgers, security/scope/readiness, formatting, strict types, 385 runnable tests including 33/33 Studio cases, React 18.3.1 SSR, and production builds. The 15/15 Chromium/Firefox/WebKit shell, all-destination WCAG, five-width light/dark containment, keyboard/zoom/forced-color, and published-render checks pass, as do the separately isolated 3/3 authoring-to-delivery slices. BUG-0391 through BUG-0395 are resolved.
- [x] **STUDIO-012** Add an accessible pop-out action to the Live page preview.
  - Owner/start: Codex, 2026-08-25. Delivery tier: T1 because this is a user-visible but reversible Studio interaction and regression-coverage change that reuses the existing preview-session boundary without changing any API, schema, storage, authorization, content, or published-render contract.
  - Defects: `BUG-0396` tracks the restricted Windows child-process denial encountered during focused Studio verification; `BUG-0397` tracks the downstream WebKit draft-save confirmation missed after the new popup assertions passed.
  - Restated ask/assumptions: place an icon in the Live page preview header that opens the selected draft in a new browser window where only the application preview is visible. Retain every existing preview control and feature, including the labeled Standalone action.
  - Necessity/minimal form: the existing secure standalone-preview session already supplies the requested isolated window and live draft synchronization. The minimal change invokes that established flow from one accessible header icon and adds direct regression evidence; it adds no route, popup protocol, dependency, generic icon library, or backend work.
  - Scope fence: `apps/studio/src/App.tsx`, `apps/studio/src/studio.css`, `apps/studio/test/App.test.tsx`, `tests/e2e/vertical-slice.spec.ts`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: the Live page preview header exposes a keyboard-accessible icon with a clear accessible name and disabled state when no page is selected; activating it opens exactly one new standalone preview window through the existing secure session; the new window renders the live application draft without Studio navigation, header, editor, controls, or preview shell; edits continue to synchronize to the window; existing iframe, Standalone, and close controls remain available; focused Studio tests, the three-browser vertical slice, full repository gate, formatting, ledger, and whitespace checks pass before `[x]` and commit.
  - Risks/rollback: a header action can wrap poorly at narrow widths or regress popup semantics. Detect through component assertions, cross-browser delivery coverage, and responsive browser inspection; roll back by reverting the single STUDIO-012 commit, with no stored content or server state to restore.
  - Delivered/verification: added the accessible pop-out icon to the Live React preview heading and routed it through the existing secure standalone session without removing iframe, Standalone, or close controls. The unchanged live bridge opens one application-only window, contains no Studio shell/navigation/header/preview panel/Save action, and synchronizes draft edits in Chromium, Firefox, and WebKit. In-app inspection passed at the default desktop viewport and 390×844, where the icon remains a contained 38×38px target with no horizontal overflow. The focused Studio suite passes 33/33; `pnpm check` passes 385 runnable tests, lint/ledgers/security/scope/readiness, formatting, generated contracts, strict types, React 18.3.1 SSR, and production builds. BUG-0396 and BUG-0397 are resolved.
- [x] **STUDIO-013** Normalize authoring actions through the global Studio button system.
  - Owner/start: Codex, 2026-08-25. Delivery tier: T1 because this is a contained, reversible presentation and interaction-state correction with no API, schema, storage, authorization, content, or application-preview change.
  - Defects: `BUG-0398` records native browser button styling leaking into asset and relation option cards and the sibling authoring controls that omit the shared primitive; `BUG-0399` tracks the formatter-only selector layout found by the full delivery gate.
  - Restated ask/assumptions: classify the screenshot's asset and relation choices as neutral outlined selectable option cards, then ensure semantically similar authoring controls use global primary/secondary/outline/danger/compact/icon variants rather than isolated or browser-default styling. Preserve every authoring feature and the established page/content palette.
  - Necessity/minimal form: the global `.button` primitive and primary/secondary/danger variants already exist, but `authoring-controls.tsx` does not opt into them and `.picker-grid button` supplies only four incomplete declarations. The minimal fix adds reusable outline, option-card, and icon variants, applies the global taxonomy to every authoring-control button, and removes the masking container fallback. It adds no dependency, component framework, new control abstraction, backend work, or global selector that could enter the application preview.
  - Scope fence: `apps/studio/src/authoring-controls.tsx`, `apps/studio/src/studio.css`, `apps/studio/test/App.test.tsx`, `tests/e2e/accessibility.spec.ts`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: asset and relation choices share `.button.button--outline.button--option-card`; each option has an explicit pressed state; rich-text, add, remove, clear, and chip actions use an intentional global variant with no classless button remaining inside `.authoring-field`; light/dark enabled, hover, focus, pressed, and disabled states are readable and distinct; option cards retain at least 40px target height, wrap content, and remain contained at desktop and 320px; unit, browser, full repository, formatting, ledger, and whitespace gates pass before `[x]` and commit.
  - Risks/rollback: removing the old high-specificity authoring selector could expose an unclassified control or let a global variant change layout. Guard with a no-classless-authoring-button regression plus computed-style, accessibility, state, and narrow-width checks; roll back the single STUDIO-013 commit if required.
  - Delivered/verification: classified every authoring action through the global base and explicit outline/option-card, secondary, danger, compact, and icon variants; removed the container-specific fallback; added pressed semantics to asset, relation, and embed choices. Live light/dark inspection confirms consistent enabled, hover, focus, and selected presentation, zero classless authoring buttons, no browser errors, and two 228×56.8px cards contained at 320px without page overflow. The focused style contract passes 3/3 and the complete Chromium/Firefox/WebKit Studio matrix passes 15/15 across all destinations, five responsive widths, WCAG, keyboard, 200% zoom, dark mode, and published rendering. `pnpm check` passes 385 runnable tests, lint/ledgers/security/scope/readiness, formatting, generated contracts, strict types, React 18.3.1 SSR, and production builds. BUG-0398 and BUG-0399 are resolved.
- [x] **STUDIO-014** Prevent Studio prose and metadata from collapsing into one-character columns.
  - Owner/start: Codex, 2026-08-25. Delivery tier: T1 because this is a user-reported, reversible global presentation correction with no API, schema, storage, authorization, content, feature, or application-preview change.
  - Defect: `BUG-0400` records the shared `overflow-wrap: anywhere` rule changing intrinsic sizes for all headings, prose, labels, and spans, allowing dense grid metadata to collapse to a one-character column.
  - Restated ask/assumptions: correct the sentence and word fragmentation everywhere in the Studio while retaining intentional containment for genuinely unbroken identifiers, URLs, and code. Treat normal language as readable prose and stack or reflow dense controls before a readable text column becomes unusably narrow.
  - Necessity/minimal form: existing global typography and responsive-grid seams already own this behavior. The minimal fix replaces aggressive wrapping for normal language with a readable shared policy, limits character-level wrapping to technical tokens, hardens affected grid tracks, and adds a rendered-word regression. It adds no dependency, typography framework, component abstraction, route, content rewrite, or preview styling.
  - Scope fence: `apps/studio/src/studio.css`, `tests/e2e/accessibility.spec.ts`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: no visible non-technical Studio word of five or more characters spans more than two rendered lines at 1440px, 1280px, 1024px, 768px, 390px, 320px, or 200% zoom; collaboration metadata retains a readable width and controls reflow without root horizontal overflow; long code/identifier content remains contained; all 19 destinations retain their features and application preview styling; focused browser regression, complete cross-browser Studio matrix, full repository, formatting, ledger, and whitespace gates pass before `[x]` and commit.
  - Risks/rollback: restoring normal intrinsic word sizes can reveal a dense grid that previously relied on character fragmentation for containment. Detect every sibling through the rendered-word and root-overflow sweeps, fix layouts at their shared grid seams, and roll back the single STUDIO-014 commit if required.
  - Delivered/verification: ordinary Studio headings, prose, labels, legends, and spans now use readable `break-word` sizing while code and bounded technical identifiers retain character-level containment; collaboration metadata occupies a full-width grid row and global fieldsets plus the Releases UUID line remain contained. The regression first failed on computed `anywhere`, and live 1280px light/dark inspection changed the summary from a 27.3px three-line-per-word column to one readable line with zero root overflow. Chromium and Firefox pass 5/5 complete scenarios; WebKit's all-route six-width sweep and three other scenarios passed together, and its one full-run Axe timeout passed unchanged in isolation for 15/15 scenario evidence without weakening a timeout. `pnpm check` passes 321-file lint/policy validation, 318-file formatting, generated contracts, all strict types, 385 active tests with 17 documented PostgreSQL-only skips, React 18.3.1 SSR certification, and all production builds. BUG-0400 through BUG-0403 are resolved.
- [x] **STUDIO-015** Replace the inline preview panel with a header-owned pop-out and modularize Studio styling in SCSS.
  - Owner/start: Codex, 2026-08-25. Delivery tier: T2 because this changes the Studio preview composition, adds one build-time preprocessor dependency, and restructures the complete cross-cutting stylesheet while preserving runtime contracts.
  - Defects: `BUG-0404` records panel-specific text/select declarations overriding the shared form treatment; `BUG-0405` records missing and inconsistent spacing between adjacent Studio elements.
  - Restated ask/assumptions: remove the complete inline preview panel and its third workspace column; place one accessible preview pop-out control in `.studio-header`; retain the existing secure standalone draft-preview session, live editing/selection bridge, and explicit close/revoke behavior through that header control. Give ordinary text-like inputs, textareas, and native selects one global visual/state contract while leaving specialized checkbox, radio, range, color, and file controls semantic. Replace `studio.css` with one ordered SCSS entry and purpose-owned partials including `_typographic.scss`, `_cta.scss`, `_form.scss`, and `_cards.scss`, plus bounded foundation, feature, shell, state/theme, responsive, and accessibility modules.
  - Proposed decision: `docs/adr/0026-studio-scss-and-header-preview.md`. Use Vite's built-in SCSS integration with the pure-JavaScript Dart Sass development package and Sass `@use`; add no UI library, CSS-in-JS runtime, CSS Modules conversion, Sass framework, or new application component system. Await explicit T2 approval before application code changes.
  - Scope fence: `apps/studio/package.json`, `pnpm-lock.yaml`, `apps/studio/src/main.tsx`, `apps/studio/src/App.tsx`, `apps/studio/src/studio.css` (removed), new `apps/studio/src/styles/*.scss`, `apps/studio/test/App.test.tsx`, `tests/e2e/accessibility.spec.ts`, `tests/e2e/vertical-slice.spec.ts`, `README.md`, `docs/adr/0026-studio-scss-and-header-preview.md`, `TASKS.md`, `CHANGELOG.md`, and `BUGS.md`. Any additional path requires an amendment here.
  - Acceptance: no inline `.preview-panel`, preview canvas, breakpoint/perspective toolbar, or preview iframe remains in the Studio DOM; the header control opens one preview-only standalone window, remains contained and reachable from 1440px through 320px, synchronizes draft edits and node selection, indicates its open state, and closes/revokes the session. Text-like inputs, textareas, and selects share computed height, radius, border, padding, surface, text, placeholder, focus, hover, and disabled treatment in light/dark/forced-colors modes, with a consistent select indicator and no specialized-control regression. All Studio CSS is emitted from one `studio.scss` entry through responsibility-based underscore partials with no deprecated Sass `@import`, legacy catch-all partial, duplicated global control system, or styling leakage into application preview content. Every existing destination and feature remains reachable; adjacent card, field, action, metadata, and section stacks have a predictable gap and no overlap/root overflow at 1440px, 1280px, 1024px, 768px, 390px, 320px, and 200% zoom. Focused unit/browser regressions, complete cross-browser Studio coverage, production build, full repository, compatibility, formatting, ledger, and whitespace gates must pass before `[x]` and commit.
  - Risks/rollback: splitting a 90KB cascade can change selector order; a broad form rule can capture specialized controls; removing the third column can regress preview-session lifecycle or header containment. Preserve source order at the SCSS entry, scope global primitives beneath the Studio shell and explicitly exclude specialized/application controls, replace removed-panel tests with header-session/live-bridge assertions, and verify every destination across themes and widths. Roll back the single STUDIO-015 commit and its dev dependency if required; no stored content, API, schema, authorization, tenant, cache, or published-render state requires restoration.
  - Delivered/verification: removed the entire inline preview panel, iframe, renderer canvas, and preview toolbar; the two-column authoring workspace now exposes one header toggle that creates the existing scoped standalone draft session, reports open state, synchronizes edits and node selection, and closes/revokes the popup. Responsive overrides remain authorable in the component inspector. Replaced the monolithic CSS source with one ordered `studio.scss` entry and 13 responsibility-owned partials, added portable Dart Sass 1.103.1 as a development-only compiler, consolidated ordinary text-like inputs/textareas/selects plus CTA, typography, cards, themes, accessibility, and spacing without entering specialized inputs or application content. The focused Studio suite passes 33/33; `pnpm check` passes 320-file lint/policy validation, 317-file formatting, generated contracts, all strict types, 385 active tests with 17 documented PostgreSQL-only skips, React 18.3.1 SSR certification, Sass-enabled Studio and all production builds. The canonical `pnpm test:e2e` gate passes 18/18 across Chromium, Firefox, and WebKit, covering all 19 destinations at 1440px, 1280px, 1024px, 768px, 390px, and 320px plus dark mode, WCAG, keyboard, 200% zoom, forced colors, popup-only live preview, and published delivery. BUG-0404 through BUG-0415 are resolved.

### Example React application and developer experience

- [x] **EX-001** Create a Vite React example consuming published GridStory content.
- [x] **EX-002** Demonstrate code-owned Hero, RichText, and Callout components with editor-owned composition.
- [x] **EX-003** Document integration without replacing the example application's router or styling.
- [x] **DX-001** Add root dev/build/test/typecheck commands and concurrent local development.
- [x] **DX-002** Add `.env.example`, one-command setup, seeded data, and troubleshooting guidance.
- [x] **DOC-001** Replace the placeholder README with architecture, quick start, scripts, and repository map.

### Verification

- [x] **TEST-001** Add schema/core unit tests for validation, drafts, publish, conflicts, and history.
- [x] **TEST-002** Add API integration tests using isolated temporary SQLite databases.
- [x] **TEST-003** Add React renderer and Studio component tests.
- [x] **TEST-004** Verify production builds for server, Studio, packages, and example application.
- [x] **TEST-005** Exercise the complete vertical slice in Edge and record every discovered defect in `BUGS.md`.

### Milestone 1 verification evidence — 2026-07-17

- `pnpm typecheck`: passed for all five packages, API, Studio, and Vite example.
- `pnpm test:run`: 8 test files and 14 tests passed (schema 3, client 1, core 3, React renderer 3, API 2, Studio 2).
- `pnpm build`: all packages, compiled API, Studio production bundle, and Vite example production bundle passed.
- Compiled API smoke test: health `ok`; seeded `welcome` entry returned as `published` with three blocks and an exact published revision ID.
- Playwright Edge walkthrough: dirty-edit guard, immutable draft save, publish, and delivery into the standalone Vite React application passed.
- `BUG-0001` through `BUG-0005` were recorded before correction; no defects remain open at this checkpoint.

## Milestone 2 — Production content foundation

- [x] **M2-001** Add organizations, tenants, workspaces, sites, environments, locales, and explicit request context.
- [x] **M2-002** Add OIDC identity, sessions, service accounts, scoped tokens, RBAC, and ABAC foundations.
- [x] **M2-003** Add PostgreSQL production adapter and cross-adapter conformance tests.
- [x] **M2-004** Add relations, reusable objects, arrays, blocks/unions, taxonomies, routes, slugs, and redirects.
- [x] **M2-005** Add schema-as-code/visual-model round-trip, diff, migration plans, drift detection, and generated types.
- [x] **M2-006** Add REST filtering/sorting/cursor pagination/projection and GraphQL delivery/management APIs.
- [x] **M2-007** Add localization, fallback graphs, locale-specific status, routes, and translation completeness.
- [x] **M2-008** Add transactional outbox, durable jobs, cache tags, signed webhooks, and replay tooling.
  - Implemented atomic SQLite/PostgreSQL outbox emission, leased durable jobs, signed webhook dispatch, cache invalidation/tagging, management APIs/SDK, scope-aware worker execution, retry/dead-letter/replay, and an operations runbook.
- [x] **M2-009** Add logical export/import with checksums, dry-run, streaming, stable IDs, and rollback boundaries.
  - Implemented versioned canonical archives, per-record/aggregate SHA-256 verification, JSON/JSON Lines export and import, stable content/history/translation IDs, schema checks, dry-run and reject/skip/replace policies, cross-scope protection, and atomic SQLite/PostgreSQL rollback.
- [x] **M2-010** Add immutable/tamper-evident audit exports and administrator operational views.
  - Implemented scope-bound, sequence-stable per-entry SHA-256 audit chains with legacy backfill, persisted-tamper detection, integrity verification and JSON/JSON Lines export, administrator-only APIs/SDK, a bounded operations summary, and an on-demand Studio integrity panel.

### Milestone 2 foundation evidence — 2026-07-17

- Full storage scope is mandatory across organization, tenant, workspace, site, environment, and locale; cross-site reads return no content.
- A legacy tenant-only SQLite file migrates scope columns before the composite scope index and remains writable.
- Deny-by-default RBAC/ABAC, OIDC verifier/session contracts, service accounts, hashed opaque scoped tokens, and API 403 behavior are covered by tests.
- `pnpm test:run`: 11 test files and 22 tests pass; the authorized/scoped Edge edit-publish-deliver walkthrough also passes.
- PostgreSQL 17 conformance: three shared repository suites plus the real API create-publish-deliver integration pass in an automatically removed Docker container.
- `pnpm check`: 13 test files and 26 tests pass alongside all lint, format, ledger, boundary, type, and production-build gates; the authorized Edge walkthrough remains green.
- `BUG-0022` and `BUG-0023` were logged before repair; no defects remain open at the M2-003 checkpoint.
- Advanced modeling now covers reusable objects, scalar/array/union/block fields, scoped relation integrity, hierarchical taxonomies, semantic model validation, exact generated types, canonical routes, publication-time slug/path uniqueness, and loop-safe redirects.
- M2-008 operations verification: 60 normal tests pass across 22 files, six live PostgreSQL repository suites plus its API integration pass, the complete lint/format/ledger/boundary/type/build gate passes, and the Edge edit-publish-deliver walkthrough remains green.
- `BUG-0042` through `BUG-0045` were recorded before repair; no defects remain open at the M2-008 checkpoint.
- M2-009 portability verification: 65 normal tests pass across 23 files, seven live PostgreSQL repository suites plus its API integration pass, all quality/build gates are clean, and the Edge authoring/delivery walkthrough remains green.
- `BUG-0046` through `BUG-0048` were recorded before repair; no defects remain open at the M2-009 checkpoint.
- M2-010 audit/administration verification: 70 normal tests pass across 24 files, seven live PostgreSQL repository suites plus its API integration pass, persisted SQLite tampering and legacy backfill are covered, all quality/build gates are clean, and the Edge walkthrough remains green.
- `BUG-0049` through `BUG-0053` were recorded before repair; no defects remain open at the Milestone 2 completion checkpoint.
- Route-based API delivery returns published content or explicit redirects; Studio safely edits every new field family and retains existing schema compatibility.
- `pnpm check`: 15 test files and 32 tests pass with all lint, format, boundary, ledger, type, and production-build gates; four live PostgreSQL tests and the Edge walkthrough also pass.
- `BUG-0024` through `BUG-0028` were recorded before correction; no defects remain open at the M2-004 checkpoint.
- The versioned canonical IR now round-trips losslessly through schema-as-code, deterministic JSON, and the visual-model envelope; stable IDs and immutable schema/component versions govern evolution.
- Semantic diff and migration plans classify safe/backfill/destructive changes, affected surfaces and entries, backfill hooks, data scans, lock estimates, exact approval identity, reversibility, and rollback policy.
- Full-scope SQLite/PostgreSQL deployment state, drift-aware readiness, lifecycle APIs/SDK, four-source drift, exported generated contracts, and the `schema:check` CI gate are verified.
- `pnpm check`: 18 test files and 42 tests pass with generation drift, lint, format, boundary, ledger, type, and production-build gates; five live PostgreSQL tests and the Edge walkthrough also pass.
- `BUG-0029` through `BUG-0031` were recorded before correction; no defects remain open at the M2-005 checkpoint.
- A single bounded query contract now provides recursive boolean filtering, 13 predicate operators, deterministic multi-sort with stable ID tie-breaking, signed query-bound cursors, nested projections, and connection metadata across REST, GraphQL, and the SDK.
- REST management/delivery and GraphQL management/delivery preserve full hierarchy scope, resolver/route RBAC/ABAC, draft/published isolation, public REST delivery caching, and private GraphQL caching; PostgreSQL exercises all three new API paths.
- `pnpm check`: 19 test files and 47 tests pass with all schema-drift, lint, format, boundary, ledger, strict-type, and production-build gates; five live PostgreSQL tests and the Edge walkthrough also pass.
- `BUG-0032` through `BUG-0038` were recorded before correction; no defects remain open at the M2-006 checkpoint.
- Canonical schema localization now declares versioned localized fields; ordered site fallback graphs reject invalid references/cycles and resolve explicitly to published locale variants without merging draft caches.
- SQLite/PostgreSQL translation groups preserve full base scope, locale-specific immutable revisions/status/routes, canonical shared fields, legacy-entry backfill, completeness percentages, missing fields, and all-required-locales publication state.
- Localized REST/GraphQL/SDK management and delivery are verified with explicit fallback metadata; all public REST delivery now includes six-dimension scope `Vary` protection.
- `pnpm check`: 21 test files and 55 tests pass with schema generation, lint, format, boundary, ledger, strict-type, and production-build gates; six live PostgreSQL tests and the Edge walkthrough also pass.
- `BUG-0039` through `BUG-0041` were recorded before correction; no defects remain open at the M2-007 checkpoint.

## Milestone 3 — Visual composition and authoring depth

- [x] **M3-001** Add component palette, layers, slots, nesting rules, drag/drop, keyboard movement, and undo/redo.
  - Added the immutable composition command/history foundation with recursive node lookup, layer flattening, root/slot acceptance and cardinality enforcement, cycle-safe add/remove/reparent/reorder operations, prop updates, selection retention, and bounded branching undo/redo.
  - Wired root and slot palettes, recursive layer selection, drag/drop reparenting, keyboard reorder/nesting/removal, selected-node property editing, slot capacity guidance, and undo/redo into Studio.
  - Added a slot-capable Stack example component and advanced the page contract to version 3 so nesting is exercised by the same application-owned React renderer used for delivery.
  - Focused verification found `BUG-0054` through `BUG-0059`; all corrections are retained in the permanent bug ledger.
  - Verification: `pnpm check` passes with 74 tests across 25 passing files, all schema/lint/format/ledger/boundary/type/build gates are clean, seven focused Studio tests cover commands and user interactions, and the Edge author-save-publish-deliver walkthrough passes.
- [x] **M3-002** Add design-token bindings, variants, responsive controls, reusable symbols, and templates.
  - Preserved serializable design data and application-owned React rendering through an explicit versioned contract and resolver boundary.
  - Added canonical design-system and node-presentation contracts for tokens, ordered breakpoints, component variants, controlled linked symbols, templates, and responsive prop overrides.
  - Focused contract verification found `BUG-0060`; its correction is retained in the permanent bug ledger.
  - Added a versioned example design system plus authorized control-plane and typed-client delivery for tokens, breakpoints, variants, a controlled linked symbol, and a nested campaign template.
  - Added deterministic React presentation resolution for controlled symbol overrides, variants, token values, and explicit responsive breakpoints, retaining application-owned rendering and preview source identity.
  - Wired API-delivered variants, compatible token selectors, breakpoint override capture/preview, governed symbol insertion, recursive template cloning, and linked-symbol override restrictions into Studio.
  - Focused interaction verification found `BUG-0061`; its test correction is retained in the permanent bug ledger.
  - Pinned every bound presentation to an immutable design-system version; canonical validation rejects unpinned bindings and runtime resolution safely falls back when application/content versions differ.
  - Verification found and resolved `BUG-0060` through `BUG-0064`; `pnpm check` passes with 83 tests across 27 files, all schema/lint/format/ledger/boundary/type/build gates are clean, nine Studio tests cover composition/design authoring, browser bundles are 203 KB/234 KB, and the Edge walkthrough passes.
- [x] **M3-003** Add secure iframe and standalone preview sessions, route synchronization, live patches, and click-to-edit overlays.
  - Implemented the preview protocol, credential isolation, exact source/origin validation, startup retry, ordered transport lifecycle, and scope-checked management cleanup.
  - Added versioned session/message contracts and an HMAC-signed, audience/scope/origin/route/mode/expiry-bound preview service with allow-listed HTTPS targets, revocation, monotonic sequences, nonce replay protection, and bounded replay memory.
  - Preview service review found and resolved `BUG-0065`; its stable-error correction is retained in the permanent bug ledger.
  - Added authorized session issuance, token-authenticated draft retrieval, replay-checked message acceptance, self-revocation, and scope-checked management revocation endpoints with private/no-store separation.
  - Added an explicit browser client entry with controller/application runtimes, token bootstrap outside URLs, queued live draft patches, bidirectional routes, and source-click selection.
  - Wired sandboxed iframe and standalone popup previews into Studio and the ordinary Vite application with application-owned routing/rendering, source overlays, environment configuration, and lifecycle cleanup.
  - Verification found and resolved `BUG-0066` through `BUG-0077`; `pnpm check` passes with 93 tests across 30 files plus two intentionally skipped tests, all lint/format/ledger/boundary/type/build gates are clean, Studio/example bundles are 241.57/208.09 KB across 28/24 modules, and the Edge walkthrough passes the complete iframe/standalone/edit/select/route/revoke/save/publish/delivery flow.
- [x] **M3-004** Add rich text, references, asset pickers, inline editing, comments, mentions, assignments, and presence.
  - Added canonical semantic rich-text, asset-reference, comment-thread, mention, assignment, resolution, and expiring presence contracts with strict field validation and generated content types.
  - Added a scope-bound framework-neutral collaboration service, permissions, private API routes, browser-safe CORS, stable errors, and typed universal-client methods; collaboration remains outside published delivery and caches.
  - Replaced raw JSON authoring for the new fields with semantic block controls, asset and searchable content-reference pickers, selected-component inline preview controls, and a Studio comment/presence workspace.
  - Added a version 4 example page contract and authoring/collaboration guide, including the explicit process-local collaboration-adapter and demonstration asset-library boundaries ahead of M4 durability and asset infrastructure.
  - Focused and full verification found and resolved `BUG-0078` through `BUG-0090`; schema/core/API/client/Studio regressions cover canonical validation, scope isolation, permissions, CORS, stable due dates, mentions, assignments, resolution, presence expiry, field controls, inline edits, and localization compatibility.
  - Verification: `pnpm check` passes with 100 tests across 32 files plus two intentionally skipped tests, all lint/format/ledger/boundary/schema/type/build gates are clean, Studio/example bundles are 254.55/209.45 KB across 29/24 modules, and the bounded Edge walkthrough passes in 16.5 seconds.
- [x] **M3-005** Add component versioning, migrations, deprecation, usage impact, and visual regression hooks.
  - Added canonical active/deprecated component lifecycle contracts, replacement/sunset guidance, deterministic serializable visual scenarios, and non-executable rename/default/remove migration chains with ambiguity and version validation.
  - Added fully scoped draft/published usage impact, stable migration and visual plan identities, revision-safe draft-only migration, private REST/client surfaces, preview version selectors, and a Studio Components governance panel.
  - Preserved application-owned React execution, schema-declared scan boundaries, optimistic immutable revisions, published-content isolation, and existing private/public cache policies; documented application screenshot-runner integration.
  - Verification: `pnpm check` passes with 107 tests across 35 files plus two intentional skips; focused lifecycle/API/client/React/Studio tests pass; production builds are 258.37/210.13 KB across 29/24 modules; desktop and 700×900 in-app Studio walkthroughs showed no console errors or horizontal overflow and the canonical Edge flow passed in 13.3 seconds.
- [x] **M3-006** Add SEO, accessibility, link integrity, content-quality checks, and configurable publish gates.
  - Added canonical serializable policies, deterministic SEO/accessibility/link/editorial findings, stable paths and IDs, explainable deductions/scores, channel/locale/content-type selection, severity thresholds, and authorized role bypasses.
  - Added a scope-bound framework-neutral quality service with published-reference isolation, injected external-link adapters, candidate assessment, private REST/client routes, and revision-safe pre-publication enforcement before repository mutation.
  - Added a responsive Studio Quality panel with candidate rechecks, score/severity summaries, responsible field/block paths, remediation guidance, stale-report invalidation, and authoritative blocked-publish details; documented application-rendered audit boundaries.
  - Verification found and resolved `BUG-0094` through `BUG-0108`; `pnpm check` passes with 114 tests across 34 files plus two intentional skips, all lint/format/ledger/boundary/schema/type/build gates are clean, production bundles are 260.75/210.64 KB across 29/24 modules, desktop and 700x900 in-app checks show no console errors or horizontal overflow, and the canonical Edge flow passes in 11.5 seconds.

## Milestone 4 — Assets, workflow, releases, and search

- [x] **M4-001** Add S3-compatible assets, resumable uploads, renditions, focal points, metadata, and usage tracking.
  - Added canonical fully scoped asset/upload/rendition/metadata/focal-point/usage contracts, immutable asset revisions, framework-neutral repository/storage/rendition boundaries, durable local SQLite metadata, and an S3-compatible multipart client adapter.
  - Added exact recorded-part completion integrity, resume/abort state, private no-store REST authorization, universal-client methods, draft/published usage locations, and injected image-rendition processing without leaking preview or draft credentials into published caches.
  - Added a responsive Studio asset library with negotiated browser chunking, governed card metadata, focal/rendition/revision details, usage inspection, and managed schema-field choices; documented production adapters and the explicit M4-002 MIME/quarantine/private-delivery boundary.
  - Verification found and resolved `BUG-0109` through `BUG-0139`; the full pre-walkthrough `pnpm check` passed with 124 tests across 42 files plus two intentional skips, final lint/format/Studio typecheck and all 16 Studio tests pass, production builds are 265.56/211.97 KB across 29/24 modules, the canonical Playwright flow passes in 19.5 seconds, and 1440x900 plus 700x900 in-app asset upload/usage checks show responsive 3/1-column layouts with no console errors or horizontal overflow.
- [x] **M4-002** Add MIME/content verification, SVG sanitization, malware quarantine hooks, and signed private delivery.
  - Added canonical per-revision security verdicts, built-in MIME/signature and asset-kind inspection, a conservative fail-closed SVG sanitizer, injected malware scanning, and immutable quarantine findings without vendor dependencies in the control plane.
  - Made infection and scanner failure fail closed, denied legacy/unverified/quarantined object reads and renditions, retained full-scope storage isolation, and extended S3-compatible adapters with private object reads.
  - Added 30-900 second HMAC delivery grants bound to full tenant scope plus exact asset/revision identity, private no-store streaming with tamper/expiry checks and hardened response headers, API/client/runtime configuration, verified-only Studio choices, security badges, and the completed asset guide.
  - Verification found and resolved `BUG-0140` through `BUG-0154`; `pnpm check` passes with 133 tests across 46 files plus two intentional skips, all lint/format/ledger/boundary/schema/type/build gates are clean, production bundles are 266.29/212.19 KB across 29/24 modules, the canonical Edge flow passes in 7.5 seconds, and real sanitized-SVG walkthroughs at 1440x900 and 700x900 show verified state, 3/1-column layouts, no console warnings, and no horizontal overflow.
- [x] **M4-003** Add custom workflows, approvals, separation of duties, schedules, notifications, and escalations.
  - Added canonical versioned workflow contracts, graph validation, fully scoped in-memory/SQLite/PostgreSQL repositories, and a framework-neutral service for conditional approvals, distinct-actor separation of duties, escalation deadlines, persisted schedules, bounded notifications, and immutable history metadata.
  - Integrated workflow initialization and revision reset into the shared content lifecycle, enforced approval before REST/GraphQL/worker publication, preserved tenant and private-cache boundaries, and added authorized definition, transition, approval, schedule, cancellation, and due-processing API/client surfaces.
  - Added a responsive Studio governance panel for current state, available actions, approval decisions, Asia/Kolkata-safe IANA scheduling, cancellation, and activity; updated worker/seed behavior and documented authoring, adapter, security, schedule, notification, and M4-004/M4-005 boundaries.
  - Verification found and resolved `BUG-0155` through `BUG-0168`; `pnpm check` passes lint, format, ledger, boundary, schema, type, 142 tests across 48 passing files plus two intentional PostgreSQL skips, and production builds (273.05/213.50 KB across 29/24 modules); the governed Edge flow passes in 4.7 seconds, and 1440x900 plus 700x900 live walkthroughs show the full distinct-reviewer/schedule/cancel/publish lifecycle with no console errors or horizontal overflow.
- [x] **M4-004** Add atomic multi-entry releases, future-state preview, validation, scheduling, and rollback policy.
  - Added versioned release contracts, explicit authorization actions, fully tenant-scoped in-memory/SQLite/PostgreSQL repositories, and an atomic content publication primitive that verifies pinned draft and published pointers before updating every member, audit record, and outbox event in one transaction.
  - Added immutable draft pinning, whole-future-state schema/component/workflow/quality/route/reference validation, exact-revision private previews, immediate publication, IANA-aware scheduling/cancellation/due processing, and manual/time-window/disabled rollback policies with safe stale-publication guards.
  - Integrated the release lifecycle across REST, the universal client, worker processing, and a responsive Studio compose/validate/preview/schedule/publish/rollback workbench; documented security, caching, adapter, scheduling, rollback, and M4-005 durability boundaries.
  - Verification found and resolved `BUG-0169` through `BUG-0173`; `pnpm check` passes lint, format, ledger, boundary, schema, type, 152 tests across 52 passing files plus two intentional PostgreSQL skips, and production builds (282.35/214.83 KB across 29/24 modules); the canonical Edge flow passes, and 1440x900 plus 700x900 live walkthroughs verify atomic publish/rollback, schedule/cancel, future-state routes, no console warnings, and no horizontal overflow.
- [x] **M4-005** Add durable workflow designer/actions, retries, dead letters, idempotency, and delivery logs.
  - Added bounded notification, signed webhook, and cache-tag actions to versioned transitions, exact applied-version action snapshots in workflow history, and restart-safe idempotent reconciliation into the fully tenant-scoped durable job queue.
  - Reused expiring leases, claim recovery, capped exponential retries, per-action attempt limits, dead letters, immutable replay jobs, and bounded results/errors as delivery logs across SQLite/PostgreSQL adapters and framework-neutral notifier/cache/HTTP boundaries.
  - Added dedicated authorization, private/no-store REST and universal-client operations, worker execution, a responsive Studio state/action designer and delivery log, focused cross-layer regressions, and the workflow-action security and operations guide.
  - Verification found and resolved `BUG-0174` through `BUG-0180`; `pnpm check` passes lint, format, ledger, boundary, schema, type, 155 tests across 52 passing files plus two intentional PostgreSQL skips, and production builds (290.78/215.21 KB across 29/24 modules); the canonical Edge flow passes in 6.3 seconds, and the live 1280x720 action-design/save/transition/drain walkthrough shows a succeeded attempt with no console errors or horizontal overflow.
- [x] **M4-006** Add pluggable search, indexing/rebuild/status, taxonomies, backlinks, and related content.
  - Added public search/query/document/facet/status contracts, a framework-neutral `SearchAdapter`, a repository-backed default, explicit tenant/perspective keys, schema-derived canonical taxonomies, and generated example topics.
  - Expanded the transactional outbox with identifier-only `search.index` jobs, added leased `search.rebuild` jobs and combined adapter/queue status, wired API and worker runners, and preserved retry, dead-letter, idempotency, and replay behavior.
  - Added authorized private/no-store REST and universal-client methods for search, taxonomies, status/rebuild, backlinks, and deterministic related content; added a responsive Studio discovery panel and integration/operations documentation.
  - Verification found and resolved `BUG-0181` through `BUG-0190`; `pnpm check` passes lint, format, ledgers, boundaries, schema generation, all typechecks, 161 tests across 55 passing files plus two intentional PostgreSQL skips, and production builds (295.18/216.05 KB across 29/24 modules). The canonical Edge walkthrough passes in 5.1 seconds, and live 1280x720 plus 700x900 search/rebuild checks show correct taxonomy/index state, no console warnings/errors, and no horizontal overflow.

## Milestone 5 — Security, reliability, and v1 GA

- [x] **M5-001** Complete OWASP ASVS-aligned threat models and security requirements.
  - Added the canonical OWASP four-question/STRIDE model with eight actors, twelve protected assets, nine trust boundaries, twelve data flows, 22 stable owned threats, quantified risk, mitigations, evidence, assumptions, and lifecycle triggers.
  - Added an explicitly non-certifying OWASP ASVS 5.0.0 Level 2-oriented profile covering all 17 chapters, 29 normative GridStory requirements, current evidence, residual gaps, owners, threat links, and M5/M6 delivery ownership.
  - Added human security/threat/ASVS guides and `pnpm security:check`; the validator's negative self-tests cover risk, duplicate-ID, and broken-reference failures and the root lint gate enforces model/profile structure, applicability, traceability, and evidence paths.
  - Verification found and resolved BUG-0191 through BUG-0193; `pnpm security:check` passes, and unrestricted `pnpm check` passes lint, formatting, ledgers, boundaries, schema generation, every typecheck, 161 tests across 55 passing files plus two intentional PostgreSQL skips, and all production builds.
- [x] **M5-002** Harden tenant isolation across storage, cache, search, assets, jobs, events, and telemetry.
  - Added one validated, collision-safe six-field scope contract for keys, object paths, cache prefixes, adapter checks, and bounded secret-safe telemetry envelopes; OIDC roles and service-account grants are explicitly tenant bound.
  - Made asset, audit, search, webhook, outbox, durable-job, cache, and operational boundaries fail closed on mismatched scope; hostile search totals, facets, highlights, and taxonomy metadata are rebuilt from accepted authoritative records.
  - Added `pnpm tenant:check`, cross-scope/hostile-adapter regressions, and updated security/threat/ASVS/operations/search/asset documentation while leaving production identity/proxy and telemetry sink lifecycle under M6-002, M5-008, and M5-004.
  - Verification resolved BUG-0194 through BUG-0202; `pnpm check` passes 171 tests with two intentional PostgreSQL skips plus all lint, format, security, tenant-contract, type, and build gates, and `pnpm test:e2e` passes the canonical Edge workflow.
- [x] **M5-003** Add plugin SDK capability manifests, grants, isolation, signatures, test harness, and lifecycle.
  - Delivery tier: T2 (public contracts, cryptographic verification, authorization, durable tenant state, and a new external execution boundary).
  - Assumptions: production server plugins execute in an operator-provided process/container runtime; Studio plugins use a sandboxed-frame protocol; GridStory never imports arbitrary plugin modules into the control-plane process.
  - Sequence: publish versioned SDK schemas; verify Ed25519 manifest signatures and artifact digests; persist tenant grants/lifecycle; mediate bounded external invocations; expose authorized REST/client operations; update threat/ASVS evidence and operator guidance.
  - In scope: capability constraints, publisher trust records, compatibility checks, install/enable/disable/revoke/uninstall preview, immutable lifecycle history, rate/timeout/size limits, a test-only harness, SQLite/PostgreSQL conformance, and focused API/client tests.
  - Out of scope: marketplace download/review, dependency scanning/SBOMs, publisher enrollment UX, an actual Studio extension loader, and provisioning a process/container sandbox; those remain owned by M6-005, M5-007, later Studio delivery, and deployment adapters.
  - Rollback: disable or revoke the tenant installation, remove the injected runtime adapter, and retain lifecycle history; uninstall requires an explicit preview and leaves plugin-owned external data cleanup to the plugin/runtime contract.
  - Acceptance: unsigned/untrusted/incompatible/tampered manifests fail closed; grants cannot exceed requested constrained capabilities; all state is fully tenant scoped and durable; only enabled plugins can invoke an injected external runtime within bounds; privileged operations are authorized and recorded; `pnpm check`, PostgreSQL conformance, and focused API/client tests pass.
  - Verification found and resolved `BUG-0205` through `BUG-0211`; four focused files pass 24 tests, `pnpm security:check` and tenant/ledger gates pass, real PostgreSQL 17 conformance passes 8 core plus 1 API tests against rebuilt packages, and `pnpm check` passes 180 tests with three intentional PostgreSQL-only skips plus every lint, format, boundary, security, scope, generated-schema, type, and production-build gate.
- [x] **M5-004** Add OpenTelemetry logs/metrics/traces, dashboards, retention, health, and operational runbooks.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (new production dependencies, telemetry export boundary, security-sensitive metadata policy, and operator-facing health/configuration contracts); approved by the user's 2026-08-21 instruction to proceed to the next task.
  - Assumptions: OpenTelemetry remains disabled by default; production sends OTLP/HTTP to an operator-managed private Collector; durable audit remains the source of truth; metrics never carry raw tenant IDs or request values; JavaScript log SDK support is explicitly documented as developmental.
  - Sequence: adapt the existing tenant envelope and API/worker lifecycle to the official SDK; add bounded request/worker signals and private exporter health; provide Collector, dashboard, alert, retention, and incident-response artifacts; update security evidence; verify focused, full, PostgreSQL, and HTTP smoke paths.
  - Scope fence: `apps/api`, `.env.example`, root/API dependency manifests and lockfile, `deploy/observability`, `docs/observability.md`, `docs/operations.md`, `docs/adr/0006-opentelemetry-boundary.md`, security evidence documents, `README.md` (amended to correct capability/documentation drift found as BUG-0216), and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: optional OTLP logs/metrics/traces, trace/log correlation, safe attributes and low-cardinality metrics, exporter/Collector health state, shutdown flush, API and worker coverage, deployable reference configuration, Grafana/Prometheus artifacts, log inventory, retention targets, alert response, and failure-mode tests.
  - Out of scope: hosting a telemetry backend, browser/Studio instrumentation, customer analytics, tail sampling, per-tenant metric labels, telemetry as an audit replacement, production identity telemetry before M6-002, backup/restore and rolling shutdown work owned by M5-005, and benchmark-derived capacity limits owned by M5-007.
  - Rollback: set `GRIDSTORY_OTEL_ENABLED=false` or remove the injected runtime; API, worker, Pino stdout, durable audit, and health/readiness continue without an exporter. Remove the reference deployment independently because no application schema or content migration is introduced.
  - Observable acceptance: disabled startup is network-free; enabled API/worker produce OTLP logs, metrics, and traces without bodies, headers, query strings, credentials, or tenant metric labels; public health remains minimal; authorized operations health exposes only bounded signal state; degraded Collector health does not take the content plane down; checked-in configs parse; focused negative tests, `pnpm check`, PostgreSQL conformance, and an HTTP smoke pass succeed.
  - Verification found and resolved `BUG-0212` through `BUG-0218`; three focused API files pass 23 tests including live OTLP/HTTP export and negative leakage assertions; the pinned contrib 0.158.0 Collector validates, Prometheus 3.13.2 accepts all five rules, and dashboard/security JSON parses.
  - Final gates: `pnpm check` passes clean lint/format/ledgers/boundaries/security/scope/generated-schema/type gates, 185 tests with three intentional PostgreSQL-only skips, and every production build; `pnpm test:postgres` passes 8 core plus 1 API tests; a hidden built-API HTTP smoke reports `ok`, `ready`, and disabled telemetry/Collector with empty stderr. Browser E2E was not applicable because no browser behavior changed.
- [x] **M5-005** Add backups, point-in-time recovery guidance, restore tests, graceful shutdown, and rolling-upgrade checks.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (destructive-recovery guidance, a new operator CLI contract, and cross-cutting API/worker process lifecycle); approved by the user's 2026-08-21 instruction to proceed to the next task.
  - Assumptions: production uses PostgreSQL and database-native WAL/base-backup services; SQLite remains a single-host development/small-deployment option with snapshot recovery rather than PITR; operators provide encrypted off-host storage and deployment orchestration; a 25-second application shutdown budget fits inside the common 30-second platform grace period.
  - Sequence: implement and verify checksummed SQLite snapshots plus native PostgreSQL logical-backup wrappers; add isolated restore drills; centralize bounded signal handling and interruptible worker drain; add current/candidate HTTP rollout preflight; publish RPO/RTO, PITR, restore, rollback, and rollout guidance; run focused, full, PostgreSQL, recovery, and HTTP/process smokes.
  - Scope fence: `apps/api`, `scripts/test-postgres.mjs`, root `package.json`, `.env.example`, `docs/recovery-and-rollouts.md`, `docs/operations.md`, `docs/schema-lifecycle.md`, `docs/adr/0007-recovery-and-rollout.md`, `docs/security`, `security/threat-model.json`, and `security/asvs-v5.0.0-profile.json` (amended because the recovery CLI creates a new whole-database artifact/storage trust boundary), `README.md`, and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: fail-closed backup manifests/checksums, online SQLite snapshots, custom-format PostgreSQL logical dumps, non-production-target restore verification, corruption/refusal tests, bounded first-signal drain and timeout/second-signal force behavior, interruptible worker polling, exact liveness/readiness rollout preflight, and a database-native PostgreSQL PITR runbook.
  - Out of scope: provisioning backup buckets, keys, schedules, or a managed PostgreSQL service; implementing WAL archival inside GridStory; in-place production restore automation; tenant-selective physical recovery; Kubernetes/Terraform/Helm deployment packages; backward-incompatible schema migrations; secret rotation exercises still owned by the existing security/GA work.
  - Prior art: SQLite documents `VACUUM INTO` as a consistent live snapshot; PostgreSQL separates portable `pg_dump`/`pg_restore` drills from base-backup plus continuous-WAL PITR; Fastify drains in-flight requests through `close()`; Node signal listeners must explicitly terminate; Kubernetes combines readiness, bounded termination, surge/unavailable limits, and rollout status. ADR 0007 records the comparison and necessity gate.
  - Risks: a corrupted or mismatched archive could replace valid data, signal listeners could leave a process alive indefinitely, or an exact-schema readiness gate could stall an unsafe rolling change. Early warnings are checksum/integrity failure, shutdown timeout/second signal, or either rollout endpoint returning non-200/non-exact health contracts.
  - Rollback: revert the task commit; remove the optional CLI/check commands and shutdown-timeout variable; the database schema is unchanged. Recovery commands refuse an existing target by default, and production restores remain a reviewed stop/restore/verify/cutover operation with the original database retained until acceptance.
  - Observable acceptance: a live SQLite database and a disposable PostgreSQL database can be backed up, mutated independently, restored to isolated targets, and verified; corrupt/mismatched/existing-target cases fail closed; API and worker complete current work, close repositories/telemetry, and stop inside the configured budget while timeout/second signal force non-zero exit; current and candidate instances must both pass exact `/health` and `/ready` contracts; `pnpm check`, PostgreSQL conformance/recovery, and process/HTTP smokes pass.
  - Verification found and resolved `BUG-0219` through `BUG-0224`; the focused recovery gate passes 10 tests covering live SQLite and PostgreSQL command contracts, tamper/target refusals, shutdown success/failure/timeout/second signal, interruptible worker drain, and live two-origin HTTP preflight.
  - Final gates: `pnpm check` passes clean lint, format, ledger, boundary, security, tenant-scope, generated-schema, type, 196-test (four intentional PostgreSQL-only skips), and production-build gates. `pnpm test:postgres` passes 8 core plus 2 API tests, keeps current/candidate instances ready against one PostgreSQL database, and recovers one published entry after native dump plus source deletion.
  - Operational smokes: the documented root SQLite backup, verify, and isolated restore commands produced and accepted the checksummed native artifact; the built API returned exact HTTP 200 health then completed first-signal repository/telemetry shutdown with exit zero; the built worker drained four jobs, interrupted its long poll, completed cleanup, and exited zero. Browser E2E was not applicable because no browser behavior changed; provider physical PITR and orchestrator behavior remain explicitly named deployment evidence rather than claimed repository proof.
- [x] **M5-006** Complete WCAG 2.2 AA/ATAG-informed review and supported browser/framework certification.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (new rendered-accessibility dependency, cross-browser CI matrix, compatibility fixtures, and a public support contract); approved by the user's 2026-08-21 instruction to proceed to the next task.
  - Assumptions: this is a repository-owned conformance review and tested-support statement, not third-party WCAG/ATAG certification; Playwright's pinned Chromium, Firefox, and WebKit builds are engine evidence rather than claims about every branded browser/OS/assistive-technology combination; only shipping integrations can be certified.
  - Sequence: establish the reviewed support/conformance boundary; add visible keyboard/focus and bypass fundamentals; enforce rendered WCAG 2.2 A/AA automation and critical keyboard/reflow checks; run the complete flow across all three browser engines; prove both declared React majors and the Vite production target; publish exact evidence, limitations, ATAG Part A/B findings, and browser-header ownership.
  - Scope fence: `apps/studio/src/App.tsx`, `apps/studio/src/studio.css`, focused Studio tests, `examples/vite-site`, `packages/react`, a React 18 compatibility fixture under `tests/compatibility`, browser tests under `tests/e2e`, `playwright.config.ts`, `.github/workflows/quality.yml`, `pnpm-workspace.yaml`, root/package manifests and lockfile, root `.gitignore`/`biome.json` (amended because the new SSR fixture emits a conventional generated directory that source gates must exclude), `docs/accessibility-and-compatibility.md`, `docs/troubleshooting.md`, `docs/adr/0008-accessibility-and-compatibility.md`, relevant security evidence documents, `README.md`, and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: WCAG 2.2 A/AA-tagged axe checks without suppressions, keyboard bypass/focus/equivalent authoring paths, 200% zoom/reflow and reduced-motion/forced-colors review, ATAG-informed authoring assistance inventory, Chromium/Firefox/WebKit E2E, React 18.3/19 rendering plus SSR/hydration evidence, Vite 8 SPA production builds, exact supported/unsupported matrix, and application-owned CSP/header guidance.
  - Out of scope: claiming full WCAG or ATAG conformance from automation, a third-party audit, live screen-reader/user acceptance, branded Safari/iOS certification, implementing or claiming Next.js/React Router/TanStack/Astro/Gatsby/React Native adapters, testing arbitrary consumer components, visual-regression infrastructure, and deployment-specific CSP/proxy proof; later adapter delivery and M5-008 own those claims.
  - Prior art: W3C requires human plus automated evaluation and splits ATAG between an accessible tool UI and assistance producing accessible content; Playwright recommends axe for detectable issues and projects for Chromium/Firefox/WebKit; Vite 8 defines a fixed Baseline Widely Available production target. ADR 0008 records the comparison and six-part necessity gate.
  - Risks: an engine-specific popup/preview behavior may fail, axe may expose existing semantic/contrast defects, a second React major may resolve the wrong peer, or documentation may overstate evidence. Early warnings are a project-specific E2E failure, an unsuppressed axe violation, a fixture-reported React version mismatch, or a matrix row without an executable command.
  - Rollback: revert the task commit, remove the optional audit dependency/fixture/projects, and restore single-engine CI; no schema, API, database, content, or deployment migration is introduced.
  - Observable acceptance: all scanned critical Studio/application states have zero WCAG 2.2 A/AA-tagged axe violations without exclusions or disabled rules; keyboard bypass, authoring equivalence, 200% zoom/reflow, reduced-motion, and forced-colors checks are evidenced; the complete edit/preview/publish/deliver flow passes on pinned Chromium, Firefox, and WebKit; isolated React 18.3 plus current React 19 Vite/SSR/hydration checks pass; the public matrix names exact tested versions, browser/header boundaries, ATAG findings, and untested surfaces; focused gates and `pnpm check` pass.
  - Verification found and resolved `BUG-0225` through `BUG-0231`: the actual frozen CI installation path succeeds, restricted Windows child-process checks pass through the approved path, shared contrast/native-control/focus-order defects are covered across engines, the WebKit flow has an explicit bounded timeout, and generated SSR output is excluded from source gates.
  - Compatibility evidence: `pnpm test:compatibility` passes five React 19 renderer/static/hydration tests, the exact React 18.3.1 Vite SPA plus SSR artifact/version assertion, and the React 19.2.7 Vite production build on Vite 8.1.5.
  - Final gates: `pnpm test:e2e` passes all 12 checks—unsuppressed rendered axe audits, keyboard/target/zoom/adaptation checks, and the complete secure preview/workflow/publish/deliver journey on Chromium, Firefox, and WebKit. `pnpm check` passes clean lint, format, ledger, boundary, security, tenant-scope, generated-schema, type, 196-test (four intentional PostgreSQL-only skips), and production-build gates.
  - Claim boundary: the repository review and exact engine/framework matrix are complete; branded Safari/iOS, live assistive technology and disabled-author sessions, arbitrary consumer components, independent conformance review, and deployment header proof remain explicitly assigned to M5-008 or application owners rather than represented as completed certification.
- [x] **M5-007** Publish tested limits, benchmark profiles, support policy, SBOM, signatures, and vulnerability process.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (public capacity/support contracts plus release and vulnerability CI); approved by the user's 2026-08-21 instruction to proceed to the next task.
  - Assumptions: GridStory is not yet publishing public npm packages or deployment images; release evidence therefore covers deterministic workspace package archives and repository-native application profiles. GitHub artifact attestations are the signing boundary, while npm trusted publishing, container signing, and an executed GA release remain later release decisions.
  - Sequence: inventory and enforce resource limits; build repeatable SQLite/PostgreSQL application profiles; publish the tested envelope and support/security processes; create deterministic package, checksum, SPDX SBOM, provenance-attestation, and vulnerability workflows; run focused and full gates; record exact evidence and remaining deployment obligations.
  - Scope fence: API request/GraphQL limits and tests, asset/archive schema and service limits and tests, root release/benchmark scripts and manifest, release/security workflows, Dependabot configuration, `SECURITY.md`, `SUPPORT.md`, release/operations/security documentation, ADR 0009, relevant threat/ASVS evidence, README, package manifest/lockfile only if required, and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: explicit ordinary/asset/import body bounds; bounded asset and portability archive shapes; GraphQL depth/alias/selection controls; existing query/job/plugin limits in one machine-readable profile; repeatable in-process API benchmarks for SQLite and PostgreSQL with percentile/throughput/memory output and budgets; packed package archives with SHA-256 manifest; SPDX JSON SBOM and GitHub/Sigstore provenance attestations; lockfile vulnerability scanning, dependency-update policy, private vulnerability reporting, supported-version/severity/response policy, and evidence-bounded support limits.
  - Out of scope: public package publication, package/version naming changes, deployment/container images, long-lived signing keys, marketplace publisher enrollment/review, automatic production sizing, CDN/WAF/database quotas, distributed rate limiting, destructive retention automation, database query pushdown, full-scale soak/chaos tests, and executing alpha/beta/RC/GA reviews owned by M5-008.
  - Prior art: CycloneDX and SPDX define machine-readable component inventories; Syft produces both, GitHub artifact attestations bind provenance/SBOM claims through Sigstore, OSV-Scanner understands pnpm lockfiles, and GitHub supports private vulnerability reporting plus repository security policies. ADR 0009 compares those options, includes the mandatory do-nothing alternative, and records the six-part necessity gate.
  - Risks: a documented limit may not be enforced, a microbenchmark may be mistaken for production capacity, package archives may include unintended files, or CI signing may imply a publication promise. Early warnings are boundary tests accepting an oversized payload, a benchmark report missing environment/sample metadata, a package inventory mismatch, or documentation claiming network/TLS/edge performance from in-process evidence.
  - Rollback: revert the task commit and remove the additive scripts/workflows/policies; request limits can be restored to their former defaults without a database migration, and no package is published or external release state changed by this slice.
  - Observable acceptance: explicit body/archive/GraphQL boundaries reject oversized work with stable errors; SQLite and disposable PostgreSQL profiles produce schema-validated JSON containing dataset, environment, p50/p95/p99, throughput, and memory evidence within published budgets; deterministic package archives and SHA-256 manifest verify locally; release CI generates an SPDX SBOM and attests the archives/manifest; OSV and dependency-update automation are configured; support and vulnerability policies name supported versions, response targets, private reporting, ownership, tested limits, and untested/deployment-owned boundaries; focused gates and `pnpm check` pass.
  - Verification found and resolved `BUG-0232` through `BUG-0241`; focused schema/core/API suites pass 41 tests with two intentional PostgreSQL-only skips, including ordinary/asset/import, archive, GraphQL, query/search/job, and plugin boundaries.
  - Capacity evidence: the 250-entry full SQLite profile passes at 1.667/26.988/22.674/6.644 ms p95 for published read/management query/GraphQL query/serial write, 6,343.970 published reads/s, and 145 MiB peak RSS. The disposable PostgreSQL 17 profile passes at 20.946/29.719/23.796/49.041 ms p95, 2,038.542 published reads/s, and 141 MiB peak RSS. Both are Fastify-injection application profiles, not network, TLS, proxy, CDN, multi-node, or provider capacity claims.
  - Release evidence: all five reviewed workspace archives pack deterministically, the six-file SHA-256 manifest verifies, and intentional tampering fails closed. The release, OSV, and Dependabot YAML parses; hosted SPDX generation and GitHub/Sigstore attestations are configured for the M5-008 release-candidate execution rather than represented as locally issued artifacts.
  - Final gates: `pnpm check` passes clean lint, format, ledger, boundary, security, tenant-scope, generated-schema, type, 200-test (four intentional PostgreSQL-only skips), and production-build gates; `pnpm test:postgres` passes 8 core plus 2 API tests and native dump/delete/isolated-restore recovery. Browser E2E was not applicable because no browser behavior changed.
- [x] **M5-008** Run alpha, design-partner beta, release candidate, and v1 GA readiness reviews.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (release-governance contract, machine-enforced evidence semantics, and public maturity claims); approved by the user's 2026-08-21 instruction to proceed to the next task.
  - Assumptions: this task runs evidence-based readiness reviews rather than fabricating partner, deployment, independent-assessor, or hosted-release results; a no-go is a valid completed review; the reviewed candidate is the last completed product commit `b31193a`, while M5-008 itself adds only review governance.
  - Sequence: define stage-specific gates and fail-closed outcome rules; assess the current candidate from private alpha through GA in dependency order; validate repository evidence and record absent external evidence as explicit blockers; publish owners/next actions and update security/support claim boundaries; rerun all repository, browser, compatibility, PostgreSQL, recovery, benchmark, and package-evidence gates.
  - Scope fence: `release/readiness`, `scripts/check-release-readiness.mjs`, root `package.json`, `README.md`, `SUPPORT.md`, `docs/release-readiness.md`, `docs/release-and-support.md`, `docs/accessibility-and-compatibility.md`, ADR 0010, relevant canonical/human security evidence, and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: exact alpha/beta/RC/GA criteria; repository-versus-external evidence labels; prerequisite ordering; deterministic go/no-go derivation; stable unmet-criterion IDs, owners, and next actions; negative validator self-tests; one dated review of `b31193a`; honest maturity/support documentation; full local gate execution.
  - Out of scope: recruiting or impersonating design partners, collecting user/customer data, provisioning a production deployment, enabling private GitHub reporting, pushing commits, publishing packages/images, triggering hosted workflows, issuing attestations, commissioning an external penetration/accessibility audit, certifying unimplemented framework adapters, or declaring beta/RC/GA ready without that evidence.
  - Prior art: Google SRE launch coordination favors curated, concrete, evidence-backed checklists and explicit sign-off; GOV.UK separates alpha learning from beta user evidence and live readiness; NIST SSDF requires release-integrity information; GitHub attestations require hosted verification; W3C requires human as well as automated accessibility evaluation. ADR 0010 compares free-form, hosted-platform, and repository-native options and records the six-part necessity gate.
  - Risks: a checklist may become ceremonial, a repository pass may be misrepresented as production evidence, later stages may bypass failed prerequisites, or sensitive partner details may enter source control. Early warnings are a `go` with an unmet required criterion, a missing evidence path, an RC/GA decision above a no-go predecessor, or person/customer/credential data in the artifact.
  - Rollback: revert the additive review script/artifact/docs and remove the root check hook; no application schema, package version, deployment, publication, user data, or external release state changes.
  - Observable acceptance: `pnpm readiness:check` accepts the dated review and rejects malformed, path-traversing, duplicate, unsupported, overclaimed, or prerequisite-bypassing variants; all four stages are reviewed in order; the current candidate receives only decisions supported by repository evidence; every unmet required criterion names an owner and concrete next action; security/support docs match the result; full release and repository gates pass.
  - Decision: candidate `b31193a` is `go` for controlled private technical alpha only; design-partner beta, RC, and v1 GA are `no-go`. The dated self-review records exact required gaps and never substitutes repository automation for partner, production deployment, hosted attestation, independent assessment, or operating acceptance.
  - Validator evidence: `pnpm readiness:check` accepts the four fixed stages and rejects nine negative variants covering missing/reordered stages, invalid commit identity, duplicate criteria, repository path traversal, external-only proof marked met, beta/RC prerequisite bypass, and required not-applicable status.
  - Product evidence: `pnpm check` passes all lint/format/ledger/boundary/security/tenant/readiness/schema/type/build gates and 200 tests with four intentional PostgreSQL-only skips; `pnpm test:e2e` passes 12/12 checks across Chromium, Firefox, and WebKit; `pnpm test:compatibility` passes five React 19 tests plus React 18.3.1 Vite/SSR and React 19 Vite builds.
  - Reliability/capacity evidence: `pnpm test:recovery` passes 10 tests; `pnpm test:postgres` passes 8 core plus 2 API tests and native recovery. The 250-entry SQLite/PostgreSQL profiles pass at 2.234/17.904 ms published-read p95, 6,219.518/1,920.824 reads/s, and 151/141 MiB peak RSS, with all query/write budgets passing.
  - Release evidence and residuals: five private archives pack and the six-artifact SHA-256 manifest verifies locally. `BUG-0242` is resolved; `BUG-0243` remains open under M5-009 because the archives lack README/license metadata. No hosted workflow/attestation, partner session, deployment, independent assessment, or publication was executed, so beta/RC/GA correctly remain no-go.
- [x] **M5-009** Make the five library archives publication-ready with required README/license metadata, mandatory inventory validation, and isolated consumer-install checks.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (cross-package release archive contract and installed-consumer verification); approved by the user's 2026-08-21 instruction to proceed to the next task.
  - Origin: `BUG-0243` discovered during the M5-008 local release-evidence review; this is the first repository-owned `RC-003` follow-up and does not by itself authorize public publication.
  - Assumptions: the existing package names, private `0.0.0` identities, Apache-2.0 repository license, pnpm 10 packer, Node 22 floor, and React 18.3/19 peer range remain unchanged; publication metadata means a package-specific root README, SPDX license field, and complete matching license text inside every archive.
  - Sequence: define the release-package decision and exact metadata contract; add package-specific READMEs and manifest license metadata; make archive validation require and inspect README/license contents; install all five tarballs together outside the workspace with offline dependency resolution; exercise every JavaScript export and type-check the installed declarations; run negative, focused release, full repository, and ledger gates.
  - Scope fence: the five `packages/{schema,client,core,react,example-kit}/README.md` and `package.json` files; `scripts/release-evidence.mjs`; root `package.json` only if a focused command is needed; `README.md`, `docs/release-and-support.md`, `docs/release-readiness.md`, ADR 0011, and the three mandatory project ledgers. Any discovered installed-declaration dependency defect may amend the affected package manifest and lockfile after it is logged.
  - In scope: useful package-root install/export guidance; exact `Apache-2.0` manifest metadata; mandatory single README, LICENSE, and manifest inventory; rejection of missing, duplicate, unexpected, empty, mismatched, or incorrectly licensed contents; a temporary non-workspace consumer using only packed tarballs plus declared external/peer dependencies; offline install, all-export runtime resolution, stylesheet resolution, and strict declaration type checking; deterministic archive/checksum evidence.
  - Out of scope: removing `private`, changing versions/names/exports, choosing registry visibility or dist-tags, npm organization/maintainer enrollment, publishing or staging packages, network dependency fetching during the consumer check, framework adapters, hosted attestations/SBOM execution, or changing the historical M5-008 candidate decision.
  - Prior art: npm requires name/version for publication, recommends a package-root README with install/configuration/use guidance, defines SPDX license metadata, always includes package-root README/LICENSE files, and recommends testing packages from an external install; pnpm packs tarballs, rewrites workspace dependency specs, and supports inspected output. ADR 0011 compares source metadata, pack-time mutation, permanent fixtures, and a temporary consumer and records the six-part necessity gate.
  - Risks: a workspace link could create a false consumer pass, offline resolution could hide an undeclared dependency, repeated license copies could drift, or a README could be present but useless. Early warnings are a temp consumer path under the workspace, `workspace:` remaining in a packed manifest, registry access, missing/duplicate metadata, license bytes differing from the canonical root file, or an installed import/typecheck failure.
  - Rollback: revert the additive README/manifest/validator/docs changes; the check uses a validated OS temporary directory and removes it in `finally`, and no package, version, registry, credential, deployment, or customer data is created.
  - Observable acceptance: all five archives contain exactly one useful `README.md`, one `LICENSE` matching the repository file, and a packed `license: Apache-2.0`; validator negative self-tests reject each metadata/identity/path overclaim; packed manifests contain no `workspace:` dependency; an offline temporary consumer installs all five tarballs, runs every exported JavaScript entry, resolves the stylesheet, and strictly type-checks public declarations without workspace links; preparation is deterministic, the SHA-256 manifest verifies, full repository/ledger gates pass, and `BUG-0243` is resolved without altering the historical M5-008 no-go artifact.
  - Metadata/inventory evidence: all five package-specific READMEs name install status, purpose, exports, boundaries, and Apache-2.0; packed manifests declare exact `Apache-2.0`, contain no `workspace:` values, and every archive has one README plus one LICENSE byte-identical to the canonical root file. Ten negative self-tests reject missing/duplicate/unexpected metadata, wrong identity/license/internal version, workspace dependencies, unrelated README text, and changed license bytes.
  - Installed-consumer evidence: two independent `pnpm release:prepare` runs create a unique consumer under the OS temporary root, install only the five reviewed tarballs plus declared React/ambient type requirements with pnpm offline and scripts disabled, resolve 10 JavaScript entry points plus the example stylesheet, strictly type-check all declarations, reject workspace/link lockfile resolutions, and remove the consumer afterward.
  - Determinism/checksum evidence: the five archives and `resource-limits.json` from `.gridstory/m5-009-release-c` and `-d` have identical SHA-256 digests; `pnpm release:manifest` writes a six-artifact manifest and `pnpm release:verify` verifies every byte. `pnpm install --frozen-lockfile --offline` confirms the core declaration dependency placement is lockfile-complete.
  - Final gates: `pnpm check` passes package boundaries, ledgers, security/readiness/tenant validators, format, schema generation, strict types, 200 tests with four intentional PostgreSQL-only skips, and all production builds. No UI/runtime behavior changed, so browser, PostgreSQL, recovery, and capacity suites are unchanged and not rerun for this release-archive slice.
  - Defects: `BUG-0243` through `BUG-0247` are resolved with exact regression evidence; the corrected record distinguishes pnpm's existing canonical LICENSE injection from the previously absent package README and manifest SPDX field.
  - Claim boundary: M5-009 clears only the repository-owned package metadata and tarball-install sub-gap of `RC-003`. Private `0.0.0`, registry/version approval, framework adapters, hosted attestations/SBOMs, beta prerequisites, deployment evidence, and independent sign-off remain unmet; the immutable M5-008 review remains alpha go and beta/RC/GA no-go.

## Milestone 6 — Collaboration and enterprise governance

- [x] **M6-001** Add CRDT-compatible field/block collaboration, suggestions, branches, merge, and conflict UI.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (new public contracts and APIs, durable tenant-scoped state, causal merge semantics, and author-facing conflict resolution); approved by the user's instruction to proceed to the next task.
  - Assumptions: content revisions remain the authoritative save/publish boundary; collaboration documents remain private control-plane state; online clients may omit causal metadata while offline/distributed adapters can supply stable operation IDs, actor sequences, and dependencies; application rendering and published delivery remain unchanged.
  - Prior art: Yjs uses commutative, associative, idempotent updates and state vectors; Automerge retains concurrent same-property values behind a deterministic winner; ProseMirror's central authority requires stale steps to rebase; Git merges histories from a common ancestor and pauses on overlapping changes. ADR 0012 records the comparison and necessity gate.
  - Sequence: publish bounded JSON collaboration contracts; persist scoped documents in memory/SQLite/PostgreSQL with optimistic versions; implement causal field/block registers, suggestions, branch ancestry, merge, and conflict resolution; expose authorized REST/client operations; add Studio controls and focused cross-layer regressions; verify full, PostgreSQL, and browser paths.
  - Scope fence: collaboration schema/core/repositories/tests, API collaboration wiring/tests, universal client methods/tests, Studio collaboration UI/tests/styles, `docs/authoring-and-collaboration.md`, `docs/adr/0012-causal-collaboration.md`, README if needed, and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: stable idempotent operations, actor clocks, causal dependency heads, deterministic multi-value field/block state, durable comments and collaboration history, suggestions with accept/reject, named branches, source-to-target merge, preserved competing values, explicit resolution, full tenant isolation, and private/no-store transport.
  - Out of scope: character-level rich-text CRDT encoding, peer-to-peer/WebSocket infrastructure, offline browser storage, binary Yjs/Automerge persistence, applying unsaved collaboration values directly to published revisions, and presence durability; these require separate transport/editor lifecycle work and presence intentionally remains expiring.
  - Risks and rollback: malformed or cross-scope histories could corrupt authoring intent, concurrent writers could overwrite a document, or collaboration state could leak into delivery. Bounded schema parsing, exact scope keys, optimistic repository versions, preserved conflict variants, and delivery regressions fail closed. Rollback is the task commit plus removal of additive collaboration tables; content revisions and published caches are not migrated.
  - Observable acceptance: duplicate operations are idempotent; equivalent causal histories converge; non-overlapping branch changes merge; concurrent same-target values remain visible until an author resolves them; suggestions and conflicts are operable in Studio; comments survive API restart through configured storage; cross-scope and viewer-write attempts fail; focused tests, `pnpm check`, PostgreSQL conformance, and browser smoke pass.
  - Implementation evidence: published schema/client contracts and authorized private REST operations cover stable causal changes, suggestions, branches, merges, conflicts, and resolutions; repositories retain fully scoped documents with optimistic versions in memory, SQLite, or the explicitly qualified PostgreSQL application schema; Studio exposes labelled responsive review controls while saved revisions remain the only publication input.
  - Focused verification: 3 schema, 6 core collaboration, 16 client, 15 API, and 16 Studio tests pass; focused Biome lint is clean. PostgreSQL verification passes 9 core and 2 API tests, proves live/new/reopened collaboration reads, and completes the logical backup/restore drill.
  - Browser verification: `pnpm test:e2e` builds production/e2e surfaces and passes all 12 Chromium, Firefox, and WebKit accessibility and governed edit/publish/delivery checks.
  - Final gate: `pnpm check` passes boundaries, ledgers, security/readiness/tenant validators, formatting, generated schemas, strict types, 207 tests with five intentional PostgreSQL-only skips, React 18 SSR certification, and every production build; Studio remains 29 modules/304.36 kB and the example delivery app remains 24 modules/219.06 kB.
  - Defects: `BUG-0248` through `BUG-0258` are resolved with regression evidence; the PostgreSQL restart regression was caused by search-path-dependent unqualified table creation and is guarded by explicit validated schema qualification plus cross-instance tests.
- [x] **M6-002** Add SAML, SCIM, group mapping, WebAuthn, session policy, and audited break-glass access.
  - Owner/start: Codex, 2026-08-21. Delivery tier: T2 (authentication/authorization behavior, security-critical protocol dependencies, durable identity/session state, browser/API contracts, and privileged emergency access); the user approved this scoped T2 implementation checkpoint before code changes began.
  - Restated ask: replace the development-only identity edge with a production-selectable enterprise relying-party boundary, while retaining local development mode; provide SAML/OIDC federation, SCIM user/group lifecycle, explicit group-to-scoped-role mapping, WebAuthn enrollment/step-up, bounded durable sessions, and tightly constrained audited emergency access.
  - Assumptions: GridStory is a SAML/OIDC relying party and SCIM service provider, never an IdP; SCIM remains tenant-bound and authenticated by a dedicated short-lived service credential; WebAuthn is an additional enrollment/step-up factor after federation, not passwordless account recovery; production secrets and TLS/proxy enforcement remain deployment inputs and cannot be claimed from repository tests alone.
  - Prior art/decision: OASIS SAML Web Browser SSO, OpenID Connect Authorization Code + state/nonce/PKCE, RFC 7643/7644 SCIM, W3C WebAuthn, and NIST SP 800-63B session/authenticator guidance converge on verified assertions, one-time challenges, server-held session state, lifecycle revocation, and phishing-resistant authenticators. ADR 0013 selects maintained protocol verifiers at the Node edge plus a durable framework-neutral identity kernel; it rejects hand-written XML/WebAuthn crypto, trusted caller identity headers in production, and building an IdP/password-recovery system.
  - Sequence: define bounded identity/provisioning/session/security-event contracts because every later layer consumes them; add tenant-scoped in-memory/SQLite/PostgreSQL repositories and lifecycle service because API authentication must survive restarts; implement OIDC/SAML and WebAuthn adapters at the Node edge because they own protocol crypto; switch production request context to opaque secure-cookie or scoped-service-token identity; expose SCIM and authorized identity-management APIs/client methods; add the Studio sign-in/identity controls; update security/readiness documentation; run focused, negative, full, PostgreSQL/recovery, and three-engine browser verification.
  - Scope fence: `packages/schema/src/context.ts`, new identity schema module plus schema exports/tests; `packages/core/src/identity.ts`, new identity repository/service modules plus core exports/tests/PostgreSQL conformance; `apps/api/src` identity adapters, config/index/request-context/server wiring and focused API tests; API/Studio/root dependency manifests and `pnpm-lock.yaml`; `packages/client/src/index.ts` and client tests; `apps/studio/src/App.tsx`, styles, and tests; `.env.example`; new `docs/identity-and-access.md`; `docs/adr/0013-enterprise-identity-boundary.md`; relevant security/threat/ASVS/observability/readiness docs and artifacts; `README.md`; and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - Scope amendment: `scripts/check-tenant-scope.mjs` is included so the existing mandatory static isolation gate covers the new identity repository, role materializer, production header boundary, and SCIM tenant authentication instead of leaving M6-002 dependent only on dynamic tests.
  - Scope amendment: `tests/e2e/accessibility.spec.ts` is included so the existing Chromium/Firefox/WebKit accessibility matrix directly opens the new Identity administration panel by keyboard and audits its rendered controls instead of relying only on simulated Studio unit interaction.
  - In scope: provider discovery/start/callback contracts for OIDC and SAML; signature/issuer/audience/time/state/nonce/PKCE or InResponseTo validation through maintained libraries; hashed opaque cookie sessions with idle/absolute/reauthentication/concurrency policy; immediate user-disable and administrative revocation; RFC-shaped tenant SCIM Users/Groups, supported filter/PATCH discovery, ETags, and deprovisioning; scoped group mappings; WebAuthn registration and step-up with one-time challenges, exact RP/origin, user verification, credential counters, and revocation; single-use high-entropy break-glass credentials with short non-renewable sessions, reason/incident, rate bounds, lifecycle/audit events, and safe telemetry.
  - Out of scope: operating an identity provider, local passwords/password reset, passkey-first account recovery, LDAP, SAML artifact/ECP bindings or complete SLO, unconstrained SCIM filter/bulk/password support, automatic remote metadata trust, authenticator attestation allow-lists, customer IdP onboarding UX, distributed abuse counters, external approval/ticket integrations, production certificate/secret-manager/proxy/TLS provisioning, or claiming hosted IdP/interoperability/security certification.
  - Risks and rollback: forged/replayed assertions, caller-overridden scope, stale sessions after deprovisioning, SCIM cross-tenant writes, or emergency access becoming routine would be critical. Production mode fails closed without configured providers/session secrets, never trusts development identity headers, stores only credential/session hashes, consumes protocol challenges once, resolves scope server-side, revokes on lifecycle changes, and records immutable security events. Rollback disables production identity mode and reverts the additive task commit/tables in non-production; no content revision or published delivery schema changes.
  - Observable acceptance: production mode refuses missing/unsafe configuration and rejects development identity headers; valid injected/real protocol fixtures establish a secure cookie session while altered/replayed issuer/audience/state/nonce/PKCE/InResponseTo inputs fail; idle/absolute/reauthentication/concurrency and disable/revoke behavior persist across adapters; SCIM user/group CRUD, supported filter/PATCH, ETag conflict, deprovision/revocation, bearer authorization, and tenant isolation pass; mapped groups grant only configured scoped roles; WebAuthn challenges are origin/RP/user-verification bound and one-time with counter updates; break-glass is single-use, short, reason/incident-bound, non-renewable, rate-limited, and fully audited; Studio flows are keyboard-operable; `pnpm check`, `pnpm security:check`, PostgreSQL conformance/recovery, and Chromium/Firefox/WebKit smokes pass.
  - Implementation evidence: bounded schema/client contracts and the framework-neutral core kernel persist provider, directory, scoped mapping, session, WebAuthn, SCIM credential, break-glass, protocol-transaction, and ordered audit state in memory, SQLite, or the qualified PostgreSQL application schema; maintained Node adapters verify OIDC, SAML, and WebAuthn protocol cryptography before results cross that boundary.
  - API/Studio evidence: production request authentication rejects development actor/role headers and resolves tenant-bound hashed opaque sessions from Secure HttpOnly cookies or scoped service tokens; RFC-shaped SCIM discovery/users/groups/filter/PATCH/ETag behavior and authorized identity administration remain private/no-store; Studio and the universal client expose provider, policy, mapping, directory credential, break-glass, session, and event operations without placing identity state in published delivery.
  - Focused verification: 22 schema/core/API identity and configuration regressions pass, including durable replay denial, mapped-role reconstruction, deprovisioning, SCIM tenant isolation/ETag conflicts, SAML invalid/replay rejection, WebAuthn step-up, break-glass rate/single-use behavior, exact audit events, and production cookie defaults; API/core/Studio strict typechecks and the 26-file scoped Biome check pass.
  - Security/dependency verification: `pnpm security:check`, `pnpm tenant:check`, and `pnpm readiness:check` pass their negative self-tests and canonical artifacts; a frozen-lockfile install passes, and `pnpm audit --prod` reports no known vulnerabilities after patched compatible transitive overrides.
  - PostgreSQL/browser verification: `pnpm test:postgres` passes 10 core and 2 API tests, cross-instance identity/session/mapping/deprovisioning durability, and the logical backup/restore drill with the identity table present; `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit checks, including keyboard opening and unsuppressed axe coverage of enterprise Identity administration at adapted display settings.
  - Final gate: `pnpm check` passes without lint warnings: boundaries, ledgers, security/readiness/tenant validators, formatting, generated schemas, strict types, 226 tests with six intentional PostgreSQL-only skips, React 18 SSR certification, and every production build. Studio remains 29 modules/312.77 kB and the example delivery app remains 24 modules/220.72 kB.
  - Defects: `BUG-0259` through `BUG-0277` are resolved with regression evidence, including fail-closed mutation/audit/session corrections, forced-colors accessibility, patched production transitive dependencies, and final security-document consistency.
- [x] **M6-003** Add retention, legal hold, data-subject workflows, customer-managed key adapters, and residency policies.
  - Owner/start: Codex, 2026-08-23. Delivery tier: T3 (personal-data workflows and destructive erasure can be irreversible); the user's 2026-08-23 `continue` explicitly approved the previously presented guarded implementation, with destructive verification restricted to isolated fixtures.
  - Assumptions: customers remain responsible for identifying applicable law, lawful bases, deadlines, exceptions, data classifications, subject identity verification, and production restore evidence; GridStory can enforce declared policy and record evidence but cannot certify legal compliance. Existing content revisions, assets, directory identities, and immutable audit/security events remain authoritative; direct identifiers can be removed while pseudonymous event identity and held evidence remain when policy requires it.
  - Prior art: GDPR separates storage limitation, erasure with exceptions, restriction, portability, and transfer safeguards; EDPB guidance requires clear data-subject procedures; U.S. Federal Rule of Civil Procedure 37(e) makes reasonable preservation relevant when electronically stored information should have been preserved; NIST key-management guidance and AWS/Google KMS location behavior require explicit key lifecycle and region metadata. ADR 0014 records the source comparison, alternatives, and necessity gate.
  - Sequence: publish bounded governance contracts; persist a scoped optimistic governance document and hash-chained events in memory/SQLite/PostgreSQL; add explicit resource classification/subject links and content, asset, and identity processors; implement legal holds and restriction precedence; add data-subject access/export/restriction/erasure workflows; add retention-plan generation, digest-bound approval, worker execution, and receipts; add provider-neutral envelope-key and placement boundaries with maintained AWS KMS/Google Cloud KMS Node adapters; enforce declared region gates on governed writes/exports; expose private/no-store REST/client/Studio operations; update operational/security documentation; verify focused, full, PostgreSQL, recovery, and browser paths.
  - Scope fence: governance schema/core/repositories/tests; the minimum content/asset/identity repository and service changes needed by built-in resource processors and residency gates; API configuration, injected official-client-compatible AWS/Google key wrappers, routes, worker integration, and tests; universal-client operations/tests; Studio governance UI/tests/styles; `docs/data-governance.md`, ADR 0014, README/operations/recovery/security evidence and validators where their claims change; and the three mandatory project ledgers. Any additional path requires an amendment here with its reason.
  - In scope: versioned tenant/environment/locale/variant-scoped retention rules; explicit resource classifications and subject links; active/released legal holds; access, portable export, restriction, and erasure request states; identity-verification evidence references without evidence bodies; immutable dry-run candidate plans and effect summaries; legal-hold/restriction dominance; separate approval with exact plan digest, fresh enterprise reauthentication, reason, and backup reference; idempotent worker execution with per-resource revalidation and receipts; content aggregate removal/cache invalidation, asset metadata/object removal, identity session revocation/direct-identifier anonymization, and explicit unsupported/external-resource blockers; CMK describe/wrap/unwrap only with no stored plaintext key material; envelope-encrypted export packages; deployment/key/resource placement inventory; allowed-region gates; ordered hash-chained governance events and bounded redacted telemetry.
  - Out of scope: legal advice or automatic jurisdiction selection; free-form PII discovery, inferred subject identity, or proof that customer mappings are exhaustive; unattended date-triggered deletion without reviewed approval; CMK creation, rotation, disablement, destruction, escrow, or recovery; whole-database transparent encryption; backup scheduling/storage/key custody; deleting or rewriting immutable audit/security evidence; automatic region migration, replication, failover, or traffic routing (M8-001); third-party plugin/provider deletion without an explicit resource adapter and operator receipt; and claims about production cloud configuration not proven by deployment evidence.
  - Safety/rollback: implementation and tests may erase only isolated fixtures created by the test that performs the action; they may not erase existing repository, customer, cloud, or operator data or invoke live KMS endpoints. Product execution is preview -> digest-bound approval -> worker revalidation -> receipt, and remains blocked by any active hold, restriction, changed plan/policy version, stale reauthentication, missing verified backup reference, disallowed/unknown placement, unavailable key, or unsupported external resource. Before production use, operators run and verify `pnpm database:backup -- --output <artifact>` plus coordinated object/plugin backups; SQLite recovery uses `pnpm database:restore -- --backup <artifact> --target <absent-path>`, PostgreSQL recovery uses an isolated empty target with `GRIDSTORY_RECOVERY_TARGET_DATABASE_URL` and `--confirm-target`. Code rollback is `git revert <M6-003-commit>` followed by redeploy; erased production data is recoverable only from the coordinated backups and provider-specific recovery process, never from a code revert.
  - Observable acceptance: unauthorized or cross-scope governance access fails and all responses are private/no-store; optimistic repository conflict/restart/PostgreSQL tests pass; holds and restrictions block retention and erasure before and during execution; changed plans, policy versions, placement, key state, reauthentication, backup references, and repeated execution fail closed or remain idempotent; access/export includes only explicit subject-linked resources and produces a verified machine-readable package, optionally envelope-encrypted without plaintext DEK persistence; content, asset, and identity processors remove/anonymize only isolated fixtures and emit exact receipts/events while unsupported external resources block completion; residency gates reject disallowed destinations without pretending to migrate data; key adapters enforce tenant-bound key references and allowed locations without key-management authority; Studio flows are keyboard-operable and explain destructive effects; `pnpm check`, `pnpm security:check`, `pnpm tenant:check`, PostgreSQL conformance/recovery, production dependency audit, and Chromium/Firefox/WebKit checks pass before `[x]` and commit.
  - Implementation evidence: the shared governance contract and optimistic in-memory/SQLite/qualified-PostgreSQL document persist rules, subjects, links, holds, restrictions, requests, plans/receipts, CMK/placement policy, and hash-chained events; built-in content, asset, and identity processors have exact export/erase/anonymize boundaries and ordinary governed writes pass the placement gate.
  - API/client/Studio evidence: distinct governance read/manage/execute authorization protects private/no-store REST operations; fresh approval time is derived from the authenticated server session; typed client methods cover the lifecycle; Studio presents scope counts, holds, dry-run resource effects/blockers, exact digest, irreversible warning, and backup/approval inputs with responsive keyboard-native controls.
  - Key/residency evidence: fresh AES-256-GCM export envelopes contain only ciphertext, authenticated metadata, wrapped DEK, provider reference, and checksum; mocked AWS KMS and Google Cloud KMS wrappers bind provider calls to tenant/request context and report regional key state without key-administration authority; configured placement is explicitly an attestation and never data movement/routing.
  - Focused verification: governance schema, repository/service, processor, API/adapter/configuration, client, and Studio regressions pass, including optimistic persistence, scope isolation, hold/restriction precedence, approval separation, stale-state rejection, execution idempotency, access/export packaging, envelope encryption, placement denial, exact receipts/events, and keyboard-native destructive review.
  - PostgreSQL/recovery/dependency verification: `pnpm test:postgres` passes 11 core and 2 API tests plus the logical backup/restore drill with the governance table present; `pnpm test:recovery` passes all 10 recovery scenarios including governance state; `pnpm audit --prod` reports no known vulnerabilities.
  - Browser verification: `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit checks, including unsuppressed WCAG 2.2 A/AA analysis, keyboard operation at 200% zoom, and the governed edit/protect/publish/deliver vertical slice.
  - Final gate: `pnpm check` passes without lint warnings: boundaries, ledgers, security/readiness/tenant validators, formatting, generated schemas, strict types, 238 tests with seven intentional PostgreSQL-only skips, React 18 SSR certification, and every production build. Studio remains 29 modules/320.15 kB and the example delivery app remains 24 modules/222.77 kB.
  - Defects: `BUG-0278` through `BUG-0285` are resolved with regression evidence, covering exact optional adapter composition, API fixtures, scoped formatting/import quality, and stable full-suite Studio timing.
- [x] **M6-004** Add official major-CMS importers, repeatable mapping recipes, dual-run sync, and cutover validation.
  - Owner/start: Codex, 2026-08-23. Delivery tier: T3 because migrations process customer content and can publish imported revisions; the user's 2026-08-23 `proceed to next` is the explicit go-ahead to deliver this guarded slice, with all automated verification restricted to isolated fixtures and mocked source transports.
  - Restated ask: provide maintained read-only Contentful, Sanity, and WordPress source adapters; versioned deterministic mapping recipes; repeatable initial and dual-run synchronization that detects source/target drift; and a cutover-readiness report that proves the target is current without changing DNS, routes, or the source system.
  - Assumptions: migration sources are configured by trusted server composition and credentials remain in the operator's secret boundary; one GridStory scope represents the isolated migration target; source IDs are authoritative only inside one migration project; media binaries, arbitrary custom code, and inferred field/reference semantics require separate reviewed work; the source remains authoritative until an operator completes an external cutover.
  - Inward prior art: the existing portability service already supplies bounded checksums, dry-runs, transactional writes, cross-scope protection, and recovery guidance, while content services supply schema/reference/quality/workflow/governance gates. M6-004 reuses those seams and adds source normalization, mapping/link state, and drift evidence instead of building another archive or content repository.
  - Outward prior art: Contentful's Sync API supplies initial snapshots, page/sync tokens, localized fields, and explicit entry/asset tombstones; Sanity's authenticated export supplies bounded NDJSON snapshots and documents that cursor-mode changes can be inconsistent; WordPress REST collections cap pages at 100 and expose total-page headers plus modified-time filters but not a complete deletion feed. ADR 0015 records the comparison and selects full-snapshot reconciliation where a trustworthy delta/deletion contract is absent.
  - Necessity gate: traceable to this roadmap task; not solved by same-format GridStory portability; minimal compared with a generic ETL/runtime or automated traffic cutover; adds no dependency; introduces one source-adapter boundary only after three concrete providers; and remains reversible because source systems are read-only, target writes retain revisions, mutating runs require an exact reviewed plan digest, and production use begins in a backed-up shadow environment.
  - Sequence: publish bounded migration contracts; persist optimistic scoped projects/recipes/links/plans/runs/reports in memory, SQLite, and qualified PostgreSQL; add deterministic mapping and restart-safe synchronization above `ContentService`; implement hardened injected-fetch Contentful/Sanity/WordPress adapters; expose private authorized REST and typed-client operations; add a responsive Studio migration workbench; document operator/cutover/rollback boundaries and ADR 0015; update security and project ledgers; then run focused, full, PostgreSQL, recovery, dependency, and Chromium/Firefox/WebKit verification.
  - Scope fence: `packages/schema/src/{migration,index,resource-limits}.ts` and migration schema tests; `packages/core/src/{migration-repository,migration-service,index,authorization,types,sqlite-repository,postgres-repository}.ts` and migration/PostgreSQL tests; `apps/api/src/{migration-adapters,migration-routes,server}.ts` and focused API/adapter/recovery tests; `packages/client/src/index.ts` and client tests; `apps/studio/src/{App.tsx,studio.css}` and Studio/browser tests; `.env.example`, `README.md`, `docs/{migration-and-cutover,operations,recovery-and-rollouts}.md`, ADR 0015, affected security artifacts, and `TASKS.md`, `CHANGELOG.md`, `BUGS.md`. Any additional path requires an amendment here with its reason.
  - Out of scope: source-CMS writes or credential administration; arbitrary JavaScript/liquid/GROQ mapping execution; automatic schema inference/deployment; binary media transfer or URL downloading; historical source revision/comment/user migration; destructive propagation of source deletions or unpublishes; unattended scheduling; automatic GridStory release approval; DNS/CDN/router changes; source decommissioning; and assertions that a report proves application, SEO, analytics, identity, legal, or infrastructure readiness outside the observed content checks.
  - Safety/rollback: adapters use injected read-only transports, fixed configured origins, bounded response sizes/record counts, disabled redirects, and redacted errors; plans persist exact normalized fixture data privately, expire, and require their SHA-256 digest before execution; target manual edits, changed recipes/project versions, incomplete snapshots, unmapped/deleted sources, unsupported media, invalid target schemas/references, and stale publication state fail closed. Before production use, take `pnpm database:backup -- --output <artifact>` plus provider-specific asset/config backups, run in a separate shadow environment, and retain the source as traffic authority. Abort by pausing the project and leaving source traffic unchanged; code rollback is `git revert <M6-004-commit>`, while target-data rollback uses the verified database restore command or retained GridStory revisions—not a source write or automated delete.
  - Observable acceptance: provider adapters normalize deterministic records from mocked official response shapes, preserve opaque checkpoint semantics without exposing credentials, reject redirects/origin changes/oversize/malformed responses, and make no source mutations; recipes are schema-bounded, versioned, deterministic, and reject duplicate targets/unsupported paths or transforms; dry-run plans expose exact creates/updates/publications/no-ops/deletions/blockers and a stable digest without writes; exact-digest execution is idempotent across retries, advances checkpoints only after all effects succeed, respects content/governance/workflow/quality gates, and blocks manual target drift or recipe/project changes; full reconciliation detects missing source records and cutover reports are ready only when every mapped record is current and required publication is present; state survives SQLite/PostgreSQL reopen and remains tenant/environment/locale scoped; routes are private/no-store and deny unauthorized/cross-scope callers; Studio is keyboard-operable and explains preview, drift, deletion, publication, and external cutover boundaries; all proportionate repository gates pass before `[x]` and commit.
  - Delivered: bounded read-only Contentful, Sanity, and WordPress adapters; deterministic recipe/project/plan/link/run/cutover contracts; optimistic in-memory, SQLite, and PostgreSQL persistence; restart-safe dry-run/execution/reconciliation above the existing content gates; private authorized API and typed-client operations; a responsive Studio migration workbench; ADR 0015; and operator, recovery, and security guidance. No source mutation, binary transfer, unattended scheduling, or traffic cutover was added.
  - Focused evidence: migration schema tests pass 2/2, core migration repository/service tests pass 7/7, API adapter/workflow/recovery tests pass 8/8, client tests pass 21/21, Studio tests pass 23/23, and strict Studio/core/API type checks pass.
  - Persistence/recovery/dependency evidence: `pnpm test:postgres` passes 12 core and 2 API PostgreSQL tests plus the logical backup/isolated-restore drill with `gridstory_migration_documents` present; `pnpm test:recovery` passes all 10 recovery scenarios including recipe/project recovery; `pnpm audit --prod` reports no known vulnerabilities.
  - Browser evidence: `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit checks, including unsuppressed WCAG 2.2 A/AA analysis, keyboard operation at 200% zoom, and the governed edit/protect/publish/deliver vertical slice with the migration region expanded.
  - Final gate: `pnpm check` passes without lint warnings: boundaries, ledgers, security/readiness/tenant validators, formatting across 234 files, generated schemas, strict types, 254 tests with eight intentional PostgreSQL-only skips, React 18 SSR certification, and every production build. Studio remains 29 modules/331.12 kB JavaScript and 38.46 kB CSS; the example delivery app remains 24 modules/223.78 kB.
  - Security/defects: THREAT-0026/THREAT-0027, GS-SEC-032, and the migration security/operations artifacts cover fixed origins, redaction, private-cache containment, digest-bound execution, retry receipts, gate enforcement, deletion preservation, and content-only cutover claims. `BUG-0286` through `BUG-0297` are resolved with regression or corrected-audit evidence.
- [x] **M6-005** Add signed marketplace packages, verified publishers, automated review, compatibility, and support metadata.
  - Owner/start: Codex, 2026-08-23. Delivery tier: T2 because this adds security-sensitive marketplace trust, authorization, durable state, and installation contracts; the user's 2026-08-23 `proceed to next` is the explicit go-ahead for this roadmap slice, while all verification remains scoped to local fixtures, mocked DNS, and injected artifact-review transports.
  - Restated ask: extend the existing signed Plugin SDK into an operator-scoped marketplace where publishers can prove control of a declared domain and receive an explicitly reviewed badge, releases carry signed discovery/compatibility/support metadata, automated policy evaluates exact package evidence, and only an approved immutable release can enter the existing disabled-by-default installation lifecycle.
  - Assumptions: a self-hosted GridStory operator curates each fully scoped catalog; DNS possession is one identity signal rather than proof that a publisher or package is trustworthy; the control plane authenticates human approval and trusted review adapters; artifact bytes remain in an operator-selected external package/scanner system; support commitments are publisher declarations; and installed execution still requires the separate hardened runtime boundary from M5-003.
  - Inward prior art: Plugin SDK v1 already supplies canonical Ed25519 signatures over exact SHA-256 artifact identity, SDK/protocol compatibility, transparent requested capabilities, scoped grants, durable lifecycle/revocation, and external runtime isolation. M5-007/M5-009 already supply bounded archive inventory, isolated consumption, SPDX/provenance evidence, and checksum verification. M6-005 reuses those seams instead of creating a second manifest, signature format, installer, SBOM parser, or in-process sandbox.
  - Outward prior art: the VS Code Marketplace combines immutable publisher IDs, DNS-domain possession, human legitimacy review, required engine compatibility, and support/resource links; GitHub and Sigstore require cryptographic verification of artifact provenance but explicitly warn that provenance does not prove safety; SPDX standardizes component/provenance/license/security metadata; and OSV provides version-aware vulnerability queries. ADR 0016 records the comparison and selects evidence-bound review with an injected scanner rather than trusting publisher assertions or executing artifacts in the control plane.
  - Necessity gate: traceable to M6-005 and the explicit M5-003 marketplace gap; not solved by signature verification because authorship/integrity do not establish package safety or support; minimal compared with hosted global distribution, billing, reputation, or automated code execution; adds no dependency and uses Node crypto/DNS plus injected scanners; extends the plugin boundary only after the concrete manifest/install/release-evidence seams exist; and is reversible through one commit revert because it creates no external package, DNS, registry, runtime, or billing state.
  - Sequence: extend the signed manifest with bounded marketplace, compatibility, and support metadata; publish publisher/challenge/catalog/release/review schemas and summaries; persist one optimistic complete-scope marketplace document in memory, SQLite, and qualified PostgreSQL; implement domain challenge, explicit publisher approval/suspension, immutable release submission, deterministic built-in review, injected artifact inspection, separate release approval/rejection/yank, and approved-only install handoff; expose private authorized REST/client operations; add a responsive Studio workbench; document trust/support/rollback boundaries and ADR 0016; update security and project ledgers; then run focused, full, PostgreSQL, recovery, dependency, and Chromium/Firefox/WebKit verification.
  - Scope fence: `packages/schema/src/{plugins,marketplace,index,resource-limits}.ts` and focused schema tests; `packages/core/src/{marketplace-repository,marketplace-service,plugin-service,index,authorization,types,sqlite-repository,postgres-repository}.ts` and marketplace/plugin/PostgreSQL tests; `apps/api/src/{marketplace-adapters,marketplace-routes,server}.ts` and marketplace/recovery tests; `packages/client/src/index.ts` and client tests; `apps/studio/src/{App.tsx,studio.css}` and Studio/browser tests; `README.md`, `SUPPORT.md`, `docs/{marketplace,plugins,operations,recovery-and-rollouts}.md`, ADR 0016, affected security artifacts, and `TASKS.md`, `CHANGELOG.md`, `BUGS.md`. Any additional path requires an amendment here with its reason.
  - Out of scope: a GridStory-hosted global marketplace; package binary upload/storage/download/CDN; npm or container publication; long-lived registry credentials; billing, revenue share, license enforcement, ads, rankings, ratings, download counts, or publisher reputation scoring; identity-document collection; automatic legal/business legitimacy claims; executing package code, install scripts, or native binaries during review; implementing malware, vulnerability, provenance, or license engines inside the framework-neutral core; automatic approval, installation, enablement, or upgrade; runtime OS/container certification; and the deferred Studio sandbox loader.
  - Safety/rollback: challenges are bounded, expire, and prove only a TXT value at an exact validated DNS name; publisher approval is separate, evidence-referenced, and revoked by suspension; public catalog summaries omit pending challenges, opaque artifact references, public-key bodies, and reviewer internals. Release review binds the exact publisher/key/manifest/signature/digest/size/version/policy/adapter evidence, treats capability risk transparently, blocks missing/stale/mismatched/failed evidence, requires a distinct approver, and retains rejected/yanked history. Tests use generated keys, mocked DNS, and injected fixture inspectors only. Code rollback is `git revert <M6-005-commit>`; catalog data can remain inert, and any approved installation is still disabled by default and can be revoked through the existing lifecycle.
  - Observable acceptance: publisher registration rejects invalid domains/keys and duplicate identities; challenge verification requires the exact unexpired TXT token; approval requires verified domain evidence and a distinct reviewer; suspension immediately removes marketplace trust. Signed release metadata is bounded and covers exact discovery links, compatibility range, support status/policy, permissions, digest, and size; duplicate version/digest changes are rejected. Automated review never imports/executes the artifact, accepts results only from a configured trusted adapter, verifies returned digest/size/SBOM/provenance linkage, records vulnerability/license/malware/inventory results and capability warnings, and cannot pass required failed/missing checks. Release approval requires a passing current review and distinct reviewer; later publisher suspension, key mismatch, or yanking blocks install. Marketplace trust integrates with the existing signature/grant/disabled-install boundary; state survives SQLite/PostgreSQL reopen and remains fully tenant/environment/locale scoped; routes are private/no-store and deny unauthorized/cross-scope callers; Studio is keyboard-operable and clearly distinguishes verified identity, automated evidence, human approval, declared support, permissions, installation, and runtime safety; all proportionate repository gates pass before `[x]` and commit.
  - Delivered: extended the canonical signed Plugin SDK manifest; added bounded publisher/challenge/release/review/catalog schemas; optimistic complete-scope memory, SQLite, and qualified PostgreSQL persistence; Node DNS plus injected inspector adapters; distinct publisher/release decisions; immutable rejection/yank history; current-trust approved-only disabled/no-grant installation; private authorized REST and typed-client methods; the responsive Studio marketplace workbench; ADR 0016; operator/support/recovery guidance; `THREAT-0028`/`THREAT-0029`; and verified `GS-SEC-033`. No package hosting/download, scanner engine, code execution, automatic approval/enablement, support guarantee, or runtime sandbox was added.
  - Focused evidence: two schema files pass six marketplace/plugin cases; three core files pass 10 marketplace/plugin cases; three API files pass four DNS/workflow/plugin cases; client passes 22 cases; Studio passes 24 cases; and strict schema/core/client/API/Studio/workspace types pass. Regressions prove private-field projection, signature/identity/compatibility checks, challenge expiry and separation of duties, exact configured-inspector identity, exact-artifact policy, adapter failure/redaction, yanking/suspension, disabled/no-grant install handoff, cross-scope denial, private caching, and accessible evidence messaging.
  - Persistence/recovery/dependency evidence: `pnpm test:postgres` passes 13 core and two API PostgreSQL tests plus the native logical backup/isolated-restore drill with `gridstory_marketplace_documents` present; `pnpm test:recovery` passes all 10 scenarios including earlier marketplace publisher recovery; `pnpm audit --prod --audit-level high` reports no known vulnerabilities.
  - Browser evidence: `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit checks, including unsuppressed WCAG 2.2 A/AA analysis with the marketplace region expanded, keyboard operation at 200% zoom, and the governed edit/protect/publish/deliver vertical slice.
  - Final gate: `pnpm check` passes without lint warnings: boundaries, ledgers, security/readiness/tenant validators, formatting across 244 files, generated schemas, strict types, 268 tests with nine intentional PostgreSQL-only skips, React 18 SSR certification, and every production build. Studio remains 29 modules/345.33 kB JavaScript and 41.78 kB CSS; the example delivery app remains 24 modules/225.52 kB.
  - Security/defects: the marketplace documents and Studio explicitly separate DNS possession, identity approval, authorship/integrity, automated evidence, declared support, release approval, installation, grants, and runtime safety. `BUG-0298` through `BUG-0303` are resolved with schema/API/type/lint/accessibility/security-audit regressions; scanners, publisher business legitimacy, external evidence/package recovery, and OS/container isolation remain deployment responsibilities.

## Milestone 7 — Personalization, analytics, and governed AI

- [x] **M7-001** Add consent-aware audiences, deterministic targeting, preview, edge decisions, and cache guidance.
  - Owner/start: Codex, 2026-08-23. Delivery tier: T2 because targeting introduces privacy-sensitive inputs, public cache decisions, authorization, durable state, and operator publication; the user's 2026-08-23 `proceed to next` is the explicit approval for this roadmap slice.
  - Restated ask: let operators define bounded audiences and resource variants, privately preview hypothetical decisions, publish an exact targeting revision, and let anonymous delivery evaluate only that published policy with consent-aware, deterministic, cache-safe results.
  - Assumptions: applications supply short-lived request context and consent; GridStory stores policy rather than visitor profiles; a Global Privacy Control signal suppresses only configured privacy-sensitive purposes; public delivery needs a variant but not internal audience identity; and experiments, allocation, analytics, sticky identity, and external profile/CDP integration remain separate work.
  - Inward/outward evidence: existing complete-scope repositories, exact-revision publication, scoped authorization, private preview, public delivery, and qualified SQLite/PostgreSQL seams are reused. ADR 0017 compares W3C privacy/GPC guidance, OpenFeature evaluation context, Unleash activation strategies, Vercel Flags, and Cloudflare cache-key behavior before selecting bounded typed inputs and conservative shared caching.
  - Necessity/scope gate: traceable directly to M7-001; narrower than a profile store or experimentation platform; adds no dependency; preserves the framework-neutral core and explicit server/browser entry points; and is reversible through one commit because all persistence is additive and existing rendering falls back independently of targeting.
  - Delivered: strict consent-purpose, attribute, audience, decision, preview, result, and cache-guidance contracts; deterministic first-match/fallback evaluation with purpose/GPC gates; complete-scope optimistic memory, SQLite, and PostgreSQL repositories; private draft/preview/publish REST operations; published-only anonymous decisions; typed client methods; and a responsive JSON-first Studio workflow that prevents publication of unsaved visible edits.
  - Privacy/cache boundary: attributes are bounded booleans or finite identifier tokens; personal/authentication inputs are private and purpose-bound; no raw profile, email, URL, behavior history, preview credential, draft policy, or stable visitor identifier is persisted or admitted to public cache guidance. Public results redact audience IDs, and shared cache keys use fixed SHA-256 scope/input digests only for public consent-independent inputs; all other decisions are private/no-store.
  - Focused evidence: 28 selected schema, repository, evaluator, API, client, and Studio personalization/compatibility checks pass; schema regression verification passes all 56 package tests; strict core and Studio type checks pass; authorization, scope isolation, revision conflicts, invalid values, GPC/consent suppression, deterministic priority, cache-key completeness, public redaction, and the save-preview-publish interaction are covered.
  - Persistence/recovery/dependency evidence: `pnpm test:recovery` passes 10 recovery scenarios with exact targeting restoration; `pnpm test:postgres` passes 14 core and two API cases plus restart and logical dump/isolated-restore checks with a recovered published targeting document; `pnpm audit --prod --audit-level high` reports no known vulnerabilities.
  - Browser evidence: `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit scenarios, including WCAG 2.2 A/AA analysis, keyboard operation, 200% zoom, forced colors, and the governed edit/protect/publish/deliver vertical slice. The WebKit contrast defect found by this gate is resolved as BUG-0313/STUDIO-007.
  - Final gate: `pnpm check` passes without lint warnings: package boundaries, ledgers, security/readiness/tenant validators, formatting across 252 files, generated schemas, strict types, 281 tests with 10 intentional PostgreSQL-only skips, React 18 SSR certification, and all production builds. Studio builds 29 modules at 350.81 kB JavaScript and 43.39 kB CSS; the example delivery app builds 24 modules at 226.15 kB JavaScript.
  - Security/defects: THREAT-0030 and verified GS-SEC-034 cover minimization, consent/GPC enforcement, preview confidentiality, public projection, cache partitioning, and tenant scope. BUG-0304 through BUG-0313 are resolved with regression, audit, recovery, or browser evidence; M7-002 retains experiment lifecycle/allocation, and M7-003 retains analytics/event ingestion.
- [x] **M7-002** Add experiment lifecycle, allocation, guardrails, metrics contracts, and winner promotion.
  - Owner/start: Codex, 2026-08-23. Delivery tier: T2 because experiments add a public allocation contract, consent-sensitive pseudonymous input, persistent lifecycle/evidence, guardrail enforcement, and targeting-draft mutation; the user's 2026-08-23 `proceed to next` is the explicit go-ahead.
  - Restated ask: extend M7-001 with governed experiment design, immutable running allocation, operator-controlled pause/complete/cancel transitions, bounded aggregate metric evidence, enforced allocation/metric guardrails, and explicit winner promotion into the targeting draft without bypassing publication.
  - Assumptions: applications create a random per-experiment UUID only after the configured purpose is granted; GridStory hashes but never stores that token, sets no cookie, and ingests no raw exposure/conversion events. External analytics produces aggregate snapshots and owns statistical analysis; GridStory verifies declared samples, deterministic allocation tolerance, absolute guardrails, evidence identity, and explicit operator choice.
  - Inward/outward evidence: M7-001 already provides complete-scope optimistic persistence, consent/GPC enforcement, published targeting, bounded variants, private management, anonymous decisions, and recovery. Unleash uses weighted sticky variants; GrowthBook documents deterministic hashing without persistent assignments; LaunchDarkly freezes result-affecting design into iterations, checks allocation health, and promotes by changing the serving variation; Statsig separates primary and guardrail metrics and accepts precomputed metrics; OpenFeature keeps tracking/provider integration separate from evaluation. ADR 0018 records the comparison.
  - Necessity/scope gate: traceable directly to M7-002; not solved by deterministic targeting because targeting has no randomization unit, lifecycle, evidence, guardrails, or promotion; minimal compared with a statistics/analytics warehouse; adds no dependency; introduces one experiment service over the existing document rather than a new generic platform; and is reversible through one commit with application fallback to ordinary targeting.
  - Sequence/scope fence: extend `packages/schema/src/{personalization,resource-limits,index}.ts`; update the existing personalization document default and evaluator seam; add `packages/core/src/experiment-service.ts` plus focused tests and exports/authorization; add `apps/api/src/experiment-routes.ts`, server wiring, API/PostgreSQL/recovery tests; add typed client methods/tests and one Studio workbench/test/style slice; update README, personalization/experiment docs, ADR 0018, security model/profile/requirements, PostgreSQL restore assertions, and `TASKS.md`, `CHANGELOG.md`, `BUGS.md`. Any additional path requires a recorded amendment.
  - Verification amendment: the full parallel gate exposed the new experiment and existing guarded-migration Studio scenarios narrowly above Vitest's default five-second per-test limit with no assertion failures. BUG-0316/BUG-0317 authorize only scenario-specific 15-second bounds; the suite-wide timeout remains unchanged.
  - Out of scope: raw event/exposure ingestion, analytics adapters and normalized events (M7-003), Bayesian/frequentist significance engines, automatic winner selection or automatic publication, cookies or subject/profile storage, arbitrary identifiers, cross-experiment layers/holdouts, progressive rollouts, warehouse/CDP credentials, and third-party statistical validity claims.
  - Observable acceptance: designs require one primary metric, bounded variants whose basis-point weights total 10,000, a declared consent purpose, sample/allocation thresholds, and valid targeting references. Only draft designs are editable; start pins the published targeting revision and blocks overlapping active targets; allocation is deterministic for scope/experiment/revision/token, never stores or echoes the token, and returns the current targeting baseline without participation on denied/GPC consent, ineligibility, inactive state, or targeting drift. Aggregate snapshots are immutable and complete; failed guardrails pause a running experiment; promotion requires completion, sufficient samples, acceptable allocation, passing guardrails, an operator-selected primary-metric winner, and unchanged targeting, then atomically updates only the targeting draft and retains experiment history. Private management and public allocation are fully scoped/no-store; focused, full, PostgreSQL, recovery, dependency, security, and Chromium/Firefox/WebKit gates must pass before `[x]` and commit.
  - Delivered: bounded design/lifecycle/evidence schemas and limits; one complete-scope experiment service over the targeting document; immutable running designs; exact deterministic SHA-256 allocation; purpose/GPC, eligibility, overlap, and revision gates; aggregate snapshots; allocation/sample/absolute guardrails with automatic pause; completion/cancellation; and explicit supported-treatment promotion into a new targeting draft only. Separate authorization, private management/metric/promotion routes, an anonymous no-store allocation route, universal-client methods, and the responsive Studio workbench keep browser/server/RSC and control-plane boundaries explicit.
  - Focused evidence: all 59 schema tests, 12 experiment/personalization core tests, five experiment/personalization/recovery API scenarios, 24 client tests, and 22 Studio interaction scenarios pass; the Studio lifecycle now exercises start/pause/resume, aggregate evidence, completion, and draft promotion. Invalid weights/references/overlap, design immutability, consent/GPC/ineligibility/drift fallbacks, missing token, stable allocation, guardrail pause/resume, immutable snapshot IDs, insufficient data, unsupported winners, tenant isolation, authorization, no-store output, and non-echoed tokens are covered.
  - Persistence/recovery/dependency evidence: `pnpm test:recovery` passes 10 scenarios and restores the running experiment with its pinned targeting revision; `pnpm test:postgres` passes 14 core and two API cases, allocates after restart, and recovers published targeting plus the running experiment through a native dump/mutation/isolated restore. `pnpm audit --prod --audit-level high` reports no known vulnerabilities.
  - Security/documentation evidence: accepted ADR 0018, `docs/experiments.md`, updated targeting/README guidance, `THREAT-0031`, and verified `GS-SEC-035` define token minimization, consent/GPC, scope, cache, evidence, guardrail, promotion, recovery, rollback, and statistical-claim limits. The threat-model/ASVS validator and negative self-tests pass; raw event ingestion and analytics adapters remain M7-003.
  - Browser evidence: `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit scenarios. The expanded-panel WCAG 2.2 A/AA scan now includes the experiments workbench, alongside keyboard, 200% zoom, forced-colors, published-rendering, and edit/protect/govern/publish/deliver coverage.
  - Final gate: `pnpm check` passes package boundaries, ledgers, security/readiness/tenant validators, formatting across 256 files, generated schemas, strict types, 292 tests with 10 intentional PostgreSQL-only skips, React 18 SSR certification, and all production builds. Studio builds 29 modules at 360.87 kB JavaScript and 44.58 kB CSS; the example delivery app builds 24 modules at 227.01 kB JavaScript.
  - Defect audit: BUG-0314 through BUG-0318 are resolved with focused/full/security/browser evidence; there are no open defects. No dependency was added, published targeting remains unchanged until the ordinary exact-draft publish step, and the slice remains revertible through its single delivery commit.
- [x] **M7-003** Add analytics adapters, normalized content/component events, release annotations, and content-operations metrics.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because the slice adds a public event-ingestion contract, durable complete-scope aggregates and adapter jobs, and a release-lifecycle integration; the user's repeated `proceed to next` is the explicit go-ahead.
  - Restated ask: provide one provider-neutral way for delivery applications and GridStory lifecycle events to describe content/component activity, fan that evidence out through injected adapters, correlate published/rolled-back releases with changes in content performance, and expose bounded operational metrics without making GridStory an identity store or analytics warehouse.
  - Assumptions: applications collect an explicit configured analytics-purpose decision before sending engagement events; events are anonymous and published-only. GridStory accepts no visitor/session/account token, URL/referrer, network/device value, draft content, or free-form property bag. Provider credentials and statistical analysis stay outside the schema/core boundary.
  - Inward/outward evidence: the existing scoped durable-job worker already supplies idempotent enqueue, lease, retry, replay, and dead-letter behavior, while release/content services provide exact published revision and release state. Segment separates page/action calls, PostHog centers named events/properties plus retry deduplication, GA4 uses bounded named events/parameters and warns its server protocol supplements client tagging, Grafana models release-like annotations as timestamp/text/tags, and OpenTelemetry recommends namespaced signals with controlled metric cardinality. ADR 0019 records the comparison and selected smaller contract.
  - Necessity/scope gate: traceable directly to M7-003 and M7-002's explicit deferral; not solved by operational telemetry, webhooks, or aggregate experiment snapshots because none supplies a public engagement vocabulary, adapter fan-out, release correlation, or content/component report. Reuses durable jobs and adds no dependency; one analytics service/repository is smaller than raw-event retention, a CDP, or provider SDKs and is reversible through one commit.
  - Sequence/scope fence: add bounded analytics schemas/limits and tests; add one aggregate analytics repository/service plus memory/SQLite/PostgreSQL conformance; extend durable-job processing for normalized lifecycle/engagement events and injected adapter delivery; annotate successful release publication/rollback without allowing adapter health to alter release truth; add public no-store ingestion and authenticated private metrics routes; add typed client methods/tests and a compact Studio operations view; update README, analytics/release/operations docs, ADR 0019, security model/profile/requirements, recovery/PostgreSQL assertions, and the three mandatory ledgers. Any additional path requires a recorded amendment.
  - Out of scope: user or anonymous profiles, stable visitor/session/assignment identifiers, cookies, URL/referrer/IP/user-agent/device capture, arbitrary event names/properties, raw event warehouse/history/export, funnels/cohorts/attribution, statistical significance or experiment snapshot synthesis, provider credentials/configuration UI, synchronous provider delivery, and draft/preview analytics.
  - Observable acceptance: only the declared content view, component view, and component interaction inputs are publicly accepted; a configured granted purpose and non-GPC signal are mandatory, event time and identifiers are bounded, and scope comes only from validated delivery request context. Existing content outbox events and successful release publication/rollback become normalized server evidence. One idempotent processing job updates complete-scope bounded aggregates, one job per injected adapter retries independently, release success is never reversed by analytics failure, and reports expose event/content/component/release plus adapter-job health without raw or personal data. Schema/core/API/client/Studio, scope isolation, duplicate/replay, denial/GPC, invalid/future/stale events, hostile adapters, recovery, PostgreSQL, dependency, security, and Chromium/Firefox/WebKit gates must pass before `[x]` and commit.
  - Delivered schema/control-plane evidence: a strict closed public union accepts only `content.viewed`, `component.viewed`, and `component.interacted`; configured-purpose denial, `Sec-GPC`, event age/future skew, exact current published ID/type/revision, unknown fields, and bounded token/cardinality rules fail closed. Server lifecycle and release evidence use separate explicit unions with no visitor/session/account/assignment, URL/referrer/network/device, draft payload, or provider credential fields.
  - Durable/persistence evidence: one idempotent `analytics.process` job updates an optimistic complete-scope bounded document and creates one independently leased/retried/dead-lettered/replayable `analytics.deliver` job per injected adapter. Memory, SQLite, and qualified PostgreSQL repositories conform; private receipts prevent duplicate aggregation, reports omit receipt IDs/raw evidence and expose truncation plus bounded adapter health, and hostile adapter diagnostics are generically redacted without changing aggregate/content/release truth.
  - Surface evidence: anonymous published delivery exposes `POST /api/v1/analytics/events`; authenticated `operations.read` exposes private/no-store `GET /api/v1/analytics/report`; the universal client mirrors both contracts; and Studio Operations shows content/component engagement, release markers, and adapter dead deliveries in the existing responsive administrator panel.
  - Recovery/database evidence: `pnpm test:recovery` passes 10 live-WAL SQLite backup/mutation/isolated-restore and rollout scenarios, restoring the earlier lifecycle aggregate. `pnpm test:postgres` passes 15 core and two API cases, persists analytics across restart, includes the analytics table in the native dump, and restores the earlier aggregate after mutation.
  - Security/documentation evidence: ADR 0019, `docs/analytics.md`, README/release/operations guidance, `THREAT-0032`, and verified `GS-SEC-036` define minimization, purpose/GPC, current publication, scope, idempotency, adapter isolation/redaction, limits, recovery, and interpretation boundaries. Security negative self-tests and the complete threat-model/ASVS validator pass.
  - Browser/dependency evidence: `pnpm test:e2e` passes all 12 Chromium, Firefox, and WebKit WCAG 2.2 A/AA, keyboard, 200% zoom/forced-colors, published-rendering, and vertical-slice scenarios with the expanded Operations panel. `pnpm audit --prod --audit-level high` reports no known vulnerabilities; no dependency was added.
  - Final gate: `pnpm check` passes package/tenant/security/ledger/readiness boundaries, formatting across 264 files, generated contracts, strict workspace types, 302 tests with 11 intentional PostgreSQL-only skips, React 18.3 SSR certification, and production builds. Studio builds 29 modules at 362.08 kB JavaScript and 44.91 kB CSS; the Vite delivery app builds 24 modules at 227.23 kB JavaScript.
  - Defect/reversibility audit: BUG-0319 through BUG-0325 are resolved with focused and full evidence; there are no open defects. The slice remains provider-neutral, release annotations remain non-authoritative, external raw/statistical/provider state is untouched, and the application change is reversible through its single delivery commit while already delivered external provider effects require provider-side reconciliation.
- [x] **M7-004** Add provider-neutral AI gateway, scoped retrieval, prompt registry, budgets, redaction, and kill switches.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because this adds a new external model adapter boundary, authenticated management/execution APIs, persisted prompt/policy/usage contracts, and sensitive-content handling; the user's `proceed to next` selects this queued item, while project-delivery-loop requires the frozen plan below before implementation.
  - Restated ask: give trusted GridStory applications one provider-neutral, deny-by-default way to execute governed text generation using immutable active prompt versions and explicitly selected in-scope content fields, while enforcing per-scope provider/model allow-lists, conservative daily/request budgets, deterministic redaction, bounded time/output, and an emergency kill switch. Execution produces an untrusted, non-mutating result only; structured authoring suggestions and human approval remain M7-005.
  - Assumptions: the trusted server composition injects adapters and credentials; GridStory persists no provider secret, raw rendered prompt, retrieved field value, or model output. Callers supply a UUID request ID, an active prompt ID, bounded user input, and explicit source entry IDs. Retrieval is allow-list based by perspective/content type/field path and re-authorizes every resolved entry. Provider usage/cost is operational evidence, not invoice truth.
  - Inward evidence: complete-scope optimistic memory/SQLite/PostgreSQL documents, scoped authorization grants, ContentService retrieval, stable private/no-store management routes, injected marketplace/migration/analytics adapters, generic external-error redaction, recovery drills, and Studio workbench patterns already exist. Search, analytics, OpenTelemetry, plugins, and workflows do not version prompts, control model egress, reserve AI budgets, redact provider-bound text, or disable model execution.
  - Outward evidence: Vercel AI SDK demonstrates provider-standard and middleware seams but would add a broad model/tool/streaming dependency; Langfuse uses immutable prompt versions plus movable deployment labels; AWS Bedrock documents input/output guardrails and token/request quotas; OpenAI separates usage from invoice costs and exposes provider data-retention controls; OpenTelemetry warns prompt/output telemetry can contain PII; OWASP states prompt injection has no foolproof filter and prioritizes instruction/data separation, least privilege, output validation, monitoring, and kill switches; NIST emphasizes governance, testing, documentation, and oversight. ADR 0020 records the comparison and selected smaller contract.
  - Necessity gate: traceable directly to M7-004 and the roadmap's pre-governance AI risk; not solved by search/adapters/telemetry because those cannot authorize or meter a model call. Minimal form is one text-only adapter, one scoped policy document, immutable prompt versions with active pointers, explicit-ID retrieval, deterministic redaction, conservative reservation/reconciliation, three private routes groups, and one compact Studio panel. No dependency is added; no generic middleware/plugin framework is introduced before real provider repetition; removal is an additive route/table/UI revert after disabling all scopes/adapters.
  - Sequence/scope fence: first add bounded AI policy/prompt/request/result schemas and resource limits; then add complete-scope repositories plus a service for prompt activation, source extraction/redaction, budget reservation/settlement, timeout, adapter isolation, and kill-switch recheck; then add `ai.read`/`ai.manage`/`ai.execute`, private REST composition, client methods, and an operable Studio policy/prompt/test panel; finally add memory/SQLite/PostgreSQL/recovery/hostile-adapter/security/browser evidence and update README, `docs/ai-gateway.md`, ADR 0020, threat/ASVS/requirements, PostgreSQL restore script, and all ledgers. Files are fenced to `packages/schema`, `packages/core`, `apps/api`, `packages/client`, `apps/studio`, `docs`, `security`, `scripts/test-postgres.mjs`, README, and the ledgers; any additional path requires an amendment here.
  - Scope amendment: `tests/e2e/accessibility.spec.ts` is included so the existing Chromium/Firefox/WebKit keyboard and WCAG matrix directly opens and audits the new AI workbench instead of relying only on simulated Studio interaction.
  - Out of scope: provider SDKs or credentials in core; browser-direct model calls; anonymous execution; conversations/memory, streaming, images/audio/video, embeddings/vector indexes, automatic retrieval/search, tools/function calls/agents, provider fallback/routing, prompt composition, arbitrary provider options, persistent raw prompts/sources/outputs, model training, full PII/DLP certification, factuality/safety guarantees, structured field mutation, provenance/edit lineage/evaluations, automatic approval/publication, and billing-grade cost reconciliation.
  - Observable acceptance: only authorized complete-scope callers can read/manage/execute; prompt versions are immutable and only an explicitly activated version runs; disabled scope/provider/model/prompt, stale policy writes, duplicate request IDs, disallowed/missing/cross-scope sources, non-allowlisted fields/models, budget exhaustion, timeout, invalid/oversized/hostile provider results, and a kill switch changed during flight all fail closed. Provider input contains only the fixed instructions, redacted bounded user text, and allowlisted redacted source fields; it contains no scope credentials, neighboring/draft data beyond the declared perspective, or raw hidden fields. Conservative request/input/output/cost reservations prevent concurrent overspend, failures retain their reservation, successful usage can only reconcile downward, and reports expose bounded metadata/counters without raw content. Schema/core/API/client/Studio, authorization/scope/duplicate/redaction/injection/timeout/hostile adapter/concurrency/recovery/PostgreSQL/dependency/security and Chromium/Firefox/WebKit gates must pass before `[x]` and the requested commit.
  - Implementation evidence: strict AI contracts and limits feed one provider-neutral text-only service with complete-scope optimistic memory/SQLite/PostgreSQL persistence, immutable prompt activation, positive explicit-ID/type/field retrieval, deterministic input/source/output redaction, atomic conservative daily reservations, bounded metadata-only receipts, generic provider failures, timeout aborts, and settlement-time policy/prompt/model/kill-switch rechecks. Private REST routes, typed universal-client methods, and a non-mutating Studio policy/prompt/test workbench expose that boundary without bundling a provider or credential path; the default remains disabled.
  - Security/test evidence: focused schema, repository, service, authorization, API, client, and Studio regressions pass for stale/duplicate/model/prompt/source/scope/field/budget/redaction/timeout/invalid-metering/hostile-adapter/in-flight-disable cases. `THREAT-0033`, verified `GS-SEC-037`, ADR 0020, and `docs/ai-gateway.md` record the injection/data separation, least privilege, untrusted output, deployment, retention, billing, and no-mutation boundary; the complete security validator and its negative self-tests pass.
  - Recovery/database evidence: `pnpm test:recovery` passes all 10 live-WAL SQLite backup, mutation, isolated-restore, shutdown, worker, and rollout scenarios, including restoration of the earlier enabled AI policy. `pnpm test:postgres` passes 16 core and two API cases, persists policy and active prompts across restart, includes the qualified AI table in native dump verification, and restores the earlier enabled active prompt after mutation.
  - Browser/dependency evidence: the complete Chromium and Firefox projects pass all four WCAG 2.2 A/AA, keyboard, 200% zoom/forced-colors, published-rendering, and vertical-slice scenarios. WebKit passes all three accessibility scenarios in the cross-browser run and all four scenarios in the clean complete rerun; the unchanged vertical slice also passes independently. `pnpm audit --prod --audit-level high` reports no known vulnerabilities, and no dependency was added.
  - Final gate and defect audit: `pnpm check` passes boundaries, ledgers, security, tenant/readiness validators, formatting, generated contracts, strict types, complete workspace unit/integration tests, React 18.3 SSR certification, and production builds. BUG-0326 through BUG-0329 are resolved with focused/full evidence and no open defects remain. Removal is one additive commit after disabling scopes/adapters; provider effects require provider-side reconciliation, while GridStory retains no raw prompt, source, output, secret, or diagnostic history.
- [x] **M7-005** Add structured AI authoring suggestions, provenance, evaluation, human approval, and semantic search.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because this extends the AI/provider contract, adds persisted reviewed content proposals and a derived semantic-index boundary, changes authorization, and adds private API/client/Studio behavior; the user's `please proceed` selects this queued item, while project-delivery-loop requires approval of the frozen plan below before implementation.
  - Restated ask: turn M7-004's governed, untrusted text result into a strictly validated field-level proposal with exact prompt/model/source/target provenance, deterministic evaluation evidence, explicit human approve/reject, and a safe handoff into the existing unsaved draft/editorial workflow. Add private semantic content search through an injected, tenant-aware adapter whose index is rebuilt from positively allowlisted, redacted fields by the existing durable search jobs.
  - Assumptions: the first authoring contract targets one current draft entry and top-level `text`/`slug` fields only; one action may propose a bounded set of fields. Provider output is a fixed versioned JSON contract but GridStory still parses and validates it independently. Automated evaluation proves only structural/content-schema and configured deterministic term/length rules, never factuality, brand quality, safety, or legal correctness. Only a user principal with a separate review permission can approve/reject. Approval does not save or publish: Studio can copy approved values into its local unsaved editor, after which the normal immutable draft, workflow, quality, and publication gates apply. Semantic indexes/vectors are derived adapter state, not authoritative backup data.
  - Inward evidence: `AiGatewayService` already supplies scoped generation, source allowlists/redaction, prompt/model identity, budgets, and kill-switch settlement; `ContentService.validateCandidate` validates a complete prospective draft; collaboration suggestions demonstrate bounded accept/reject UI but lack AI provenance/evaluation and cannot safely enforce AI-specific reviewer rules; `SearchService` already supplies authorized result re-resolution plus durable `search.index`/`search.rebuild` jobs. None persists evaluated AI proposals, enforces a human-only review transition, or defines semantic adapter/index provenance.
  - Outward evidence: OpenAI Structured Outputs separates valid JSON from schema adherence and requires explicit incomplete/refusal handling; OpenAI evals bind representative test data to declared criteria and human ground truth; Contentful scopes AI actions to content targets and enriches audit records with invocation/action/model/affected-entry metadata; NIST requires documented human oversight and repeatable evaluation; pgvector documents cosine/hybrid search plus the recall and tenant-isolation hazards of filtered shared approximate indexes. ADR 0021 records the comparison and selected smaller contract.
  - Necessity gate: traceable directly to M7-005 and ADR 0020's explicit deferral; not solved by raw gateway output, collaboration, lexical search, or workflow approval alone. Minimal form is one authoring policy/document/repository/service, one fixed structured-output contract, deterministic candidate evaluation, one human review transition, local unsaved Studio handoff, and one injected semantic adapter reused by existing durable search jobs. No dependency, vector database, provider SDK, generic evaluator/plugin framework, or automatic content write is added. Disable authoring/semantic policy and adapters before reverting the additive commit; persisted suggestions are bounded review history and derived indexes can be discarded/rebuilt.
  - Sequence/scope fence: first add bounded action/policy/proposal/provenance/evaluation/review/semantic schemas and limits; then add complete-scope optimistic memory/SQLite/PostgreSQL authoring persistence plus proposal/review and semantic rebuild/query services; then add `ai.review`, fixed structured provider output, private routes, client methods, Studio proposal/review/use-in-editor and semantic search controls, and existing-worker integration; finally add recovery/PostgreSQL/hostile-adapter/security/browser evidence and update README, a dedicated guide, ADR 0021, security model/profile/requirements, native restore checks, and all ledgers. Files are fenced to `packages/schema`, `packages/core`, `apps/api`, `packages/client`, `apps/studio`, `tests/e2e/accessibility.spec.ts`, `docs`, `security`, `scripts/test-postgres.mjs`, README, and the ledgers; any additional path requires an amendment here.
  - Out of scope: browser-direct provider calls; bundled model/vector providers or credentials; public/anonymous semantic search; persisted raw prompts, provider JSON, source values, semantic queries, embeddings, or hidden reasoning; conversations/memory, streaming, images/audio/video, tools/agents, fallback/routing, automatic retrieval for generation, arbitrary JSON Schema/evaluation code, learned/LLM judges, factuality/bias/brand/legal/safety guarantees, rich-text/component/array/object/relation/taxonomy/asset suggestions, bulk/multi-entry actions, machine translation/glossaries, automatic draft save/workflow transition/approval/publication, hybrid ranking/recommendations, pgvector/ANN tuning, and final content-to-suggestion lineage beyond the reviewed proposal and human draft audit.
  - Observable acceptance: authoring and semantic policy are complete-scope, optimistic, bounded, private, and disabled by default. A proposal requires `ai.execute`, an active action/prompt/model, the exact current draft revision, allowed text/slug targets, and ordinary source authorization; provider output must match the fixed contract, survive redaction, alter only declared fields, and produce a complete schema-valid candidate that passes every configured deterministic criterion. Invalid/refused/incomplete/oversized/duplicate/stale/cross-scope/disallowed output fails closed without an approvable value or raw retention. Provenance records exact request/action/prompt/model/target/source revisions, usage/redaction metadata, actor/time, evaluation criteria, and review history without source values or raw provider output. Only an authorized human `ai.review` principal may approve/reject once; failed/stale proposals cannot be approved, and approved values enter Studio only as visible unsaved changes before the normal save/workflow/publish path. Semantic rebuild/index receives only allowlisted redacted fields from the requested perspective; jobs contain no field/query/vector body. Queries are private/bounded/redacted, and every adapter hit is finite, unique, same-scope/perspective, current-revision, content-authorized, and returned with adapter/model/revision/field provenance; hostile/stale/cross-scope hits fail closed. Focused schema/core/API/client/Studio, authorization, isolation, structured-output/evaluation/review, semantic poisoning/staleness, recovery/PostgreSQL, dependency, security, full, and Chromium/Firefox/WebKit gates must pass before `[x]` and commit.
  - Implementation evidence: fixed `gridstory.authoring-suggestions.v1` schemas feed one complete-scope optimistic memory/SQLite/PostgreSQL authoring document. The service snapshots exact action criteria and prompt/model/source/target revisions, independently parses/redacts/validates provider JSON, evaluates the complete candidate, retains no values on failure, and allows one authorized user-principal review without changing content. Private REST/client/Studio controls keep approval as a visible unsaved editor handoff. An injected semantic adapter consumes only positive redacted fields from identifier-only durable jobs, while private queries and every returned hit are revalidated for finite score, uniqueness, scope, perspective, current revision, field provenance, and ordinary content authorization.
  - Security/test evidence: focused schema/core/API/client/Studio and authorization regressions cover stale revisions, immutable action criteria, invalid/refused/oversized/evaluation-failed output, no failed-value retention, service-account review denial, one-time review, no automatic save/publish, poisoned/cross-scope/stale semantic hits, and disabled defaults. Accepted ADR 0021, the operator guide, `THREAT-0034`, and verified `GS-SEC-038` record the human-oversight, least-privilege, data-minimization, untrusted-output, and derived-index boundary.
  - Recovery/database evidence: `pnpm test:recovery` passes 4 files and all 10 live-WAL backup, isolated-restore, shutdown, worker, and rollout scenarios, including restoration of the earlier authoring policy. `pnpm test:postgres` passes 17 core and 2 API cases, persists authoring state across restart, includes `gridstory_ai_authoring_documents` in the qualified native dump, and recovers the earlier reviewed-authoring policy through logical restore.
  - Browser/dependency evidence: `pnpm build:e2e` builds both applications, and the complete accessibility specification passes 9/9 checks across Chromium, Firefox, and WebKit for WCAG 2.2 A/AA, keyboard/200% zoom, disabled AI defaults, and published rendering. `pnpm audit --prod --audit-level high` reports no known vulnerabilities; no dependency was added.
  - Final gate and defect audit: `pnpm check` passes boundaries, ledgers, security, tenant/readiness validators, formatting, generated contracts, strict types, 328 active workspace tests with 13 documented skips, React 18.3 SSR certification, and production builds. BUG-0330 through BUG-0339 are resolved and no open defects remain. Removal is one additive commit after disabling authoring/semantic policies and adapters; proposals remain bounded review history, and derived semantic indexes can be discarded and rebuilt.

## Milestone 8 — Platform leadership

- [x] **M8-001** Add regional reads, multi-region control-plane options, failover, consistency indicators, and residency routing.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because this is an additive provider-neutral application contract with authenticated management, persisted topology/operation state, and changed published delivery behavior; runtime provider promotion is hazardous and receives T3-style preview, independent approval, readiness/backup, idempotency, reconciliation, and rollback safeguards. The user's `proceed to next` selects this queued item; project-delivery-loop requires approval of the frozen plan below before implementation.
  - Restated ask: let a trusted GridStory deployment route published content reads to an eligible regional adapter with explicit strong or bounded-staleness evidence, while keeping every control-plane/draft/preview/write path on one strong primary and governing planned switchover, emergency failover, and failback as reviewed human operations. GridStory records and validates the contract; it does not provision or claim provider replication, databases, DNS, traffic, backups, or RPO/RTO.
  - Assumptions: trusted server composition injects the local deployment region plus capability-limited published-read and failover adapters; public callers cannot choose a region. The governance residency policy remains the source of truth for admitted content regions. A strong read uses the ordinary primary repository; bounded staleness is accepted only from finite, complete-scope, topology-matching adapter evidence. Regional delivery defaults to private/no-store unless composition attests a cache partition covering scope, region, consistency, topology, and content revision.
  - Inward/outward evidence: existing complete-scope optimistic documents, immutable published revisions, private management/preview paths, public delivery services, authorization, durable state, SQLite/PostgreSQL qualification, provider-neutral adapters, and guarded residency policy are reused. Cloudflare D1 and PostgreSQL show asynchronous replicas can be stale; Spanner and Cosmos DB make consistency an explicit trade-off; Aurora separates zero-loss planned switchover from lag-bound emergency failover; AWS ARC separates readiness, routing controls, and safety rules. Proposed ADR 0022 records the comparison and selected smaller contract.
  - Necessity gate: traceable directly to M8-001; not solved by current cache headers or placement attestations because neither selects a replica, verifies freshness, or governs promotion. Minimal form is one disabled complete-scope topology document, one published-only regional reader, one failover adapter, four delivery integrations, bounded consistency indicators, and one reviewed idempotent operation workflow. No dependency or provider SDK is added, and no generic provisioning, multi-writer, DNS, distributed transaction, or automation framework is introduced. Revert after switching scopes to primary-only and reconciling external state to a known primary.
  - Sequence/scope fence: first add bounded topology, evidence, consistency, operation, and failover schemas plus limits; then add complete-scope optimistic memory/SQLite/PostgreSQL persistence and a service that authorizes regional policy, validates residency/freshness/result scope, records preflight/approval/pending/success/failure, and reconciles ambiguous adapter calls; then integrate only published entry/localized/query/route reads, add private management/status/failover REST routes, typed client methods, consistency response headers, and a compact Studio topology/readiness/operation panel; finally add hostile-adapter, concurrency/crash, cache, recovery/PostgreSQL/security/browser evidence and update README, an operator guide, ADR 0022, threat/ASVS/requirements, native restore checks, and all ledgers. Files are fenced to `packages/schema`, `packages/core`, `apps/api`, `packages/client`, `apps/studio`, `tests/e2e/accessibility.spec.ts`, `docs`, `security`, `scripts/test-postgres.mjs`, README, and the ledgers; any additional path requires an amendment here.
  - Out of scope: replica/database/DNS/load-balancer provisioning; provider SDKs, endpoints, credentials, or raw diagnostics in core; public caller-selected regions; regional draft/preview/control reads or cross-region writes; multi-writer conflict resolution; automatic failover/failback; session/read-your-writes tokens; provider-independent proof of replication; RPO/RTO or legal-compliance guarantees; global CDN configuration; identity, asset, plugin, search-index, AI-provider, telemetry, backup, key, or object-store routing; and residency migration.
  - Observable acceptance: disabled/default operation is byte-compatible with current primary delivery. Only authorized complete-scope callers can read/manage/operate topology; all management is private/no-store and optimistic. Only published delivery may use a regional adapter, and every result must be unique, bounded, same-scope, published, schema-valid, revision-consistent, eligible by residency, and accompanied by matching finite region/role/topology/observation/lag/watermark evidence. Strong delivery always uses primary. Missing, hostile, stale, cross-scope, disallowed, future-dated, non-finite, or contradictory evidence follows the configured explicit strong fallback or bounded unavailable path without leaking provider details. Responses expose only bounded region, role, consistency, observation, lag, topology, content-revision, and digest-safe watermark indicators. Regional responses remain no-store unless complete cache-partition evidence is attested. A failover plan is expiring and digest-bound to exact version/source/target/mode/readiness/backup/RPO/RTO/reason; a different recently reauthenticated human approves it. Planned execution requires caught-up zero-loss evidence; emergency execution requires explicit acceptance of the observed nonzero-loss bound. Execution is idempotent, writes pending before adapter invocation, updates the active primary only after validated success, and reconciles ambiguous calls before any later transition. Focused schema/core/API/client/Studio, authorization/isolation/residency/hostile-adapter/concurrency/crash/cache/recovery/PostgreSQL/dependency/security/full and Chromium/Firefox/WebKit gates must pass before `[x]` and commit.
  - Implementation evidence: strict shared regional schemas and limits feed one complete-scope optimistic memory/SQLite/PostgreSQL document. The framework-neutral service defaults to the strong primary, admits only trusted-local published-read adapters, validates every scope/perspective/revision/type/slug/count plus exact role/region/topology/time/lag/residency/watermark/cache attestation, and integrates direct, localized, query, and route delivery without giving adapters draft/write authority. Private authorized REST, typed universal-client methods, and the neutral Studio Regions panel cover policy, preflight, different-human approval, execute, and reconciliation.
  - Failover/security evidence: planned preflight requires caught-up zero-loss evidence; emergency preflight requires a nonzero RPO covering observed loss and explicit approval. Exact expiring digests, current governance residency/backup evidence, recent reauthentication, persisted execution, stable idempotency, generic malformed-adapter errors, ambiguous reconciliation, single-writer result proof, topology increment, and primary-only reset fail closed. Response-bound `regionalCachePartitionDigest` is required for shared caching; mismatches remain private. Accepted ADR 0022, the operator guide, `THREAT-0035`, and verified `GS-SEC-039` record the boundary.
  - Recovery/database evidence: `pnpm test:recovery` passes 4 files and all 10 live-WAL backup, isolated-restore, shutdown, worker, and rollout scenarios, including restoration of the earlier regional policy. `pnpm test:postgres` passes 18 core and 2 API cases, persists regional policy across restart, includes `gridstory_regional_documents` in the qualified native dump, and recovers the earlier regional topology through logical restore.
  - Browser/dependency evidence: the complete browser specification passes 12/12 Chromium, Firefox, and WebKit scenarios for unsuppressed WCAG 2.2 A/AA analysis, keyboard/200% zoom, the disabled regional default, the requested established neutral page/content palette, and published rendering. `pnpm audit --prod --audit-level high` reports no known vulnerabilities; no dependency was added.
  - Final gate and defect audit: `pnpm check` passes boundaries, ledgers, security, tenant/readiness validators, formatting, generated contracts, strict types, 344 active workspace tests with 14 documented PostgreSQL-only skips, React 18.3 SSR certification, and production builds, including the post-gate localized fallback regression. Studio remains 29 modules/384.03 kB and the example delivery app remains 24 modules/229.45 kB. BUG-0341 through BUG-0352 are resolved and no open defect remains. Removal is one additive commit only after all scopes are primary-only, external state is reconciled to one known writer, traffic/cache behavior is verified, and adapters/policies are disabled; a code revert cannot undo provider promotion or data loss.
- [x] **M8-002** Add federated external content types, cross-instance syndication, and contractual attribution.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because this adds a new external trust boundary, signed public/private contracts, complete-scope persistence, authorization, and delivery API behavior. The user's `please proceed` selected this queued item; the frozen project-delivery-loop plan was approved before implementation.
  - Restated ask: let one complete GridStory scope offer exact published schema versions and content to another trusted deployment. Consumers can either resolve a namespaced external type live without copying every record or run a reviewed pull synchronization into a separate read-only mirror. Every response must carry immutable source/type/revision and attribution/license terms; ordinary local drafts, publication pointers, and rendering code remain independent.
  - Assumptions: the first maintained transport is GridStory-to-GridStory HTTPS through trusted server composition, while deployments may inject another source adapter only if it emits the same signed contract. A producer explicitly enables a bounded offer containing allowlisted canonical GridStory schema versions and attribution terms; a consumer explicitly pins the source instance/scope, offer version/digest, Ed25519 public key, allowed types, and `live` or `mirror` mode. Live and mirror delivery are public only after that agreement is active but remain `no-store` in this slice. Mirror content is source-published, read-only, and separate from local `ContentEntry` records. Source withdrawal is an explicit reviewed tombstone effect, not a local content deletion.
  - Inward evidence: canonical serializable schema IR and immutable published revisions supply exact type/revision identity; complete-scope optimistic documents, authorization, private/no-store management routes, SQLite/PostgreSQL recovery, canonical checksums, Ed25519 manifest verification, generic external-error redaction, guarded migration snapshots, and regional published-reader validation supply the required seams. Migration/portability copy local content but do not preserve a live remote type or mandatory ongoing attribution; regional readers validate published adapter results but do not negotiate remote schemas, source rights, or cross-instance checkpoints. Accepted ADR 0023 records the full comparison.
  - Outward evidence: Contentful and Sanity require the remote space/dataset and target types to be declared and expose permission, dereference, deletion, depth, and caching limitations; JSON Schema uses canonical URI identities and warns that identifiers are not automatic network locators; W3C PROV separates derivation from responsible-agent attribution; ActivityStreams supplies global IDs and `attributedTo` but is a broader social vocabulary; WebSub supplies hub/callback/lease distribution but no CMS schema, tenant, publication, or rights contract; RFC 9421/9530 require an application-specific signature/content-digest profile and replay coverage. M8-002 therefore uses the repository's canonical schema/checksum/Ed25519 envelope pattern and defers a public HTTP interoperability profile to M8-004.
  - Necessity gate: traceable directly to M8-002 and the roadmap's remote-backed-type/cross-organization syndication requirements; not solved by migration, portability, regional delivery, webhooks, or ordinary relations. Minimal form is one disabled producer offer, one disabled consumer agreement, one source interface with a maintained safe GridStory HTTP adapter, one signed published snapshot/record envelope, live-by-ID delivery, reviewed mirror synchronization, tombstones, and mandatory attribution. No dependency, second schema dialect, generic pub/sub hub, remote code/runtime, local write bridge, or provider catalog is added. The adapter boundary follows several existing external capability seams rather than creating a new general framework. Disable offers/agreements, stop live reads, export any required receipt history, and revert the additive commit; sources and ordinary local content are never mutated.
  - Units and sequence: `M8-002A` adds bounded offer/agreement/type/record/attribution/signature/sync schemas and limits first because every later boundary depends on exact identity and byte bounds. `M8-002B` adds complete-scope optimistic memory/SQLite/PostgreSQL persistence plus producer/consumer services for immutable offer versions, agreement pinning, Ed25519/request/expiry/schema/scope verification, live reads, exact preview digests, idempotent mirror execution, and withdrawal tombstones. `M8-002C` adds least-authority producer/source and consumer/delivery REST composition, the same-origin bounded GridStory HTTP adapter, separate federation read/manage/consume/sync authorization, typed client methods, and a compact neutral-palette Studio panel. `M8-002D` adds hostile-adapter/replay/drift/withdrawal/cross-instance/recovery/PostgreSQL/security/browser evidence and documentation. Risk-bearing contract/signature tests precede UI work; database and browser gates follow the runnable vertical slice.
  - Scope fence: `packages/schema`, `packages/core`, `apps/api`, `packages/client`, `apps/studio`, `tests/e2e/accessibility.spec.ts`, `docs`, `security`, `scripts/test-postgres.mjs`, README, and the three ledgers. Any additional path requires an amendment here with the reason.
  - Out of scope: ActivityPub/ActivityStreams/WebSub server conformance, RFC 9421 HTTP-signature profiles, open peer discovery, global registries, anonymous source exports, arbitrary caller URLs/scopes, inbound push callbacks, unattended scheduling, bidirectional edits, remote draft/preview/workflow/release/identity federation, copying live records, promoting mirrors into ordinary content, local edits or republishing of mirrors, recursive federation, federated relations/backlinks/search/GraphQL, remote component code or manifests, assets/binaries, rich external references, provider SDKs, key/credential provisioning or automatic rotation, legal/license verification, usage enforcement, billing/royalties, public interoperability guarantees, and shared-cache invalidation.
  - Risks and rollback: a hostile or stale source could substitute scope, type, revision, rights, or content; fail closed on exact signed source scope, offer/version/digest, request ID, expiry, schema fingerprint, record checksum, revision sequence, and attribution terms, with generic adapter errors and no caller-selected egress. Live availability remains coupled to the source; mirror withdrawal becomes unavailable only through an explicit digest-reviewed tombstone plan. Contract fields are source assertions, not legal clearance. Roll back by disabling producer offers and consumer agreements, reconciling or expiring every in-flight plan, retaining/exporting required attribution receipts, then reverting the one additive M8-002 commit; no source or ordinary local record requires restoration.
  - Observable acceptance: producer offers and consumer agreements are complete-scope, optimistic, private/no-store, disabled by default, immutable by version, and available only to separately authorized read/manage/consume/sync actions. Producer reads return only exact published revisions for allowlisted supported schemas; drafts, preview credentials, tokens, private keys, tenant-routing headers, raw errors, and disallowed field kinds never enter envelopes or public delivery. Every envelope is bounded canonical JSON signed with the pinned Ed25519 key and binds protocol/source scope/instance, request ID, issue/expiry time, offer/version/digest, type/version/fingerprint, record/checkpoint, exact source revision/sequence/checksum, canonical URL, license URL, credit text, and attributed agents. Unsigned, replayed, expired/future, duplicate, non-complete, cross-scope, downgraded, schema-substituted, attribution-incomplete, oversized, off-origin, redirecting, or malformed results fail closed. Live delivery performs a fresh signed by-ID read and persists no record. Mirror plans expose exact create/update/no-op/withdraw effects, expire, bind a digest, revalidate the agreement/offer/types and source snapshot on execution, write pending before application, return the same receipt on retry, retain immutable attribution, and tombstone withdrawn mirrors without touching local content. Public projections expose namespaced type/data plus required attribution and source revision, omit source scope/key/adapter internals, and remain no-store. Focused schema/core/API/client/Studio, authorization/isolation, signature/replay/SSRF/schema/attribution/withdrawal/crash/idempotency, two-instance HTTP, recovery/PostgreSQL, dependency/security/full, and neutral-page/content-color Chromium/Firefox/WebKit gates must pass before `[x]` and the requested commit.
  - Implementation evidence: bounded canonical federation schemas and one complete-scope optimistic document back immutable offer versions, pinned agreements, live records, read-only mirrors, digest-bound plans, and idempotent receipts across memory, SQLite, and PostgreSQL. The framework-neutral service signs and verifies exact Ed25519 envelopes, admits only published supported schemas, performs fresh non-persisting live reads, revalidates mirror snapshots before execution, and keeps withdrawal tombstones and immutable attribution separate from ordinary local content.
  - API/security evidence: distinct `federation.read`, `federation.manage`, `federation.consume`, and `federation.sync` authorization gates protect private no-store management/source routes. The maintained credential-free HTTPS adapter rejects redirects and off-origin responses, bounds time and bytes, supplies complete source scope, and redacts transport diagnostics. Public delivery is no-store and omits source scope, keys, adapter configuration, and management state. Accepted ADR 0023, the operator guide, `THREAT-0036`, and verified `GS-SEC-040` record the trust boundary.
  - Recovery/database evidence: `pnpm test:recovery` passes 4 files and all 10 live-WAL backup, isolated-restore, shutdown, worker, and rollout scenarios, including restoration of federation state. `pnpm test:postgres` passes 19 core conformance and 2 API cases, includes `gridstory_content_federation_documents` in the qualified native dump, and recovers the earlier federation document through logical restore.
  - Browser/dependency evidence: the complete browser specification passes 12/12 Chromium, Firefox, and WebKit scenarios for unsuppressed WCAG 2.2 A/AA analysis, keyboard/200% zoom, published rendering, the complete vertical slice, and the requested established neutral page/content palette. `pnpm audit --prod --audit-level high` reports no known vulnerabilities; no dependency was added.
  - Final gate and defect audit: `pnpm check` passes boundaries, ledgers, security, tenant/readiness validators, formatting, generated contracts, strict types, every active workspace test, React 18.3 SSR certification, and production builds. BUG-0353 through BUG-0361 are resolved and no open defect remains. Rollback requires disabling all offers/agreements, reconciling or expiring pending mirror plans, retaining required attribution receipts, and then reverting the single additive commit; a code revert cannot retract already consumed external copies.
- [x] **M8-003** Add knowledge graph exploration, explainable recommendations, and sandboxed agentic operations.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because this adds graph and agent contracts, complete-scope persisted policy/run state, new authorization actions, an injected runtime boundary, and reviewed draft mutation. The user's `proceed to next` selects this queued item; project-delivery-loop requires approval of the frozen plan below before implementation.
  - Restated ask: let an authorized editor explore how current scoped content is connected, receive deterministic related-content recommendations with exact path and score evidence, and ask a configured agent runtime to investigate through fixed read-only tools and propose one bounded draft patch. The runtime never receives ambient repository, network, plugin, workflow, publication, credential, or code-execution authority; a human must review and explicitly execute the exact expiring plan through ordinary content validation and authorization.
  - Assumptions: the first graph is an on-demand derived projection of authoritative local content relations and taxonomy membership, not a second source of truth. Exploration is private/no-store, explicitly selects `draft` or `published`, and reauthorizes every returned entry. Recommendations are item-to-item and deterministic, not user-personalized or engagement-trained. Trusted server composition may inject one or more agent runtime adapters; adapters receive bounded goals, declared tool schemas, and a mediated read callback, while GridStory validates every call/result and alone performs a later approved draft update. Agent plans target exactly one existing draft and only declared top-level `text` or `slug` fields in the first slice.
  - Inward evidence: `SearchService` already extracts scoped relations, backlinks, taxonomy membership, and one-hop related reasons but has no bounded multi-hop query, path proof, stable contribution model, or reviewed operation lifecycle. Reviewed AI authoring already supplies strict provider output, deterministic evaluation, redaction, immutable provenance, human-only review, and a deliberately non-mutating handoff; the governed gateway supplies fixed prompts, model allowlists, budgets, and generic provider failures. Workflow actions, regional failover, migration, federation, and plugin invocation supply complete-scope optimistic documents, expiring digests, idempotent receipts, revalidation, capability grants, external-runtime timeouts, and recovery patterns, but none lets a model select mediated read tools and propose an executable content change. Proposed ADR 0024 records the full comparison.
  - Outward evidence: RDF models graphs as directed subject-predicate-object statements, while PROV separates derivation evidence from responsibility; Neo4j documents exponential variable-path growth and the need for finite depth plus relationship predicates; content recommenders distinguish candidate scores/reasons from later action and Google Recommender binds mutable recommendations with fingerprints and reviewable state; MCP treats tools as dangerous capabilities and recommends visible human consent; OWASP identifies excessive agency and indirect prompt injection as core agent risks; OpenAI recommends starting with one agent, bounded exits, risk-rated tools, layered guardrails, and human intervention for sensitive actions. M8-003 adopts bounded typed paths, evidence-bearing scores, fixed host tools, explicit approval, and stale-state revalidation without claiming RDF/SPARQL/MCP conformance or importing a graph/agent framework.
  - Necessity gate: traceable directly to M8-003 and the existing documented deferral of recommendations and autonomous tools; not solved by direct related-content results, semantic search, AI field proposals, plugins, workflows, or federation because none supplies multi-hop proof plus a model-controlled yet host-mediated read loop and reviewed execution. Minimal form is one derived graph query, one deterministic recommendation query, one disabled complete-scope agent policy/document, one injected runtime interface, three read-only tools (`content.get`, `graph.explore`, `recommendation.list`), one strict single-target draft-patch plan, one human review, and one idempotent execution receipt. No dependency, graph store, RDF/SPARQL parser, generic tool registry, MCP transport, multi-agent framework, or scheduler is added. Disable policy/adapters, expire or reject pending plans, retain/export required receipts, and revert the additive commit after successful executions remain ordinary draft history.
  - Units and sequence: `M8-003A` adds bounded graph/node/edge/path/recommendation/policy/tool-call/plan/review/receipt schemas and limits first because services and hostile-runtime checks depend on exact contracts. `M8-003B` adds deterministic graph/recommendation derivation plus complete-scope optimistic memory/SQLite/PostgreSQL agent persistence and a service that mediates bounded read tools, redacts goals, validates strict runtime output, binds expiring digests, requires human review, revalidates policy/target/schema/revision, and idempotently applies one ordinary draft update. `M8-003C` adds distinct knowledge/agent read/manage/plan/review/execute authorization, private/no-store REST routes, typed client methods, and a compact accessible neutral-palette Studio Knowledge panel. `M8-003D` adds hostile-runtime/prompt-injection/scope/staleness/replay/crash/recovery/PostgreSQL/security/browser evidence and documentation. The deterministic graph and risk-bearing runtime/execution tests precede UI work; database and browser gates follow the runnable vertical slice.
  - Scope fence: `packages/schema`, `packages/core`, `apps/api`, `packages/client`, `apps/studio`, `tests/e2e/accessibility.spec.ts`, `docs`, `security`, `scripts/test-postgres.mjs`, README, and the three ledgers. Any additional path requires an amendment here with the reason.
  - Out of scope: a graph database, materialized index, RDF/JSON-LD/SPARQL/Cypher/GQL import or conformance, ontology inference, entity resolution, federated/asset/plugin/user/audit/workflow/release nodes, public graph or recommendation delivery, user profiles, engagement events, collaborative filtering, embeddings/semantic or learned ranking, opaque recommendation explanations, feedback training, arbitrary tool discovery or caller-defined schemas, MCP server/client transport, browser/computer use, shell/filesystem/network/HTTP/database/plugin invocation, secrets, raw prompt/tool-result/history retention, conversations or long-term memory, multi-agent handoffs, unattended/scheduled runs, retries chosen by the model, cross-entry/bulk/create/delete operations, relations/taxonomy/rich-text/component/asset mutation, workflow transitions/approvals, releases, publication, or bypass of ordinary content validation/audit.
  - Risks and rollback: graph traversal can amplify cardinality or leak unauthorized nodes; agent goals or content can contain prompt injection; a hostile adapter can forge tool calls/results, overrun limits, substitute scope/revision, or return dangerous operations; approved plans can become stale or replayed. Fail closed on complete scope, perspective, seed, direction, edge kind, depth, node/edge/path/call/byte/time limits, per-node content authorization, exact adapter/model/policy/tool identity, canonical trace and plan digest, expiry, human user review, executor authorization, current draft revision, allowed field type/path, full schema/reference validation, and idempotency. Only core-mediated read callbacks run during planning and only the service performs the later reviewed draft update. Roll back by disabling agent policy and adapters, rejecting/expiring pending plans, retaining required receipts, and reverting the additive commit; successful drafts remain ordinary auditable revisions and can be reverted through existing content history.
  - Observable acceptance: disabled/default operation adds no public delivery behavior and performs no adapter call. Graph queries accept only complete request scope plus bounded perspective/seeds/direction/depth/types, return unique authorized current nodes and typed relation/taxonomy edges with exact revision IDs and deterministic path evidence, terminate cycles, signal truncation, and reject cross-scope/unknown/oversized inputs. Recommendation queries return only authorized current entries with finite deterministic contribution totals whose complete relation/taxonomy/path evidence exactly sums to the score; no user, analytics, semantic, or hidden model signal participates. Agent policy and plans are optimistic, complete-scope, private/no-store, disabled by default, and separately authorized. A configured runtime sees only the redacted bounded goal, policy-pinned adapter/model, declared fixed read-tool schemas, and validated bounded tool results; it cannot execute writes. Unknown/duplicate/late/excessive/cross-scope/tool-result-substituting calls and malformed/oversized/stale or policy-violating outputs fail closed with generic errors and no retained raw diagnostics. A strict plan targets one exact current draft and only allowlisted top-level text/slug changes, records visible rationale plus checksummed metadata-only tool provenance, expires, and binds a canonical digest. Only a human user can approve/reject once; execution requires the approved digest and explicit idempotency key, reauthorizes agent execution plus ordinary content update, revalidates unchanged policy/adapter/model/target revision and the complete resulting content schema/references, persists pending before mutation, returns the same receipt on retry, and never transitions workflow, approves, releases, publishes, or mutates another entry. Focused schema/core/API/client/Studio, authorization/isolation/cardinality/cycle/prompt-injection/hostile-runtime/staleness/replay/crash/idempotency, recovery/PostgreSQL, dependency/security/full, and neutral-page/content-color Chromium/Firefox/WebKit gates must pass before `[x]` and the requested commit.
  - Implementation evidence: strict bounded graph, recommendation, policy, tool-call, plan, review, receipt, and complete-scope document contracts now drive request-local cycle-safe relation/taxonomy traversal, deterministic contribution-equal recommendations, optimistic memory/SQLite/PostgreSQL persistence, one injected runtime interface, redacted fixed mediated reads, strict expiring single-draft text/slug plans, human review, pending-first idempotent execution, and ordinary draft history. Disabled operation remains the default and adds no dependency, graph store, public delivery, ambient runtime authority, or autonomous mutation path.
  - API/security evidence: distinct `knowledge.read` plus `agent.read/manage/plan/review/execute` authorization protects private no-store graph and agent routes, with per-entry content-read checks and ordinary draft-update authorization on execution. Accepted ADR 0024, the operator guide, `THREAT-0037`, and verified `GS-SEC-041` document the boundary; BUG-0365 additionally proves pending crash retries revalidate expiry and current knowledge/AI policy before any still-needed mutation.
  - Recovery/database evidence: `pnpm test:recovery` passes 4 files and all 10 live-WAL backup, isolated-restore, shutdown, worker, and rollout scenarios with knowledge policy restored. `pnpm test:postgres` passes 20 core conformance and 2 API cases, includes `gridstory_knowledge_documents` in the qualified native dump, and recovers the disabled knowledge-agent policy through logical restore.
  - Browser/dependency evidence: the complete browser specification passes 12/12 Chromium, Firefox, and WebKit scenarios for unsuppressed WCAG 2.2 A/AA analysis, keyboard/200% zoom, published rendering, the complete vertical slice, and exact preservation of the established neutral page/content colors in the Knowledge panel. `pnpm audit --prod` reports no known vulnerabilities; no dependency was added.
  - Final gate and defect audit: the corrected `pnpm check` passes 307-file lint, formatting, package boundaries, ledgers, generated schemas, security/tenant/readiness policy, all strict types, 368 active workspace tests, React 18.3 SSR certification, and production builds. BUG-0362 through BUG-0365 are resolved and no open defect remains. Rollback requires disabling the agent policy/adapters, rejecting or expiring pending plans, retaining required receipts, and reverting the single additive commit; already successful draft revisions remain ordinary auditable content history.
- [x] **M8-004** Add self-hosted fleet management and public interoperability specifications for archives, schemas, components, and preview source maps.
  - Owner/start: Codex, 2026-08-24. Delivery tier: T2 because this publishes new durable machine-readable contracts, adds a public discovery surface, introduces complete-scope fleet persistence and authorization, and observes configured external instances through a maintained HTTP adapter. The user's `proceed to next` selects this final queued roadmap item; project-delivery-loop requires approval of the frozen plan below before implementation.
  - Restated ask: make GridStory's existing logical-content archive, canonical schema IR, serializable component manifest, and preview node-source mapping independently implementable through immutable versioned public specifications, while letting an authorized operator inventory and explicitly inspect a bounded set of self-hosted GridStory instances from one private fleet view. Public specification routes expose formats and capability metadata only; they never expose tenant scopes, content, drafts, components in use, credentials, fleet membership, or management state.
  - Assumptions: “archive” means the existing `gridstory.logical-content` portable archive rather than database backups or npm tarballs. “Fleet management” means configured instance registration, sanitized on-demand observation, compatibility/health conditions, pause/remove controls, and retained bounded evidence—not remote provisioning or upgrade execution. One stable local instance ID and service version come from trusted server composition. Fleet members name only a preconfigured adapter ID plus an expected remote instance ID; origins and any credentials remain deployment-owned. Public v1 JSON Schema documents are generated from the canonical Zod contracts and committed under `specifications/v1`, with semantic checksum/reference rules documented beside the generated shape.
  - Inward evidence: `PortabilityService` already owns a versioned canonical-JSON archive with per-record/aggregate SHA-256 checksums but keeps its shape in core interfaces and handwritten validation; schema IR and component manifests are canonical Zod contracts; preview protocol v1 and `@gridstory/react` already carry entry/node/component/version selection markers; rolling-upgrade checks already perform bounded exact health/readiness reads; federation supplies same-origin redirect-free HTTP, generic errors, source pinning, and digest patterns; recent regional/federation/knowledge work supplies complete-scope optimistic memory/SQLite/PostgreSQL documents, distinct authorization, private/no-store Studio controls, and recovery evidence. None publishes drift-checked external schemas or persists a multi-instance inventory/condition view.
  - Outward evidence: JSON Schema 2020-12 recommends absolute `$id` identities and stable bundling; OpenAPI 3.1 demonstrates explicit JSON Schema dialect declaration but a whole API description exceeds these four data formats. RFC 8615 requires registration for new `/.well-known` names, so v1 uses an ordinary `/api/v1/interoperability` discovery path rather than squatting on an unregistered name. OCI distribution demonstrates immutable media-type/digest descriptors but a registry/blob/tag protocol is unnecessary. Custom Elements Manifest is a useful versioned JSON-schema catalog for Web Components but does not express GridStory's React-owned props/slots/migrations. Sanity Content Source Maps demonstrate explicit result-to-document/path mappings and preview-only overlays, while GridStory needs only its existing entry/node/component source identity. Kubernetes conditions and ClusterProfile inventory separate desired identity from observed generation/status; full agents, leases, credentials, placement, and workload distribution are deliberately excluded. Proposed ADR 0025 records the comparison.
  - Necessity gate: traceable directly to M8-004 and ADR 0023's explicit deferral of public archive/schema/component/source-map formats; not solved by authenticated schema/component routes, implementation-only TypeScript types, logical portability, federation offers, release package evidence, or one-off rollout checks because none gives independent consumers immutable generated schemas plus a retained fleet-wide compatibility view. Minimal form is four Draft 2020-12 schema artifacts, one versioned discovery descriptor, one preview source-map builder over existing node markers, one disabled/empty complete-scope fleet document, one pull-only adapter interface with a maintained safe GridStory HTTP implementation, three fleet actions, private REST/client/Studio controls, and no remote write. Zod 4 already generates the schema documents, so no dependency is added. Disable/remove fleet members and adapters, then revert the additive commit; public specs contain no customer state and external instances are never mutated.
  - Units and sequence: `M8-004A` moves the logical archive shape into canonical schema contracts, adds the preview source-map/discovery/specification contracts and fixed resource limits, generates the four committed Draft 2020-12 documents plus examples, and makes drift checking part of `schema:check`; this precedes fleet work because compatibility observations compare exact version/digest descriptors. `M8-004B` adds an empty-by-default complete-scope fleet document with bounded members/events/observations, optimistic memory/SQLite/PostgreSQL repositories, and a service that validates configured adapter identity, expected instance, protocol/spec digests, service version, freshness, finite conditions, and generic failure evidence. `M8-004C` exposes publicly cacheable discovery/immutable schema routes, composes the same-origin bounded HTTP observer, adds separate `fleet.read/manage/check` authorization, private/no-store fleet REST and typed client methods, and a compact accessible neutral-palette Studio Fleet panel. `M8-004D` adds public conformance/drift fixtures, hostile-adapter/SSRF/scope/staleness/restart/recovery/PostgreSQL/security/browser evidence and updates README, a normative specification/operator guide, ADR 0025, threat/ASVS/requirements, native restore checks, and all ledgers. Contract and hostile-observer tests precede UI work; database and browser gates follow the runnable vertical slice.
  - Scope fence: `packages/schema`, `packages/core`, `packages/client`, `packages/react`, `apps/api`, `apps/studio`, `specifications`, `scripts/generate-interoperability-specs.mjs`, `scripts/test-postgres.mjs`, root `package.json`, `tests/e2e/accessibility.spec.ts`, `docs`, `security`, README, and the three mandatory ledgers. Any additional path requires an amendment here with the reason.
  - Out of scope: public content/draft/archive exports or anonymous schema/component inventories; database backup/PITR/npm package formats; IANA well-known or media-type registration; full OpenAPI/API-client generation; JSON Schema import as a second authoring/runtime schema language; OCI registry/blob/tag/referrer support; Custom Elements Manifest, Sanity Content Source Maps, or other third-party conformance claims; framework component code/bundles, visual assets, field/slot-level preview mappings, steganographic metadata, or published-render annotations; automatic instance discovery, inbound enrollment, fleet agents, push callbacks, heartbeats, leases, polling schedules, background jobs, alerts, placement, traffic, regional failover, remote shell/API writes, provisioning, deployment, upgrade/rollback execution, Kubernetes CRDs/controllers, secrets, caller-supplied origins, redirects, credential exchange, cross-scope aggregation, billing/licensing, hosted control-plane claims, or provider certification.
  - Risks and rollback: generated documents can drift from runtime validation; public discovery can leak operational detail or be cached under the wrong identity; a hostile member can substitute instance/spec/version/time evidence or return oversized/redirected content; a fleet view can confuse stale observation with current readiness or cross tenant boundaries. Fail closed on exact format/version/schema ID/digest/media type, deterministic generation, complete scope, optimistic version, configured adapter, expected instance, same origin, HTTPS, redirect denial, timeout/bytes, finite unique conditions, observed generation/time/expiry, and local spec compatibility; retain only sanitized metadata and generic reason codes. Public specs are immutable by version and discovery exposes only local instance ID, service version, protocol, spec descriptors, and fixed health/readiness paths. Roll back by pausing/removing member records, removing adapters, exporting any required observation evidence, and reverting the additive commit; no remote or content restoration is required.
  - Observable acceptance: `schema:generate` deterministically writes and `schema:check` byte-checks four versioned Draft 2020-12 JSON Schema artifacts and examples for logical archives, canonical schema IR, component manifests, and preview source maps; valid fixtures pass canonical runtime parsing and corrupted/unsupported/oversized/checksum-invalid cases fail closed. Public discovery and schema endpoints require no tenant headers, return exact protocol/spec version, stable URN identity, media type, SHA-256 digest, and links with bounded public caching/ETag behavior, and contain no scope, content, draft, credential, adapter, fleet, or raw health data. Preview source maps deterministically map the existing preview-only `data-gridstory-node` markers to exact entry/node/component/version identity and never appear in normal published rendering. Fleet state is empty by default, complete-scope, optimistic, bounded, private/no-store, and accessible only through distinct authorized read/manage/check actions. Callers can name only configured adapter IDs; the maintained observer accepts only credential-free HTTPS origins, rejects redirects/origin changes, bounds time and bytes, and normalizes errors. Each on-demand check records exact member generation, instance/protocol/service/spec versions and digests, checked/expiry times, and finite Reachable/Ready/Compatible conditions; malformed, duplicate, cross-instance, stale/future, oversized, wrong-digest, unsupported-version, or unavailable results cannot be marked compatible and never leak raw diagnostics. No fleet action writes to a remote instance or changes content, traffic, deployment, regional, workflow, release, or publication state. Focused schema/core/API/client/React/Studio, public-cache/secret/scope/hostile-adapter, generator-drift, recovery/PostgreSQL, dependency/security/full, and exact neutral-page/content-color Chromium/Firefox/WebKit gates must pass before `[x]` and the requested commit.
  - Implementation evidence: canonical Zod contracts now own the portable archive, schema IR, component manifest, and deterministic preview source-map formats; eight generated v1 schema/example artifacts are exact-drift checked. The empty-by-default framework-neutral fleet service persists complete-scope optimistic members, bounded observations/events, and finite Reachable/Ready/Compatible conditions through memory, SQLite, and PostgreSQL without any remote mutation authority.
  - API/security evidence: anonymous discovery/specification routes expose only immutable descriptors and schemas with bounded public caching plus ETags, while distinct `fleet.read`, `fleet.manage`, and `fleet.check` actions protect private no-store state. The maintained observer accepts only deployment-configured credential-free HTTPS origins, denies redirects/origin changes, bounds time/bytes, emits generic failures, and never forwards actor, tenant, authorization, or preview credentials. Accepted ADR 0025, the interoperability/operator guide, `THREAT-0038`, and verified `GS-SEC-042` record the boundary.
  - Recovery/database evidence: `pnpm test:recovery` passes 4 files and all 10 live-WAL backup, isolated-restore, shutdown, worker, and rollout scenarios with paused fleet state restored. `pnpm test:postgres` passes 21 core conformance and 2 API cases, includes `gridstory_fleet_documents` in the qualified native dump, and recovers the earlier fleet document through logical restore.
  - Browser/dependency evidence: Chromium, Firefox, and WebKit each pass 4/4 scenarios for unsuppressed WCAG 2.2 A/AA analysis, keyboard/200% zoom, published rendering, the complete vertical slice, the Fleet panel, and exact preservation of the established neutral page/content colors. React 18.3.1 client/SSR compatibility passes, and `pnpm audit --prod --audit-level high` reports no known vulnerabilities; no dependency was added.
  - Final gate and defect audit: the corrected `pnpm check` passes 321-file lint/policy validation, 318-file formatting, strict types, all eight generated interoperability artifacts, 383 active workspace tests with 17 documented PostgreSQL-only skips, React 18.3.1 SSR certification, and all production builds. BUG-0366 through BUG-0374 are resolved and no open defect remains. Roll back by pausing/removing members, removing configured observers, exporting any required sanitized observation evidence, and reverting the single additive commit; public specs contain no customer state and remote instances/content were never mutated.

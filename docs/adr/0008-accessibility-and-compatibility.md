# ADR 0008: Evidence-bounded accessibility and compatibility certification

- Status: Accepted
- Date: 2026-08-21
- Task: M5-006

## Context

GridStory is both a web authoring tool and a React integration surface. The repository already has semantic controls, keyboard composition operations, content-quality checks, SSR/hydration tests, a React 18.3-to-19 peer range, a Vite example, and one Chromium-family walkthrough. Those checks do not establish rendered WCAG coverage, the declared React range, cross-engine behavior, or an honest public boundary between shipped and planned adapters.

W3C WCAG evaluation requires automated and human evaluation. ATAG 2.0 has two distinct concerns: Part A makes the authoring tool accessible to authors, and Part B helps authors produce accessible content. Playwright recommends `@axe-core/playwright` for detectable rendered issues while warning that automation is incomplete, and supports isolated projects for Chromium, Firefox, and WebKit. Vite 8 publishes a fixed Baseline Widely Available production target.

## Prior-art comparison

| Approach | Who does it this way | Fits GridStory? | Cost | What we deliberately skip |
|---|---|---|---|---|
| Semantic lint and unit tests plus axe, focused manual/proxy checks, three Playwright engines, and exact framework fixtures | W3C evaluation guidance, Playwright accessibility/browser guidance, Deque axe rule tags | Yes; extends existing seams and keeps claims tied to executable evidence | One audit dependency, browser downloads, fixtures, and review documentation | No automated-only conformance claim and no adapters that do not exist |
| Chromium-only Lighthouse or axe score presented as certification | Common CI dashboards | No; misses Firefox/WebKit behavior and cannot establish WCAG/ATAG conformance | Low | Rejected because the resulting claim would be misleading |
| Independent expert/user audit across assistive technologies, operating systems, branded browsers, and every roadmap framework | Mature GA programs | Valuable later, but not a repository-only slice and most adapters are not implemented | High recurring program and external coordination | Deferred to readiness/design-partner work after the surfaces exist |
| Do nothing; reuse Biome accessibility lint and current React 19/Edge walkthrough | Current repository | No; static lint cannot inspect runtime names, relationships, contrast, hidden states, or the declared React/browser matrix | Zero | Rejected because M5-006 has explicit acceptance gaps |

## Necessity gate

1. **Traceable:** M5-006 and GA release gates explicitly require accessibility critical journeys and a green compatibility matrix; security requirements also assign browser/rendering review here.
2. **Not already solved:** Biome catches source-level semantics, content-quality checks inspect structured author input, and current Vitest/Playwright tests prove logic plus one engine; none audits rendered states or the complete declared support range.
3. **Minimal form:** certify only the shipping React renderer/client, React 18.3 and 19, Vite 8, Node 22, and Playwright's three engines. Do not build roadmap framework adapters or claim third-party conformance.
4. **Dependency justified:** axe maintains hundreds of WCAG/ACT mappings, DOM algorithms, cross-frame injection, contrast/name computation, and incomplete-result semantics that would exceed 100 locally maintained lines and should not be reimplemented.
5. **Rule of three:** use Playwright's existing project and fixture mechanisms; introduce no generic certification plugin, adapter base class, or policy framework.
6. **Reversible:** remove the audit dependency, projects, fixture, and documentation in one revert; no persisted data or public API shape changes.

## Decision

Adopt an evidence-bounded review: axe runs only official WCAG 2.x A/AA tags including `wcag22aa`, without rule suppression or excluded UI; executable tests cover keyboard bypass/equivalent operations and display adaptations; the full critical journey runs on Playwright-pinned Chromium, Firefox, and WebKit; isolated fixtures prove every declared React major and the current Vite integration. Publish exact versions, commands, gaps, ATAG findings, and application/deployment ownership.

Use the word **certified** only to mean “this exact repository matrix passed its named checks.” Do not call the result third-party certification or claim complete WCAG/ATAG conformance until experienced manual evaluation, live assistive-technology testing, and user evaluation are completed.

## Sources

- <https://www.w3.org/TR/WCAG22/>
- <https://www.w3.org/WAI/standards-guidelines/atag/>
- <https://www.w3.org/WAI/test-evaluate/>
- <https://playwright.dev/docs/accessibility-testing>
- <https://playwright.dev/docs/browsers>
- <https://github.com/dequelabs/axe-core/blob/develop/doc/API.md#axe-core-tags>
- <https://v8.vite.dev/config/build-options#build-target>
- <https://react.dev/versions>

## Consequences

- Compatibility claims fail closed when their executable evidence fails.
- Browser-engine coverage is repeatable in CI, but branded Safari, OS accessibility APIs, and media-codec differences remain outside the claim.
- GridStory can review Studio and its example components; consuming applications remain responsible for their components, routes, semantics, styles, CSP, and deployment headers.
- Future framework adapters receive their own fixtures before appearing as supported.

## CI-002: retain native click actionability without duplicate scrolling

- Accepted: 2026-08-26T19:41:21+05:30 by Codex; T1, BUG-0459, parent investigation CI-001.
- Evidence before correction: an unchanged `CI=1 pnpm test:e2e` run passes Chromium and Firefox 14/14 each, but WebKit's responsive test reaches its existing 300-second deadline and passes retry #1 in 2.2 minutes. Its retained trace's `pw:api@1066` spends 172.16 seconds inside `selectStudioPanel`'s standalone `scrollIntoViewIfNeeded` for Marketplace at 768px; the next normal click succeeds in 571 ms. The eventual timeout is cumulative, not a recorded containment assertion. The exact browser/OS mechanism behind that stall is unproven.
- Decision: remove only the separate scroll call preceding the normal click in the existing panel and Pages selection helpers. [Playwright's scrolling guidance](https://playwright.dev/docs/input#scrolling) and [pointer actionability documentation](https://playwright.dev/docs/input#mouse-click) confirm that native clicks already scroll and check visibility, stability and event reception. Retain those checks, the explicit visibility assertions, mobile drawer settlement, exact selected route/surface checks and all responsive/theme/keyboard assertions.
- Rejected: forced clicks, DOM-dispatched clicks, dropping destinations or widths, increasing deadlines/retries, splitting away coverage, or adding an abstraction/dependency. The fix removes a redundant automation path, not an application feature or a test assertion. Rollback restores the two calls; runtime/styles/API authorization are untouched.
- Verification required: a source-preservation audit, repeated WebKit responsive scenarios without retries, then the complete quality and 42-scenario browser gates, run serially. Keep the original failing artifacts under ignored `test-results/ci002-before`.
- Limit: PR #13's hosted browser failure (BUG-0458) exposes only exit code 1 publicly; its protected failed assertion is unavailable. CI-002 does not claim equivalence to that failure, a green hosted replacement, permission to push/merge, or a broader platform-certification result. CI-001 remains blocked on the specific hosted output.
- Verified result (2026-08-26T19:59:37+05:30, Codex): the source-preservation audit confirms only the two scroll removals and one comment. The unchanged six-width/19-destination responsive sweep passes `--project=webkit --repeat-each=3 --retries=0`, 3/3 in 4.7 minutes (1.5 minutes each). Post-change `pnpm check` passes 626 tests with 17 existing optional skips and all policy/type/build gates; subsequent `CI=1 pnpm test:e2e` passes all 42 scenarios without retries (Chromium 1.8m, Firefox 2.3m, WebKit 3.9m; full WebKit responsive case 2.2m). Normal production bundles are restored, isolated test servers are stopped, and no new manual UI verification is claimed. This resolves the bounded CI-002 helper path, not the unavailable hosted assertion.

## CI-001: synchronize responsive test navigation with rendered mobile state

- Accepted: 2026-08-26T20:05:54+05:30 by Codex; T1, BUG-0458, baseline `51eb5d2`. This continues the investigation after the user supplied the earlier job's log; the historical CI-002 access limitation above describes that earlier checkpoint.
- Supplied evidence: Ubuntu WebKit run 32970401448 / job 98183440363, prior head `0790711`, merge ref `dee4303`, fails all three attempts at `openMobileStudioNavigation`'s 5-second `studio-navigation--open` class assertion. Chromium/Firefox pass 10 each. The latest `286979b` run's assertion is not in this attachment; its equivalence and a future hosted success are not asserted.
- Inward research: `App.tsx` intentionally uses the toggle for desktop condensation and mobile drawer visibility. Its resize listener updates the mobile mode; CSS media queries can already reflect the narrower viewport while that render is pending. Desktop `aria-expanded=true` does not mean the mobile drawer is open; desktop compact `false` does not prove mobile readiness either. The existing standalone shell browser test already waits for the closed ARIA state, and compact mode uniquely omits group disclosure buttons that both full desktop and mobile navigation render. Existing unit tests initialize width synchronously and do not exercise the helper's timing.
- Initial reproduction (superseded fixture): the first WebKit run against the unchanged helper fails both transition cases with the supplied expected/received class and 5-second timeout. However, cross-engine verification then exposes BUG-0460: native resize suppression interferes with Firefox viewport completion, Chromium does not retain the intended setup state, and isolated-world ARIA assertions do not trigger the page-world getter that releases the event. Those initial failures are not sufficient regression evidence; retain their artifacts in ignored `test-results/ci001-before` and replace the fixture before drawing conclusions.
- Decision: inspect the drawer's own open class to decide whether it needs opening. If closed, use existing web-first assertions to wait for both `aria-expanded=false` and the first grouped-navigation disclosure button before the normal click. The former waits out expanded desktop state; the latter waits out compact desktop state. Assert expanded ARIA, open class and settled transform after either path, including an already-open drawer. Retain all prior route, surface, layout, keyboard, theme, viewport, actionability and timeout checks. No runtime or style changes.
- Prior art: [Playwright assertions](https://playwright.dev/docs/test-assertions) explicitly retry observable DOM conditions; [viewport sizing](https://playwright.dev/docs/api/class-page#page-set-viewport-size) changes the browser viewport, not an application's completion contract; [window resize events](https://developer.mozilla.org/en-US/docs/Web/API/Window/resize_event) provide the application's update notification. Reuse these existing seams, rather than assuming a one-shot ARIA read is synchronized with React.
- Necessity: (1) directly maps to supplied BUG-0458; (2) current helper confuses desktop and mobile meanings; (3) only helper synchronization plus two real-page regression controls; (4) no dependencies; (5) no new hook/framework/config abstraction; (6) reversible by restoring the helper and removing the two added cases, without data/API changes.
- Rejected: forced clicks, repeated click retries, fixed sleeps, longer deadlines, dropped destinations/widths/assertions, new runtime test attributes, changing the app's navigation behavior to satisfy automation, and workflow/protection bypasses. Revisit application scope only if a real user navigation failure remains after rendering settles.
- Verification required: both new cases passing in all three engines, no-retry repeated WebKit responsive sweeps, full repository quality/build and browser gates. The two regressions also check already-open idempotence, destination-close behavior, Pages return, desktop return and preservation of the desktop compact preference. No new manual UI smoke is required for this test-only correction, and none is claimed.
- Fixture correction: hold only the app's `innerWidth` observation at the original desktop value while the actual CSS viewport becomes 768px. After verifying the stale desktop ARIA state and closed drawer, schedule restoration of the original property descriptor and a normal resize event after a bounded 500ms fault-injection delay. Native events and ARIA accessors remain untouched; require the real width to be 768 after opening. This delay simulates pending application state; the helper has no sleep, forced click, extended deadline or retry loop. All six expanded/compact cross-engine cases pass in 41.6s with no retries. Recheck this corrected fixture against the exact baseline helper before claiming the regression gate.
- Confirmed regression (2026-08-26T20:13:24+05:30): the corrected fixture also fails both WebKit cases against the exact baseline helper, at the supplied class assertion with the same expected/received values and 5-second timeout. Command: `CI=1 pnpm exec playwright test tests/e2e/accessibility.spec.ts --project=webkit --grep 'navigation helper waits' --retries=0 --output=test-results/ci001-regression-before`; artifacts are retained there. A read-only source comparison confirms that run contains only the baseline spec plus the two new cases. Restore the already six-case-green correction, then run the full gates. This establishes the state-ordering failure path, not the unavailable hosted trace's precise event schedule.
- Full-gate interruption (BUG-0461, tracked separately in CI-003): the first `pnpm check` passes lint/policies/format/types and all non-Studio active unit cases, but the unchanged App suite times out in the experiment lifecycle test (reported 61.24s versus a 15s deadline) and its following AI proposal test misses the expected pending-review result. The source/config comparison against `51eb5d2` is clean; the browser helper is not loaded by Vitest. `pnpm --filter @gridstory/studio exec vitest run test/App.test.tsx -t 'runs a governed experiment lifecycle|operates governed generation'` then passes both unchanged cases in 16.27s total (54 cases excluded by the focused filter). This does not fix or explain the intermittent full-run failure; repeat the full gate without edits and retain BUG-0461 for investigation rather than relaxing assertions.
- Quality rerun: unchanged `pnpm check` passes all 626 active tests (243 Studio), 17 existing optional database/recovery skips, lint/format, policy/security/tenant/readiness gates, generated-contract checks, strict types, React 18.3.1 SSR and every production build. No unit/runtime/config edit was made between runs; BUG-0461 remains unresolved in CI-003. The test-only fix retains identical Studio production CSS/JS hashes (`index-CfYjAOw5.css`, `index-BQWQhFEC.js`).
- Complete browser gate: `CI=1 pnpm test:e2e` passes all 48 scenarios without retries: Chromium 16/16 in 2.1m, Firefox 16/16 in 2.7m, WebKit 16/16 in 5.0m. Each includes both new transition regressions and all 14 original cases; the complete responsive sweeps take 59s, 1.1m and 2.1m respectively. Permission/session/history/preview/publishing, WCAG checks, six widths, 19 destinations and light/dark coverage remain intact. These are local Windows browser-engine results, not a new Ubuntu GitHub run or branded-browser/assistive-technology certification.
- Final repeated gate and cleanup (2026-08-26T20:41:56+05:30, Codex): `CI=1 pnpm exec playwright test tests/e2e/accessibility.spec.ts --project=webkit --grep 'every Studio surface' --repeat-each=3 --retries=0` passes 3/3 in 8.3m (3.2m, 2.0m, 2.7m), within every original deadline; no broader performance guarantee is inferred from these timings. Restored normal bundles with `pnpm --filter @gridstory/studio --filter @gridstory/example-vite run build`; the production hashes remain unchanged, and no listener remains on isolated ports 44000/44173/44174. The source audit proves all prior test bodies and both destination helpers are byte-equivalent outside the corrected mobile helper; every pre-existing task status except CI-001 and every historical bug row except the investigated BUG-0458 is preserved. Five files only; no new manual UI smoke, hosted success, push, merge or deployment. CI-001's bounded local fix is complete; CI-003 retains BUG-0461, and the next external step needs authorization to push and inspect fresh GitHub checks.
